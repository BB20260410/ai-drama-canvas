# 全项目行为保持重构 · Phase A 验证报告

## 结论

Phase A 已通过，整个重构计划仍为**执行中 / 部分完成**。本阶段没有重写已验收 owner，
而是完成了两个依赖方向整理和三条 Renderer 只读热路径限流。

## 已完成改动

1. 新建 `studio-higgsfield-connector-contract.ts`，承载固定 profile、能力观察类型、
   Unlimited 门禁与远端观测脱敏；queue/video owner 共同依赖纯合同，video 保留兼容导出。
2. Core 运行时 SCC 从 3 组降为 2 组；Higgsfield queue ↔ video 两节点循环已消失。
3. `NovelWorkspaceSnapshot` 移入 `novel-types`，`novel-manuscript` 保留兼容 type re-export；
   写作状态与源导入不再反向依赖仓库实现。
4. 15 秒向导资产读取抽为独立纯 helper，保持首次出现顺序、缺失忽略和异常失败关闭，
   同时把最大在途 IPC 限制为 4。
5. 受管画布的普通文稿与固定文稿读取均改为并发 4；原有 root/sequence 防串写、
   缺失 pin 清理与失败占位行为保持。
6. 新增依赖方向与向导资产 helper 回归测试。

## 验证

- Higgsfield：4 files / 28 tests PASS。
- Renderer/向导：5 files / 47 tests PASS。
- Novel：3 files / 60 tests PASS。
- 最终边界复验：2 files / 4 tests PASS。
- `npm run typecheck`：PASS。
- `npm run typecheck:app`：PASS。
- `git diff --check`：PASS。
- 独立只读终审：CLEAN。
- TypeScript AST 运行时依赖图：SCC `3 → 2`，大小从 `[15,4,2]` 变为 `[15,4]`。

## 没有执行

本阶段没有运行全量测试分区、正式 build、candidate、package smoke 或安装版替换。
原因是完整重构仍在进行，重复构建/打包会制造无收益循环；这些只在最终源码冻结后执行。

## 数据与外部边界

- 未写正式工程、CAS、SQLite、Review 或生成账本；
- 未启动 App；
- 未调用外部服务、上传、付费或生成；
- 未公证、发布或部署；
- 未执行 Git stage/commit/push/reset/clean。

## 后续

后续严格按 `docs/全项目行为保持重构路线图_20260810.md` 推进。Phase B 先冻结
Active Studio 8 份 canonical JSON 旧向量；只有证明字节完全等价后才迁移，禁止修改 golden
来掩盖 fingerprint 漂移。
