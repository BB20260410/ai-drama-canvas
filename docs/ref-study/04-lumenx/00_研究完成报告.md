# LumenX · 深研报告（TaskQueue 取消/跳转/预览）

| 字段 | 值 |
|------|-----|
| pin | `743683387384fb1d9fff72038933e7249d416076` |
| LICENSE | MIT |
| clone | `/Users/hxx/Documents/_ref-study/lumenx-743683387384fb1d9fff72038933e7249d416076` |

## 目录地图

| 路径 | 作用 |
|------|------|
| `frontend/src/components/modules/storyboard-r2v/shot-panel/TaskQueuePanel.tsx` | 侧栏队列：tabs Active/Done/Failed |
| 同文件 Task row | onCancel / onJumpToShot / onRetry + PreviewImage/Video |
| `TaskQueueButton.tsx` | inFlight 角标 |
| `PreviewImage.tsx` / `PreviewVideo.tsx` | 缩略图预览 |

## 用户路径

1. StoryboardR2V 聚合 tasks → TaskQueuePanel  
2. Active tab：in-flight 可 **Cancel** → `onCancel(task)`  
3. 行点击 **jump** → `onJumpToShot(frame_id)` 滚到 shot  
4. 展开行 **Preview** 输入 thumb / 输出 video  

## 本库映射（不碰供应商）

| LumenX | 本库 |
|--------|------|
| Cancel | GenerationQueueView `cancel` + existing `cancelGenerationJob` |
| Jump | `resolveStudioGenerationQueueJumpTarget` + emit jump |
| Preview | 展示 resultPath/companion/isolated 路径投影 |

## 路径到函数（本库）
`GenerationQueueView.vue` → `cancel` / `jumpJob` / `previewJob` →  
`buildStudioGenerationQueueView` / `resolveStudioGenerationQueueJumpTarget`  
