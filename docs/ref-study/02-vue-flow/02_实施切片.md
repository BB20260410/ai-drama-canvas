# Vue Flow · 实施切片（Phase S）

## VF-1 · MiniMap（L2）

| 项 | 内容 |
|----|------|
| 依赖 | `@vue-flow/minimap@1.5.4`（与上游 monorepo 一致） |
| UI | `ManagedStudioCanvasView` 内 `<MiniMap />` |
| 样式 | `main.ts` import `@vue-flow/minimap/dist/style.css` |
| 验收 | typecheck；`managed-studio-canvas-ui` 结构测含 MiniMap |

## VF-2 · 框选 → 工作流组（L1，接 LMD-1）

| 项 | 内容 |
|----|------|
| 事件 | `@selection-change` → `extractStudioCanvasPanelIdsFromSelection` |
| 动作 | 「从选中创建工作流」→ `createStudioCanvasWorkflowGroup` + layout save patch.workflowGroups |
| 验收 | UI 结构测 + workflow-groups 单测回归 |

## 验收命令

```bash
npm run typecheck
npx vitest run tests/managed-studio-canvas-ui.test.ts tests/studio-canvas-workflow-groups.test.ts tests/studio-canvas-layout.test.ts
```
