# 第三季 Artlist 额度预检可恢复检查点验证报告

验证时间：2026-07-15 22:45（Asia/Shanghai）  
工作区：`/Users/hxx/Documents/无限画布`  
正式工程：`/Users/hxx/Documents/无限画布/productions/gushujuan-s3-f1a688020bfb7af6`  
长期 Goal：`active`

## 1. 结论

本轮没有生成正式图片，完成的是阻塞首张正式资产生产的真实软件纵向切片：Artlist 已登录，但页面明确提示积分不足、Generate 禁用，且当前可见参数与冻结任务合同不一致。执行在上传、填词、`submit_intent` 和 Generate 前停止。

软件现已把这类状态持久化为可恢复的 `preflight_blocked` 检查点，而不是丢失预检事实、误终止任务或重新入队。正式 P01 仍是同一 job：

- job：`gen-2026-07-15T11-19-38-303Z-23ac427f`；
- 状态：`waiting_external / preflight_blocked / R2`；
- blockers：`insufficient_credits`、`generation_mode_mismatch`；
- Publication：仍为 `reserved`，receipt 0；
- remote identity、result path、正式 raw/labeled：均不存在；
- 同一 job 可在阻塞解除后经 CAS 从 `preflight_blocked` 推进到 `preflight`，不得重新排队。

因此，旧交接的“等待 Chrome 登录”已经被当前文件系统和网页证据推翻。下一唯一最高优先级仍是 P01，但外部前置条件已改为：用户自行恢复 Artlist 可用额度；本轮没有购买授权。

## 2. Chrome 真实预检

权威网页观测：

| 项目 | 观测值 |
| --- | --- |
| URL | `https://toolkit.artlist.io/image-video-generator?mode=image` |
| 登录态 | 已登录工作台；可见账户菜单、Sessions 和额度入口 |
| 页面提示 | `您的积分不足以生成奖励。` |
| Generate | 禁用 |
| 页面当前模型 | `纳米香蕉 2` |
| 页面当前画幅 | `16:9` |
| 页面当前分辨率 | `2K` |
| 页面当前数量 | `1 张` |
| P01 冻结合同 | `GPT Image 2 / 9:16 / Medium / 1 Image / 并发1` |
| P01 上传白名单 | 空；text-only |

采取的失败关闭动作：

- 没有切换参数；
- 没有上传任何本地文件；
- 没有把提示词写入网页；
- 没有创建 `submit_intent`；
- 没有点击 Generate；
- 没有购买积分；
- 没有下载或登记结果。

机器证据：`docs/evidence/artlist-credit-preflight-20260715-2212.json`，SHA-256 `8ca6a168c5ccd78b38281042b1b9a7c4ca27a8f1de7bddaf691c112170a8a2c7`。

## 3. 实现范围

### 3.1 Core 与领域合同

- 新增结构化网页阻塞代码：登录、页面就绪、模式不匹配、额度不足、付费未授权、供应商错误等。
- `BrowserPreflightEvidence` 新增 blockers 与当前可见生成参数证据。
- 网页状态机支持 `plan_ready/preflight → preflight_blocked → preflight`，保持同一 job、输出预留和 Publication。
- `preflight_blocked` 后上传、`submit_intent` 和远端提交全部失败关闭。
- 额度不足必须同时证明已检查余额、需要付费动作、未获付费授权、Generate 禁用且可见额度提示。
- 参数不匹配必须保存 `generationModeVerified=false` 和当前可见模型/画幅/分辨率。

### 3.2 MCP、Doctor 与统一快照

- `update_browser_generation_job` 的直接工具和 `execute_command` schema 均支持 `preflight_blocked`、blockers 和 observedGeneration。
- Doctor 把可恢复阻塞与 `submission_unknown` 分开计数，给出“读取同一 job 当前修订并恢复”的定向调用链。
- 项目快照暴露脱敏后的 R2 note、blockers 和当前网页参数。
- MCP 工具总数保持 148，没有新增旁路写接口。

### 3.3 Electron UI

- 生成队列把 P01 显示为“预检阻塞”。
- 明确显示“点击前已停止 · 额度不足 / 生成参数不匹配”。
- 正式 UI smoke 证明 6 个任务仍在原批次中，P01 没有上传槽位或 submit-intent 误显示。
- 页面无横向/纵向溢出，page errors 为 0。

### 3.4 正式幂等检查点

通过正式命令总线，以稳定 request/idempotency key 写入 P01 R2。写入后：

| 正式侧车 | SHA-256 |
| --- | --- |
| `generation-jobs.json` | `3fc7dd327fb0d4c51feefab39d568e89d3248f26925cb89732f9188b436919f7` |
| `publications.json` | `6d7a0d603abc669c3e400fe3edeefa0301f2293350f02f99056f7ef38f5dd70c` |
| `asset-consistency-batches.json` | `9fbff5e15cfbc305ce943f49464719fd53489bf634da9b22e528b2eb21afe9a7` |
| `command-ledger.json` | `671120e72b6eb40df3385ada746bdd571d8a582e8871ab936dd83a05cfa5ebd7` |

UI 只读烟测前后四份 SHA 完全相同。正式生成任务仍为 6 个；首批资产顺序仍为 P01、S01、P30、S02、C07、P29；第七项门禁保持关闭。

## 4. 验证结果

| 验证 | 结果 |
| --- | --- |
| `npm run typecheck` | PASS |
| `npx vitest run tests/advanced.test.ts tests/mcp.test.ts tests/codex.test.ts` | 3 files / 24 tests PASS |
| `npm test` | 49 files / 286 tests PASS，0 failed，104.07s |
| `npm run build` | PASS；main/preload/renderer/compiled MCP 均构建成功，仅既有 Vite chunk 提示 |
| `npm run mcp:smoke` | PASS，148 tools |
| `npm run fusion:validate-asset-consistency -- ... artlist-preflight-blocked-formal-20260715.json` | PASS |
| `npm run ui:artlist-preflight-blocked-smoke` | PASS；正式 production Electron build |

注意：整个 `doctor_project` 的 `healthy` 仍可能因工程其他未完成阶段为 false；本轮只验证 generation 检查正确报告 6 个活跃任务、1 个可恢复预检阻塞、0 个提交不明，不能把局部警告冒充整个 Doctor 健康。

## 5. 源目录只读复核

正式验证在读取生产状态前后各做一次完整源快照：

| 边界 | before | after | 结论 |
| --- | --- | --- | --- |
| 第三季完整源根 | 3344 files / 24,570,877 bytes / `649160f22663ca4c45ee4a4084e278ef0edc61ec66db01bb84da38cbea3f8d26` | 完全相同 | 未变化 |
| 受控融合输入 | 1354 files / 14,093,144 bytes / `f1a688020bfb7af6a39e2d9e7f383f773fdd4fdc27a9562695017509a1600a8c` | 完全相同 | 未变化 |

没有回写 `/Users/hxx/Documents/古蜀卷第三季`，也没有回写第二季或三项权威参考源。

## 6. UI 与机器证据

- 正式状态与源边界：`docs/evidence/artlist-preflight-blocked-formal-20260715.json`，SHA-256 `f45bde2d6541b7e95005fe43af54f8fa74becc88821ff747c5e1fe272d9a70ec`。
- Electron UI：`docs/evidence/artlist-preflight-blocked-ui-smoke-20260715.json`，SHA-256 `e619190dc92d60c94d673cd72fce5a88af8a058a09b81bdee96fba8324939ee6`。
- UI 截图：`docs/evidence/artlist-preflight-blocked-ui-smoke-20260715.png`，SHA-256 `b3516cb59ef6e3cbcc779e75bf60175f19e64a7d57a7bf1e9572a14fa68cf24b`。
- 本轮 final validation：`docs/evidence/final-validation-20260715-2245.json`。

## 7. 下一唯一动作

用户自行确认 Artlist 页面不再提示额度不足后回复：`Artlist 额度已恢复`。

随后必须继续同一 P01 R2：读取当前修订 → 复核登录/页面/额度 → 设置并复核 GPT Image 2 / 9:16 / Medium / 1 Image → 写入 `preflight` → 因 text-only 保持零上传 → `submit_intent` → Generate 只点击一次 → 记录远端身份 → 隔离下载 → Publication → 机械验收 → 主代理原图目视 → 内容绑定单图 Review。

长期 Goal 继续 active；正式资产图、EP01 逐格图和正式中文故事板当前仍全部为 0，不能把本轮软件检查点或 EP01 冒充全季完成。
