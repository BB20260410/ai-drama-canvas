# AI 漫剧无限画布 · 正式封神篇项目 NLE 校准闭环验证报告

验证日期：2026-07-15（Asia/Shanghai）  
正式只读源：`/Users/hxx/Desktop/豆包版本/剧本_封神篇`  
权威隔离工程：`/Users/hxx/Documents/无限画布/formal-calibration/fengshen-bcbd8aca-20260715-0729`  
结论：通过。正式项目 NLE 校准缺口已关闭；长期 Goal 保持 `active`。

## 1. 最高优先级核验

本轮开始时，`docs/当前开发交接.md` 所列下一唯一 P1 为 `real-project-nle-validation`。现场源码、三组能力断言和最新 final evidence 都仍保留这一项；其他扫描、Revision CAS、关键帧、嵌套、Effect/Transition、Publication、package 与 ReviewStudio 已有直接证据，不应重复建设。因此该优先级仍成立。

用户随后明确授权：把 `/Users/hxx/Desktop/豆包版本/剧本_封神篇` 作为只读权威源，允许在当前工作区建立隔离可写副本，禁止回写源目录。本轮没有网站登录、上传、付费、安装、签名、公证、发布或 Git 外部动作授权。

## 2. 正式源事实与零回写

- 正式源共 30 个常规文件、2,436,824 bytes，无符号链接、无 `.aicanvas`、无原生视频或音频。
- 23 集剧本共 968 镜、8,637 秒；EP12 的 `03A`、`10A` 字母镜号被正确保留。
- 845 镜显式给出帧率：24×686、48×144、60×4、96×4、120×7；EP09/14/15 的 123 镜明确标为 `default-24`，没有伪称原稿提供。
- 本模块定义的全树聚合算法为 `sha256-relative-path-bytes-content-v1`；聚合 SHA-256 为 `bcbd8aca412c6f48021624ef07d9b627e9635c17095adb70fc47f8487f7ec0c0`。
- 校准结束后再次逐文件核对 inode、bytes、SHA-256、mtimeNs 与 ctimeNs，和 core evidence 的 `metadataAfter` 完全一致；源根仍无 `.aicanvas`。

`src/core/formal-drama.ts` 新增正式剧本解析、清单检查和隔离物化入口。目标必须是工作区内不存在的新目录；源内目标、既存目标、符号链接、非常规文件、读取期间变化或复制 SHA 不一致均失败关闭。

## 3. 隔离导入与派生媒体边界

权威成功根使用源 aggregate 前缀命名。完整 Markdown 与 4 张正式参考 PNG 进入 `source_snapshot`，EP19/21/22 被规范化为 140 个真实分镜单元，其中 EP22 为 53 个单元。产品 `prepareProjectImport → commitProjectImport → scanAndPersist` 正式路径完成导入；两次扫描的业务身份稳定。

源目录没有原生音视频，因此本轮明确区分：

- 正式输入：23 集剧本与 4 张参考板；
- 隔离派生：24/48/96/120fps H.264 校准视频、457 秒 AAC 校准音轨；
- 前 4 个 EP22 单元：各有独立 1920×1080 raw/labeled PNG 与视频，`00_信息.md` 明示“隔离派生、非源目录原生媒体”；
- 其他镜头不伪造独立成片素材。

4 对 raw/labeled 均为有效 PNG，98,457–221,423 bytes，不是空文件或占位图。时间线抽帧和 Continuation 另产生 360×640 raw，不与这 4 对生产参考图混算。

## 4. 正式 NLE 纵向切片

主工程为 EP22《斩鼎》：

- 源剧本语义：53 镜、456 秒；
- 精确 timebase：`24000/1001`，这是校准工程构造值，不冒充源剧本元数据；
- 拆分后主轨 54 片段、10,933 帧、455.997208333 秒；与源语义只差 -0.002791667 秒；
- 逐镜按累计剧本时间量化，并满足 48/96/120fps 慢放所需 2/4/5 帧整除网格；最大中间边界误差 2 帧；
- 1 条独立音轨、6 条字幕、1 条 EP21 镜08 冻结嵌套覆盖层；
- 镜44 有 transform/cubic-bezier 关键帧并在 LinearTimeWarp 整数源帧网格上无损拆分；
- 主视觉含标准 `SMPTE_Dissolve`，短 proof 含 `LinearTimeWarp.1` 与独立音轨；
- master OTIO 导出回导保留 `24000/1001`、54 主片段、nested 和 transition。

同步 proof 和后台 master 都经过真实 FFmpeg 与 ffprobe：

| 成片 | 视频/音频 | 时长 | bytes | SHA-256 |
| --- | --- | ---: | ---: | --- |
| `EP22_formal_proof_sync.mp4` | H.264 360×640 + AAC 48kHz | 16.016s | 432,769 | `e295c8fad7b84d63f478c223c91974fd8ac137d9ddc2e456ca38713de476d3e3` |
| `EP22_formal_master_456s.mp4` | H.264 360×640 + AAC 48kHz | 455.997208s | 4,654,358 | `df8e6bfc61a0a1943720e80130cd6d017a1d9ad467d8134d79f43fc51719eae3` |

两份成片均有 Publication receipt，receipt 的 size/SHA 与现场文件一致。主片首、中、尾三帧已提取；fresh Node 进程用末帧建立 `enqueue:false` 的 timeline Continuation，末帧 SHA 与尾帧相同。最终通过 compiled MCP 按 revision CAS 显式取消未入队 pack，没有创建 GenerationJob，Doctor 恢复到 0 error / 0 warning。

## 5. Electron 与 MCP 真实恢复

最终 Electron evidence `0751` 在 MCP 把 master 推进到 r6 后执行，两次完整关闭/重启均要求 UI revision 与 calibration state r6 相同。直接断言包括：

- `24000/1001`、54 主片段、10,933 帧；
- 1 audio、6 subtitle、1 nested、2 个关键帧片段、20 个 timewarp 片段、1 个 SMPTE dissolve；
- nested inspector 显示冻结 SHA；
- `pageErrors=0`；
- 主预览 HTMLVideo `readyState≥2`，视频宽高与 currentSrc 有效；
- 首启和重启 preview 都不是黑帧，bright ratio 90.31%、chromatic ratio 12.99%。

主代理已用原始分辨率目视 5 张最终 PNG：首启/重启全图均真实显示授权参考板派生内容，独立 preview 一致；nested 图用于结构证据，其黑色预览区不被冒充为内容证据。

MCP 最终 evidence `0748` 证明：

- fresh source 与 fresh compiled 各 134 tools；
- 两边 `missingForFullNle=[]`；
- source 从 succeeded ledger 反推原 r5 CAS，幂等 replay 后保持 r6，不产生 r7；
- compiled 读取同一 r6、持久镜头备注、原 succeeded ledger；
- Continuation 清理有独立 succeeded 幂等账本；
- Doctor 为 0 error / 0 warning / 25 ok。

## 6. 失败关闭记录

正式 core 共使用 6 个全新隔离根，前 5 次分别在以下真实门禁失败，均未复用或覆盖：

1. 高帧慢放片段不能映射整数源帧；
2. 关键帧端点与产品 1ms 规范化口径不一致；
3. 同一端点问题的修复不足，随后按验证器真实顺序修正；
4. 镜44 正中拆分形成 109+109，破坏 0.5× 整数源帧；
5. master OTIO available_range 过度声明 576 帧，真实只可证明 575 帧。

第 6 个全新根完整通过。MCP smoke 的中间失败也都由幂等账本保护：业务写入或取消若已成功，后续只恢复/重放，不重复推进 revision。失败根保留作诊断历史，最终权威根仅为 `0729`。

## 7. 测试、构建与永久证据

| 门禁 | 结果 |
| --- | --- |
| 正式解析定向测试 | 1 file / 5 tests passed |
| 编辑器相关定向测试 | 3 files / 29 tests passed |
| capability/MCP 定向测试 | 4 files / 15 tests passed |
| `npm run typecheck` | passed |
| `npm run build` | Electron main/preload/renderer + compiled MCP passed |
| 全量 Vitest | 40 test files / 247 tests passed，0 failed |
| source Electron | 两次完整启动，r6，0 pageerror，人工视觉通过 |
| source/compiled MCP | 各 134 tools，r6，零 NLE 缺口，Doctor 0/0 |

| 永久文件 | bytes | SHA-256 |
| --- | ---: | --- |
| `docs/evidence/formal-project-nle-core-20260715-0729.json` | 61,906 | `4d74c97eee1a4b7b19d2c62e2b35c5f4de4d429b7eeed55e52ebf631e7cfb31c` |
| `docs/evidence/formal-project-nle-ui-20260715-0751.json` | 11,917 | `1fd60dbb10ddef5e76937f72ce3afcec9e97a40de9b8d709816f8241f0e8cc00` |
| `docs/evidence/formal-project-nle-mcp-20260715-0748.json` | 6,739 | `7de8c507c213e67ab21722e755c329e95f24912dbc8aef13a6308091f158c9e2` |
| `formal-source-manifest.json` | 145,332 | `f94e3d800052ae3985c5e792b43d016a7c78b98f387dd041659df59d550ab7b7` |
| `calibration-state.json` | 1,477 | `f7ca9f57072d1313231ed78f0460ee689ccc3846e9b1240d48c0a60268067b6b` |

最终 UI 还包含首启、preview、nested、重启和重启 preview 共 5 张 PNG；JSON 内绑定其路径、bytes、SHA 与像素门禁。

## 8. 能力合同与边界

`src/core/codex.ts` 的 `missingForFullNle` 已由 `['real-project-nle-validation']` 变为 `[]`，三组测试与 source/compiled MCP 运行证据一致。该结论只表示当前声明的本地 NLE 能力矩阵没有已知缺项，不表示整个长期产品目标完成，也不表示源目录拥有原生视频或音频。

旧 DMG 仍为 136,652,595 bytes，SHA-256 `ee7c78f16104b3881650353052669fa60c8fd4deb196cc5f49a2f1c765a20972`；本轮未生成、覆盖、安装、签名、公证或发布。Git 仍为 `main`、No commits yet、全项目 untracked；未 stage、commit、push 或创建 PR。

## 9. 下一唯一最高优先级

切片后现场审计未发现 src/tests/scripts 中的 TODO、FIXME、skip 或 todo；`missingForFullNle=[]`。长期完成矩阵中下一项是：

### P1：真实生图 / 图生视频网站闭环校准（`waiting_authorization`）

继续前需要用户明确提供：

1. 网站或域名，以及允许使用哪个已登录浏览器会话；
2. 允许上传的本地目录和具体文件范围；
3. 允许提交的任务类型、次数与是否允许付费；
4. 结果下载目录；
5. 提交后是否在视觉验收前暂停。

在取得这些授权前，不得登录、上传、点击提交、付费或下载，也不得用 mock、browser fixture、重复 package 或重复测试冒充真实网站闭环。长期 Goal 保持 `active`，不能因本切片完成而标记整体完成。
