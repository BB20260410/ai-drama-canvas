# 全项目行为保持重构 · Phase C 验证报告

## 结论

Phase C 已通过，整个重构计划仍为**执行中 / 部分完成**。MCP 的两层
`registerTool` monkey-patch 已改为显式 registrar，但 220 个工具的名称、顺序、Schema、
元数据、effect/gate 分类和受保护写入语义保持不变。

## 已完成改动

1. 新建 `src/mcp/tool-registrar.ts`，显式组合 runtime currentness/effect/metric 外层、
   guarded-write 内层和既有 command-bus。
2. `src/mcp/server.ts` 的 220 处工具注册机械切换为 `registrar.registerTool`，
   删除两层运行时改写；Resource 与 Prompt 继续由 SDK server 直接注册。
3. 保留 87 条 guarded map、3 个 payload normalizer、4 个 tool→command alias，以及
   历史未命中项 `migrate_fusion_storyboard_evidence`。
4. guarded Schema 继续分两次 `safeExtend/spread`；Zod 只做门禁，实际业务仍消费
   normalizer 后的原 payload，不使用 `parsed.data` 改变命令哈希。
5. 冻结当前源码 220 工具 ABI，总摘要为
   `71365a8259901be5c3d24c3e2c08363b7d3b7ecc7b2c14da587fc03e405c5c9c`。
6. `countDeclaredMcpTools` 仅扩展为识别显式 `server|registrar` 接收者，声明工具数仍为 220。

## 验证

- 实现代理定向回归：6 files / 18 tests PASS。
- 主线扩大影响复验：7 files / 22 tests PASS。
- release/build identity：2 files / 8 tests PASS。
- registrar/ABI：4/4 PASS。
- 当前源码 stdio：220 tools / 2 resources / 8 prompts PASS。
- `npm run typecheck`：PASS。
- `npm run typecheck:app`：PASS。
- `git diff --check`：PASS。
- 独立只读终审：CLEAN；未发现 P0/P1 或高价值 P2。

终审确认：currentness 拒绝发生在 bridge、Schema 和账本之前；guarded 工具不执行旧 handler；
gate、handler、flush 与 `isError` 的错误/metric 语义保持；旧 `dist-mcp` 没有被修改。

## 旧产物 smoke 的解释

`npm run mcp:smoke` 指向尚未重建的旧 `dist-mcp`，其中仍是改造前的源码计数规则，
因此不把它作为当前源码证据。本阶段规则禁止 build；当前代码已由 source stdio 直接验证通过。
最终 Phase H 构建后才会再次验证产物 smoke，不能在 Phase C 提前重建来掩盖边界。

## 数据与外部边界

- 未写正式工程、CAS、SQLite、Review 或生成账本；
- 未启动 App；
- 未调用外部服务、上传、付费或生成；
- 未构建 candidate、package 或安装版；
- 未执行 Git stage/commit/push/reset/clean。

## 后续

下一步只进入 Phase D：把 generation ledger 的 schema/bootstrap/storage 与同一 error class
抽到内部模块，让 continuity ledger 不再反向依赖完整业务 ledger。必须保持 schema v7、
SQL/迁移顺序、同一 SQLite、120 秒与 5 秒两种 timeout、测试 hook 单例和 class identity。
