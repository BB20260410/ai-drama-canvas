# Grok 复验收口（2026-08-02）

## 最终裁决

**CURRENT_SOURCE_AND_ISOLATED_PACKAGE_VERIFIED；GROK_SESSION_RESTART_REQUIRED；INSTALLED_APP_NOT_REPLACED。**

Grok 报告中的三类问题需要分开裁决：

1. 当前源码与 current MCP 已通过，最终身份为 `3f7964a892e9a9f36383484a13c87dee86518fe8fcd838e11b66acfd23c6bbfe / 8ab98fd6c950cf307e5e59d6fb0ed1c8 / 213 tools`。
2. Writing OS 驾驶舱与候选 Diff 裁决台已在当前源码、真实 Electron 和同源隔离 `.app` 中验证，不再属于“只有 CLI、没有桌面闭环”。
3. Grok 的既有会话仍须重启或重连；本轮不能替外部 Grok 会话热换 MCP。`/Applications/AI 漫剧画布.app` 也仍是旧安装版，本轮没有获得覆盖安装授权。

本轮没有把“机械闭环通过”扩大表述为“百万字文学与人物语义永不漂移”。人物声口/外形硬锁、禁揭扫描、陌生 AI 的单入口章租约、旧章修订后的状态谱系重建，仍是后续产品能力。

## 本轮实际修复

### 1. 修复安装包内 MCP 误判 `source-changed`

- `src/core/runtime-write-gate.ts`
  - 启动身份显式区分 `workspace` 与 `release-manifest` 两种来源。
  - 安装包运行时以同目录发行清单的 `sourceDigest` 做 currentness 校验，不再扫描安装包里本来就不存在的源码树。
  - 发行清单缺失、损坏或 digest 漂移仍失败关闭。
- `src/mcp/server.ts`
  - 仅当显式发行清单与运行 workspace 同目录时采用安装包模式。
  - current candidate 继续采用源码 workspace 模式，因此仍能发现 Grok 旧会话的源码漂移，未削弱门禁。
- `tests/runtime-write-gate.test.ts`
  - 增加安装包清单模式、清单漂移拒绝的定向覆盖。

### 2. 让同源隔离打包成为可复现验收入口

- `scripts/isolated-package-smoke.ts`
  - 不再把历史 DMG 是否存在当作运行前置条件；存在或不存在都必须在验收前后保持不变。
  - 使用短路径 `/private/tmp/aic-pkg-*`，规避 `tsx` Unix socket 路径上限和 electron-builder 超长 glob。
  - stage 输入从权威源码 digest 列表派生，纳入此前遗漏的 `tsconfig.app.json`、`tsconfig.production-runs.json`。
  - 打包前强制对比 workspace 与 stage 的完整源码身份。
- `scripts/isolated-package-guards.ts`、`scripts/lib/owned-fixture-root.ts`
  - 在 Darwin 上显式识别 `/private/tmp` 的 canonical temp 路径，同时继续要求 owner marker 与 inode 一致。
- `tests/isolated-package-guards.test.ts`、`tests/owned-fixture-root.test.ts`
  - 增加对应路径与所有权保护测试。

### 3. 修复真实 Electron 首次启动竞态

- `scripts/ui-review-content-identity-smoke.mjs`
  - 兼容首次启动自动打开项目与测试脚本点击最近项目的竞态。
  - 最终 Review Studio 可见性仍是硬门，没有把 UI 缺失降级为通过。
- `tests/mcp-editor-effect-transition.test.ts`
  - 失败时输出完整工具响应，便于定位安装包内 currentness 门的真实拒绝原因。

## 决定性证据

### A. current MCP

`npm run mcp:current:check`：PASS

```text
candidateId = mcp-candidate-3f7964a892e9a9f3-305af2c54bd5fa4c-8273d4ee
sourceDigest = 3f7964a892e9a9f36383484a13c87dee86518fe8fcd838e11b66acfd23c6bbfe
buildId = 8ab98fd6c950cf307e5e59d6fb0ed1c8
mcpToolCount = 213
entrySha256 = 06d1fcaced1019611d9167155ef03d9801a4ac2120bc50c776a675551ebbf8cd
invalidCandidates = 0
```

### B. 同源隔离 `.app` 全链验收 R2

主证据：

- `docs/evidence/novel-mode-v1/writing-os-desktop-p0/packaged-current-source-20260802-grok-recheck-r2.json`
- `docs/evidence/novel-mode-v1/writing-os-desktop-p0/packaged-current-source-20260802-grok-recheck-r2-electron-ui.json`
- `docs/evidence/novel-mode-v1/writing-os-desktop-p0/packaged-current-source-20260802-grok-recheck-r2-review-ui.json`

裁决：`status=passed`

- workspace 与 stage 完整身份相同：`948 files / 19,484,077 bytes / sourceDigest=3f7964…`。
- 安装包 MCP：`213 tools`，effect-transition integration 通过。
- 空工程完整 MCP 生产链重启后可恢复，`uncertainCommands=0`。
- 打包 Electron UI 保存、撤销、重做、应用重启恢复全部通过。
- Review Studio 旧内容提交被拒绝、刷新哈希、正式通过、重启恢复和历史保留全部通过，`pageErrors=0`。
- 临时根目录、隔离 registry、媒体 runtime、userData 与残留进程均完成清理。
- 正式工程、外站、上传、付费、安装、发布、DMG、签名、公证均未使用或执行；workspace `dist` 未覆盖。

R2 是当前权威包证据。R1 因 stage 少纳入两个 TypeScript 配置文件，虽流程通过但不作为“完整同源”证据。

### C. Writing OS 驾驶舱与 Diff R6

证据：

- `docs/evidence/novel-mode-v1/writing-os-desktop-p0/desktop-writing-os-p0-20260802-r6/electron-smoke.json`
- `docs/evidence/novel-mode-v1/writing-os-desktop-p0/desktop-writing-os-p0-20260802-r6/01-dashboard-state-debt.png`
- `docs/evidence/novel-mode-v1/writing-os-desktop-p0/desktop-writing-os-p0-20260802-r6/02-candidate-diff.png`
- `docs/evidence/novel-mode-v1/writing-os-desktop-p0/desktop-writing-os-p0-20260802-r6/03-state-accepted.png`

裁决：`verdict=PASS`

- 写前状态债与机械冲突可见。
- 候选 Diff 显示人物状态八项及正文证据。
- reviewer 由 Main 固定注入为 `desktop-human-owner`。
- 人工接受后 completion 为 `committed`、探针为 `pass`、待裁决候选为 `0`。
- 三张 `1728×1029` 截图已做原尺寸人工复核，无明显遮挡、截断、空白或错误状态；页面与控制台错误均为 `0`。

## 验证命令与结果

- `npm run typecheck`：PASS。
- `npx vitest run tests/runtime-write-gate.test.ts tests/isolated-package-guards.test.ts tests/owned-fixture-root.test.ts tests/mcp-editor-effect-transition.test.ts --maxWorkers=1`：`4 files / 28 tests PASS`。
- 较宽回归：`13 files / 83 tests PASS`；此后只调整隔离打包源码输入清单，当前最终版又通过上述 `4/28`、typecheck 和完整 R2 隔离包验收。
- `npm run build`：PASS；最终 release manifest 为 `3f7964… / 8ab98… / 213`。
- `npm run mcp:candidate:build && npm run mcp:current:check`：PASS。
- `NOVEL_WRITING_OS_UI_RUN_ID=desktop-writing-os-p0-20260802-r6 npx tsx scripts/ui-novel-writing-os-dashboard-smoke.ts`：PASS。
- `git diff --check`：PASS。

## 安装版边界

当前 `/Applications/AI 漫剧画布.app` 没有被覆盖，仍是旧版：

```text
sourceDigest = b4439e7bd9032caf8a0c511c6016dc46e868d0ebd2e3561db8486b0a32801d22
buildId = 38dbb556ec8ff28a7e669ba2e72b8e8b
mcpToolCount = 202
app.asar sha256 = 0221689c653895bba3df28c2e1ee98f8416fec3ff6c8b8ba00b8f30af2667dce
app.asar bytes = 10,301,700
app.asar mtime = 2026-07-28 07:50:26
```

因此：

- 验收“当前源码能否打成真实可运行 App”：**通过**。
- 验收“用户现在从 `/Applications` 打开的 App 是否已是当前版”：**未通过，尚未安装**。
- 本轮生成的是隔离、未签名、未公证的测试 `.app`，验证后已删除；不能冒充正式分发包。

覆盖 `/Applications` 属于外部安装状态变更，必须在用户明确授权后另做，并在安装前后分别核对 bundle、manifest、哈希与启动实况。

## 对 Grok 原报告逐项回应

| Grok 项目 | 本轮裁决 |
|---|---|
| CLI/源码 Writing OS 可用 | 保持 PASS，并更新到最终 `3f7964… / 8ab98… / 213` |
| Grok 会话 MCP stale | 仍需重启/重连；不能由仓库内修复替代会话刷新 |
| 驾驶舱 / Diff 裁决台未齐 | 当前源码与隔离 App 已用 R2、R6 证明交付 |
| 桌面安装版勿默认等同 current | 正确；安装版实测仍是 202-tools 旧版 |
| formal baseline / brief / cast 未锁 | 仍是正式内容数据前置条件；门禁正确阻断，不自动伪造锁版 |
| 声口外形硬锁 / 禁揭扫描 | 本轮未交付，仍是产品缺口 |
| 百万字永不漂 | 不作绝对保证；机械一致性显著增强，但下列 P0 尚未闭环 |

## 百万字一致性仍需完成的 P0

本轮多代理只读复审确认，下面三条不能被 UI/打包通过掩盖：

1. **陌生 AI 单入口与并发归因**：需增加 `prepare_novel_chapter_write`、章级 lease/fence、actor attribution，以及 MCP/JSON 共用的结构化 `nextTools`，否则多 AI 仍需自行拼 SOP，且章级互斥与身份审计不足。
2. **旧章修改后的状态谱系重建**：当前能发现旧 completion 的 revision/SHA 过期，但已推进至后文时无法安全 rewind 后依次重建；需 append-only event/checkpoint、shadow lineage、intent recovery 和 200→500 章回归。
3. **状态候选与受管 Story Bible**：需候选 V2 audit scope、accepted 前重读真实正文、完整 cast/no-change 审计，以及 owner 裁决的 entity/hard-canon/chapter-brief/人物声口 profile 演化路径。

这些能力完成前，准确产品口径是：**可用的 Writing OS 机械闭环与桌面裁决台，能够显著降低长篇漂移；不能单独保证一百万字在任意改稿、任意多 AI 并发下绝对一致。**

## 给 Grok 4.5 的复验提示词

```text
请新开或完整重启 Grok 会话，并重新连接：
/Users/hxx/Documents/无限画布

第一步只调用 get_capabilities。必须同时满足：
- toolCount = 213
- buildCurrentness.allowed = true
- candidateId = mcp-candidate-3f7964a892e9a9f3-305af2c54bd5fa4c-8273d4ee
- sourceDigest = 3f7964a892e9a9f36383484a13c87dee86518fe8fcd838e11b66acfd23c6bbfe
- buildId = 8ab98fd6c950cf307e5e59d6fb0ed1c8

再在仓库只读运行 npm run mcp:current:check，确认两边身份完全一致。

然后只读核验：
1. docs/evidence/novel-mode-v1/writing-os-desktop-p0/packaged-current-source-20260802-grok-recheck-r2.json
2. docs/evidence/novel-mode-v1/writing-os-desktop-p0/desktop-writing-os-p0-20260802-r6/electron-smoke.json
3. 三张 R6 截图原尺寸状态：状态债/机械冲突 → 八项 Diff → accepted 后候选清零与 probe pass。

请分别裁决：
- LOCAL_CURRENT
- GROK_SESSION_MCP
- CURRENT_SOURCE_ISOLATED_APP
- WRITING_OS_DASHBOARD_DIFF
- INSTALLED_APPLICATION
- MILLION_WORD_ABSOLUTE_GUARANTEE

边界必须保持：
- 不写《黑页》正式源，不把 pilot 同步回源。
- 不把隔离 unsigned .app 说成已安装、已签名或已公证。
- /Applications/AI 漫剧画布.app 仍是 202-tools 旧版，除非另有安装后新证据。
- 声口外形硬锁、禁揭扫描、陌生 AI 章租约、旧章状态谱系重建尚未完成，不得裁决“百万字绝对一致”。

若会话仍返回 209/旧 build 或 allowed=false，请裁决 GROK_SESSION_MCP_STALE 并禁止会话写入；不要用旧会话结果否定 current 源码证据。
```

## 工作区与授权边界

- 未修改《黑页》正式源目录。
- 未覆盖 workspace `dist`、历史安装包或 `/Applications`。
- 未签名、未公证、未发布、未上传、未付费。
- 未 Git 暂存、提交或推送。
- 工作区进入本轮前已有大量未提交修改；本轮只在上述修复文件和证据目录内增量工作，没有清理或重置用户已有变更。
