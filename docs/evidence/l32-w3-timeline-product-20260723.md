# L32 W3 · 时间线产品化

## 交付

- 多单元 sequence 排布 + 系统边（既有 `buildStudioCanvasTimelineLayout`）
- 新增 `filterStudioCanvasTimelineProgress`：unitId / 角色 / review 状态
- UI `data-testid="managed-canvas-panel-timeline"` 保持；reviewDecision 类型修复

## 测试

- `tests/studio-canvas-timeline-layout.test.ts`（含多单元过滤）
