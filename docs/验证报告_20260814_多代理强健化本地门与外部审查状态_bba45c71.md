# 多代理强健化：本地门完成，外部审查通道阻塞

## 结论

候选 `bba45c71… / 90941534… / 220 tools` 的本地交付门已全部通过：完整四分区、审计、构建、MCP、P5、P17、T23 strict interactions、隔离 Electron 与隔离 package 均绑定同一源码摘要。

三席外部审查没有完成：Kimi K3 返回 `PASS`、无 P0–P2 finding，但无可信服务端身份回执且未重跑门，标记 `IDENTITY_NOT_VERIFIED`；GLM 5.2 与豆包在本机适配器连接阶段失败，均没有模型正文或可验证身份。因此本轮状态为 **部分完成：本地交付 PASS，外部审查 BLOCKED**，不能称“三模型终审完成”。

## 同身份本地验证

| 门 | 实际结果 |
|---|---|
| 源码 / 构建身份 | `bba45c715c035b7c24e09796ec01e9208a7a5ffb77541f001f4b8f8f032c50ef`；buildId `90941534f2a5d3eb617fadd5386f2c0b`；220 tools |
| 分区 | fast 263/1524、medium 91/788、integration 36/127、heavy 5/18，全部 PASS |
| 安全与构建 | production audit 0 漏洞；build PASS；live digest 与 manifest 一致；MCP smoke 220 tools PASS |
| P5 | 隔离副本、源 sentinel 不变、视频/音频实际推进、0 page/console/external error，PASS |
| P17 | 22 条导航与支持路径，PASS |
| T23 | 36 单元；首卡 422ms、首 raw 3534ms、全参考 4493ms、IPC 峰值 4；strict interactions PASS |
| 隔离交付 | Electron 43.1.0 arm64 provenance PASS；isolated package PASS，未安装/发布/上传/付费 |

## 外部审查事实

- Kimi K3：实调 `kimi-code/k3`，结论 `PASS`、findings `[]`。它只核对冻结包与引用证据，未复跑门；CLI 输出没有可信 provider identity receipt，故 `IDENTITY_NOT_VERIFIED`。
- GLM 5.2：实调两次均在本机 `novel_chat.py` 的 Python 3.14 `urllib` 调用处失败：timeout tuple 不能被 `socket.settimeout` 接受。没有到达 provider，没有正文、模型 ID 或 finding，状态 `BLOCKED`。
- 豆包：实调一次，在同一适配器路径发生同一错误。没有到达 provider，没有正文、模型 ID 或 finding，状态 `BLOCKED`。

这些失败是外部审查适配器的运行时问题，不是本候选的产品红测。未经新授权，不修补用户目录下的适配器；后续只需在适配器可用后对同一未变摘要各提交一次审查，不应重跑已绿产品链。

## 边界

未写正式工程、未安装 App、未发布/上传、未付费生成、未 Git stage/commit/push，未删除 `.analysis-src.tgz`、未跟踪文件或历史 evidence。

结构化证据：`docs/evidence/multi-model-hardening-closeout-20260814-bba45c71.json`；脱敏审查包：`docs/evidence/multi-model-review-pack-20260814-bba45c71.json`。
