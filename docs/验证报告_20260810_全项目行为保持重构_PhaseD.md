# 全项目行为保持重构 · Phase D 验证报告

## 结论

Phase D 已通过，整个重构计划仍为**执行中 / 部分完成**。generation ledger 的纯合同与
v7 schema/bootstrap/storage 已从业务 owner 中分离，Active Studio 四模块运行时循环已解除；
数据库 Schema、路径、SQL、迁移和正式数据均未改变。

## 已完成改动

1. 新建 `studio-generation-ledger-contract.ts`，作为 error class、结果 variant 与 ledger state
   的唯一构造器/类型来源；旧 ledger 兼容 re-export，同一 `instanceof` 身份保持。
2. 新建 `studio-generation-ledger-storage.ts`，承载 v7 DDL、v1–v7 迁移、preflight、
   sidecar/inode/CAS identity、事务、cache、WAL 和 writable-open test hook。
3. `studio-continuity-ledger.ts` 改为依赖 storage 初始化，不再反向导入完整 business ledger；
   generation 连接仍先关闭，continuity 再以原 5 秒 timeout 打开同一数据库。
4. detached observation/disposition 深身份校验由 storage 单一实现，业务读写复用，未弱化。
5. `beforeImagegenIntentTransactionHookForTests` 继续留在业务 ledger；storage writable hook
   保持单例并由旧入口直接 re-export。

## 字节与状态合同

- Schema 仍为 v7；路径仍为 `.aicanvas/studio-generation-ledger.sqlite`。
- `createCurrentSchema` 与抽取前逐字节相同，SHA-256 为 `dda5eefd…07c8`。
- 3 个建表函数和 6 个迁移函数共 9 个函数与 HEAD 旧实现逐字节相同。
- `BEGIN IMMEDIATE`、rollback、marker、cache identity、foreign keys、synchronous/WAL 顺序保持。
- generation busy timeout 仍为 120 秒；continuity 仍为 5 秒。
- outbox、waiver、continuity 继续是同库 extension，不纳入 core v7 object 集合。

## 验证

- source module boundaries：7/7 PASS。
- generation ledger 历史迁移/冲突/回滚：35/35 PASS。
- continuity / continuity gate / unit-grid read epoch：3 files / 23 tests PASS。
- 主线邻接复验：4 files / 30 tests PASS。
- TypeScript AST：Active Studio SCC `4 → 0`；只剩历史 legacy/fusion 15 模块 SCC。
- `npm run typecheck -- --pretty false`：PASS。
- `npm run typecheck:app -- --pretty false`：PASS。
- `git diff --check`：PASS。
- 最高风险独立终审：CLEAN；未发现 P0/P1 或高价值 P2。

## 纠正记录

机械抽取首轮遗漏了 business CAS 的 `ensureConfinedDirectory` import；第二个失败链把
provider normalizer 的 owner 放错。两者均表现为确定性 `ReferenceError`，分别统一修正后
35/35 通过；没有改 SQL、DDL、迁移或降低测试门禁。

## 数据与外部边界

- 所有 SQLite 测试使用临时夹具；未打开或迁移正式工程数据库；
- 未启动 App、未调用外部服务、未上传或付费；
- 未 build、candidate、package、安装或公证；
- 未执行 Git stage/commit/push/reset/clean。

## 后续

下一步进入 Phase E，严格按“公共 UI 类型合同 → 纯只读 mapper → 旧画布纯投影”三个
独立切片执行。不得直接搬迁受管画布的 formal PASS、root/generation token、分页或 Review
裁决，也不得让 Renderer 重新推导 Core 的 `nextAction`。
