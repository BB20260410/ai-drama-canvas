# 验证报告：P1 可恢复生图执行状态机

时间：2026-07-17（Asia/Shanghai）  
工作区：`/Users/hxx/Documents/无限画布`  
正式工程：`/Users/hxx/Documents/无限画布/productions/gushujuan-s3-f1a688020bfb7af6`

## 结论

P1“可恢复的生图执行状态机”已完成并通过完整纵向验收。

一图一子代理执行现已使用可证明的租约 owner CAS、心跳、TTL、fence、续租、
释放和过期接管；模型调用前后分别持久化 call intent 与 call receipt。无法证明
是否已调用供应商的任务进入 `generation_unknown`，不得自动领取、取消、接管、
重试或再次调用。生成结果先进入任务隔离候选目录，只有人工视觉通过后才以
raw/labeled 双成员 Publication bundle 原子登记；拒绝、崩溃或目标冲突均不留下
半套正式产物。

## 正式卡死任务安全处置

正式样本：`gen-2026-07-16T12-10-57-215Z-892023c0`

- Job ID 与 attempt 保持不变，仍为 attempt 1。
- 旧 `waiting_external/leased` 因缺少模型调用前 intent 与调用后 receipt，无法证明
  未发生远端调用，已迁移为 `generation_unknown`。
- 未伪造 call intent、call receipt、远端身份、候选图或正式结果。
- 旧 lease 原样保留用于审计；旧协议没有 owner/TTL/fence，因此不具备安全接管资格。
- raw 与 labeled 两个目标已组成同一 bundle，两个 intent 均为 `reserved`，均无 receipt。
- 两个正式目标文件都不存在，隔离候选目录为空。
- 迁移与验证过程没有调用 image_gen、Artlist 或其他供应商，也没有认领来源不明图片。

EP01_008 当前仍为 4/6：宫格 01–04 `awaiting_review`、宫格 05
`generation_unknown`、宫格 06 `missing`，单元保持 `待尾帧`，不能进入完整 Review
或正式成板。

## 产品闭环

- Core：GenerationJob v2 执行检查点、租约 CAS/TTL/心跳/fence、未知态对账、候选视觉验收与安全迁移。
- Publication：raw/labeled bundle 预检、扩展、登记、取消与失败均为事务式、幂等且禁止覆盖。
- 并发：项目级与供应商级并发均固定为 1；未知态不计作可重提工作。
- MCP：任务摘要、Doctor、统一 Snapshot、恢复 Prompt 与 compiled server 均公开未知态、候选态、双成员 bundle 和下一安全动作。
- UI：队列只读刷新；未知态显示原租约和对账要求，隐藏重试、取消与接管；候选态提供明确视觉通过/拒绝入口。
- 迁移：先在隔离副本演练，再备份并迁移正式 sidecar；投影刷新仅把宫格 05 从旧 `generating` 修正为 `generation_unknown`。

## 正式工程结果

- GenerationJob：30 个，26 `succeeded`、3 `failed`、1 `generation_unknown`。
- Publication：31 个 intent，26 个 receipt；其中未知任务的 raw/labeled 两个 intent 保持 reserved。
- 正式图片：26 raw + 26 labeled，仍为 26 对；没有新增图片。
- Doctor：0 error / 1 expected warning / 26 ok；唯一 warning 要求对未知任务做结构化人工对账。
- 正式 guard 文件在最终类型、测试、构建、UI 与 MCP 烟测前后完全一致。

## 验证结果

- 类型检查：通过。
- P1 定向：9 files / 76 tests，通过。
- 全量：49 files / 302 tests，通过。
- production build / MCP build：通过。
- 正式 Electron UI：未知态、旧 lease 不可接管、操作按钮失败关闭、双成员 bundle、并发 1、0 page error 和无横向溢出均通过。
- compiled MCP：未知态可观测，Doctor/Snapshot/Continuation/恢复 Prompt 均优先对账且禁止重放。
- 第三季只读源前后均为 3344 files / 24,570,877 bytes /
  `649160f22663ca4c45ee4a4084e278ef0edc61ec66db01bb84da38cbea3f8d26`。

## 证据

- 机器总验收：`docs/evidence/final-validation-20260717-p1-generation-reliability.json`
- 正式迁移：`docs/evidence/p1-generation-execution-migration-20260717.json`
- 隔离演练：`docs/evidence/p1-generation-execution-migration-rehearsal-20260717.json`
- 投影刷新：`docs/evidence/p1-generation-projection-refresh-20260717.json`
- UI：`docs/evidence/p1-generation-recovery-ui-final-20260717.json`
- UI 截图：`docs/evidence/p1-generation-recovery-ui-final-20260717.png`
- compiled MCP：`docs/evidence/p1-generation-recovery-mcp-final-20260717.json`
- 可重复验证器：`scripts/validate-p1-generation-reliability-final.ts`

## 下一唯一最高优先级

进入 P2“逐宫格引用闭包”。为全季当前 4330 格建立唯一
`PanelReferenceResolution`，合并分镜行、原镜时间段、连续性轨、硬锁版本与人工覆盖；
已知人物、场景和道具不得静默退化为 text-only，超过 6 项必须使用经审核、可追溯的
派生组合资产。P2 必须把未解析、已知资产漏绑、未处理溢出和时间段连续性不一致
四项审计都降为 0。P0–P4 全部通过前继续冻结扩大正式生图。
