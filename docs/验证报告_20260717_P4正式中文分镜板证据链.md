# 验证报告：P4 正式中文分镜板证据链

日期：2026-07-17（Asia/Shanghai）  
工作区：`/Users/hxx/Documents/无限画布`  
正式工程：`/Users/hxx/Documents/无限画布/productions/gushujuan-s3-f1a688020bfb7af6`

## 1. 结论

P4 已完成并通过完整纵向验收。正式中文分镜板 v2 已贯通当前宫格合同、P3 requirement、有效 Review、GenerationJob、Publication、raw/labeled SHA、渲染策略、内容寻址输出、正式 Artifact、Scanner、命令总线、MCP、IPC 和 Electron UI。

正式 EP01_001 正确保持 `current=0`：现有 4 格图片没有当前 P3 的 28 条逐规则人工证明，因此未伪造新的 formal v2 current 板。两个历史版本被如实登记为 `stale` 与 `legacy-invalid`，不能成为权威缩略图或下游完成证据。EP01_008 保持 4/6，宫格 05 为 `generation_unknown`、宫格 06 为 `missing`，不得成板或重试不明任务。

P4 没有访问浏览器或供应商，没有生成图片，没有新增 Review/Publication，没有回写第三季只读源。

## 2. 成板身份与失效模型

- v2 `sheetId` 由项目内容地址、当前合同、P3 requirement、有效 Review、每格 Job/Publication/raw/labeled 身份和渲染策略派生。
- 正式输出使用含 `sheetId` 的新目录；同一输入幂等复用，输入变化产生新版本，禁止覆盖。
- PNG、SVG 和 receipt 同时登记为正式 Artifact；只有机械验真且当前的完整组才能 authoritative/accepted。
- 任一宫格重做、Review/requirement 漂移、Publication 身份不一致、raw/labeled SHA 漂移或渲染策略改变，旧板立即派生为非 current。
- P1 raw/labeled bundle 任务必须同时核对 primary/companion intent、receipt、bundleId、member、路径与 SHA；历史 non-bundle 任务不得通过删除 bundle 字段降级绕过。

## 3. 渲染与路径安全

- 正式图片默认 `contain`，不允许无记录 `cover`。
- crop 必须显式给出原因和归一化焦点或裁切矩形，并冻结到 receipt；重启后不传 placements 仍保留已审计策略。
- 2 格和 6 格长中文均先测量后动态布局；文本无法完整容纳时失败关闭，禁止用省略号静默删除动作、台词或连续性。
- `infoPath`、单元父目录和 `AI画布生成` 输出目录在任何 `mkdir/rename` 前均执行 realpath containment 与 symlink 拒绝。
- 回归已证明：篡改 index 使单元目录或输出子目录指向工程外时，命令在任何外部写入前拒绝。

## 4. 历史迁移与正式 Scanner 投影

正式 store：`.aicanvas/storyboard-sheet-index.json`

- revision：1
- records/current：0 / 0
- legacy records：2
- store 文件：3,947 bytes
- store SHA-256：`11533501e7a2a6dedd1dbfa6f108c3a4e8f02141bafbaa16aa706125832e3fd6`
- 候选指纹：`76d02dcf4bf5a789f8dc2155843b944192d09d9d035d882f613050cccd6e4fbd`
- 首次迁移新增 2 条，同幂等键严格重放零写入。

正式 `.aicanvas/index.json` 已安全刷新并与随后只读复扫精确一致：

- P4 Artifact：4（EP01_001 恰好 4，EP01_008 为 0）
- 版本汇总：`current=0 / stale=1 / invalid=0 / legacy-invalid=1 / pages=1`
- 4 个 Artifact 均机械验真，且 `authoritative=false / accepted=false / deprecated=false`
- 受保护工程文件与第三季只读源在刷新前后不变。

## 5. Core、命令总线、MCP 与 UI

- Core 公开 state/list/migrate/render，返回 readiness、当前指纹、历史状态和结构化阻塞。
- render 必须携带 state 签发的 `expectedInputFingerprint`；旧指纹、孤儿输出和 CAS 冲突均失败关闭。
- 命令总线只在当前 store/receipt/输出 SHA/当前输入指纹可重建时恢复已持久结果；证据不足保持 `unknown`，绝不重复渲染。
- source/compiled stdio MCP 均为 165 tools；正式烟测验证 state/list schema、受保护迁移重放和零 render/generation 调用。
- Electron UI 明确显示 EP01_001 的 `current 0/1` 与 stale/legacy-invalid 历史；EP01_008 显示 4/6，宫格 05 unknown 入队按钮禁用，宫格 06 missing 入队按钮可用，烟测未点击任何写操作。
- UI 记录区间内 page error 为 0、外网请求为 0；验证器没有把 `firstWindow` 之前的启动期网络误报为已覆盖。

## 6. 测试与最终验收

- typecheck：通过
- P4 定向：8 files / 78 tests，无 skip
- 全量：55 files / 352 tests，无 skip
- production build / MCP build：通过
- compiled MCP：165 tools
- 七项独占关账命令：全部 exit 0，14 份 stdout/stderr 日志均冻结 SHA
- 源码验证窗口：238 files，聚合 SHA-256 `ba8bdb25f1f09932c0661eafd0d263f54fdb6c9c6e76e05c50efd65a8dc1b851`，前后不变
- P2 revision 2 的 1288 合同 / 4330 格与 P3 revision 1 的 4330 约束在验证前后仍严格 current

最终验证器首次运行在写 final 前发现它把全局迁移候选指纹错与按 item 限定的预览指纹比较，正确失败关闭。修正为与当前全局预检比较后，唯一正式关账运行全部通过。

## 7. 源目录与零外部副作用

第三季只读源在验收前后均为：

- 3344 files
- 24,570,877 bytes
- 聚合 SHA-256：`649160f22663ca4c45ee4a4084e278ef0edc61ec66db01bb84da38cbea3f8d26`

正式账保持 30 个 GenerationJob（26 succeeded / 3 failed / 1 generation_unknown）、31 个 Publication intent、26 个 receipt、20 条历史 Review、26 raw + 26 labeled。未知宫格 05 的双成员 reserved bundle 仍未被认领。

## 8. 权威证据

- `docs/evidence/final-validation-20260717-p4-storyboard-sheet.json`
- `docs/evidence/runs/p4-storyboard-sheet-final-20260717/`
- `docs/evidence/p4-fusion-storyboard-sheet-migration-final-20260717.json`
- `docs/evidence/p4-storyboard-sheet-index-refresh-final-20260717.json`
- `docs/evidence/p4-storyboard-sheet-mcp-final-20260717-r2.json`
- `docs/evidence/p4-storyboard-sheet-ui-final-20260717-r2.json`
- `docs/evidence/p4-storyboard-sheet-ui-final-20260717-r2-ep01-001.png`
- `docs/evidence/p4-storyboard-sheet-ui-final-20260717-r2-ep01-001-ep01-008.png`
- `scripts/validate-p4-storyboard-sheet-final.ts`

## 9. 下一唯一最高优先级

P5“规范资产知识库”：建立唯一 `CanonicalAsset / Alias / AssetVersion / Authority` 模型，以真实文件和 SHA 为媒体事实源，无损迁移现有 77 项角色/场景/道具与权威图，让 Core、MCP 和 UI 读取同一规范资产视图。

P5 开始后仍冻结浏览器、外部生图、上传、付费和正式批量生产；P4 软件关账不等于第三季 1288 单元成片完成。
