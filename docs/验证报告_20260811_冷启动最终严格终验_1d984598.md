# 冷启动最终严格终验（1d984598）

## 结论

本轮有界冷启动整改为 **部分完成 / 严格性能门失败**。

- T23 证据链、同 Renderer 原子取证、CSP 兼容、36 单元 / 4 raw / 4 reference、交互、IPC 排空与错误监控均闭合。
- 严格性能门仍有两项未通过：首卡 `2010ms > 1500ms`，首 raw `5635ms > 5000ms`。
- 全参考 `7020ms <= 8000ms`、投影 IPC 峰值 `4 <= 4`、全部交互预算均通过。
- 本源码身份只运行这一次严格 build-mode T23；失败后没有改一处再重跑，也没有进入 candidate、package smoke 或安装。

决定性证据：

- `docs/evidence/runtime-stability-t23-build-strict-final-20260811-1d984598.json`
- SHA-256：`811f7fc2c190e2e68301d3196f0a2e135118f7e316a9efc97037e384c11e7938`

## 本轮实际完成

1. 启动安全与性能路径
   - 受管工程启动对账保持 root + activationId CAS、manifest fail-close 与原子 UI 提交。
   - 启动对账移除重复 activation fence，只保留 project-registry 临界区。
   - units 预取仅在 startup reconcile 成功且 workspace 为 drama 后启动；模块 warm 仍可提前并行。
   - Dashboard units 热路径减少外层重复 generation-ledger 初始化，完整只读校验仍 fail-close。
2. T23 测量可靠性
   - 首卡使用真实单元节点 DOM 插入里程碑，不再使用 CDP 后置轮询值。
   - raw/reference 使用单调 span、root/flight/currentness 与 latest-span fail-close。
   - timeline、IPC、产品只读 hook 与 raw snapshot 在同一 Renderer 文档原子读取。
   - 页面函数迁入原生 `.mjs`，Playwright 直接序列化函数对象；不使用字符串表达式、`eval` 或 tsx `__name` helper。
   - 初始 / cycle / 最终 `performance.timeOrigin` 一致，`reloadCount=0`；单次 30 秒只作为证据闭合截止时间，不放宽 1.5 / 5 / 8 秒性能预算。

## 验证结果

- `npx vitest run tests/t23-scale-performance-contract.test.ts tests/t23-raw-reference-span.test.ts tests/managed-studio-canvas-ui.test.ts --maxWorkers=1 --reporter=dot`：3 files / 60 tests PASS。
- 启动对账与 units/coalescer 相邻定向矩阵：已分别通过，独立只读复核均为 CLEAN。
- `npm run typecheck`：PASS。
- `npm run typecheck:app`：PASS。
- 严格 CSP headless Chromium canary：`script-src 'self'` 下直接传 `.mjs` 页面函数，PASS。
- `node --check scripts/lib/t23-renderer-cycle-page-function.mjs`：PASS。
- `git diff --check`：PASS。
- `npm run build`：PASS；身份：
  - sourceDigest `1d9845980776fe57e29922d0c777b627e15110e0d457e22829a0fd3b9ce77298`
  - buildId `0635db91d152e36d2fc6b01935289623`
  - MCP tools `220`

严格 T23 的机械与交互结果：

| 门 | 实际 | 预算 | 状态 |
|---|---:|---:|---|
| build 到 CDP | 892ms | ≤30000ms | PASS |
| 首单元卡 DOM | 2010ms | ≤1500ms | **FAIL** |
| 首 deep-verified raw | 5635ms | ≤5000ms | **FAIL** |
| 全部 4 个 PASS reference | 7020ms | ≤8000ms | PASS |
| 投影 IPC 峰值 | 4 | ≤4 | PASS |
| 暖切换 P95 | 61.69ms | ≤500ms | PASS |
| 缩放反馈 P95 | 49.24ms | ≤100ms | PASS |
| 拖动反馈 P95 | 3ms | ≤100ms | PASS |
| 平移帧 P95 | 9.1ms | ≤20ms | PASS |
| long task | 0 | ≤200ms | PASS |

`consoleErrors=[]`、`pageErrors=[]`、IPC 29/29 完成且最终 outstanding=0。运行后未发现 T23/Electron 残留进程，隔离 fixture 已删除。

## 有证据的瓶颈

- runtime gate：`108.9 → 711.8ms`，约 `602.9ms`。
- startup reconcile：`718.2 → 1252.8ms`，约 `534.6ms`。
- units 读取：`1282.4 → 2010.0ms`，约 `727.6ms`；首卡随其完成落地。
- 首卡后的 overview：约 `1057.2ms`。
- raw activation 到 span start：约 `1055.6ms`。
- span start 到首 raw：约 `1512.3ms`。

独立 projection bundle 往返为 `4492.8ms`，Core total `4213.3ms`；它是后台深投影慢路径，但本轮交互门仍通过，不能把它单独当成首卡根因。

## 边界与恢复规则

- 当前证据是 build-mode，不证明已安装 App 的相同性能；当前安装版仍为 `c9bb2c87… / 02a1bf9d… / 220 tools`。
- JSON 没有自然退出 / hidden snapshot 字段；只能确认脚本启用了 background smoke、运行后无残留，不能把它表述为“自然退出证据”。
- fixture 是纯色机械图，不代表人工视觉质量或正式成片质量。
- 未修改正式项目、CAS、Review 或生成账本；外部生成、上传、付费为 0；Git stage/commit/push 为 0。
- 禁止重跑 `1d984598…` 的 T23，也禁止跳过失败门去打包或安装。
- 若用户以后再次明确开启新性能切片，唯一入口为 `PERF-UNITS-READ-HOTPATH-01`：先对 `buildUnits → listStudioBindingUnits` 内部的 inspect / ledger / page / binding / successor 阶段做只读计时；只有重复 ledger 校验确认为主体，才建立“已核验 shell 的只读 units 内部入口”。若假设不成立立即停止，不扩大重构。

