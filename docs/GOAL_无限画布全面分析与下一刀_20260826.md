# 全面分析 · 无限画布（2026-08-26）

> Goal：全面分析项目并持续优化。本文件是分析切片，不是整目标关账。  
> 北星：`docs/GOAL_双轨_生图与画布互补契合.md`  
> 有序 formal PASS ≠ 产品 100% 契合。禁止写「运行速度已关账」。

## 1. 项目是什么

Vue 3 + Electron 桌面「AI 漫剧无限画布」。事实源是文件系统 / CAS / SQLite；UI 不推导 nextAction。正式生图走 Codex `agent-imagegen`，应用不内嵌模型。

双轨：

| 轨 | 要什么 | 本 Cloud |
|---|---|---|
| A 产线生图 | 隔离工程 readiness→freeze→dispatch→真实出图→commit→Review | **未开始**（无正式工程、无安装版 App、无 owner 点名单元） |
| B 画布辅助 | 角色/场景/站位一致性 + 剧本存/读/对照/15s | 合同半齐；本切片补 SSL-5 只读计划入口 |

## 2. 已关账 / 保持

- P0–P14 owner：final-validation PASS，禁止重建
- 速度/内存计划 Wave 1–7：字母切片已齐；§1 八条 1/2/4/7/8 已完成，3/5/6 部分完成
- 外部席 2026-08-26：千问主审无 must-fix；不拆 command-bus ledger；不全量异步化 `rebuildGraph`
- 工作队列：`open+claimed=0`

## 3. 本 Cloud 不能冒充完成的门

1. 安装版 T23
2. 列表翻页 GUI 探针 `aicanvas-studio://media/* = 0`
3. P7 owner 有界对照
4. 正式工程真实生图闭环（Goal 完成门第 1、3 条）

## 4. B 轨已有 vs 缺口

| 能力 | 已有 | 缺口 |
|---|---|---|
| 一致性四态 | MCP + 审片横幅 + 对照行 peek + session-snapshot peek + history 信封 / 生成控制 peek | 真生图约束证据不足；独立 MCP 新进程常 unevaluated |
| SSL-0…4 | 库/读/对照/向导 + span 映射 + 阅读器选区配图 + 对照表宫格列 | 对照仍是导演面，未接真生图 |
| SSL-5 | MCP `ssl5-missing-to-gen-plan` + 对照横幅 + create-plan 只读草稿；焦点即 earliest 且 wait/retry/Review 时禁止再建议 create-plan | 不执行建计划、不 dispatch（保持） |
| Phase D | D1/D3/D4/D5 | D2 画布投影刷新仍内联 |
| 速度计划 §1.3 | 展示路径有界 + lean 身份 | earliest/diagnostics 仍整集全序 |

## 5. 本切片做了什么

把已有 `planSsl5MissingToGen` 变成 Agent 零说明可调的只读入口，并在图文对照面显示焦点 unit 与 freeze 链。随后补齐 span 映射、阅读器选区配图、对照投影按 panel 读 pack。

- MCP：`get_studio_script_library_projection` operation `ssl5-missing-to-gen-plan` / `script-span-media-map`
- 冷启动：经 `withStudioSsl5MissingToGen` 动态 import，不静态拉 align
- 桌面：对照横幅 + 导演动作「缺图下一步」+ 阅读器「这段配了哪些图」（命中列表带构图/前镜）
- 新冻结：单镜 renderedPrompt 与 unit-grid brief 写入前镜站位；首格不写；不 dispatch
- 单镜 Agent brief：从冻结 renderedPrompt 还原 previousStanding；不读 head
- session-snapshot / 生成控制 / 审片：只从该包 renderedPrompt 露前镜；历史包无该行则为 null；不读 head
- unit-grid Agent brief.previousStandings + toolHints；画布检查器构图/前镜（有冻结包不读 head）
- get_studio_trace 投影 previousStandings（无该行则省略；不读 head；不改 P24 golden）
- 导演「生成追溯」真调 `canvas:get-studio-trace` 只读抽屉；无 pack/run 不猜第一格
- 对照侧栏 / 阅读器选区点穿同一只读追溯抽屉
- 15s 向导 G2+ 露出前镜；物化 prompt 写入「前镜交接」行（首格不写；App/Core 共用）
- 15s 向导可填本格光线/服化；物化 prompt 非空才写；G2+ 只读上一格光线/服化（不自动写入、不加冻结前镜光线行）
- 画布检查器 / 生成控制 / 审片从已加载冻结包露「光线/服装（宫格覆盖）」；无冻结包时检查器退本格锁版光线/服化（只读 SQL，不改 dashboard 投影）
- 画布检查器跨单元场景回指（当前 head 只读 SQL；点穿精确宫格；不改 get_studio_trace）
- 15s 向导跨单元场景回指（只扫已加载对照板；不写冻结提示词）
- session-snapshot 投影 frozenPanelLighting / frozenPanelCostume（无该行则 null，不进 fingerprint）
- 单镜/整板 Agent brief + get_studio_trace 投影冻结宫格光线/服装（全空省略；不改 P24；不改 renderedPrompt）
- 对照侧栏跨单元场景回指（已加载快照提及；可点穿更早同场景宫格；≠ BindingSet）
- session-snapshot 跨单元场景回指（只读 SQL 快照提及；brief 只加 tool note；≠ BindingSet）
- SSL-5 横幅 / 阅读器选区命中复用对照同款场景回指（已加载 board；≠ BindingSet）
- SSL-5 / 阅读器露出锁版光线服化；G2+ 只读上一格光线/服化；场景回指可点穿精确宫格（阅读器仅用户点穿才加载对照板）
- 生成控制无冻结包退本格锁版光线/服化（只读 SQL）；当前 head 场景回指可点穿精确宫格。有包仍只认覆盖行，不从 head 补冻结光线。不改 dashboard / get_studio_trace
- 对照/SSL-5/阅读器/向导/检查器/生成控制/session-snapshot 跨单元道具回指（只认 `category=prop`；一次 IPC 同开库返回 scene+prop；点穿复用场景回指；不写冻结提示词；整板 brief 最后一条仍是前镜句）
- 对照/SSL-5/阅读器/向导/检查器/生成控制/session-snapshot 跨单元角色回指（只认 `category=character`；一次 IPC 同开库返回 scene+prop+character；点穿复用场景回指；不写冻结提示词；整板 brief 最后一条仍是前镜句）
- 对照/SSL-5/阅读器/向导/检查器/生成控制/审片/session-snapshot 扩写格镜头类型（只解析已有「镜头类型：」行；锁版 `shot_type` 可选列；审片只认冻结包；不写新冻结行；整板 brief 最后一条仍是前镜句）
- 对照/SSL-5/阅读器/向导/检查器/生成控制/审片/session-snapshot 15s 节拍（只读冻结包 target 起止秒，不写新冻结行；无包检查器/生成控制用驾驶舱已有 start/end/duration；审片只认冻结包；整板 brief 最后一条仍是前镜句）
- unit-grid Agent brief.BEATS 带 startSeconds/endSeconds/shotType；`promptContractText` 含起止秒与原镜/扩写；不改冻结 renderedPrompt
- 单镜 Agent brief 带 shotTypeLine / beatLine（只从冻结包；不读 head）
- SSL-5 create-plan 只读草稿：`focusPackId` + `generationPlanDraft`；recommendedPath 在 freeze 与 dispatch 之间插入 create-plan；只认焦点缺图格自己的 pack，禁止用同行已出图 preview pack；不执行、不 dispatch
- session-snapshot / 生成控制复用 `composeStudioGenerationPlanDraft`：只认 query.panelId / 当前宫格已落盘单镜包；无 panelId 禁止猜第一格；整板 pack 不当单镜节点；草稿不进 fingerprint；不执行、不 dispatch
- 生成控制 / pack envelope 露出 unit-grid create-plan 只读草稿：节点 `{ targetKind: unit-grid, unitId }`；只认已落盘 pack，不用 readiness 候选；SSL-5 / session-snapshot 保持单镜；不执行、不 dispatch
- session-snapshot 无 panelId 时只认账本已落盘 unit-grid pack 起草 create-plan；有 panelId 仍只走单镜；readiness next 含 create-plan；不执行、不 dispatch
- 驾驶舱 unit-grid nextAction：已落盘 pack 无计划 → create-plan；readiness 候选不当冻结包；不执行、不 dispatch
- earliest 只对一个 pending 单元按落盘 pack/计划精炼 freeze/plan/dispatch；全槽位仍不读 pack；statusLine 带 code；不执行、不 dispatch
- 风格锁只读露出：只认已有 `category=style` 控制参考 / 快照提及 / 冻结包 refs；对照/SSL-5/阅读器/向导/检查器/生成控制/审片/session-snapshot/brief 合同句；不开 category=style 回指；不写新冻结「风格：」行；不加第 8 槽；不改 `get_studio_trace`
- 已有计划时 create-plan 草稿改标 dispatch：SSL-5 只精炼焦点缺图格；session-snapshot / 生成控制 / pack envelope 同步；不执行、不派发
- 已有计划时按节点状态精炼下一步：planned→dispatch，dispatched→wait，failed/cancelled→retry，succeeded→Review；未传 status 保持 dispatch 文案；retry-superseded→planned；earliest / 驾驶舱 nextAction 已同步；不执行、不派发、不重试
- SSL-5 / 对照与 earliest wait/retry/Review 对齐：对照板带 `earliestCode` / `earliestLabel`；焦点即 earliest 时禁止再建议 create-plan/dispatch；单镜 persist refine 之后 earliest wait 仍赢；对照横幅跟 `blockedReason`；不二次 `getStudioEpisodeEarliest`
- session-snapshot / 生成控制单镜草稿跟 unit-grid 在途对齐：复用 `bundle.nextAction` / 已加载 `progress.nodes`；planned 不挡；草稿不进 fingerprint
- pack envelope 单镜草稿/next 跟 unit-grid 在途对齐：单镜 pack 另读同单元 unit-grid 节点；不加 inspect；planned 不挡
- readiness next 跟 unit-grid 在途对齐：`agentExecution.next` 在同单元 unit-grid 已在途/待重试/待审时改 wait/retry/Review；freeze writeCommand 不变
- 画布节点下一步跟 unit-grid 在途对齐：检查器「下一步」用已加载 `unitDetail.nextAction`；wait/retry/Review/对账时禁用 freeze-dispatch；planned 不挡；零额外 IPC
- active-runs envelope next 跟本槽/unit-grid 对齐：T4 恢复面补 `nextAction`；`generationBlocked` 仍只认本槽；不改 ledger
- 15s 向导物化后下一步含 create-plan：不跳过 Binding，不自动派发，不改冻结提示词
- generation-control operation=plan 信封 next 跟节点状态对齐：按 planId/单元查 wait/retry/dispatch/Review/create-plan；未限定列表不猜单元；生成控制零额外 IPC；标准环先读 plan next
- generation-control operation=history 信封 next 跟本页结果对齐：未成对→wait、成对 pending→Review、成对 rejected→retry；要看最新请 newest-first；生成控制零额外 IPC；不自动 Review PASS
- session-snapshot 一致性四态 peek：当前宫格 newest-first 结果 run，否则整板 latest；只读 LRU；未评估 ≠ 无法检查；不 evaluate；不进 fingerprint；机器不自动 Review PASS
- generation-control operation=history 信封 consistencyPeek：本页优先成对 run；生成控制用已加载信封零额外 IPC；单镜 history 改走同一信封；Vue 不 import evaluator；不自动 Review PASS
- episode-unit-media-map：`listStudioGenerationPacksByUnit({ unitId, panelId })`；unit-grid 不摊派
- **不** dispatch、**不**抢写租约、**不**自动 Review PASS

## 6. 下一刀候选（未做）

1. 仍等 owner：T23 / GUI 探针 / P7 / 真生图
2. 站位/构图/光线/场景/道具/角色/扩写格/15s 节拍/风格锁真实验证需正式工程；源码已把前镜、宫格光线服装、跨单元回指、扩写格镜头类型、15s 节拍与风格锁接到导演/Agent 面（快照提及 ≠ BindingSet；扩写格只解析已有冻结行；节拍只读已有 target / 驾驶舱时间字段；风格锁只认已有 category=style，不开回指）
3. 检查器/生成控制锁版光线/服化与场景/道具/角色回指、向导/SSL-5/阅读器只读面、扩写格镜头类型、15s 节拍、风格锁只读面、unit-grid brief.BEATS 起止秒/扩写格、单镜 brief shotTypeLine/beatLine/styleLockLine、SSL-5 / session-snapshot / 生成控制 / pack envelope create-plan 只读草稿（含 unit-grid；session-snapshot 无 panelId 走整板）、驾驶舱 create-plan nextAction、earliest 单单元 pack/计划 next、已有计划时按节点状态写 dispatch/wait/retry/Review、earliest / 驾驶舱 nextAction 按节点状态、SSL-5/对照 earliest wait/retry/Review 对齐、session-snapshot/生成控制/pack envelope 单镜草稿跟 unit-grid 在途对齐、readiness next 跟 unit-grid 在途对齐、画布节点下一步跟 unit-grid 在途对齐、active-runs envelope next 跟本槽/unit-grid 对齐、15s 向导物化后下一步含 create-plan、plan 信封 next 跟节点状态对齐、history 信封 next 跟本页结果对齐、session-snapshot / history 信封一致性四态 peek 已接。`StudioDashboardPanelSummary` 仍无 `sceneLighting`（保持）。勿改 Binding inspect / evaluate 像素 / get_studio_trace head 回指。向导不自动加载对照板。审片仍只从冻结包露身份面（有包不从 head 补）。北星 §4 不要再克隆第四套回指 / 风格锁平行面。真生图约束仍等 owner。SSL-5 一键对该 unit 真生图仍等 owner / 写租约。earliest 非焦点槽位仍不读落盘 pack（保持有界）。勿改 T5 无 run → `ready_to_freeze`。勿改 T4 `listStudioGenerationActiveRuns`

## 7. 整 Goal 完成门对照

| 北星完成门 | 现状 |
|---|---|
| 产线用户点名集/单元关账 | **未开始** |
| 角色/场景/站位可演示辅助 + 真实验证 | **部分完成**（对照/SSL-5/阅读器/session-snapshot/检查器/向导/生成控制 含场景/道具/角色回指 + 冻结提示词 + brief/session-snapshot/审片/画布检查器/get_studio_trace + 导演·对照·阅读器追溯 + 15s 向导前镜/光线/服化 + 冻结宫格光线服装只读 + 检查器/生成控制锁版光线/服化 + SSL-5/阅读器锁版光线服化与回指点穿 + 扩写格镜头类型只读面 + 15s 节拍只读面 + 风格锁只读面 + unit-grid brief.BEATS 起止秒/扩写格 + 单镜 brief shotTypeLine/beatLine/styleLockLine + SSL-5 / session-snapshot / 生成控制 / pack envelope create-plan 只读草稿（含 unit-grid；无 panelId 走整板）+ 驾驶舱 create-plan nextAction + earliest 单单元 pack/计划 next + 已有计划则按节点状态写 dispatch/wait/retry/Review + earliest/驾驶舱 nextAction 按节点状态 + SSL-5/对照 earliest wait/retry/Review 对齐 + session-snapshot/生成控制/pack envelope 单镜草稿跟 unit-grid 在途对齐 + readiness next 跟 unit-grid 在途对齐 + 画布节点下一步跟 unit-grid 在途对齐 + active-runs envelope next 跟本槽/unit-grid 对齐 + 15s 向导物化后下一步含 create-plan + plan 信封 next 跟节点状态对齐 + history 信封 next 跟本页结果对齐 + session-snapshot / history 信封一致性四态 peek；真生图约束证据不足） |
| 至少一轮画布约束真实生图 | **未开始**（本 Cloud 无正式工程） |
| 红线未破 | 保持 |

结论：整目标 **部分完成**。本切片只推进 B 轨 SSL-5 只读面。
