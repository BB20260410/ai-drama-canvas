# 全面分析 · 无限画布（2026-08-26）

> Goal：全面分析项目并持续优化。本文件是分析切片，不是整目标关账。  
> 北星：`docs/GOAL_双轨_生图与画布互补契合.md`  
> 有序 formal PASS ≠ 产品 100% 契合。禁止写「运行速度已关账」。

## 1. 项目是什么

Vue 3 + Electron 桌面「AI 漫剧无限画布」。事实源是文件系统 / CAS / SQLite；UI 不推导 nextAction。正式生图走 Codex `agent-imagegen`，应用不内嵌模型。

双轨：

| 轨 | 要什么 | 本 Cloud |
|---|---|---|
| A 产线生图 | 隔离工程 readiness→freeze→dispatch→真实出图→commit→Review | **未开始**（无正式工程、无安装版 App、无 owner 点名单元） |
| B 画布辅助 | 角色/场景/站位一致性 + 剧本存/读/对照/15s | 合同半齐；本切片补 SSL-5 只读计划入口 |

## 2. 已关账 / 保持

- P0–P14 owner：final-validation PASS，禁止重建
- 速度/内存计划 Wave 1–7：字母切片已齐；§1 八条 1/2/4/7/8 已完成，3/5/6 部分完成
- 外部席 2026-08-26：千问主审无 must-fix；不拆 command-bus ledger；不全量异步化 `rebuildGraph`
- 工作队列：`open+claimed=0`

## 3. 本 Cloud 不能冒充完成的门

1. 安装版 T23
2. 列表翻页 GUI 探针 `aicanvas-studio://media/* = 0`
3. P7 owner 有界对照
4. 正式工程真实生图闭环（Goal 完成门第 1、3 条）

## 4. B 轨已有 vs 缺口

| 能力 | 已有 | 缺口 |
|---|---|---|
| 一致性四态 | MCP + 审片横幅 + 对照行 peek | 真生图约束证据不足 |
| SSL-0…4 | 库/读/对照/向导 + span 映射 + 阅读器选区配图 + 对照表宫格列 | 对照仍是导演面，未接真生图 |
| SSL-5 | MCP `ssl5-missing-to-gen-plan` + 对照横幅 | 不 dispatch（保持） |
| Phase D | D1/D3/D4/D5 | D2 画布投影刷新仍内联 |
| 速度计划 §1.3 | 展示路径有界 + lean 身份 | earliest/diagnostics 仍整集全序 |

## 5. 本切片做了什么

把已有 `planSsl5MissingToGen` 变成 Agent 零说明可调的只读入口，并在图文对照面显示焦点 unit 与 freeze 链。随后补齐 span 映射、阅读器选区配图、对照投影按 panel 读 pack。

- MCP：`get_studio_script_library_projection` operation `ssl5-missing-to-gen-plan` / `script-span-media-map`
- 冷启动：经 `withStudioSsl5MissingToGen` 动态 import，不静态拉 align
- 桌面：对照横幅 + 导演动作「缺图下一步」+ 阅读器「这段配了哪些图」（命中列表带构图/前镜）
- 新冻结：单镜 renderedPrompt 与 unit-grid brief 写入前镜站位；首格不写；不 dispatch
- 单镜 Agent brief：从冻结 renderedPrompt 还原 previousStanding；不读 head
- session-snapshot / 生成控制 / 审片：只从该包 renderedPrompt 露前镜；历史包无该行则为 null；不读 head
- unit-grid Agent brief.previousStandings + toolHints；画布检查器构图/前镜（有冻结包不读 head）
- get_studio_trace 投影 previousStandings（无该行则省略；不读 head；不改 P24 golden）
- 导演「生成追溯」真调 `canvas:get-studio-trace` 只读抽屉；无 pack/run 不猜第一格
- 对照侧栏 / 阅读器选区点穿同一只读追溯抽屉
- 15s 向导 G2+ 露出前镜；物化 prompt 写入「前镜交接」行（首格不写；App/Core 共用）
- 15s 向导可填本格光线/服化；物化 prompt 非空才写；G2+ 只读上一格光线/服化（不自动写入、不加冻结前镜光线行）
- 画布检查器 / 生成控制 / 审片从已加载冻结包露「光线/服装（宫格覆盖）」；无冻结包时检查器退本格锁版光线/服化（只读 SQL，不改 dashboard 投影）
- 画布检查器跨单元场景回指（当前 head 只读 SQL；点穿精确宫格；不改 get_studio_trace）
- 15s 向导跨单元场景回指（只扫已加载对照板；不写冻结提示词）
- session-snapshot 投影 frozenPanelLighting / frozenPanelCostume（无该行则 null，不进 fingerprint）
- 单镜/整板 Agent brief + get_studio_trace 投影冻结宫格光线/服装（全空省略；不改 P24；不改 renderedPrompt）
- 对照侧栏跨单元场景回指（已加载快照提及；可点穿更早同场景宫格；≠ BindingSet）
- session-snapshot 跨单元场景回指（只读 SQL 快照提及；brief 只加 tool note；≠ BindingSet）
- SSL-5 横幅 / 阅读器选区命中复用对照同款场景回指（已加载 board；≠ BindingSet）
- SSL-5 / 阅读器露出锁版光线服化；G2+ 只读上一格光线/服化；场景回指可点穿精确宫格（阅读器仅用户点穿才加载对照板）
- 生成控制无冻结包退本格锁版光线/服化（只读 SQL）；当前 head 场景回指可点穿精确宫格。有包仍只认覆盖行，不从 head 补冻结光线。不改 dashboard / get_studio_trace
- 对照/SSL-5/阅读器/向导/检查器/生成控制/session-snapshot 跨单元道具回指（只认 `category=prop`；一次 IPC 同开库返回 scene+prop；点穿复用场景回指；不写冻结提示词；整板 brief 最后一条仍是前镜句）
- 对照/SSL-5/阅读器/向导/检查器/生成控制/session-snapshot 跨单元角色回指（只认 `category=character`；一次 IPC 同开库返回 scene+prop+character；点穿复用场景回指；不写冻结提示词；整板 brief 最后一条仍是前镜句）
- 对照/SSL-5/阅读器/向导/检查器/生成控制/审片/session-snapshot 扩写格镜头类型（只解析已有「镜头类型：」行；锁版 `shot_type` 可选列；审片只认冻结包；不写新冻结行；整板 brief 最后一条仍是前镜句）
- 对照/SSL-5/阅读器/向导/检查器/生成控制/审片/session-snapshot 15s 节拍（只读冻结包 target 起止秒，不写新冻结行；无包检查器/生成控制用驾驶舱已有 start/end/duration；审片只认冻结包；整板 brief 最后一条仍是前镜句）
- unit-grid Agent brief.BEATS 带 startSeconds/endSeconds/shotType；`promptContractText` 含起止秒与原镜/扩写；不改冻结 renderedPrompt
- episode-unit-media-map：`listStudioGenerationPacksByUnit({ unitId, panelId })`；unit-grid 不摊派
- **不** dispatch、**不**抢写租约、**不**自动 Review PASS

## 6. 下一刀候选（未做）

1. 仍等 owner：T23 / GUI 探针 / P7 / 真生图
2. 站位/构图/光线/场景/道具/角色/扩写格/15s 节拍真实验证需正式工程；源码已把前镜、宫格光线服装、跨单元回指、扩写格镜头类型与 15s 节拍接到导演/Agent 面（快照提及 ≠ BindingSet；扩写格只解析已有冻结行；节拍只读已有 target / 驾驶舱时间字段）
3. 检查器/生成控制锁版光线/服化与场景/道具/角色回指、向导/SSL-5/阅读器只读面、扩写格镜头类型、15s 节拍、unit-grid brief.BEATS 起止秒/扩写格已接；`StudioDashboardPanelSummary` 仍无 `sceneLighting`（保持）。勿改 dashboard / Binding inspect / evaluate 像素 / get_studio_trace head 回指。向导不自动加载对照板。审片仍只从冻结包露身份面（有包不从 head 补）。北星 §4 下一候选不是第四套回指 / 风格锁平行面；风格锁未做专门面，真生图约束仍等 owner

## 7. 整 Goal 完成门对照

| 北星完成门 | 现状 |
|---|---|
| 产线用户点名集/单元关账 | **未开始** |
| 角色/场景/站位可演示辅助 + 真实验证 | **部分完成**（对照/SSL-5/阅读器/session-snapshot/检查器/向导/生成控制 含场景/道具/角色回指 + 冻结提示词 + brief/session-snapshot/审片/画布检查器/get_studio_trace + 导演·对照·阅读器追溯 + 15s 向导前镜/光线/服化 + 冻结宫格光线服装只读 + 检查器/生成控制锁版光线/服化 + SSL-5/阅读器锁版光线服化与回指点穿 + 扩写格镜头类型只读面 + 15s 节拍只读面 + unit-grid brief.BEATS 起止秒/扩写格；真生图约束证据不足） |
| 至少一轮画布约束真实生图 | **未开始**（本 Cloud 无正式工程） |
| 红线未破 | 保持 |

结论：整目标 **部分完成**。本切片只推进 B 轨 SSL-5 只读面。
