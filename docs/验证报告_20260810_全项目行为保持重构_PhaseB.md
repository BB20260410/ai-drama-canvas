# 全项目行为保持重构 · Phase B 验证报告

## 结论

Phase B 已通过，整个重构计划仍为**执行中 / 部分完成**。本阶段只统一了 8 个已经证明
字节语义相同的 Active Studio canonical JSON 实现，没有推广到其余 owner，也没有改变数据库、
CAS、命令、IPC、MCP 或 UI 合同。

## 已完成改动

1. 新建 `src/core/studio-canonical-json.ts`，集中提供规范化、紧凑 SHA-256 摘要和 pretty JSON。
2. 精确迁移 generation、unit-grid、generation-ledger、generation-review、checkpoint、
   post-result-observation、production-dashboard 与 video-package 共 8 个 owner。
3. 保持旧语义：数组保序；对象 `undefined` 省略；数组 `undefined` 由 `JSON.stringify`
   转为 `null`；对象键按英文 locale 排序；摘要继续使用 UTF-8 SHA-256；pretty JSON
   继续为 2 空格并保留单个末尾换行。
4. 固定历史向量摘要 `7bf5bb5b…d0d0d`，并新增结构门禁，防止 owner 重新定义本地实现。
5. 复核 `tests/fixtures/p24-trace-golden.json` 与 HEAD 字节一致，没有通过更新 golden
   掩盖 fingerprint 漂移。

## 验证

- 8 个 owner 影响范围：10 files / 136 tests PASS。
- canonical 内核与依赖边界：2 files / 5 tests PASS。
- `npm run typecheck`：PASS。
- `npm run typecheck:app`：PASS。
- `git diff --check`：PASS。
- 独立只读终审：CLEAN；未发现 P0/P1 或高价值 P2。
- 终审逐项确认独立文件字节哈希仍保留，没有误删非 canonical 的 `createHash` 用途。

## 没有执行

本阶段没有运行全量测试分区、正式 build、candidate、package smoke 或安装版替换。
这些交付动作仍只允许在全部源码阶段冻结后各执行一次，避免每个内部切片重复打包。

## 数据与外部边界

- 未写正式工程、CAS、SQLite、Review 或生成账本；
- 未启动 App；
- 未调用外部服务、上传、付费或生成；
- 未公证、发布或部署；
- 未执行 Git stage/commit/push/reset/clean。

## 后续

下一步仅进入 Phase C：先冻结 220 个 MCP 工具的 ABI 与 effect/gate 分类，再把两层
`registerTool` monkey-patch 合成显式 registrar。Phase C 不移动业务 Schema，不改工具数，
不构建 candidate，也不改变 currentness → guarded-write → command-bus 的调用顺序。
