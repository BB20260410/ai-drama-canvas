# 全项目行为保持重构 · Phase E 验证报告

## 结论

Phase E 已通过，整个重构计划仍为**执行中 / 部分完成**。Renderer 的公共类型、
Material Studio 只读映射和旧画布同步投影已分层；分页、视口边界、异步 token、Core
`nextAction`、formal PASS 与媒体身份均未改变。

## 已完成改动

1. 新建 `material-studio-ui-contract.ts`，承载 28 个跨视图公共 type/interface；
   `MaterialStudioView.vue` 保留兼容 type re-export，三个消费者直接导入纯合同。
2. 新建 `material-studio-read-mapper.ts`，只组装已读取的 shell/material/production/
   dashboard/currentUnit；Promise、IPC、project root、epoch/token 仍在 App。
3. 新建 `legacy-canvas-flow-projection.ts`，只接收 `visibleItems`、已冻结状态/坐标和 actions，
   输出 VueFlow nodes/edges；布局读取、generation token、末端 stale check、refs 与
   `nextTick` 仍在 App。
4. 旧叙事投影中重复的 `filter/some` 改为保持插入顺序的 Map/Set，降低纯计算复杂度，
   不改变节点/边顺序、计数或可见范围。

## 保持的性能与裁决合同

- Material Studio mapper 不生成 `nextAction`，只透传 Core dashboard 对象。
- `completedUnitCount` 继续取 `checkpoint.completedSlotCount`。
- 旧画布投影只消费 `visibleItems`，不扩大为全库 DOM，也不读取媒体。
- 受管画布仍为单页 36 单元、每单元最多 6 宫格、每宫格最多 6 控制资产。
- formal PASS、Review、root/generation token、媒体 SHA/CAS 路径合同均未修改。

## 验证

- 实现阶段：9 files / 69 tests PASS。
- 主线 fixture/性能边界复验：6 files / 23 tests PASS。
- 公共类型 AST：旧/新均 28 个，缺失/额外/结构漂移均为 0。
- 旧 `rebuildFlow` 与新投影逐字段审查：节点、边、顺序、style、zIndex、actions 等价。
- `npm run typecheck`：PASS。
- `npm run typecheck:app`：PASS。
- `git diff --check`：PASS。
- 独立只读终审：CLEAN；P0=0、P1=0、高价值 P2=0。

## 数据与外部边界

- 未启动 Electron App 或执行 UI smoke；
- 未写正式工程、CAS、SQLite、Review 或生成账本；
- 未调用外部服务、上传、付费或生成；
- 未 build、candidate、package、安装或公证；
- 未执行 Git stage/commit/push/reset/clean。

## 后续

下一步进入 Phase F：先用权威 Studio command names 修复 58/48 的手写窄化类型漂移，
再把 58 个业务 case 抽到内部 executor。command-bus 必须继续掌握幂等账本、锁序、lease、
busy retry、active fence、一次性结果脱敏和 terminal 状态分类。
