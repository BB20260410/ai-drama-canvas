# 再次代码审查对比与本机安装闭环验证报告

时间：2026-08-10 05:58 CST  
范围：当前工作树、最终 MCP candidate、隔离打包 App、本机 `/Applications/AI 漫剧画布.app`  
结论：**PASS（本机 local-only 范围）**

## 1. 交付结论

- 前次全面代码审查的 9 个问题（CORE-01/02/03、UI-01/02、PERF-01、MCP-01/02、DEP-01）均保持修复，没有确认回归。
- 本轮差异化复审确认并修复 **3 个 P1、4 个 P2**；三路独立终审最终均为 CLEAN，冻结范围内没有剩余可确认的 P0/P1 或高价值 P2。
- 最终源码身份为 sourceDigest `d8e48caa6e635332eae06d49717f23a21d3d433613e488d3f49e2c42baecb20f`、buildId `0324e11699f994eafc697bb5a74865bd`、MCP `218 tools`。
- 已只构建一次最终 candidate、只运行一次隔离 App smoke，并把同一源码构建的 Developer ID 签名 App 安装到 `/Applications/AI 漫剧画布.app`。
- 用户指定仅在此 Mac 本地使用，因此没有做 Apple 公证、上传、发布或自动更新配置。本轮没有生成新 DMG；`dist` 中此前已有的 DMG 属于历史产物，未将其冒充本轮输出。

## 2. 与前次代码审查的对比

| 前次编号 | 前次问题 | 当前结果 | 当前证据摘要 |
|---|---|---|---|
| CORE-01 | rehearsal 可写权威小说正文 | **保持修复** | command 入口与 Repository 边界继续双层拒绝；定向回归通过 |
| CORE-02 | 远端 POST 已送达后的异常被误记为 failed | **保持修复** | HTTP/JSON/业务结构/本地落盘失败继续归入 `submission_unknown` |
| CORE-03 | 小说派生索引激活与正文写 TOCTOU | **保持修复** | 重建与正文写继续共用 `novel-manuscript` 互斥锁 |
| UI-01 | 切工程/关窗可静默丢失未保存正文 | **保持修复** | 项目、工作区、删除与关窗继续走统一 leave gate |
| UI-02 | 旧工程异步响应可覆盖新工程 | **保持修复** | 冻结 root、generation token、卸载失效与组件 key 均保持 |
| PERF-01 | 小说启动全量拉取并挂载最多 100000 章 | **保持修复** | 固定卷/章节分页窗口与有界 DOM 保持 |
| MCP-01 | candidate 自证、依赖回落工作区 | **保持修复** | 外部 publication、生产依赖闭包、runtime guard 与固定根保持 |
| MCP-02 | symlink/发布事务/环境注入可绕过 provenance | **保持修复** | 固定真实根、原子 cutover、死亡锁恢复、受控环境负例保持 |
| DEP-01 | sharp 处于已知受影响版本 | **保持修复** | 当前 `sharp@0.35.3`；生产依赖审计为 0 |

前次审查后另外完成的退出 ACK、销毁后访问 `webContents`、watcher 收口、原生拖出复制体、总资源分页/缓存等修复也均未发现回归。

## 3. 本轮新增发现与修复

| 本轮编号 | 严重度 | 分类 | 触发影响 | 已实施修复 |
|---|---|---|---|---|
| SIDECORE-01 | P1 | 前次未覆盖 | 活动工程注销可穿过付费调用 activation fence，使已注销工程仍获得一次调用能力 | `unregisterProject` 使用 `activation fence → registry lock` 统一锁序；增加确定性并发测试 |
| LIFE-01 | P1 | 前次未覆盖 | App 启动早期退出时，close 请求可能早于 preload/renderer 监听器而丢失，等待 30 秒后取消退出 | 新增 preload close bridge 缓冲与 ready 握手；main 仅在 bridge ready 后投递并开始 ACK 计时 |
| INSTALL-01 | P1 | 终审新增 | 安装验收证据目标若位于已签名 `.app` 内或符号链接父目录指向包内，会破坏安装工件签名 | 对 App 与证据目标做 canonical realpath 门禁；仅 ENOENT 放行；App 内、symlink parent、ELOOP/EACCES 均失败关闭 |
| EDITOR-01 | P2 | 新增 | VideoEditor 初始化 session 在卸载/换根后返回，可能遗留旧 session 和后台任务 | 增加冻结 root、lifecycle generation 与 stale session 立即回收；视图按 `projectRoot` 建 key |
| CLOSE-01 | P2 | 新增 | recoverable owner 已不可逆摘除后若取消退出并 reopen，App 会在退化状态继续运行 | 只有 critical 未收敛才取消；critical 已完成时记录 recoverable 不完整并继续自然退出 |
| PACKAGE-01 | P2 | 前次残余 | 隔离包只比较 stage/packaged SDK，无法证明 package、lock 与安装依赖一致 | 增加 package direct、lock root、lock entry、stage installed、packaged installed 精确版本门，并写入证据 |
| VERIFY-01 | P2 | 前次未覆盖 | 安装验收依赖外部隐藏变量并直接 `application.close()`，可能抢前台或永久挂住 | 验收脚本自身强制 accessory 后台模式、采集 show/focus/Dock 快照，并使用 20 秒有界关闭 helper |

## 4. 影响范围验证

定向测试采用固定影响集合，没有重新从 fast/medium/integration/heavy 全量分区开始：

- `tests/studio-imagegen-active-project-fence.test.ts`：3/3 PASS。
- `tests/app-close-coordinator.test.ts` + `tests/video-editor-dirty-guard.test.ts`：15/15 PASS。
- `tests/isolated-package-guards.test.ts`：11/11 PASS。
- `tests/sidecar.test.ts`、`tests/editor.test.ts`、`tests/electron-application-close.test.ts`、`tests/novel-workspace-routing.test.ts`：36/36 PASS。
- 最终不重复计数的影响集合：**8 files / 65 tests PASS**。
- `npm run typecheck`：PASS。
- `npm run typecheck:app`：PASS。
- `git diff --check`：PASS。
- `npm audit --omit=dev`：0（审查基线；本轮没有依赖变更）。

三路独立终审分别复核核心锁序、Electron 生命周期/UI、MCP/交付证据链；INSTALL-01 经同一终审者复核后关闭，最终均为 CLEAN。

## 5. 最终 candidate 与隔离 App smoke

- candidate：`mcp-candidate-d8e48caa6e635332-65e654aa2b49d955-ede44659`。
- current check：`ok=true`，检查 6 个 publication，invalid `0`。
- 生产依赖闭包 fingerprint：`cfa0faaf83fd7294fd98a7a195faee8e0a8f72effc0fd51dfe15b161eef0985c`。
- stable launcher 真实 MCP 握手：initialize、tools/list 均 PASS；218 tools、9 resource templates、8 prompts。
- 唯一一次隔离 App smoke：PASS。Effect/Transition 两次自然关闭为 239ms、231ms；ReviewStudio 两次为 47ms、50ms；全部 exit 0，无 TERM/KILL。
- 8 份后台快照均为 showEvents=0、focusEvents=0、窗口 hidden/unfocused、Dock hidden；运行前清理前残留进程列表与结束后列表均为空。
- 隔离 smoke 证据：`docs/evidence/isolated-package-smoke-20260809T214944761Z-23144-291902d8.json`。

## 6. 构建、签名与本机安装

- `npm run build`：PASS。
- `electron-builder --mac --dir --publish never -c.mac.notarize=false`：PASS；明确跳过公证。
- `dist/mac-arm64/AI 漫剧画布.app`：arm64，Developer ID deep/strict PASS。
- 当前安装版：`/Applications/AI 漫剧画布.app`。
- 主可执行 SHA-256：`7ab1eddb79d78a141c948cb5e93b2f66681a299bef085147ea07091a88c73c97`。
- `app.asar` SHA-256：`3fa35fb4cc253482c40305951a259bb09ff1acc0bd4b1fe7f165cb0565f083c5`。
- Developer ID：`Developer ID Application: YIHANG LI (3JS43BTTJ3)`；Team `3JS43BTTJ3`。
- 安装版独立验收：App 自带 Electron runtime 启动 MCP 218 tools；后台快照 show/focus=0、Dock hidden；57ms 自然退出，exit 0；验收后无 App/helper 残留进程。
- 安装验收证据：`docs/evidence/installed-local-verify-20260809T215613Z-d8e48caa.json`。
- 旧安装版可恢复备份：`/Users/hxx/Documents/无限画布_交付归档/local-install-20260810-055700-d8e48caa/previous-installed/AI 漫剧画布.app`。

## 7. 边界与残余风险

- `spctl` 返回 `rejected / Unnotarized Developer ID` 是用户选择“不公证”后的预期系统分发边界；不是 codesign 失败。本机 Developer ID deep/strict 验证通过。
- 本轮没有重跑 351 个测试文件的全部 fast/medium/integration/heavy 分区；结论是“审查冻结范围、定向影响集、最终 candidate 与隔离/安装 smoke 均通过”，不等价于对所有未来输入宣称绝对零 Bug。
- 没有修改正式受管工程、小说正文、素材、CAS/SQLite、Review 或生产账本；没有调用 Grok、付费生图、上传、发布、公证或自动更新。
- 没有 Git stage、commit、push、PR、reset 或 clean。当前混合脏工作树仍需按 owner 分片后才能安全提交。
- App 在验收后保持关闭，不占用前台或数据库。

