# 验证报告 · P9 运行可靠性与规模探针

日期：2026-07-18  
状态：核心合同 **PASS**（全量故障矩阵/存储迁移/10k 媒体性能基线为后续增量，不阻塞身份链）

## 已交付

- `src/core/studio-reliability.ts`
  - 写上下文强制 `projectId/projectRoot`，拒绝跨根与非法 provider
  - 故障分类：duplicate / revision / cancel / timeout / sha-drift / unknown
  - 磁盘预检（低空间阻断阈值）
  - 规模探针：Dashboard 页硬上限 ≤36，不扫正式大媒体
- `tests/studio-reliability.test.ts` 定向通过

## 未在本切片强制完成（记录为后续）

- 全部 P4–P8 写入口穷尽故障注入矩阵
- JSON command ledger 去 O(n²) 迁移
- 1k 缩略图 / 100 视频代理 / 100 波形隔离性能压测

## 边界

- 无正式生图、无浏览器、无上传、无 Git、无回写只读源
