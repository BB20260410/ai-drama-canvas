# 冷启动细分与请求缓存有界整改验收报告

日期：2026-08-11  
结论：**部分完成 / 严格门阻塞**

## 1. 实际结果

- Dashboard 顶层读取已进入单请求 `AsyncLocalStorage` Schema 校验缓存：同一 `units` 请求只对同一份 production SQLite 字节状态深验一次；下一个顶层请求重新建缓存，不缓存连接或业务结果。
- 受管画布仍在任何布局、Dashboard 或写操作前等待 `getRuntimeWriteGate()`；纯展示用的完整 build identity 已移到首屏只读链收敛后异步读取。
- T23 首卡改为记录真实单元节点插入时刻的 `MutationObserver` 里程碑，不再把 CDP 后置轮询时刻当作节点产生时刻。该指标表示 DOM ready，不代表图片首帧已经 paint。
- 最终源码身份：`98c00560949225734e727db9fa0ea9434a04c3fc927fbd5b8cb8a7d20b0583f3`；buildId `c9485a134c18eca0a61529dd1a7debf5`；MCP 220 tools。

## 2. 性能对照

同一隐藏、隔离、36 单元 / 4 raw / 4 references、严格 `p7-strict` 口径：

| 指标 | 诊断基线 | 最终 | 预算 | 结论 |
|---|---:|---:|---:|---|
| 首张单元卡 DOM ready | 3486ms | 1820ms | ≤1500ms | **FAIL，超 320ms** |
| 首张 raw 深核验 | 4783ms | 3898ms | ≤5000ms | PASS |
| 4 份参考闭合 | 5789ms | 5485ms | ≤8000ms | PASS |
| 投影 IPC 峰值 | 4 | 4 | ≤4 | PASS |

首卡减少 1666ms（约 47.8%）；首 raw 减少 885ms（约 18.5%）。最终只有首卡预算未通过。

最终交互验收同样 PASS：暖切换 p95 57.51ms、缩放反馈 p95 57.72ms、拖拽反馈 p95 2.91ms、平移帧间隔 p95 9.1ms、Long Task 0；IPC 在 9ms 内排空，console/page error 均为 0。

## 3. 实际验证

- `npx vitest run tests/managed-studio-canvas-ui.test.ts tests/t23-ipc-performance-probe.test.ts tests/t23-scale-performance-contract.test.ts tests/t23-scale-performance-probe.test.ts tests/studio-production-dashboard.test.ts tests/studio-request-schema-cache.test.ts --maxWorkers=1` — 6 files / 71 tests PASS。
- `npm run typecheck` — PASS。
- `npm run typecheck:app` — PASS。
- `npm run build` — PASS；生成上述 `98c00560… / c9485a13… / 220 tools` 身份。
- 限定范围 `git diff --check` — PASS。
- 唯一最终隐藏 T23 — 严格结果 FAIL，仅 `renderer-first-card` 未达标；没有重试同一源码。

证据：

- 诊断基线：`docs/evidence/runtime-stability-t23-cold-start-diagnostic-20260811-a74b9b04.json`，SHA-256 `d07fdec398b017d470f8fe824b04dd417964794f068576433179ccc0c718aa15`。
- 最终证据：`docs/evidence/runtime-stability-t23-cold-start-final-20260811-98c00560.json`，SHA-256 `97030c6412c54eb717b1038e0ccc6ec34bfea0b718c057a084bcd29b3fc02baf`。

## 4. 终审与剩余问题

独立只读终审未发现本切片新增 P0/P1、正式数据写入或写门禁回归，但确认两项未完成：

1. `PERF-FIRST-COLD-CANVAS-01` 仍差 320ms。最终时间线显示 Material overview 在 1218.5ms 启动，Canvas units/overview 在 1243.5/1243.6ms 同时启动；Dashboard 通道本轮 6 次调用、累计约 4151ms、单次最高约 1255ms。下一轮唯一建议是让默认 Canvas 先完成 `units` 并落卡，再启动 Canvas/Material overview；raw 仍等待必要 overview，不放宽安全顺序。
2. 低优先级兼容残差：`refreshRuntimeWriteGate` 仍把 `getRuntimeBuildIdentity` API 的存在当作 gate API 可用前提。正式 preload 无条件同时提供两者，当前不构成生产阻断；只有未来支持最小/替代 preload 时，才会把“有 gate、无 identity”误判为 `unavailable`。

## 5. 交付边界

- 因源码严格 T23 未全绿，未运行 candidate、package smoke、App 打包或安装；不得把当前 build 冒充已安装版。
- `/Applications/AI 漫剧画布.app` 保持已验收的 `c9bb2c87… / 02a1bf9d… / 220 tools`，App 已关闭。
- 未写正式工程、CAS、Review 或生成账本；未上传、未调用外部模型、未付费；未 Git stage/commit/push。
- 为防止再次进入循环，本切片在“诊断一次 → 修复一次 → 最终实测一次”后停止。继续必须作为新的有界切片，只处理 overview 与 units 的冷启动排序，不重复本轮证据。
