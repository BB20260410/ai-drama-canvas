# Doctor 与统一快照证据门禁验证报告

验证日期：2026-07-13  
工作区：`/Users/hxx/Documents/无限画布`

## 结论

本轮把既有 15 阶段实时证据审计接入 `doctor_project`、`get_project_snapshot` 和下游阶段完成门禁。Codex 不再需要先相信旧 `completed` 状态再单独猜测是否失效：

- completed 阶段证据漂移由 Doctor 作为 `production-evidence-drift` 错误返回；
- 旧 completed 状态证据仍可读但缺少核验指纹时，由 `production-evidence-verification` 作为警告返回；
- 统一快照在 `productionDesign.evidence` 返回计数、`blockers`、`legacyUnverifiedStages`、`nextStage` 和 `suggestedCalls`，不重复塞入完整 15 阶段指标；
- `productionDesign.evidence.nextRepair` 返回下一修复阶段、原因、是否先修证据、路径/节点，以及可直接传入 `execute_command(update_workflow_stage)` 的当前修订与幂等请求提示；多个阶段必须修一个、刷新一次；
- 网页付费提交结果不明仍保持最高恢复优先级；其他情况下证据修复优先于领取新任务；
- 将视频、剪辑等下游阶段标成 completed 前，会重新核验所有已有指纹的前置阶段；证据已经漂移时不能只靠旧状态越过门禁；
- Doctor 和统一快照改用只读渲染、剪辑工程、剪辑会话和 Skill 读取，不恢复任务、不创建缺失侧车、不初始化默认 Skill、不争抢写锁。

现有生产工作流仍是唯一状态结构，没有新增第二套完成状态数据库。

## 实际修改文件

- `src/core/types.ts`
  - 新增紧凑的生产证据阶段摘要和统一快照摘要类型。
- `src/core/codex.ts`
  - Doctor 接入 15 阶段证据审计、漂移错误、旧状态警告和修复调用链。
  - 统一快照返回紧凑证据摘要，并在证据异常时优先阻断任务推进。
  - Doctor/快照改用只读渲染与 Skill 读取。
- `src/core/production.ts`
  - 下游阶段完成前重新核验所有已保存指纹的前置阶段。
- `src/core/editor.ts`
  - 剪辑会话与剪辑工程列表读取不再调用 `ensureSidecar`。
- `src/core/skills.ts`、`src/core/memory.ts`
  - 增加不初始化默认 Skill 的只读读取路径；独立接续工具仍可按既有合同显式初始化默认 Skill。
- `src/mcp/server.ts`
  - 更新 Doctor/统一快照工具说明及 `resume_project`、`recover_interrupted_work` Prompt。
- `scripts/full-workflow-smoke.ts`
  - 隔离闭环不再使用伪 completed 测试状态；真实建立确认事件、故事骨架、导演/视觉 Bible、完整黄金面具权威资产及逐阶段证据指纹。
- `scripts/mcp-headless-workflow-smoke.ts`
  - 真实暴露续接首帧造成的图片验收漂移，重新验收当前首帧和视频后再完成下游阶段；失败信息会列出 Doctor 错误 ID 与详情。
- `tests/codex.test.ts`、`tests/production.test.ts`、`tests/mcp.test.ts`、`tests/editor.test.ts`
  - 覆盖漂移阻断、两阶段旧状态串行修复、过期修订 CAS 拒绝、只读无侧车副作用、MCP 结构化摘要、恢复 Prompt、下游前置指纹重验和并行真实 FFmpeg 负载。
- `vitest.config.ts`
  - 测试文件最多四并发，避免真实 FFmpeg、Electron-as-Node 与 MCP 子进程在 18 核机器上相互饥饿；没有用无限抬高超时掩盖不稳定。
- `README.md`、`docs/CODEX_MCP.md`、`docs/目标模式提示词.md`
  - 同步当前接口、接续顺序和安装包证据。

## 自动化与运行态结果

- `npm run typecheck`：通过。
- `npm test`：27 个测试文件、134 项测试全部通过；启用四文件并发后的最终全量耗时 40.73 秒。
- `npm run build`：通过；main、preload、renderer 和独立 MCP 均完成生产构建。
- `npm run mcp:smoke`：133 个工具、9 个 Resource Template、7 个 Prompt。
- 空目录纯 MCP 闭环：
  - 项目：`/tmp/ai-canvas-mcp-empty-doctor-snapshot-20260713`
  - 2 章、34 条事实、11 个节拍、4 个拆分单元、11 行正式分镜；
  - JSON/Markdown 真实落盘；
  - 19 条命令账本、0 个不明命令；
  - 跨进程重启恢复通过。
- 源码 stdio 完整生产闭环：
  - 项目：`/tmp/ai-canvas-doctor-evidence-headless-v3-20260713`
  - 输出日志：`/tmp/ai-canvas-doctor-evidence-headless-v3-result.log`
  - 真实完成 4 单元首尾帧、视觉验收、视频、剪辑渲染、时间线续接、续接首帧漂移检测与重新验收；
  - 22 条命令账本、0 个不明命令，最终 Doctor 为 0 错误、0 警告。
- 安装包内 Electron-as-Node stdio 完整生产闭环：
  - 项目：`/tmp/ai-canvas-packaged-repair-contract-v2-20260713`
  - 输出日志：`/tmp/ai-canvas-packaged-repair-contract-v2-result.log`
  - `transport=packaged-electron-node`，133 个工具、9 个资源模板、7 个 Prompt；
  - 初始续接首帧漂移被统一快照识别为 `frames/evidence_drift`，并返回 `execute_command/update_workflow_stage` 修复合同；
  - 后台渲染、发布回执、时间线续接、重新验收、22 条幂等命令和最终 Doctor 0 错误全部通过；
  - 桌面窗口未启动。

## 安装包

- DMG：`/Users/hxx/Documents/无限画布/dist/AI 漫剧画布-0.1.0-arm64.dmg`
- 大小：136,656,987 bytes
- SHA-256：`f5ecb1ff87ee5804a42af23c72bca8d78cd53f90f4105fdaf89a57e74df3b842`
- 签名：`Developer ID Application: YIHANG LI (3JS43BTTJ3)`
- 签名时间：2026-07-13 22:17:45（Asia/Shanghai）
- `codesign --verify --deep --strict`：通过。
- `hdiutil verify`：通过。
- Apple 公证：未配置；Gatekeeper 明确返回 `Unnotarized Developer ID`。

## 未越界与未完成

- 本轮只使用 `/tmp` 隔离项目，没有扫描或修改 `/Users/hxx/Desktop/Ai漫剧`、`豆包版本`、`黄金面具`、`封神篇` 及其 `.aicanvas`。
- 用户尚未提供真实生图/图生视频网站，因此登录态、真实付费提交、供应商结果质量和人物一致性仍未校准。
- 程序可验证文件、哈希、解码、引用和已有验收记录；导演审美、表演自然度、真实轴线、视线及生成画面漂移仍必须经过视觉验收。
- 安装包尚未 Apple 公证。

## 下一阶段最高优先级

在用户尚未提供真实生成网站前，继续完善无需外部授权的本地闭环：对大型真实导入结构做增量扫描/审计性能、任务取消恢复和长时运行资源上限测试。用户提供具体网站后，再按现有 `codex-browser` 合同做登录、上传、付费意图、clientJobId 对账、下载隔离、机械验收、视觉停点和画布回填校准。
