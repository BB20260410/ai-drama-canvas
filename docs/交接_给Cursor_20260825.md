# 无限画布 · 给 Cursor 的任务交接（2026-08-25）

你在 **Cursor** 里接着改这个仓库。先读本文件 + `STATUS.md` + `AGENTS.md`，不要从 N124 或 24h content-visibility 空转续。

- 仓库：`/Users/hxx/Documents/无限画布`
- GitHub：https://github.com/BB20260410/ai-drama-canvas （Apache-2.0，公开）
- HEAD：`afda7fe`（`main` 已推）
- 实时交接：`docs/当前开发交接.md`（本文件是给 Cursor 的开工卡；冲突以 STATUS / 当前开发交接为准）
- 研发规范：`AGENTS.md` + skill `ai-drama-canvas-agent`

默认简体中文。进度写 `STATUS.md` / `TASKS.md`。Git 提交信息用中文：`类型：简短描述`。

---

## 1. 项目是什么

Vue 3 + Electron 桌面「AI 漫剧无限画布」。本地优先：剧本、硬锁图、15 秒单元、宫格、CAS、MCP。正式生图走 Codex `agent-imagegen`，不走 ComfyUI / 浏览器供应链。

正式受管工程（本机，**不进 git**）：

```
/Users/hxx/Documents/无限画布/projects/codex-ai-drama-studio
```

约 1.1GB；当前人物库 **42 人 / 5 条声线**（含图腾腾）。禁止当空库、禁止全盘扫 SQLite/CAS。

本机 App：`/Applications/AI 漫剧画布.app`。不要为了改代码去弹 Playwright/Electron GUI，除非 owner 点名。

---

## 2. 你接手后立刻做

**earliest = 等待 owner。** 速度/内存计划本 Cloud 阻塞（整计划部分完成）。§1.3 by-id + 诊断懒展开 + lean 季集身份 + 身份只读打开 + 时间线账本批读只读旁路 + 历史 PASS 活库只读 + Linux 真投影 sqlite 接线 + 零 mock inspect + Wave 7 画布媒体热路径已落地；Darwin CI `43945a2`/`2d8ce47`/`4b1824a` 已绿。不要重做已落地 Wave，不要写「运行速度已关账」。仅用户能解：安装版 T23、列表 GUI 探针、P7 owner 对照。

计划：`docs/PLAN_运行速度与内存占用长期执行_20260825.md`。W1–W6 已落地；W6 复验两路审查 PASS + Linux 定向 35/305；驾驶舱 bundle 正式时间线已有界；**get_capabilities 握手不再探测 FFmpeg**。§1.5 源码合同测已补（非 GUI）。结论 **部分完成**（P7 owner 对照需 Darwin；安装版 T23 未跑；列表翻页 GUI 探针未跑）。

W2 bundle 已交付：`resolveProjectionBundleApprovedTimelineUnitIds`；多媒体 storyboard 只请求当前单元。证据 `docs/evidence/runtime-perf-memory-wave2-bundle-approved-bound-20260825.json`。

W4 cap 已交付：`DEFERRED_VIDEO_ENGINE_CAPABILITY`。证据 `docs/evidence/runtime-perf-memory-wave4-capabilities-deferred-engine-20260825.json`。两路审查 PASS。禁止写「连上 Agent 永不加载 editor」。

W4 novel 已交付：`withNovelAnalysis` / `withNovelAnalysisProvider`。证据 `docs/evidence/runtime-perf-memory-wave4-novel-analysis-lazy-20260825.json`。两路审查 PASS。禁止写「连上 Agent 永不加载小说分析」。

W6-C 已交付：§1 八条对照 + 全量审查 + Linux `npm test` 分类。纠正 composable 空 `unitIds` 与 executor 边界注释。

交付：

1. W6-A：已完成（见 `docs/evidence/runtime-perf-memory-wave6a-20260825.json`）。
2. W6-B：已写未跑（见 `docs/evidence/runtime-perf-memory-wave6b-20260825.json`）。
3. W6-C：已完成对照与审查（见 `docs/evidence/runtime-perf-memory-wave6c-20260825.json`）。整计划部分完成。
4. W2 bundle：已完成 Linux 源码合同（见 `docs/evidence/runtime-perf-memory-wave2-bundle-approved-bound-20260825.json`）。两路审查 PASS。
5. W4 cap：get_capabilities 握手 deferred engine（见 `docs/evidence/runtime-perf-memory-wave4-capabilities-deferred-engine-20260825.json`）。两路审查 PASS。
6. W4 novel：启动路径不静态拉小说分析整图（见 `docs/evidence/runtime-perf-memory-wave4-novel-analysis-lazy-20260825.json`）。
7. W4 settings：握手读分析配置走薄模块（见 `docs/evidence/runtime-perf-memory-wave4-capabilities-settings-thin-20260825.json`）。两路审查 PASS。
8. 不改 T23 SQL；不扫正式工程；不重做 N124。
9. W6 复验：两路审查 PASS；`typecheck:app` PASS；Linux 定向 35 files / 305 tests PASS（见 `docs/evidence/runtime-perf-memory-wave6-reverify-20260825.json`）。§1.4 源码合同已完成。整计划仍部分完成。
10. W5-E Main 切工程 evict：关闭/切换账本 watcher 时淘汰旧工程 `verifiedFileCache`（见 `docs/evidence/runtime-perf-memory-wave5e-project-switch-evict-20260825.json`）。Vue 不接线。
11. W5-E 在途写回拦截：leaveGeneration 拦住旧 inspect 写回；媒体请求换根淘汰上一工程（见 `docs/evidence/runtime-perf-memory-wave5e-stale-writeback-guard-20260825.json`）。
12. W6 Darwin CI：macos-latest check 在 `0b46327` success（run `32881922045`）。不是安装版 T23。
13. W5-E Darwin CI：`3f04d05` / `84b594c` macos-latest check success。不是安装版 T23。
14. W5 列表翻页源码合同：`tests/runtime-perf-wave5-list-flip-media-count.test.ts`（helper 合成 + 协议分类，非 GUI）。证据 `docs/evidence/runtime-perf-memory-wave5-list-flip-source-contract-20260825.json`、`wave5-list-flip-protocol-classify-20260825.json`。Wave 5 父项不勾。
15. W2-F M7：`frozenReferenceThumbnailCache` 同工程 LRU 硬顶 96。证据 `docs/evidence/runtime-perf-memory-wave2f-reference-cache-lru-20260825.json`。
16. W5 列表翻页源码合同 Darwin CI：`379df38` macos-latest check success（run `32886953416`）。不是安装版 T23。
17. Darwin CI flake：busy 合同测墙钟 500→5000。HEAD `e1ea3f1` macos-latest check success（run `32889586061`）。未改 120s 写库 timeout。不是安装版 T23。
18. W6 扩大复验：`typecheck:app` PASS；Linux 37 files / 320 tests PASS（含 list-flip / M7 LRU / busy 墙钟）。busy ledger 1 条仍需 Darwin dirfd。证据 `docs/evidence/runtime-perf-memory-wave6-reverify-expanded-20260825.json`。Wave 5 父项不勾。
19. W6 Darwin CI `36abafb`：macos-latest check success（run `32891408457`）。不是安装版 T23。证据 `docs/evidence/runtime-perf-memory-wave6-darwin-ci-36abafb-20260825.json`。
20. Darwin CI `5a075cf` Fast tests 红（run `32893004763`）：`-shm` 竞态 nlink=0 误判。修复 `3a3462e` macos-latest success（run `32894599796`）。symlink / 硬链仍失败关闭。
21. W2 §1.3 残差：有界 `unitIds` 按 id 直读，不再翻页扫集（见 `docs/evidence/runtime-perf-memory-wave2-unitids-by-id-20260825.json`）。§1.3 仍部分完成。Wave 5 父项不勾。
22. W2 §1.3 残差：画布详细诊断展开才读（见 `docs/evidence/runtime-perf-memory-wave2-diagnostics-lazy-open-20260825.json`）。
23. W2 §1.3 残差：省略/limit/earliest 走 lean 季集身份（见 `docs/evidence/runtime-perf-memory-wave2-episode-lean-identities-20260825.json`）。§1.3 仍部分完成。
24. W6 三刀后复验：`typecheck:app` PASS；Linux 38 files / 327 tests PASS（见 `docs/evidence/runtime-perf-memory-wave6-reverify-after-lean-20260825.json`）。安装版 T23 仍 NOT_RUN。
25. W2/3 §1.3 残差：时间线账本批读只读旁路（见 `docs/evidence/runtime-perf-memory-wave2-ledger-readonly-sql-20260825.json`）。驾驶舱 / T23 / unit-grid 可写入口未改。§1.3 仍部分完成。Wave 5 父项不勾。
26. W2/3 §1.3 残差：时间线历史 PASS 活库只读，不再 snapshot 复制整库（见 `docs/evidence/runtime-perf-memory-wave2-historical-facts-readonly-20260825.json`）。§1.3 仍部分完成。
27. W6 只读三刀后复验：`typecheck:app` PASS；Linux 41 files / 336 tests PASS（见 `docs/evidence/runtime-perf-memory-wave6-reverify-after-readonly-20260825.json`）。安装版 T23 仍 NOT_RUN。
28. W2/3 §1.3 残差：Linux 真 `getApprovedTimelineProjection` 手工 sqlite 接线（只 mock inspect；2500 末页 36；`unitTimingQueries=0`）。证据 `docs/evidence/runtime-perf-memory-wave2-projection-readonly-sql-20260825.json`。不是 P7 owner。安装版 T23 仍 NOT_RUN。
29. W2/3 §1.3 残差：Linux 手工 schema v1 侧车零 mock inspect + 真投影（2500 末页 36）。证据 `docs/evidence/runtime-perf-memory-wave2-projection-unmocked-inspect-20260825.json`。不是 P7 owner。安装版 T23 仍 NOT_RUN。
30. W6 零 mock inspect 后复验：`typecheck:app` PASS；Linux 41 files / 322 tests PASS（见 `docs/evidence/runtime-perf-memory-wave6-reverify-after-unmocked-inspect-20260825.json`）。安装版 T23 仍 NOT_RUN。
31. W6 Darwin CI 投影接线：`43945a2` run `32906441251` success（真 sqlite 5）；`2d8ce47` run `32906906873` success（零 mock inspect 6 + sqlite 5）；HEAD `4b1824a` run `32907435248` success（320 files）。见 `docs/evidence/runtime-perf-memory-wave6-darwin-ci-2d8ce47-20260825.json`。不是安装版 T23，不是 P7 owner。
32. Wave 7 运行热路径：`getStudioMedia` 只读旁路 + derivative 单次开库 + 核心裁剪 rebuild 并入 rAF（见 `docs/evidence/runtime-perf-memory-wave7-runtime-hotpath-20260826.json`）。二次 assert 与 worker 增量 schedule 保留。安装版 T23 仍 NOT_RUN。
33. 2026-08-26 外部席审查：千问 Token Plan + 方舟 Agent Plan 实调。主审 **无 must-fix**。已按主审执行：不拆 ledger、不全量异步化 rebuildGraph。入口 `python3 scripts/external-seat-review.py --brief <简报> -o /tmp/seat-review`（`--seats qwen` 可只跑主审）。证据 `docs/evidence/runtime-perf-memory-external-review-20260826.json`、`runtime-perf-memory-external-seat-runner-20260826.json`。
34. 2026-08-26 Goal 全面分析 + SSL-5 / span / 对照 peek / 阅读器选区配图 / 按 panel 读 pack / 对照表展开宫格 / SSL-5 焦点宫格与前镜交接 / 剧本库 coveredMediaCount / 对照侧栏构图·运镜·快照提及 / 阅读器选区命中带前镜并可跳对照 / 锁版站位缺口只读 / 对照表宫格列可点精确格 / 对照预览不再回退整行图 / 新冻结提示词写入前镜站位 / 单镜 Agent brief 暴露 previousStanding。不 dispatch、不自动 Review PASS。真生图 / T23 / P7 / 列表 GUI 仍等 owner。整 Goal 部分完成。
35. 2026-08-26 SSL-5 / 阅读器露出锁版光线服化，场景回指可点穿精确宫格（阅读器仅用户点穿才加载对照板）。证据 `docs/evidence/goal-ssl5-reader-lighting-costume-backrefs-20260826.json`。不是 BindingSet。整 Goal 仍部分完成。
36. 2026-08-26 生成控制无冻结包退锁版光线/服化，并露出当前 head 场景回指（点穿不猜第一格）。有包仍只认覆盖行。证据 `docs/evidence/goal-generation-control-lock-lighting-backrefs-20260826.json`。不改 dashboard / get_studio_trace。整 Goal 仍部分完成。
37. 2026-08-26 对照/SSL-5/阅读器/向导/检查器/生成控制/session-snapshot 露出跨单元道具回指（只认 `category=prop`；一次 IPC 同开库；点穿不猜第一格；不写冻结提示词）。证据 `docs/evidence/goal-prop-backrefs-20260826.json`。不是 BindingSet。整 Goal 仍部分完成。
38. 2026-08-26 对照/SSL-5/阅读器/向导/检查器/生成控制/session-snapshot 露出跨单元角色回指（只认 `category=character`；一次 IPC 同开库返回三套；点穿不猜第一格；不写冻结提示词）。证据 `docs/evidence/goal-character-backrefs-20260826.json`。不是 BindingSet。整 Goal 仍部分完成。
39. 2026-08-26 对照/SSL-5/阅读器/向导/检查器/生成控制/审片/session-snapshot 露出扩写格镜头类型（只解析已有「镜头类型：」行；锁版 `shot_type` 可选列；审片只认冻结包；不写新冻结行；整板 brief 最后一条仍是前镜句）。证据 `docs/evidence/goal-shot-type-extension-20260826.json`。不是 BindingSet。整 Goal 仍部分完成。
40. 2026-08-26 对照/SSL-5/阅读器/向导/检查器/生成控制/审片/session-snapshot 露出 15s 节拍（只读冻结包 target 起止秒；无包用驾驶舱已有时间字段；审片只认冻结包；不写新冻结行；整板 brief 最后一条仍是前镜句）。证据 `docs/evidence/goal-unit-beat-20260826.json`。不是 BindingSet。整 Goal 仍部分完成。
41. 2026-08-26 unit-grid Agent brief.BEATS 投影起止秒与扩写/原镜（`promptContractText` 含 `0–7.5s` / `原镜` / `扩写`；不改冻结 renderedPrompt；templateId / schemaVersion 不变；整板 brief 最后一条仍是前镜句）。证据 `docs/evidence/goal-unit-grid-brief-beats-20260826.json`。整 Goal 仍部分完成。
42. 2026-08-26 单镜 Agent brief 露出 `shotTypeLine` / `beatLine`（只从冻结包；无 panelId 不猜第一格；不改 renderedPrompt）。证据 `docs/evidence/goal-panel-brief-shot-type-beat-20260826.json`。整 Goal 仍部分完成。
43. 2026-08-26 SSL-5 露出 create-plan 只读草稿（`focusPackId` + `generationPlanDraft`；只认焦点缺图格自己的 pack，禁止用同行已出图 preview pack；recommendedPath 含 create-plan；不执行、不 dispatch）。证据 `docs/evidence/goal-ssl5-generation-plan-draft-20260826.json`。整 Goal 仍部分完成。
44. 2026-08-26 session-snapshot / 生成控制露出同一 create-plan 只读草稿（薄模块不拉对照板；只认当前宫格已落盘单镜包；整板 pack 不当单镜节点；草稿不进 fingerprint；不执行、不 dispatch）。证据 `docs/evidence/goal-session-control-plan-draft-20260826.json`。整 Goal 仍部分完成。
45. 2026-08-26 生成控制 / pack envelope 露出 unit-grid create-plan 只读草稿（节点 `{ targetKind: unit-grid, unitId }`；只认已落盘 history/计划 pack，不用 readiness 候选；pack `next` 从 create-plan 起；SSL-5 / session-snapshot 保持单镜；不执行、不 dispatch）。证据 `docs/evidence/goal-unit-grid-plan-draft-20260826.json`。整 Goal 仍部分完成。
46. 2026-08-26 session-snapshot 无 panelId 露出 unit-grid create-plan 草稿；readiness next 含 create-plan；生成控制露出命令名（只认账本已落盘 pack，不猜第一格；不执行、不 dispatch）。证据 `docs/evidence/goal-session-unit-grid-plan-draft-20260826.json`。整 Goal 仍部分完成。
47. 2026-08-26 驾驶舱冻完整板后下一步是建计划（`ready-to-plan` / `create-unit-grid-plan`；readiness 候选不当冻结包）。证据 `docs/evidence/goal-dashboard-create-plan-next-20260826.json`。整 Goal 仍部分完成。
48. 2026-08-26 earliest 只对一个 pending 单元按落盘 pack/计划区分 freeze/plan/dispatch（全槽位仍不读 pack；只读开库；statusLine 带 code；不执行、不 dispatch）。证据 `docs/evidence/goal-earliest-plan-next-20260826.json`。整 Goal 仍部分完成。
49. 2026-08-26 风格锁只读露出（只认已有 category=style；不开回指、不写新冻结行、不加第 8 槽；审片只认冻结包；向导不自动 loadAlign）。证据 `docs/evidence/goal-style-lock-readonly-20260826.json`。整 Goal 仍部分完成。
50. 2026-08-26 已有计划时 create-plan 草稿改标 dispatch（SSL-5 只精炼焦点缺图格；session-snapshot / 生成控制 / pack envelope 同步；单镜计划排除整板兼容 panel_id；不执行、不 dispatch）。证据 `docs/evidence/goal-create-plan-already-exists-dispatch-20260826.json`。整 Goal 仍部分完成。
51. 2026-08-26 已有计划时按节点状态区分 dispatch/wait/retry/Review（未传 status 保持 dispatch 文案；retry-superseded→planned；SSL-5 只精炼焦点；生成控制零额外 IPC；不改 earliest / 驾驶舱 nextAction；不执行、不 dispatch、不 retry）。证据 `docs/evidence/goal-plan-node-status-next-20260826.json`。整 Goal 仍部分完成。
52. 2026-08-26 earliest / 驾驶舱 nextAction 按计划节点状态区分 dispatch/wait/retry/Review（earliest 一次只读开库；驾驶舱用 latestRun.latestEventKind；inspect 优先；不改 T5；不执行、不 dispatch、不 retry）。证据 `docs/evidence/goal-earliest-dashboard-plan-status-next-20260826.json`。整 Goal 仍部分完成。
53. 2026-08-26 SSL-5 / 对照与 earliest wait/retry/Review 对齐（对照板带 earliestCode/label；焦点即 earliest 时禁止再建议 create-plan/dispatch；persist 之后 earliest wait 仍赢；不二次 getStudioEpisodeEarliest；不执行、不 dispatch、不 retry）。证据 `docs/evidence/goal-ssl5-earliest-blocking-next-20260826.json`。整 Goal 仍部分完成。
54. 2026-08-26 session-snapshot / 生成控制单镜草稿跟 unit-grid 在途对齐（驾驶舱 nextAction 或已加载 unit-grid 节点为 wait/retry/Review/对账时草稿不得再 ready；零额外 IPC；不进 fingerprint；不执行、不 dispatch、不 retry）。证据 `docs/evidence/goal-session-control-unit-grid-blocking-next-20260826.json`。整 Goal 仍部分完成。
55. 2026-08-26 pack envelope 单镜草稿/next 跟 unit-grid 在途对齐（同单元 unit-grid 节点 dispatched/failed/cancelled/succeeded 时草稿不得再 ready，next 改 wait/retry/Review；不加 inspect；planned 不挡；不执行、不 dispatch、不 retry）。证据 `docs/evidence/goal-pack-envelope-unit-grid-blocking-next-20260826.json`。整 Goal 仍部分完成。
56. 2026-08-26 readiness next 跟 unit-grid 在途对齐（单镜/整板 ready 路径的 agentExecution.next 在同单元 unit-grid 已在途/待重试/待审时改 wait/retry/Review；freeze writeCommand 不变；不加 inspect；不执行、不 dispatch、不 retry）。证据 `docs/evidence/goal-readiness-unit-grid-blocking-next-20260826.json`。整 Goal 仍部分完成。
57. 2026-08-26 画布节点下一步跟 unit-grid 在途对齐（检查器「下一步」用已加载 unitDetail.nextAction；wait/retry/Review/对账时禁用 freeze-dispatch 并露出同一下一步；planned 不挡；零额外 IPC；不执行、不 dispatch、不 retry）。证据 `docs/evidence/goal-canvas-node-unit-grid-blocking-next-20260826.json`。整 Goal 仍部分完成。
58. 2026-08-26 active-runs envelope next 跟本槽/unit-grid 对齐（unknown→对账、未审→Review、在途→wait；单镜另看同单元 unit-grid；generationBlocked 仍只认本槽 blockingRuns；不改 T4 ledger；不加 inspect；不执行、不 dispatch、不 retry）。证据 `docs/evidence/goal-active-runs-unit-grid-blocking-next-20260826.json`。整 Goal 仍部分完成。
59. 2026-08-26 15s 向导物化后下一步含 create-plan（nextSteps / 物化结果写 Binding→freeze→create-plan→dispatch；不跳过 Binding，不自动派发，不改 formatWizardPromptBody）。证据 `docs/evidence/goal-wizard-post-materialize-create-plan-next-20260826.json`。整 Goal 仍部分完成。
60. 2026-08-26 generation-control operation=plan 信封 next 跟节点状态对齐（按 planId/单元查 wait/retry/dispatch/Review/create-plan；未限定列表不猜单元；生成控制零额外 IPC；标准环先读 plan next；不执行、不 dispatch、不 retry；不改 T4/T5）。证据 `docs/evidence/goal-plan-envelope-next-20260826.json`。整 Goal 仍部分完成。
61. 2026-08-26 generation-control operation=history 信封 next 跟本页结果对齐（未成对→wait、成对 pending→Review、成对 rejected→retry；要看最新请 newest-first；生成控制零额外 IPC；不改 history list SQL；不 evaluate 像素；不自动 Review PASS；不改 T4/T5）。证据 `docs/evidence/goal-history-envelope-next-20260826.json`。整 Goal 仍部分完成。
62. 2026-08-26 session-snapshot 一致性四态 peek 辅助 Review（当前宫格 newest-first 结果 run，否则整板 latest；只读 LRU；未评估 ≠ 无法检查；动态 import peek，不 evaluate；不进 fingerprint；机器不自动 Review PASS）。证据 `docs/evidence/goal-session-consistency-peek-20260826.json`。整 Goal 仍部分完成。
63. 2026-08-26 generation-control operation=history 信封 consistencyPeek 辅助 Review（本页优先成对 run；只读 LRU；未评估 ≠ 无法检查；动态 import peek，不 evaluate；生成控制零额外 IPC；Vue 不 import evaluator；不改 history list SQL；不改 T4/T5；机器不自动 Review PASS）。证据 `docs/evidence/goal-history-consistency-peek-20260826.json`。整 Goal 仍部分完成。
64. 2026-08-26 get_studio_trace / 画布检查器一致性四态 peek 辅助 Review（追溯 by-run 用该 run、否则本包最新 run；无 run 省略以免改 P24 形状；检查器复用 history 信封；动态 import peek，不 evaluate；不改 dashboard / T4/T5 / P24 golden；机器不自动 Review PASS）。证据 `docs/evidence/goal-trace-inspector-consistency-peek-20260826.json`。整 Goal 仍部分完成。
65. 2026-08-26 阅读器 span 命中 / SSL-5 横幅一致性四态 peek（命中按 generationRunId 只读 LRU；SSL-5 复用对照板焦点格/行已有 peek，不偷同行已出图格；未评估 ≠ 无法检查；Vue 不 import evaluator；机器不自动 Review PASS）。证据 `docs/evidence/goal-reader-ssl5-consistency-peek-20260826.json`。整 Goal 仍部分完成。
66. 2026-08-26 对照/SSL-5 复用六图闸禁止再建议派发（对照板拷贝 earliest 已算 checkpoint；未放行新槽时草稿不得 ready；earliest wait/retry/Review 文案更具体时保留；不二次读闸；对照表 Review 角标；阅读器 earliestReason；不执行停检、不派发）。证据 `docs/evidence/goal-ssl5-checkpoint-gate-20260826.json`。整 Goal 仍部分完成。
67. 2026-08-26 对照/SSL-5 露出写租约与缺图报告（对照板拷贝 earliest 已算 writeLease，不暴露 token；未持有时 SSL-5 在 freeze 前插入 acquire-lease；未投影/闸/earliest 不插；不改草稿 ready；对照可复制 missingReport JSON；不抢租约、不派发）。证据 `docs/evidence/goal-ssl5-write-lease-missing-report-20260826.json`。整 Goal 仍部分完成。
68. 2026-08-26 15s 向导复用已加载对照板六图闸/写租约（未加载明示不自动查；建议路径仍不 loadAlign；不改物化 prompt / WIZARD_POST_MATERIALIZE_NEXT；不派发）。证据 `docs/evidence/goal-wizard-align-checkpoint-lease-20260826.json`。整 Goal 仍部分完成。
69. 2026-08-26 阅读器复用已算 earliest 露出六图闸/写租约（checkpointLine / writeLeaseLine；不 import 对照模块以免循环依赖；schemaVersion 仍为 1；不抢租约、不派发）。证据 `docs/evidence/goal-reader-checkpoint-lease-20260826.json`。整 Goal 仍部分完成。
70. 2026-08-26 session-snapshot 写租约只读 peek（`withStudioProjectWriteLease` + `getStudioProjectWriteLeaseReadOnly`；只拷 held/holderId/denialHint/line；不暴露 token；不进 fingerprint；不改 nextAction / 草稿 ready；失败关闭为未持有；不抢租约、不派发）。证据 `docs/evidence/goal-session-write-lease-20260826.json`。整 Goal 仍部分完成。
71. 2026-08-26 session-snapshot 六图闸只读 peek（动态 import 首屏 `getStudioGenerationCheckpointDashboardGate`；只拷 newSlotDispatchAllowed/blockingBatchNumber/line；不进 fingerprint；不改 nextAction / 草稿 ready；失败关闭为未放行；不执行停检、不派发）。证据 `docs/evidence/goal-session-checkpoint-gate-20260826.json`。整 Goal 仍部分完成。
72. 2026-08-26 画布主按钮/节点 freeze-dispatch 复用已加载 overview 六图闸（未放行时禁用主按钮；primaryStart / runNodeAction 不 persist / 不 emit；未投影不挡；unit-grid wait 文案优先；零额外 IPC；不改动作 code / dashboard / T4/T5）。`runLastWorkflowGroup` 仍未挡。证据 `docs/evidence/goal-canvas-overview-checkpoint-gate-20260826.json`。整 Goal 仍部分完成。
73. 2026-08-26 执行最近工作流组复用已加载 overview 六图闸（按钮禁用；runLastWorkflowGroup 不 executeWorkflowGroup；未投影不挡；零额外 IPC）。主按钮/`runLastWorkflowGroup` 仍未挡 unit-grid wait。证据 `docs/evidence/goal-canvas-run-workflow-checkpoint-gate-20260826.json`。整 Goal 仍部分完成。
74. 2026-08-26 画布主按钮/节点/工作流组复用已加载 unit-grid 在途闸（wait/retry/Review/对账禁用并 return；planned 不挡；零额外 IPC；Vue 从 node-action-panel 再导出 kind helper）。证据 `docs/evidence/goal-canvas-unit-grid-dispatch-gate-20260826.json`。整 Goal 仍部分完成。
75. 2026-08-26 阅读器本修订影响复用 P24 script-revision-impact（按需 IPC；不进 ScriptReaderView；非预期须人工复核；无 pack/run 不猜第一格；不改 getStudioTrace selector / P24 golden）。证据 `docs/evidence/goal-reader-script-revision-impact-20260826.json`。整 Goal 仍部分完成。
76. 2026-08-26 阅读器本修订影响点穿对照格 + 已加载 unexpected 精炼 SSL-5（有 panelId 跳精确格；无 panelId 只露单元行、不猜第一格；未加载不自动查；earliest/六图闸文案更具体时保留；薄模块不拉 studio-trace）。证据 `docs/evidence/goal-reader-impact-align-unexpected-ssl5-20260826.json`。整 Goal 仍部分完成。
77. 2026-08-26 本修订影响可翻下一页（nextCursor 追加，不一次拉全量）+ 对照表/侧栏复用已加载 unexpected 角标；SSL-5 非预期时按钮改去审片。未加载不自动查。证据 `docs/evidence/goal-reader-impact-next-align-mark-20260826.json`。整 Goal 仍部分完成。
78. 2026-08-26 MCP/Core `ssl5-missing-to-gen-plan` 接受调用方已取回的 `revisionImpact`；焦点 unexpected 则 recommendedPath=review，禁止再建议 create-plan/dispatch；省略不自动查。短环 prompt 写明未取回不要为了 ssl5 去查。SSL-5 不 import `studio-trace`。桌面对照仍 Vue 叠加。证据 `docs/evidence/goal-ssl5-revision-impact-hint-20260826.json`。整 Goal 仍部分完成。

不要自动把 drifted 改成 PASS。不要平行一致性数据库。

验证：本计划定向测已在 W6-A 与 W6 复验绿。Linux `npm test` 会因 Darwin dirfd / `/private/tmp` 红，不当新回归；CI 是 `macos-latest`。

---

## 3. 已经完成（不要重做）

| 项 | 状态 |
|---|---|
| P0–P14 owner | final-validation PASS，禁止重建 |
| 开源发布 | Apache-2.0；CI 绿；CodeQL 告警 0 |
| N1–N123 键盘/CV | 饱和；**停止 N124** |
| 角色库产品 | 入库图+音频、钉画布带出 |
| W1 Brief 合同 | `promptContract` 7 槽已挂 `buildStudioUnitGridAgentImagegenBrief`；**不改** freeze `renderedPrompt` |
| 本机人物库同步 | 42 人；声线：阿航/阿依/嘟嘟/豆姐/图腾腾 |
| 测试运行时 | `scripts/lib/ensure-test-runtime.ts`：缺 Electron / dist-mcp 会先补 |

W1 代码：`src/core/unit-grid-brief-contract.ts`，注入说明 `docs/prompts/INJECTION.md`。

人物同步脚本（幂等）：`npx tsx scripts/sync-local-character-library.ts`  
桌面硬链接图（nlink≠1）会先拷到临时普通文件再 `import_studio_media`。

---

## 4. 硬边界（碰到先停）

**禁止重做/替换 owner：**  
material-studio / studio-production / managed-project / studio-asset-binding / studio-binding-control / panel-reference-resolution-core / studio-continuity* / studio-generation* / studio-generation-ledger / studio-generation-review / checkpoint / **studio-production-dashboard** / studio-reliability / build-identity。

另外：

- 不改 T23 `unitTimingQueries === returnedUnitCount`
- 不写正式工程旁路；写入只走 `execute_command` / `executeIdempotentCommand`
- 不正式批量生图（除非 owner 指定隔离单元 canary）
- 不用 ComfyUI；不弹窗 GUI 测试
- 不把 API Key / `.env` 写进对话或 git；密钥只 `source ~/.grok/env.local`
- 黄金面具唯一权威 SHA-256 `02e9438ecee038f7d14860da37cb315bf358db4a26fa224e342eee5b592b55a9`（`prop-d01-golden-mask`）；禁止半面具。嘟嘟只锁 R07。
- 豆姐桌面 `豆姐参考图.png` 已不在；人物库豆姐主图用硬锁包 D01 干净金面。

五条系统红线仍有效：不破坏系统、不改网络底层、不泄漏凭据、不删未提交工作、不批量不确定破坏。

---

## 5. 常用命令

```bash
cd /Users/hxx/Documents/无限画布
npm run typecheck:app
npm test                          # = test:fast
npx vitest run tests/<file>.ts
npx tsx scripts/list-studio-characters.ts
npx tsx scripts/sync-local-character-library.ts
```

CI 只跑 `typecheck:app` + fast。`vue-tsc` 本地 `npm run typecheck` 目前是绿的，但 CI 还没接。medium/integration/heavy 不在每次 push 里。

提交：`类型：简短描述`。不要为 STATUS 单独 push（会再触发约 3 小时 CodeQL）。有真实代码再推。

---

## 6. 怎么调动其他外部模型

Cursor **不会**内置切到千问/豆包/GLM/DeepSeek/Grok/Codex。要调就 **Bash 跑入口**。密钥只 source / Cloud Secret，禁止把完整 Key 贴进对话或 commit。

### 6.0 Cloud Agent（这台 Linux）

Mac 上的 `~/.grok/env.local` **不会**自动进 Cloud。这台机器有 Bash、能出网。

**面板填法（给后续新 Agent）**

| 项 | 值 |
|---|---|
| Name | `TOKENROUTER_API_KEY` |
| Type | Runtime Secret |
| Scope | Personal |
| 可选非密钥 | `TOKENROUTER_BASE_URL=https://api.tokenrouter.com/v1` |

填完后 **新开** Cloud Agent。当前已在跑的 VM 通常看不到后加的 Secret。完整 Key **不要**再贴进对话或 commit。

**新窗口默认入口**（Secret 已注入成环境变量时）：

```bash
python3 ~/.grok/bin/novel_chat.py --role tr --prompt "问题" -o /tmp/tr_out.txt --timeout-sec 0
```

没有 `~/.grok/env.local` 时 **不要跑 `tr_chat`**（它会 source 那个文件）。`novel_chat.py --role tr` 能读环境变量。

只准 TokenRouter 三条入口，禁止另找官方千问 / 火山 / OpenAI。本机已有 `env.local` 时才可用：

```bash
~/.grok/bin/tr_chat "问题" -o /tmp/tr_out.txt

python3 ~/.grok/bin/novel_chat.py --role tr --prompt "问题" -o /tmp/tr_out.txt --timeout-sec 0

curl -sS https://api.tokenrouter.com/v1/chat/completions \
  -H "Authorization: Bearer $TOKENROUTER_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen/qwen3.8-max-free","messages":[{"role":"user","content":"问题"}],"max_tokens":2048}'
```

- Base URL：`https://api.tokenrouter.com/v1`（可用环境变量 `TOKENROUTER_BASE_URL`，只许这个值）
- 模型：`qwen/qwen3.8-max-free`
- Key 名：`TOKENROUTER_API_KEY`（本机 `~/.grok/env.local` 或 Cloud Runtime Secret 注入的环境变量）
- 仓库副本：`scripts/tokenrouter/`；安装：`bash scripts/install-tokenrouter-cli.sh`（装到 `~/.grok/bin`，不把 Key 写进 git）
- Cloud 引导：`bash scripts/cloud-agent-install.sh`（`npm ci` + 可选装 CLI；**不要求** Key）
- HTTP 500 / `do_request_failed` 重试 1–2 次，不是「调不了外部模型」
- 禁止把「切换自身模型」说成「调不了外部 API」

**火山方舟 Agent Plan（本 Cloud 已通）**

- Key 名：`ARK_API_KEY`（只在 `~/.grok/env.local`，chmod 600；完整值不进 git / 对话）
- 入口：`POST https://ark.cn-beijing.volces.com/api/plan/v3/chat/completions`
- 实测：`doubao-seed-evolving` / `glm-5.3` 200；普通 `/api/v3` 与 Coding Plan `/api/coding/v3` 对这条 Key 是 401
- `kimi-k3` 可能 429（过载，重试，不当成调不了）
- **不要**把 `volces.com` 写进 `scripts/tokenrouter/`（TokenRouter 入口仍只打 tokenrouter.com）

**千问 Token Plan（本 Cloud 已通）**

- Key 名：`QWEN_TOKEN_PLAN_API_KEY`（`sk-sp-` 前缀；只在 `~/.grok/env.local`，chmod 600；完整值不进 git / 对话）
- 入口：`POST https://token-plan.cn-beijing.maas.aliyuncs.com/compatible-mode/v1/chat/completions`
- 实测：`qwen3.8-max-preview` 200。国际 `token-plan.ap-southeast-1` 对这条 Key 是 401
- 同通道模型列表还含 `qwen3.7-max` / `deepseek-v4-pro` / `glm-5.2` 等
- **不要**把 `aliyuncs.com` 写进 `scripts/tokenrouter/`

**分席审查入口（仓库脚本，不经 TokenRouter）**

```bash
python3 scripts/external-seat-review.py --brief <简报> -o /tmp/seat-review
# 默认 glm + deepseek + qwen；豆包需 --with-doubao（易超时）
# 子集：--seats qwen
# --timeout-sec 0 = 无客户端超时
```

- 只读 `~/.grok/env.local` 的 `ARK_API_KEY` / `QWEN_TOKEN_PLAN_API_KEY`，不 echo Key
- HTTP 429 / 500 / `do_request_failed` 重试，401 不重试
- 主审是 qwen；无 must-fix 时不要按 DeepSeek 拆 command-bus ledger 或全量异步化 `rebuildGraph`


权威合同：

| 文件 | 用途 |
|---|---|
| `~/.grok/NOVEL_MULTI_MODEL.md` | 席位、通道、参数（以文首最新状态为准） |
| `~/.grok/DECISION_PROTOCOL.md` | 决断：多模型互评 → 主审合成 → 直接执行 |
| `~/.grok/env.local` | 密钥（chmod 600；只 source） |
| `~/.grok/bin/novel_chat.py` | **统一 CLI**，默认无客户端超时 |

### 6.1 先加载密钥

```bash
set -a && source ~/.grok/env.local && set +a
```

### 6.2 `novel_chat.py`（写作 / 审校 / 互评）

```bash
# 主笔正文 → 千问 3.8
python3 ~/.grok/bin/novel_chat.py --role qwen --file task.md \
  -o /tmp/qwen.txt --meta /tmp/qwen_meta.json

# 结构审校 → GLM（火山 Plan，低温）
python3 ~/.grok/bin/novel_chat.py --role glm --temperature 0.3 --file review.md \
  -o /tmp/glm.txt --meta /tmp/glm_meta.json

# 犬声口 / 备笔 → 豆包 evolving
python3 ~/.grok/bin/novel_chat.py --role doubao --file task.md \
  -o /tmp/doubao.txt --meta /tmp/doubao_meta.json

# 去 AI 味 / 探针 → DeepSeek V4 Pro
python3 ~/.grok/bin/novel_chat.py --role deepseek --temperature 1.0 --top-p 0.95 \
  --file task.md -o /tmp/ds.txt --meta /tmp/ds_meta.json

# 中文语感审校 → 文心 5.1（必须 Token Plan Person 通道，脚本已配）
python3 ~/.grok/bin/novel_chat.py --role wenxin --file task.md \
  -o /tmp/wx.txt --meta /tmp/wx_meta.json

# Kimi K3（火山 Agent Plan；不要用 kimi-latest）
python3 ~/.grok/bin/novel_chat.py --role kimi --file task.md \
  -o /tmp/kimi.txt --meta /tmp/kimi_meta.json
```

也可以 `--prompt "..."`。`-o` 写正文，`--meta` 写耗时/token。`--timeout-sec 0` 是默认，**禁止**短超时当默认。

| `--role` | 默认模型 | 通道 | 用途 |
|---|---|---|---|
| `qwen` | `qwen3.8-max-preview` | Token Plan `QWEN_*` | 主笔正文；一章一主笔 |
| `glm` | `glm-5.3` | 火山 `/api/plan/v3` + `DOUBAO_*` | 结构审，出问题表，不直接贴成稿 |
| `doubao` | `doubao-seed-evolving` | 同上火山 Plan | 犬声口 / 备笔 |
| `deepseek` | `deepseek-v4-pro` | 与千问同 Token Plan | 去 AI 味 / 探针；不接默认主笔 |
| `wenxin` | `ernie-5.1` | 千帆 Token Plan Person | 中文语感；`ernie-x1.1` 会 403 |
| `kimi` | `kimi-k3` | 火山 Agent Plan | 第二读感；勿用 `kimi-latest` |

**禁止**把各厂 Key 混成同一路、禁止同章拼盘正文。

### 6.3 Codex / GPT-5.6（ChatGPT 订阅，不是 OPENAI_API_KEY）

```bash
codex exec --skip-git-repo-check -m gpt-5.6-sol "任务说明……"
```

登录：`~/.codex/auth.json`（`auth_mode: chatgpt`）。默认推理 `max`。代码交叉验证、owner 点名 Codex 时用。

### 6.4 Grok（本机 grok CLI / xAI）

```bash
grok -p "任务说明……" -m grok-4.5 --reasoning-effort high
```

配置：`~/.grok/config.toml`。只读分析任务必须写死「只读不改，只输出报告」，收工 `git status` 防它落盘。

### 6.5 决断怎么用这些席

owner 要方案/改不改线时：**不要把选择题甩回用户**。默认：

1. 至少三席互评（结构 GLM、主笔/商业千问、声口豆包）
2. 你（或 Grok）合成唯一结论
3. 直接执行并落盘

协议：`~/.grok/DECISION_PROTOCOL.md`。

无限画布 **软件研发**默认你自己改代码 + 测试；不必每刀都开四模型。需要独立意见、写提示词、或 owner 说「多模型 / 用千问」时再调。

生图：本仓正式供应仍是 Codex `agent-imagegen`。owner 规则里的 Codex 生图走 `codex exec` 内置 image_gen，不走 ComfyUI。

---

## 7. 建议的 Cursor 开场提示词

把下面整段贴进 Cursor：

```
你在 /Users/hxx/Documents/无限画布。先读 docs/交接_给Cursor_20260825.md、STATUS.md、AGENTS.md。
earliest 是速度/内存计划 Wave 4-B：adaptation / story.js / novel-agent-service 动态 import。
不要做 N124 CV，不要重建 P0–P14 owner，不要全盘扫正式工程，不要弹窗 GUI。
调外部模型用 ~/.grok/bin/novel_chat.py（先 source ~/.grok/env.local），禁止把 Key 贴进对话。
做完更新 STATUS.md / TASKS.md / docs/当前开发交接.md。
```
