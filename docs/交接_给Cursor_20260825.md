# 无限画布 · 给 Cursor 的任务交接（2026-08-25）

你在 **Cursor** 里接着改这个仓库。先读本文件 + `STATUS.md` + `AGENTS.md`，不要从 N124 或 24h content-visibility 空转续。

- 仓库：`/Users/hxx/Documents/无限画布`
- GitHub：https://github.com/BB20260410/ai-drama-canvas （Apache-2.0，公开）
- HEAD：`afda7fe`（`main` 已推）
- 实时交接：`docs/当前开发交接.md`（本文件是给 Cursor 的开工卡；冲突以 STATUS / 当前开发交接为准）
- 研发规范：`AGENTS.md` + skill `ai-drama-canvas-agent`

默认简体中文。进度写 `STATUS.md` / `TASKS.md`。Git 提交信息用中文：`类型：简短描述`。

---

## 1. 项目是什么

Vue 3 + Electron 桌面「AI 漫剧无限画布」。本地优先：剧本、硬锁图、15 秒单元、宫格、CAS、MCP。正式生图走 Codex `agent-imagegen`，不走 ComfyUI / 浏览器供应链。

正式受管工程（本机，**不进 git**）：

```
/Users/hxx/Documents/无限画布/projects/codex-ai-drama-studio
```

约 1.1GB；当前人物库 **42 人 / 5 条声线**（含图腾腾）。禁止当空库、禁止全盘扫 SQLite/CAS。

本机 App：`/Applications/AI 漫剧画布.app`。不要为了改代码去弹 Playwright/Electron GUI，除非 owner 点名。

---

## 2. 你接手后立刻做

**earliest = 等待 owner。** 速度/内存计划本 Cloud 阻塞（整计划部分完成）。§1.3 by-id + 诊断懒展开 + lean 季集身份已落地。不要重做已落地 Wave，不要写「运行速度已关账」。仅用户能解：安装版 T23、列表 GUI 探针、P7 owner 对照。

计划：`docs/PLAN_运行速度与内存占用长期执行_20260825.md`。W1–W6 已落地；W6 复验两路审查 PASS + Linux 定向 35/305；驾驶舱 bundle 正式时间线已有界；**get_capabilities 握手不再探测 FFmpeg**。§1.5 源码合同测已补（非 GUI）。结论 **部分完成**（P7 owner 对照需 Darwin；安装版 T23 未跑；列表翻页 GUI 探针未跑）。

W2 bundle 已交付：`resolveProjectionBundleApprovedTimelineUnitIds`；多媒体 storyboard 只请求当前单元。证据 `docs/evidence/runtime-perf-memory-wave2-bundle-approved-bound-20260825.json`。

W4 cap 已交付：`DEFERRED_VIDEO_ENGINE_CAPABILITY`。证据 `docs/evidence/runtime-perf-memory-wave4-capabilities-deferred-engine-20260825.json`。两路审查 PASS。禁止写「连上 Agent 永不加载 editor」。

W4 novel 已交付：`withNovelAnalysis` / `withNovelAnalysisProvider`。证据 `docs/evidence/runtime-perf-memory-wave4-novel-analysis-lazy-20260825.json`。两路审查 PASS。禁止写「连上 Agent 永不加载小说分析」。

W6-C 已交付：§1 八条对照 + 全量审查 + Linux `npm test` 分类。纠正 composable 空 `unitIds` 与 executor 边界注释。

交付：

1. W6-A：已完成（见 `docs/evidence/runtime-perf-memory-wave6a-20260825.json`）。
2. W6-B：已写未跑（见 `docs/evidence/runtime-perf-memory-wave6b-20260825.json`）。
3. W6-C：已完成对照与审查（见 `docs/evidence/runtime-perf-memory-wave6c-20260825.json`）。整计划部分完成。
4. W2 bundle：已完成 Linux 源码合同（见 `docs/evidence/runtime-perf-memory-wave2-bundle-approved-bound-20260825.json`）。两路审查 PASS。
5. W4 cap：get_capabilities 握手 deferred engine（见 `docs/evidence/runtime-perf-memory-wave4-capabilities-deferred-engine-20260825.json`）。两路审查 PASS。
6. W4 novel：启动路径不静态拉小说分析整图（见 `docs/evidence/runtime-perf-memory-wave4-novel-analysis-lazy-20260825.json`）。
7. W4 settings：握手读分析配置走薄模块（见 `docs/evidence/runtime-perf-memory-wave4-capabilities-settings-thin-20260825.json`）。两路审查 PASS。
8. 不改 T23 SQL；不扫正式工程；不重做 N124。
9. W6 复验：两路审查 PASS；`typecheck:app` PASS；Linux 定向 35 files / 305 tests PASS（见 `docs/evidence/runtime-perf-memory-wave6-reverify-20260825.json`）。§1.4 源码合同已完成。整计划仍部分完成。
10. W5-E Main 切工程 evict：关闭/切换账本 watcher 时淘汰旧工程 `verifiedFileCache`（见 `docs/evidence/runtime-perf-memory-wave5e-project-switch-evict-20260825.json`）。Vue 不接线。
11. W5-E 在途写回拦截：leaveGeneration 拦住旧 inspect 写回；媒体请求换根淘汰上一工程（见 `docs/evidence/runtime-perf-memory-wave5e-stale-writeback-guard-20260825.json`）。
12. W6 Darwin CI：macos-latest check 在 `0b46327` success（run `32881922045`）。不是安装版 T23。
13. W5-E Darwin CI：`3f04d05` / `84b594c` macos-latest check success。不是安装版 T23。
14. W5 列表翻页源码合同：`tests/runtime-perf-wave5-list-flip-media-count.test.ts`（helper 合成 + 协议分类，非 GUI）。证据 `docs/evidence/runtime-perf-memory-wave5-list-flip-source-contract-20260825.json`、`wave5-list-flip-protocol-classify-20260825.json`。Wave 5 父项不勾。
15. W2-F M7：`frozenReferenceThumbnailCache` 同工程 LRU 硬顶 96。证据 `docs/evidence/runtime-perf-memory-wave2f-reference-cache-lru-20260825.json`。
16. W5 列表翻页源码合同 Darwin CI：`379df38` macos-latest check success（run `32886953416`）。不是安装版 T23。
17. Darwin CI flake：busy 合同测墙钟 500→5000。HEAD `e1ea3f1` macos-latest check success（run `32889586061`）。未改 120s 写库 timeout。不是安装版 T23。
18. W6 扩大复验：`typecheck:app` PASS；Linux 37 files / 320 tests PASS（含 list-flip / M7 LRU / busy 墙钟）。busy ledger 1 条仍需 Darwin dirfd。证据 `docs/evidence/runtime-perf-memory-wave6-reverify-expanded-20260825.json`。Wave 5 父项不勾。
19. W6 Darwin CI `36abafb`：macos-latest check success（run `32891408457`）。不是安装版 T23。证据 `docs/evidence/runtime-perf-memory-wave6-darwin-ci-36abafb-20260825.json`。
20. Darwin CI `5a075cf` Fast tests 红（run `32893004763`）：`-shm` 竞态 nlink=0 误判。修复 `3a3462e` macos-latest success（run `32894599796`）。symlink / 硬链仍失败关闭。
21. W2 §1.3 残差：有界 `unitIds` 按 id 直读，不再翻页扫集（见 `docs/evidence/runtime-perf-memory-wave2-unitids-by-id-20260825.json`）。§1.3 仍部分完成。Wave 5 父项不勾。
22. W2 §1.3 残差：画布详细诊断展开才读（见 `docs/evidence/runtime-perf-memory-wave2-diagnostics-lazy-open-20260825.json`）。
23. W2 §1.3 残差：省略/limit/earliest 走 lean 季集身份（见 `docs/evidence/runtime-perf-memory-wave2-episode-lean-identities-20260825.json`）。§1.3 仍部分完成。

不要自动把 drifted 改成 PASS。不要平行一致性数据库。

验证：本计划定向测已在 W6-A 与 W6 复验绿。Linux `npm test` 会因 Darwin dirfd / `/private/tmp` 红，不当新回归；CI 是 `macos-latest`。

---

## 3. 已经完成（不要重做）

| 项 | 状态 |
|---|---|
| P0–P14 owner | final-validation PASS，禁止重建 |
| 开源发布 | Apache-2.0；CI 绿；CodeQL 告警 0 |
| N1–N123 键盘/CV | 饱和；**停止 N124** |
| 角色库产品 | 入库图+音频、钉画布带出 |
| W1 Brief 合同 | `promptContract` 7 槽已挂 `buildStudioUnitGridAgentImagegenBrief`；**不改** freeze `renderedPrompt` |
| 本机人物库同步 | 42 人；声线：阿航/阿依/嘟嘟/豆姐/图腾腾 |
| 测试运行时 | `scripts/lib/ensure-test-runtime.ts`：缺 Electron / dist-mcp 会先补 |

W1 代码：`src/core/unit-grid-brief-contract.ts`，注入说明 `docs/prompts/INJECTION.md`。

人物同步脚本（幂等）：`npx tsx scripts/sync-local-character-library.ts`  
桌面硬链接图（nlink≠1）会先拷到临时普通文件再 `import_studio_media`。

---

## 4. 硬边界（碰到先停）

**禁止重做/替换 owner：**  
material-studio / studio-production / managed-project / studio-asset-binding / studio-binding-control / panel-reference-resolution-core / studio-continuity* / studio-generation* / studio-generation-ledger / studio-generation-review / checkpoint / **studio-production-dashboard** / studio-reliability / build-identity。

另外：

- 不改 T23 `unitTimingQueries === returnedUnitCount`
- 不写正式工程旁路；写入只走 `execute_command` / `executeIdempotentCommand`
- 不正式批量生图（除非 owner 指定隔离单元 canary）
- 不用 ComfyUI；不弹窗 GUI 测试
- 不把 API Key / `.env` 写进对话或 git；密钥只 `source ~/.grok/env.local`
- 黄金面具唯一权威 SHA-256 `02e9438ecee038f7d14860da37cb315bf358db4a26fa224e342eee5b592b55a9`（`prop-d01-golden-mask`）；禁止半面具。嘟嘟只锁 R07。
- 豆姐桌面 `豆姐参考图.png` 已不在；人物库豆姐主图用硬锁包 D01 干净金面。

五条系统红线仍有效：不破坏系统、不改网络底层、不泄漏凭据、不删未提交工作、不批量不确定破坏。

---

## 5. 常用命令

```bash
cd /Users/hxx/Documents/无限画布
npm run typecheck:app
npm test                          # = test:fast
npx vitest run tests/<file>.ts
npx tsx scripts/list-studio-characters.ts
npx tsx scripts/sync-local-character-library.ts
```

CI 只跑 `typecheck:app` + fast。`vue-tsc` 本地 `npm run typecheck` 目前是绿的，但 CI 还没接。medium/integration/heavy 不在每次 push 里。

提交：`类型：简短描述`。不要为 STATUS 单独 push（会再触发约 3 小时 CodeQL）。有真实代码再推。

---

## 6. 怎么调动其他外部模型

Cursor **不会**内置切到千问/豆包/GLM/DeepSeek/Grok/Codex。要调就 **Bash 跑本机入口**。密钥只 source，禁止把完整 Key 贴进对话或 commit。

权威合同：

| 文件 | 用途 |
|---|---|
| `~/.grok/NOVEL_MULTI_MODEL.md` | 席位、通道、参数（以文首最新状态为准） |
| `~/.grok/DECISION_PROTOCOL.md` | 决断：多模型互评 → 主审合成 → 直接执行 |
| `~/.grok/env.local` | 密钥（chmod 600；只 source） |
| `~/.grok/bin/novel_chat.py` | **统一 CLI**，默认无客户端超时 |

### 6.1 先加载密钥

```bash
set -a && source ~/.grok/env.local && set +a
```

### 6.2 `novel_chat.py`（写作 / 审校 / 互评）

```bash
# 主笔正文 → 千问 3.8
python3 ~/.grok/bin/novel_chat.py --role qwen --file task.md \
  -o /tmp/qwen.txt --meta /tmp/qwen_meta.json

# 结构审校 → GLM（火山 Plan，低温）
python3 ~/.grok/bin/novel_chat.py --role glm --temperature 0.3 --file review.md \
  -o /tmp/glm.txt --meta /tmp/glm_meta.json

# 犬声口 / 备笔 → 豆包 evolving
python3 ~/.grok/bin/novel_chat.py --role doubao --file task.md \
  -o /tmp/doubao.txt --meta /tmp/doubao_meta.json

# 去 AI 味 / 探针 → DeepSeek V4 Pro
python3 ~/.grok/bin/novel_chat.py --role deepseek --temperature 1.0 --top-p 0.95 \
  --file task.md -o /tmp/ds.txt --meta /tmp/ds_meta.json

# 中文语感审校 → 文心 5.1（必须 Token Plan Person 通道，脚本已配）
python3 ~/.grok/bin/novel_chat.py --role wenxin --file task.md \
  -o /tmp/wx.txt --meta /tmp/wx_meta.json

# Kimi K3（火山 Agent Plan；不要用 kimi-latest）
python3 ~/.grok/bin/novel_chat.py --role kimi --file task.md \
  -o /tmp/kimi.txt --meta /tmp/kimi_meta.json
```

也可以 `--prompt "..."`。`-o` 写正文，`--meta` 写耗时/token。`--timeout-sec 0` 是默认，**禁止**短超时当默认。

| `--role` | 默认模型 | 通道 | 用途 |
|---|---|---|---|
| `qwen` | `qwen3.8-max-preview` | Token Plan `QWEN_*` | 主笔正文；一章一主笔 |
| `glm` | `glm-5.3` | 火山 `/api/plan/v3` + `DOUBAO_*` | 结构审，出问题表，不直接贴成稿 |
| `doubao` | `doubao-seed-evolving` | 同上火山 Plan | 犬声口 / 备笔 |
| `deepseek` | `deepseek-v4-pro` | 与千问同 Token Plan | 去 AI 味 / 探针；不接默认主笔 |
| `wenxin` | `ernie-5.1` | 千帆 Token Plan Person | 中文语感；`ernie-x1.1` 会 403 |
| `kimi` | `kimi-k3` | 火山 Agent Plan | 第二读感；勿用 `kimi-latest` |

**禁止**把各厂 Key 混成同一路、禁止同章拼盘正文。

### 6.3 Codex / GPT-5.6（ChatGPT 订阅，不是 OPENAI_API_KEY）

```bash
codex exec --skip-git-repo-check -m gpt-5.6-sol "任务说明……"
```

登录：`~/.codex/auth.json`（`auth_mode: chatgpt`）。默认推理 `max`。代码交叉验证、owner 点名 Codex 时用。

### 6.4 Grok（本机 grok CLI / xAI）

```bash
grok -p "任务说明……" -m grok-4.5 --reasoning-effort high
```

配置：`~/.grok/config.toml`。只读分析任务必须写死「只读不改，只输出报告」，收工 `git status` 防它落盘。

### 6.5 决断怎么用这些席

owner 要方案/改不改线时：**不要把选择题甩回用户**。默认：

1. 至少三席互评（结构 GLM、主笔/商业千问、声口豆包）
2. 你（或 Grok）合成唯一结论
3. 直接执行并落盘

协议：`~/.grok/DECISION_PROTOCOL.md`。

无限画布 **软件研发**默认你自己改代码 + 测试；不必每刀都开四模型。需要独立意见、写提示词、或 owner 说「多模型 / 用千问」时再调。

生图：本仓正式供应仍是 Codex `agent-imagegen`。owner 规则里的 Codex 生图走 `codex exec` 内置 image_gen，不走 ComfyUI。

---

## 7. 建议的 Cursor 开场提示词

把下面整段贴进 Cursor：

```
你在 /Users/hxx/Documents/无限画布。先读 docs/交接_给Cursor_20260825.md、STATUS.md、AGENTS.md。
earliest 是速度/内存计划 Wave 4-B：adaptation / story.js / novel-agent-service 动态 import。
不要做 N124 CV，不要重建 P0–P14 owner，不要全盘扫正式工程，不要弹窗 GUI。
调外部模型用 ~/.grok/bin/novel_chat.py（先 source ~/.grok/env.local），禁止把 Key 贴进对话。
做完更新 STATUS.md / TASKS.md / docs/当前开发交接.md。
```
