# 严格性能版本本机安装闭环验收报告

## 结论

当前严格性能门通过的源码版本已经完成本机 local-only 交付，并安装到 `/Applications/AI 漫剧画布.app`。

- sourceDigest：`d5ce49a9fdd9a277cd49c004a68b589d9f038c0b791f49e743e8c75932ff5784`
- buildId：`6ed09cc907407bf77b4f41346cef9736`
- MCP：220 tools / 9 resource templates / 8 prompts
- App：0.2.0、arm64、Developer ID deep/strict PASS
- 安装版后台验收：Dock 隐藏、show/focus 均为 0、52ms 自然退出、无 TERM/KILL
- 分发边界：仅本机使用，不公证、不生成 DMG、不发布

## 交付前置门

本次没有重新跑已经关账的 T23。沿用同一源码身份的唯一 strict+interactions PASS 证据：

- 首卡：1246ms（预算 ≤1500ms）
- 首 raw：4033ms（预算 ≤5000ms）
- 全参考：5201ms（预算 ≤8000ms）
- IPC 峰值：4，最终 outstanding=0
- 交互、console/page error：全部 PASS

生产依赖审计使用官方 registry：190 个生产依赖、0 个漏洞。

## MCP 交付

构建并原子发布了唯一 d5 candidate：

`mcp-candidate-d5ce49a9fdd9a277-7eeb2e7c974f57eb-348414e0`

- `mcp:current:check`：16 个候选被检查，invalid=0，当前指向 d5 candidate。
- 真实 stdio：initialize、tools/list、resources/templates/list、prompts/list 均通过。
- 数量：220 / 9 / 8。

## 隔离 App smoke

唯一隐藏隔离 package smoke 已通过：

- 从 lockfile 在隔离 HOME/stage 中执行官方 registry `npm ci`。
- packaged MCP 完成空工程 2 章、34 facts、4 单元、19 条 ledger 与重启恢复。
- Effect/Transition 两轮自然退出：175ms、194ms。
- ReviewStudio 两轮自然退出：45ms、37ms。
- 所有观察点 show/focus=0、Dock hidden；无强杀、无残留进程。
- 临时工程、registry、userData 和 temp root 均已移除。

隔离 smoke 不签 Developer ID、不安装、不生成 DMG；正式签名与安装在其通过后单独执行。

## 签名与安装

使用 `YIHANG LI (3JS43BTTJ3)` 构建 arm64 目录包，明确关闭公证。签名前先把旧 workspace dist 移入仓库外归档，避免覆盖唯一回滚工件。

新 dist、`/Applications` 安装版与根 `release-manifest.json` 字节一致：

- release manifest SHA-256：`2ff843ab6e68138eb4c7e503368edd8096d972019ca06ccac0c87ed30e092cf8`
- app.asar SHA-256：`56519e88e3992576f2026549cd7d3056ea812925b0ca1c32ce419e4e74e6dae1`
- Identifier：`com.hxx.aidramacanvas`
- TeamIdentifier：`3JS43BTTJ3`
- `codesign --verify --deep --strict`：PASS
- architecture：arm64

安装版专用脚本使用 App 自带 Electron runtime 启动 packaged MCP，确认 220 tools；后台模式下 activation policy 为 accessory、Dock 不显示、窗口从未 show/focus，52ms 自然退出，exitCode=0。

## 回滚与旧版清理

旧 c9 安装版和旧 workspace dist 均保留在：

`/Users/hxx/Documents/无限画布_交付归档/local-install-20260811T085335Z-d5ce49a9`

旧安装版仍通过 Developer ID deep/strict，身份为 `c9bb2c87… / 02a1bf9d… / 220`，可用于明确回滚。

额外旧 App `/Applications/本地画布.app` 已确认是 `com.hxx.localcanvas` 0.1.0 且无 release manifest。它没有永久删除，而是移动到：

`/Users/hxx/.Trash/AI漫剧画布_旧版清理_20260811T085603Z/本地画布.app`

候选历史和既有交付归档没有清理，避免破坏 current/回滚链。

## 证据

- `docs/evidence/runtime-stability-local-install-final-20260811-d5ce49a9.json`
- `docs/evidence/isolated-package-smoke-20260811T084933Z-d5ce49a9.json`
- `docs/evidence/isolated-package-smoke-20260811T084933Z-d5ce49a9-completion.json`
- `docs/evidence/installed-local-verify-20260811T085603Z-d5ce49a9.json`
- `docs/evidence/runtime-stability-t23-strict-final-redacted-20260811-d5ce49a9.json`

## 明确边界

- 未公证、未生成 DMG、未发布、未配置自动更新。
- 未写正式工程、CAS、Review 或生成账本。
- 未上传、未付费、未调用外部生成服务。
- 未执行 Git stage、commit 或 push。
- App 验收后保持关闭。
