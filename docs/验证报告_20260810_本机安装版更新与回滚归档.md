# AI 漫剧画布本机安装版更新与回滚归档报告

完成时间：2026-08-10 05:18 CST  
使用边界：仅此 Mac 本地使用，不公证、不发布

## 结论

当前已验收的 `dist/mac-arm64/AI 漫剧画布.app` 已安全安装到 `/Applications/AI 漫剧画布.app`。旧安装版已完整归档，可恢复；新安装版的文件身份、Developer ID 签名、内置 release manifest、App 自带 Electron runtime、MCP 218 tools 和隐藏首启均通过。

## 安装前后身份

| 项目 | 旧安装版 | 当前安装版 |
|---|---|---|
| version | 0.2.0 | 0.2.0 |
| sourceDigest | `b4439e7b…` | `a4312c768158bee4f6c64a3d1764ecf0b455f6a0e541fcfe0b217b4274433bda` |
| buildId | `38dbb556ec8ff28a7e669ba2e72b8e8b` | `e0197b69e9c27800f65e68d9c8ab7173` |
| MCP tools | 202 | 218 |
| 主程序 SHA-256 | `e308b8b40ad5421769a0c48a71f38ce0af9983b8fe566406b0c82b6335b17e0b` | `b514a27ce22fa9213df93be9d4d00034a60f43207fd1b1b9845e3985fa78670b` |
| app.asar SHA-256 | `0221689c653895bba3df28c2e1ee98f8416fec3ff6c8b8ba00b8f30af2667dce` | `65a4098da8cfa0965d739e5fdcc8f5229800037dcd8a06420980b622e52e806a` |

当前安装版主程序和 `app.asar` 与已验收 `dist` 逐项相同。

## 安装与回滚方式

- 新 App 先复制到 `/Applications` 隐藏临时路径，完成主程序/app.asar 哈希与 `codesign --verify --deep --strict` 后才切换。
- 切换期间保留同卷 rollback owner；安装后再次核验失败时会恢复旧版。
- 成功后旧版移动至：
  `/Users/hxx/Documents/无限画布_交付归档/local-install-20260810-051406-a4312c76/previous-installed/AI 漫剧画布.app`
- 旧版归档本身再次通过哈希和 deep/strict 签名验证。

## 安装版运行验证

安装版专用入口 `scripts/verify-p14-installed-app.ts` 直接读取 App 自身 `Contents/Resources/release-manifest.json`，未从工作区推导身份。

结果：

- bundle：`com.hxx.aidramacanvas` / 0.2.0 / arm64。
- Developer ID：`YIHANG LI (3JS43BTTJ3)`；deep/strict PASS。
- App 自带 Electron runtime 成功启动 bundled MCP，实际 218 tools。
- 系统 Node 非必需。
- 使用隔离 registry、userData 和 media runtime；正式项目零写。
- 以 `AI_CANVAS_ELECTRON_BACKGROUND_SMOKE=1` 启动；测试期间未抢占前台，结束后无残留进程。
- 证据：`docs/evidence/installed-local-verify-20260809T211523Z-a4312c76.json`。

## 不公证边界

- `codesign --verify --deep --strict`：PASS。
- quarantine xattr：不存在。
- `spctl`：`rejected / Unnotarized Developer ID`。

这是用户明确选择的本机 local-only 边界，不是签名损坏。若以后需要发给其他 Mac，必须另开公证/分发任务；本轮没有生成新 DMG、上传、发布或配置自动更新。

## 源码恢复快照

当前工作区包含多个历史逻辑切片，不能安全合成一个 Git 提交。本轮没有修改 Git index，而是在仓库外保存：

- tracked 完整二进制补丁：`tracked-working-tree.patch`，SHA-256 `afbb53d314193330746ed00febaf112264dc7a82d6ed37d525833249c57251f1`。
- untracked 全量归档：`untracked-files.tar.gz`，974 files，SHA-256 `188210ff7d271b517c24f149933ca9eed1bf0c8f553dbc6bcf43bc6b471b201d`。
- HEAD、branch、porcelain-v2 状态、diff stat、release identity、current candidate check 与各文件哈希。

根目录：
`/Users/hxx/Documents/无限画布_交付归档/local-install-20260810-051406-a4312c76/source-recovery`

## 最终边界

- 未公证、未发布、未上传、未付费。
- 未执行 Git stage、commit、push、reset 或 clean。
- 未写正式项目、素材、CAS/SQLite 或黄金面具权威图。
- 无需重新运行 fast/medium/integration/heavy，也无需重复 candidate、物理拖拽或安装验收。
