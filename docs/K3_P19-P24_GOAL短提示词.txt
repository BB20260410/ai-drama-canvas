/goal 持续执行“AI 漫剧无限画布 P19–P24 一致性、分镜、生成交互、返工、画布编辑与追溯闭环”长期目标，直到全部阶段独立交叉审核和最终统一验收通过。

工作区：
/Users/hxx/Documents/无限画布

本轮模式：研发。

开始前必须完整读取并严格执行：

1. 当前目录及上级 AGENTS.md；
2. /Users/hxx/Documents/无限画布/.agents/skills/ai-drama-canvas-agent/SKILL.md；
3. /Users/hxx/Documents/无限画布/docs/当前开发交接.md；
4. /Users/hxx/Documents/无限画布/docs/K3_P19-P24_长期目标执行规范.md。

详细规范文件是本/goal的强制执行合同，不是参考附件。如与旧聊天、旧提示词或过期交接冲突，以当前文件系统、源码、测试、运行结果和该规范为准。不读旧线程完整JSONL。

固定顺序：P19一致性门禁 → P20剧本到15秒2–6宫格 → P21生成进度与恢复 → P22批注对比与返工 → P23画布编辑增量 → P24双向追溯与回归。同时只允许一个阶段in_progress；P19–P24全部终验前不得把长期目标标记complete。

P0–P18已验收，禁止重做。先审计现有Owner和真实缺口，复用已有SQLite/CAS/BindingSet/连续性/generation ledger/Review/Vue Flow/命令总线，禁止建第二套事实源。正式工程和权威素材只读，所有写测用隔离工程/userData。冻结当前生产checkpoint，不正式生图、不浏览器供应商、不Grok live、不上传/付费/发布/部署、不Git操作、不制作或替换安装包，不修改全局Skill/MCP/插件/密钥配置。

K3主代理是唯一产品写入者。每阶段实现前至少2个独立只读子代理预审；候选源码冻结后至少3个独立只读子代理按架构/测试/UI性能/证据分工盲审，再互查对方报告。如可选模型固定GPT-5.6 Sol“最高”；否则记录实际最高推理模型，不伪称。子代理不得修改共享工作区。互审不按多数票；任一有证据FAIL、必验NOT_VERIFIED、P0/P1或影响正确性的P2，必须由主代理修复，原发现者+另一审查者复查通过后才能进下一阶段。

严格分离探索与终验：先建完整检查表，探索证据标TEMPORARY，问题收齐后批量修复，开发期只跑定向测试。P19–P24全部阶段接受后才冻结全局摘要，统一运行一次typecheck→定向→全量测试→build:mcp→production build→真实Electron UI/错误/重启/切工程/规模验收→新JSON/截图/报告/交接→至少2个只读子代理证据终审。最终证据必须同一sourceDigest/build identity，机械验收不冒充人工视觉或真实外部服务通过。

只要有安全且已授权的下一动作就自动继续，不每交一份调研或完成一个P就询问是否继续。额度/上下文结束前更新文件化状态和`docs/当前开发交接.md`，留下唯一下一步和精确恢复指令，不把部分完成写成全部完成。

现在立即建立长期状态和检查表，核验单写者/现场摘要，恢复P18基线，然后对P19现有一致性Owner做差距审计；预审通过后默认继续实现，直到整个长期目标完成。
