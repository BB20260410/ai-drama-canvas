# 最终 MCP 候选与三单元真实连续样本验证报告

> 验证时间：2026-07-27 至 2026-07-28 CST  
> 结论：**已完成：候选、配置、探针、三单元样本与桌面客户端重启采用全部 PASS**  
> 范围：当前源码不可变 MCP 候选、Codex 配置切换与重连、5 项 live 探针、3 个真实连续单元共 10 个宫格、Codex 原尺寸视觉验收。
>
> 2026-07-28 03:38 运行时补充：ChatGPT/Codex 已完成完整重启。新 app-server PID `27305` 实际拉起最终候选 MCP PID `27940`；运行 argv、单写锁、buildId `4575ff48…`、sourceDigest `e9756c…`、202 tools 与活动工程五项全部匹配。客户端重启结论为 `MCP_CLIENT_RESTART_PASS`，无需再次重启。03:20 旧 app-server 缓存 `db96767…` 的现场记录保留为历史原因。

## 一、最终结论

1. 已从最后一处源码回归修复后的当前工作树重新构建不可变 MCP 候选；上一版 `29f740…` 候选降为历史，未冒充最终版。
2. `~/.codex/config.toml` 已切换到新候选；ChatGPT/Codex 完整重启后，新 app-server 已实际拉起最终候选并由其持有单写锁。`get_capabilities` 与活动工程只读验收全部命中，桌面客户端最终采用已 PASS。
3. 候选完整性、能力/身份、活动工程、物理零写、源码漂移拒绝 5 项 live 探针全部 PASS。
4. 已使用同一成年阿航、同一神权密室和唯一完整黄金面具 D01 完成 3 个连续 15 秒单元、10 个宫格；3 组 raw/labeled 均真实落盘、可解码、原子成对登记，3 个 current Review 均为 PASS 且 `approvedRawEligible=true`。
5. Codex 已按原尺寸检查三张 941×1672 raw 宫格板和 10 宫格 raw/labeled 联系表，最终视觉结论为 **PASS**。该结论仅覆盖静态连续宫格，不扩写为动态视频、正式成片或公开发布已通过。
6. 交付状态：不可变候选、配置、探针、真实样本与当前桌面 Codex 进程采用最终候选均为 **PASS**。

## 二、最终不可变 MCP 候选

| 字段 | 最终值 |
|---|---|
| version | `0.2.0` |
| candidate | `.aicanvas-runtime/mcp-candidates/mcp-candidate-e9756c099e6b1ec7-1597537cee368347-9888aa8b` |
| sourceDigest | `e9756c099e6b1ec79f405c5a83e4650302b3a21ed542988491336dfd23d7a2fb` |
| buildId | `4575ff48b3b96e236b68a46bc00149dd` |
| MCP tools | `202` |
| entry SHA-256 | `8c6dd0f48ee31cdc8822dd61c6da9866a881afe0b7cf74568450c7b06201a5e1` |
| runtime tree | 388 files / 11,476,951 bytes / `1597537cee368347…` |
| source inventory | 852 files / 17,068,615 bytes |
| config backup | `/Users/hxx/.codex/config.toml.bak-20260727-final-mcp-cutover-v3` |

构建隔离性：

- sourceDigest 在 `before / stageBefore / stageAfter / after` 四处完全相同。
- 活动 `dist-mcp` 构建前后指纹均为 `2a895428a9146d78…`，没有被候选构建覆盖。
- 候选 receipt、release manifest、入口 SHA、runtime tree 指纹三方一致。
- 候选目录为只读密封运行树；证据见 `.aicanvas-runtime/evidence/20260727-final-mcp-cutover-v3/candidate-build.json`。

## 三、配置切换、重连与 5 项探针

配置当前指向：

`/Users/hxx/Documents/无限画布/.aicanvas-runtime/mcp-candidates/mcp-candidate-e9756c099e6b1ec7-1597537cee368347-9888aa8b/dist-mcp/mcp/server.js`

重连事实：

- 旧候选进程 PID `20996` 的 argv 与 cwd 精确匹配历史候选后才执行终止。
- 新候选连接 PID `82580` 成功接管 `/Users/hxx/.aicanvas/mcp-os-writer.lock`。
- singleton 握手返回 buildId `4575ff48…`、sourceDigest `e9756c…`、202 tools，身份完全一致。
- MCP 为 stdio 客户端生命周期进程；独立探针关闭后正常退出。03:20 现场的旧 app-server PID `94983` / MCP PID `84576` 缓存 `db96767…` 已由完整应用重启淘汰。03:38 现场为新 app-server PID `27305` / MCP PID `27940`，运行 argv 与锁均指向最终 `e9756c…` 候选；客户端最终采用已确认。

5 项 live 探针：

| 探针 | 结果 | 决定性证据 |
|---|---|---|
| candidate-integrity-preflight | PASS | receipt / manifest / runtime tree / 只读密封一致 |
| capabilities | PASS | `buildCurrentness.allowed=true`；buildId/sourceDigest 匹配；冷启动预算内 |
| active-context | PASS | 命中 `/Users/hxx/Documents/无限画布/projects/local-import-dudu-world-prologue-b8bfcf14`；暖调用 2.85ms |
| physical-zero-write | PASS | registry 文件/目录、工程 `.aicanvas`、工程顶层前后不变；无新可疑条目 |
| drift-fail-closed | PASS | 临时源码漂移后 mutation 被 `BUILD_CURRENTNESS_MISMATCH` 拒绝 |

漂移探针临时文件 `src/livecheck-drift-probe.tmp.ts` 已清理。完整证据：

- `.aicanvas-runtime/evidence/20260727-final-mcp-cutover-v3/live-probe.json`
- `.aicanvas-runtime/evidence/20260727-final-mcp-cutover-v3/singleton-connection-probe.json`

## 四、真实连续样本

### 4.1 工程与硬锁

| 项目 | 值 |
|---|---|
| projectId | `project-500bb9f357ea` |
| 工程名 | `最终真实连续样本·阿航与黄金面具` |
| 工程根 | `output/final-real-continuity-20260727/run-01/managed-projects/ahang-golden-mask-continuity-234262e0` |
| 人物参考 SHA | `5132d4320f956cf2a2320e52fae941ef15ffb243ad79343045006a8773f4548e` |
| 场景参考 SHA | `a9861cdb6f31257b6ee49280a037fef8fb70671de9dcdddccf6218c626fa5357` |
| 黄金面具权威 SHA | `02e9438ecee038f7d14860da37cb315bf358db4a26fa224e342eee5b592b55a9` |
| 后端 | OpenAI 内置 `imagegen`，provider=`codex`；后端模型名未暴露 |
| Grok live | 本样本未使用 |

三个单元均以“一次调用生成一张完整 unit-grid 宫格板”的方式执行；U01 首次视觉 REWORK 后才进行明确的 attempt 2，没有在结果不明时盲目重复派发。

### 4.2 单元结果

| 单元 | 宫格 | 接受 attempt | raw SHA-256 | labeled SHA-256 | Review |
|---|---:|---:|---|---|---|
| ACCEPT_U01 | 4 | 2 | `4147a564…069b2` | `54b085a3…b200` | `61de374a…` PASS/current/eligible |
| ACCEPT_U02 | 3 | 1 | `69a1b589…6030` | `72c55bac…ccb` | `4b43454a…` PASS/current/eligible |
| ACCEPT_U03 | 3 | 1 | `c3db4462…1342` | `2ab9a31f…668a` | `5eb410df…bf6a` PASS/current/eligible |

三条接受调用在 generation ledger 中均存在 `result-committed` 终态，不存在待重试的生图 `generation_unknown`。

U01 历史 attempt 1：

- raw `6e9073ce…297aa`，Review=`rework`，`approvedRawEligible=false`。
- 原因：左前额银白发束未稳定复现，且面具姿态与冻结计划不一致。
- 失败结果与 Review 均保留，没有覆盖或提升为当前权威。

### 4.3 最终视觉验收

Codex 原尺寸结论：

- **人物**：三单元均为同一成年阿航；高发髻、左前额银白发束、破旧黑色古蜀长袍保持连续，无第二人物。
- **道具**：10 格始终只有一张完整刚性黄金面具；无冠角、裂损、佩戴、复制、熔化或活体化。
- **场景**：灰黑巨石密室、中央石台、左右壁龛、纵向冷金裂隙和 180 度轴线稳定。
- **动作链**：U01 靠近并悬手 → U02 接触、托起、胸前持握 → U03 从持物末态重新锚定、冷金脉冲、退至画面右侧并空出中央石台。
- **画质**：三张 raw 均为 941×1672 可解码 PNG；人物五官、双手、面具边缘、旧布和石材纹理可辨；无伪文字、水印、现代物件或明显生成破损。
- **事实优先**：U01 当前通过图中的面具实际为石台上正面竖立，后续按可见 actual-tail 承接；没有用计划中的“平放”覆盖真实画面。
- **机器辅助边界**：U03 的 P19 一致性辅助把人物/面具提示为 drifted，明细主要来自白底/黑底参考板与暗场结果的 tone-lighting 距离，且结构锁为 not-checkable。Codex 原尺寸复核确认脸、发、服装和 D01 结构硬锁成立；机器辅助未自动替代人工 Review。

联系表：

- raw：`output/final-real-continuity-20260727/run-01/visual-acceptance/10-panel-raw-contact-sheet.png`  
  941×5016，SHA-256 `ecc4a6c08085e16ce6c8ef547c30dedee95afabba37fb966785bdd5138b44235`
- labeled：`output/final-real-continuity-20260727/run-01/visual-acceptance/10-panel-labeled-contact-sheet.png`  
  941×5016，SHA-256 `5a409ff522ae76ee555cc6d329ffd4eb3124d77f6b0b4c78ad1930a78316639f`

结构化验收记录：

`output/final-real-continuity-20260727/run-01/visual-acceptance/final-visual-acceptance.json`

## 五、可选 U03 actual-tail 例外

U03 图像生产、raw/labeled 登记和视觉 Review 已完成；随后尝试的**可选** actual-tail/video-package 准备调用超时：

- command key：`final-real-mcp-accept_u03-video-package-prepare-recovery-2-key`
- command ledger：`unknown`
- 进程已退出，不重放 unknown。
- generation ledger 中 U03 的 video package export intent 数为 0；控制面为 `not-prepared`，未发现 intent 或 package 副作用。
- 该项是历史命令 unknown，不是生图 `generation_unknown`，不影响用户要求的三单元、十宫格和最终视觉验收。
- `actual-tail-decision.json` 只保存人工观察草案，未冒充已经提交的 Observation。

## 六、源码修复与验证

本轮为解决真实 readiness 卡顿，在既有 owner 上加入请求内 schema cache、unit-grid 只读 epoch/memo 与身份栅栏，并修复首次 continuity schema 初始化和损坏 marker fail-closed 的边界。没有建立平行数据库或第二真相源。

最终验证：

- `npm run typecheck -- --pretty false` — PASS
- `npx tsc --noEmit -p tsconfig.mcp.json` — PASS
- `git diff --check` — PASS
- schema cache + read epoch：2 files / 9 tests PASS
- formal gate + 缺失 marker/temp 零写负向：PASS
- 生产入口回归：6 files / 26 tests PASS
- unit-grid continuation：1 file / 4 tests PASS
- immutable/currentness/runtime gate：3 files / 9 tests PASS
- 独立只读审计：源码修复 GO；样本机械与视觉复核 PASS

## 七、恢复状态与边界

- 活动工程已恢复为 `/Users/hxx/Documents/无限画布/projects/local-import-dudu-world-prologue-b8bfcf14`。
- 样本工程 lease 已释放；当前没有样本锁文件。
- 未修改人物、场景、黄金面具权威源文件。
- 未调用 Grok 生产本样本，未执行网页上传、付费、视频生成、部署、公证或公开发布。
- 未执行 Git 暂存、提交、推送或 PR。
- 当前 Git 工作树为**有意未提交**：仅本轮源码/测试加固与报告/交接修改；`.aicanvas-runtime` 候选和 `output` 样本位于忽略边界。不得把“有未提交修改”误判为候选不可信；候选 currentness 由 sourceDigest `e9756c…` 与 5/5 live 探针证明。
