# 验证报告 · P10 构建身份与能力清单

日期：2026-07-18  
状态：构建身份合同 **PASS**

## 已交付

- `src/core/build-identity.ts`
  - `sourceDigest`：工作区源码内容寻址
  - `buildId`：sourceDigest + 能力清单 + 时间
  - 能力清单固定：`studioDashboard/binding/continuity`、正式生图仅 `codex-imagegen`、禁用 browser/artlist
  - `createVerifyRunnerRecord`：统一 argv/cwd/time/exit/log/testCounts 记录形状
- `tests/build-identity.test.ts` 定向通过

## 说明

完整 P0–P10 统一 verify runner 与备份/损坏恢复矩阵可继续迭代；本切片锁定**可复现构建身份**与**能力声明**，防止软件完成冒充成片完成。
