# 四模型协同整改与机械验收报告

## 交付结论

本轮源码候选通过机械验收，但严格“四模型同摘要终审通过”未完成，因此总状态是：**部分完成 / 外部终审通道阻塞**，不是 `COMPLETE`。

- 当前候选：`9a7fde1f… / ac77f767… / 220 tools`。
- 当前已确认 P0/P1：0。
- 当前用户流程相关高价值 P2：4 项均已关闭。
- Kimi K3 与豆包第三轮：有效 `PASS`。
- GLM 5.2：两次真实调用均输出耗尽且正文为空，`BLOCKED`。
- Grok 4.5：第三轮真实运行，但输出多段 JSON 拼接并引用不存在路径，`BLOCKED`。
- 不能用 Kimi/豆包多数票替代 GLM/Grok，也不能把软件候选 PASS 写成四模型终审 COMPLETE。

## 实际整改

Kimi 在隔离 worktree 完成三个独立提交，Codex 逐个审查后选择性 cherry-pick 到主工作树：

1. `505b2f0`：补齐总资源分类的 tablist/tab/tabpanel、稳定 ID、roving tabindex、方向键/Home/End 与焦点迁移合同。
2. `e9a9173`：为 Review 历史增加独立失败态、latest-only gate、迟到成功/失败抑制与单次错误通知。
3. `d36988e`：为剪辑素材搜索和小说全文搜索补齐真实输入控件上的可访问名称。

相对基线 `0fb7a10`，最终代码变更为 9 个文件、176 行新增、8 行删除；没有修改 Core、Main、preload、MCP、SQLite 或正式工程数据。

## 多模型三轮结果

### 第一轮：独立盲审

- Kimi K3：确认总资源页签与 Review 历史两个 P2。
- 豆包：确认剪辑素材搜索与小说全文搜索两个 P2。
- Grok 4.5：报告中的 P1/P2 假设经 Codex 实际核查均被现有 owner 合同覆盖或无法复现，只保留非阻断建议。
- GLM 5.2：两次真实调用均 `EMPTY_TRUNCATED_DRAFT`，按两轮纠正上限失败关闭。

### 第二轮：交叉复核

- Kimi K3：4 files / 30 tests 独立复核通过，四项均判 `resolved`。
- 豆包：内容层确认四项修复，原始自定义结构由 Codex机械归一；没有改写结论。
- Grok 4.5：首次多 JSON 拼接，唯一纠正又引用不存在路径，通道记 `BLOCKED`。
- GLM 5.2：沿用第一轮不可用状态，不静默换模型。

### 第三轮：冻结候选终审

- Kimi K3（`kimi-k3`）：绑定 `9a7fde1f…`，源码终审 `PASS`；明确未亲自重跑全量、Electron 或 VoiceOver。
- 豆包（`doubao-seed-evolving`）：绑定 `9a7fde1f…`，产品 QA 内容结论 `PASS`；明确未亲自做 VoiceOver、鼠标视觉和全部错误态人工验收。
- GLM 5.2（`glm-5-2-260617`）：没有可审计报告，`BLOCKED`。
- Grok 4.5（`grok-4.5`）：没有合规单一 JSON，且含不存在路径，`BLOCKED`。

四份统一 schema 报告均通过 JSON Schema 机械校验；其中 GLM/Grok 报告是阻塞事实记录，不是假造的审查内容。模型原始输出和会话材料已放仓库外审计归档，不进入正式源码摘要或 Git。

## 机械验收

- 定向前端回归：4 files / 30 tests PASS。
- `npm run typecheck`：PASS。
- `npm run typecheck:app`：PASS。
- `npm run test:partitions:audit`：387 files，分区指纹 `6af1e1d…`，PASS。
- fast：255 files / 1462 tests PASS。
- medium：91 files / 801 tests PASS。
- integration：36 files / 123 tests PASS。
- heavy：5 files / 17 tests PASS。
- 合计：387 files / 2403 tests PASS。
- 生产依赖审计：0 vulnerabilities；190 个生产依赖、754 个总依赖。
- `npm run build`：PASS；release manifest 与 live source digest 一致。
- MCP 真实 stdio：220 tools / 9 resource templates / 8 prompts，PASS。
- `git diff --check`：PASS。

隐藏 Electron P17 隔离巡检：22 条路径/动作，7 个 UI 快照；未命名可见按钮 0、重复 ID 0、横向溢出 0、page error 0、console error 0、外网请求 0；show/focus 0，Dock 隐藏，68ms 自然退出，退出码 0，无残留进程。证据见 [导航与 UI 巡检 JSON](evidence/multi-model-quality-navigation-ui-20260812-9a7fde1f.json)。

本轮没有性能代码变更，因此没有重跑 T23；这不是把性能门写成 PASS，而是明确 `NOT_RUN / N/A`。

## 边界与风险

- 没有重新安装 App；`/Applications/AI 漫剧画布.app` 仍是历史已验收 `954ac71a… / c7cb5cee… / 220`。
- 没有公证、发布、push、上传、正式工程写入或付费生成。
- Electron P17 是隐藏隔离巡检，不等于 VoiceOver/NVDA 人工辅助技术验收。
- GLM/Grok 必验通道仍阻塞，所以后续若必须取得“四模型 COMPLETE”，只能在通道真实可用并返回合规报告后补签；禁止重复原样调用或静默替换模型。

## 决定性证据

- [结构化关账证据](evidence/multi-model-quality-closeout-20260812-9a7fde1f.json)
- [隐藏 Electron 导航/UI 证据](evidence/multi-model-quality-navigation-ui-20260812-9a7fde1f.json)
- 当前构建身份：`release-manifest.json`
- 当前交接：`docs/当前开发交接.md`
