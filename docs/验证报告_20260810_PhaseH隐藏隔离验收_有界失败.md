# Phase H 最终 candidate 与后台隐藏隔离 App 验收报告

更新时间：2026-08-10 03:52 CST

## 交付结论

**部分完成，App 最终验收仍阻塞。** 一次性源码冻结门、唯一 MCP candidate、current 校验和真实 SDK 握手全部通过；唯一一次后台隐藏隔离 App smoke 有界失败，不能把当前源码或 App 标记为“全面无 BUG / 最终完成”。

失败已经从上一轮 watcher/`SingletonSocket` 问题缩小为一个明确边界：ReviewStudio 第二次启动后的最终关闭停在 `awaiting_renderer`，20 秒内没有收到 renderer close response。失败路径对 PID 执行 TERM/KILL 并明确不计 PASS；没有无变化重跑。

## 本轮固定预算

- 全面审查：0 次。
- 源码冻结门：1 次，已消费并通过。
- candidate 构建：1 次，已消费并通过。
- 隐藏 App smoke：1 次，已消费并失败。
- 不重跑 fast/medium，不重新执行 integration/heavy，不替换安装版。

## 已通过

### 源码冻结门

- `npm run typecheck:app`：exit 0。
- `node --check scripts/lib/electron-application-close.mjs`：exit 0。
- `node --check scripts/lib/exclusive-evidence-output.mjs`：exit 0。
- `node --check scripts/ui-editor-effect-transition-smoke.mjs`：exit 0。
- `node --check scripts/ui-review-content-identity-smoke.mjs`：exit 0。

### 最终 MCP candidate

- candidate：`mcp-candidate-3593b45d58c744f3-387e092d86f5b405-f107ba04`
- sourceDigest：`3593b45d58c744f3d7620f2589fa5e0368a7a7a12a348efccdcccf3f50589364`
- buildId：`61fb163545d3ad3823fa321c093757b3`
- tools：218
- `npm run --silent mcp:current:check`：`ok=true`、inspected 4、invalid 0。
- 稳定 launcher 真实 initialize/tools/resources/templates/prompts 握手：PASS，toolCount 218。
- candidate 构建前后 sourceDigest 一致；live `dist-mcp` fingerprint 未变化。

### Effect/Transition packaged UI

- 独立证据 `status=passed`。
- 两次根进程关闭：2558ms、1586ms；均 exit 0、signal null、未 TERM/KILL。
- 四个背景快照均为：activation policy `accessory`、Dock 不可见、窗口不 visible/focused、show/focus 事件均 0。
- `bringToFrontUsed=false`。
- Chromium user-data 位于项目根外；项目、registry、user-data 均已清理。
- 截图 1560×980、252,968 bytes。

## 唯一失败

ReviewStudio 的第二次启动已完成 UI 操作和截图，但最终 `application.close()` 超过 20 秒：

- PID：9717。
- 主进程最后阶段：`awaiting_renderer`。
- `rendererApproved=false`。
- 尚未进入 critical/recoverable watcher cleanup，因此不是上一轮 `SingletonSocket` 或 watcher 二次 close 的同一失败。
- 超时后失败清理使用 TERM/KILL；该回合不计 graceful PASS。
- Review 子证据 JSON 因 finally 中关闭失败而未发布；PNG 已原子发布。顶层证据正确为 `status=failed`，没有沿用旧 PASS。

现有证据只能确认“renderer close response 未返回”。listener 未存活、请求未送达、或 leave gate Promise 未收敛三者尚不能进一步裁决；本轮不以猜测修改源码。

## 终态清理与保护边界

- `/private/tmp/aic-pkg-XomDPU`：已删除。
- 顶层证据：`terminal.tempRootRemoved=true`。
- 清理前后 lingering processes：均为空。
- `/private/tmp` 下未发现 `aic-pkg-*` 残留。
- `/Applications/AI 漫剧画布.app/Contents/MacOS/AI 漫剧画布` SHA-256 仍为 `e308b8b40ad5421769a0c48a71f38ce0af9983b8fe566406b0c82b6335b17e0b`，安装版未替换。
- 未写正式项目、素材、CAS/SQLite；未 stage/commit/push/reset/clean。
- Finder/其他软件真实拖出复制体仍需前台人工交互，本轮后台静默授权下未执行。

## 证据

- 顶层失败证据：`docs/evidence/isolated-package-smoke-20260809T194741380Z-6420-7a1c80ca.json`  
  SHA-256 `7c8643ea413e6874e2a22d4e3e8e1081984544941bec483cb9c7df1ac5652730`
- Effect 通过证据：`docs/evidence/isolated-package-smoke-20260809T194741380Z-6420-7a1c80ca-electron-ui.json`  
  SHA-256 `47bcae6de7e858364135c6df17a715373ac74b3d1f87b92b63131fff682bff8e`
- Effect 截图：`docs/evidence/isolated-package-smoke-20260809T194741380Z-6420-7a1c80ca-electron-ui.png`  
  SHA-256 `c4cc380017130779e3383df02800a6b3ef800ed94c909dbb66b43b16e0584c84`
- Review 失败前截图：`docs/evidence/isolated-package-smoke-20260809T194741380Z-6420-7a1c80ca-review-ui.png`  
  SHA-256 `9959aa6a46710d43fcc45402347c7c1b6db265486c3bfbb1cb5787192c54448e`

## 下一次唯一允许的修复切片

只处理 ReviewStudio renderer ACK：在 main 发送请求、preload 收到、App listener 开始/结束 leave gate、response 发回四个点加入同一 requestId 的有界诊断，先用定向测试确定卡点，再做一个最小修复。修复后需要新的 sourceDigest，因此只有获得新的单次预算后，才可再构建一次 candidate 并执行一次全程隐藏 smoke。禁止扩大为全面审查或重新跑分区全量。
