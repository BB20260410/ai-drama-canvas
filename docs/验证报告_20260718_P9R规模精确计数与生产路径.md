# 验证报告 · P9-R 规模精确计数与生产路径（PARTIAL）

日期：2026-07-18  
状态：**PARTIAL**（本切片通过；P9-R 全部门禁未齐）

## 本切片完成

| 项 | 结果 |
|----|------|
| 宫格精确计数 | SQL `SUM(panel_count)`，dashboard `panels`/`panelsEstimated` 同源 |
| 可解码图片 | sharp 真 PNG（≥64×96），非 8 字节签名占位 |
| 真实音视频派生 | ffmpeg 生成 1 短视频 + 1 音频；poster/proxy/waveform ready |
| 生产路径非零 | 首单元 6 宫格：BindingSet×6、九字段 ready、freeze/dispatch/register/Review×6 |
| 正式工程 | 未污染 `projects/codex-ai-drama-studio` |

## 代码改动

- `src/core/studio-production.ts`：`counts.panels` 精确 SUM
- `src/core/studio-production-dashboard.ts`：overview 使用精确 panels
- `src/core/studio-scale-fixture.ts`：可解码媒体 + 生产路径 seed
- `scripts/run-p9-scale-evidence.ts`：新证据合同 schema v2
- `tests/studio-scale-fixture.test.ts`：定向回归

## 证据

| 证据 | 说明 |
|------|------|
| `docs/evidence/p9-scale-fixture-20260718-p9r-medium.json` | 48 单元 / 162 宫格 / 生产路径 PASS |
| `docs/evidence/p9-scale-fixture-20260718-p9r-full1288.json` | **1288 单元 / 4235 宫格 / 77 资产**；panels SQL=dashboard 精确一致；生产路径 6 Binding + 6 Review；真 mp4/wav 派生 ready；**PASS** |
| 定向测试 | `tests/studio-scale-fixture.test.ts` PASS |

> 注：宫格数为 4235 而非旧文案「约 4232」，因首单元固定 6 格以承载完整生产路径；精确值以 SQL SUM 为准。

## 仍未完成（P9-R 其余门）

1. 连续性/冲突/审核控制全量路径的 keyset 分页（部分仍有界扫描）
2. 10k 媒体冷启动 + UI 滚动/切换工程真实 Electron smoke
3. 故障矩阵穷尽（跨根、取消、崩溃恢复等）
4. 不能将本报告标为 P9-R PASS

## 命令摘要

```bash
npm run typecheck
npx vitest run tests/studio-scale-fixture.test.ts tests/studio-production-dashboard.test.ts
P9_SCALE_UNITS=48 ... npx tsx scripts/run-p9-scale-evidence.ts docs/evidence/p9-scale-fixture-20260718-p9r-medium.json
```
