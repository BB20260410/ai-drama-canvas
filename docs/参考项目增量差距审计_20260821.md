# 参考项目增量差距审计 · 2026-08-21

忙 fail-closed 与列表视口剔除已饱和。本轮派 8 路只读子代理检索开源对标（不复制 GPL/AGPL 进核心），对照北星「角色/场景/站位一致性」与 7 月审计。

## 本仓坐标

制片账本 + Agent 合同已重；刚落地画布角色库（图+可选音频入库，钉选自动带出）。相对 InvokeAI / 火宝 / Jaaz / Storyboarder / Kitsu / CCv3，短板在 **库交互、多视图槽、场景对称、图文对照接线**，不是再建一套引擎。

## 对标摘要（只记增量）

| 来源 | 可借 | 禁止 |
|---|---|---|
| SillyTavern / Risu / CCv3 | 别名、多视图槽、CHARX 音频、PNG 导入映射 | AGPL 源码、personality/lorebook |
| Blender Asset Browser / Penpot | 拖到视口=实例；库→画布是引用 | 3D pose |
| Kitsu/Zou | 主预览、帧批注、评论即状态 | AGPL 进核心 |
| OpenAssetIO / AYON | preflight→register；身份≠路径 | C++/Python SDK |
| Storyboarder / Fountain | 场次锚点、分镜字段、洋葱皮 | 回写剧本、平行分镜库 |
| Wan FLF2V / Seedance I2V | 首尾帧槽、15s 时长 | Comfy 当产线、网页自动化生图 |
| n8n / Excalidraw / Vue Flow | 命名可折叠框、库拖到落点、适配所选 | 换画布引擎 |
| 已有波形派生 | 画布音频节点显示波形+点播 | wavesurfer 第二套 peaks |

7 月已记且仍有效：OpenCut 观察、LosslessCut 恢复旁路、Comfy 工作流快照、Kitsu 批注字段。本轮不重复建议那些未变项。

## 下一步（按北星 + 刚做完的角色库）

| 序 | 切片 | 用户可见 | 复用 | 不做 |
|---|---|---|---|---|
| **N1** | 场景/道具对称入库 | 场景/道具 tab 也能上传参考图并钉到画布 | `ingestCharacterCanvasPack` 泛化、`create_studio_asset` | 场景不走 VoiceIdentity |
| **N2** | 画布音频试听 | 音频节点 play；角色行可试听 | `aicanvas-studio://media`、已有 waveform 派生 | TTS 合成 |
| **N3** | 库拖到落点 | 侧栏拖到指针处钉选，角色仍带出音频 | `togglePinnedNode` + `screenToFlowCoordinate` | 新坐标表 |
| **N4** | 角色多视图槽 | 正/侧/背条，冻结按机位取图 | 同资产多 version + sourceNote | InstantID 进画布 |
| **N5** | 阅读器联动 earliest | `mod+e` 滚到正文 span | ScriptMediaAlign `sourceSpans` | 平行剧本库 |
| **N6** | 命名空间组 | ⌘G 框住一集/角色墙 | 遗留 group 节点 + layout CAS | 复用 pipeline 组 |

N1 为立即执行项（与用户「角色库」请求对称，三路调研独立指向）。

## P65 附录 · 2026-08-21 16:15 CST（连续两轮无自动项）

P64–P65 无新 CV/busy 红测。digest 仍 `6c6da1e7…`。禁止再空扫 busy。本附录只加增量规格，不改产品、不复制 AGPL。

### 对标增量（相对上午表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| MaterialStudio / MultimediaTimeline 已有原生 `<audio controls>` | 画布与角色行复用同一协议 URL + 38px 原生控件 | wavesurfer / waveform-playlist 第二套 peaks |
| Vue Flow MIT `onPaneDrop` + `screenToFlowCoordinate` | N3 落点钉选 | 换画布引擎 |
| SillyTavern CharacterLibrary 库内音频播放器（行为） | 角色行试听 | AGPL 扩展源码、CHARX 解析器、lorebook |
| Excalidraw Libraries MIT | 仅「库条目拖进画布=引用」语义 | 形状库/组件替换节点 |

### N2 落地规格（下一刀，不在本轮实现）

- **用户可见**：画布音频节点可 play；角色/场景/道具库行若已绑音频可试听。
- **复用**：`aicanvas-studio://media/<sha>?projectRoot=`（MultimediaTimeline `studioMediaUrl`）；控件形态抄 MaterialStudio `.audio-player` 38px `preload="metadata"`；波形仍走已有 `audio_waveform` 派生图，不解码第二套 peaks。
- **合同**：单一共享 `HTMLAudioElement`（节点与库行互斥，切工程/unpin 停止）；`pinActionBusy` 期间禁用 play。源码测试钉 `data-testid` + `<audio`，无窗口。
- **不做**：TTS；wavesurfer；把 VideoEditor `audioElements` Map 拉进画布。

N1 已落地。earliest = **N2**。N3 仍是 Vue Flow 落点，不提前做。

## P77 附录 · 2026-08-21 17:13 CST（连续两轮无自动项）

P76–P77 无新 CV/busy 红测。digest 仍 `870f970a…` / 1089 files / 21,669,793 bytes。禁止再空扫 busy。本附录只加增量规格，不改产品、不复制 AGPL、不实现 N5。

N1–N4 已落地。24h-perf 已收口 N2 的**节点侧**：原生 `<audio>`、`canvas-audio-mutex`、busy pause、busy `pointer-events:none`。VideoEditor `audioElements` Map 仍排除。

### 对标增量（相对 P65 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 ScriptMediaAlign `focusEarliestUnit` + `sourceSpans` UTF-16 | N5 只接线盘 `mod+e` 到已有函数 | 平行剧本库、Fountain.js 解析器、回写正文 |
| Vue Flow Nested Nodes MIT `parentId` / `extent: parent` | N6 命名组框住一集/角色墙 | 换画布引擎、n8n 源码（Fair-code） |
| 本仓遗留 `CanvasGroupNode` + layout CAS | N6 复用旧 group 节点，不进 pipeline 组 | 在受管画布另起一套坐标表 |
| 本仓 `canvas-audio-mutex` | N2 残差：库行试听走同一 claim/release | wavesurfer、AGPL CharacterLibrary 源码 |

### 队列（24h-perf 不实现）

| 序 | 状态 | 用户可见 | 复用 | 不做 |
|---|---|---|---|---|
| **N2 库行** | 残差 | 已绑音频的角色行可试听 | `claimCanvasAudioPlayback` + `aicanvas-studio://media` | 新播放器、TTS |
| **N5** | earliest | `mod+e` 滚到当前 earliest 单元正文 span | 已有按钮 `@click="focusEarliestUnit"`、`setSelectionRange` | 猜选区、平行剧本库 |
| **N6** | 其后 | ⌘G 框住选中节点 | Vue Flow `parentId` + 遗留 group 节点 | 复用 pipeline 组 |

earliest 仍是 **N5**。24h-perf 不抢 ScriptMediaAlign。

## P79 附录 · 2026-08-21 17:20 CST（连续两轮无自动项）

P78–P79 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N5 已关（`onReaderKeydown` → `focusEarliestUnit`）。受管画布 `nodeTypes` 仅 `managedStudio`，N6 未开工。禁止再空扫 busy。不改 Vue/ts。不复制 n8n/AGPL。

### N6 落地规格（下一刀，不在本轮实现）

- **用户可见**：选中画布节点后 ⌘G 套命名组；拖组带动子节点；可折叠只留标题。
- **复用**：Vue Flow MIT Nested Nodes（`parentId`、`extent: 'parent'`、子坐标相对父节点）；遗留 `CanvasGroupNode.vue` + 现有 layout CAS。组 id 走已有钉选/布局写入，不新建坐标表。
- **合同**：源码测试钉 `data-testid` 组节点 + `parentId`；busy/`pinActionBusy` 时 ⌘G fail-closed；无窗口。
- **不做**：换画布引擎；抄 n8n Fair-code 源码；把组写进 generation pipeline；复用 command-bus 的「组」语义。

earliest = **N6**。24h-perf 不抢 View/Node。N2 库行试听仍是残差，排在 N6 之后。

## P81 附录 · 2026-08-21 17:27 CST（连续两轮无自动项）

P80–P81 无新 CV/busy 红测。digest 仍 `25c1df9c…`。N6 仍未开工。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。

### N7 落地规格（N6 之后，不在本轮实现）

检查器 `managed-canvas-character-audio` 只显示「已绑 N 条」，库行无 `<audio>`。

- **用户可见**：角色资产检查器（及库行若有 SHA）可试听已绑 CAS 音频。
- **复用**：`audioSha256sForCharacterAsset` + `aicanvas-studio://media/<sha>` + `claimCanvasAudioPlayback` / `releaseCanvasAudioPlayback`；控件抄 `.audio-player` 38px `preload="metadata"`。
- **合同**：busy/`pinActionBusy` 时 pointer-events:none + play 早退 pause；与画布节点互斥；卸检查器 release；无窗口。
- **不做**：wavesurfer；VideoEditor Map；TTS；AGPL CharacterLibrary 源码。

24h-perf 不抢 View/Inspector/N6。

## P83 附录 · 2026-08-21 17:35 CST（连续两轮无自动项）

P82–P83 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6/N7 规格。

### 对标增量（相对 P81 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| SillyTavern / CCv3 角色卡别名（行为） | 画布入库可选别名；检查器展示 | AGPL 扩展源码、CHARX 解析器、lorebook |
| 本仓 `create_studio_asset.aliases` + `StudioDashboardAssetSummary.aliases` | 只接线盘，不新表 | 改 Core 资产模型 |
| 本仓素材中心逗号拆别名 | 画布 ingest 同样 split `[,，\n]` | 第二套别名语法 |

首尾帧 / 续接 / 洋葱皮：Core 与 MCP 已有首尾帧与续接包；洋葱皮无现成控件，不进 N8。

### N8 落地规格（N6/N7 之后，不在本轮实现）

画布入库表单只有名称；`ingestCharacterCanvasPack` 不传 `aliases`。检查器不展示 `selection.asset.aliases`。库搜索占位已写「别名」，仪表盘摘要已带 `aliases[]`。

- **用户可见**：角色/场景/道具入库可填别名（逗号分隔）；检查器列出别名；库行 small 可带第一条别名。
- **复用**：`studioCreateAssetPayloadSchema.aliases`；`ingestCharacterCanvasPack` payload 增加可选 `aliases`；检查器读已有 `StudioDashboardAssetSummary.aliases`。split 抄 MaterialStudio `[,，\n]`。
- **合同**：源码测试钉 ingest envelope 含 aliases、`data-testid="managed-canvas-character-aliases"`；busy 时入库仍 fail-closed；无窗口。
- **不做**：CHARX/PNG 角色卡导入；lorebook；改搜索 SQL；抢 N6 ⌘G / N7 试听。

24h-perf 不抢 View/Inspector/N6。

## P85 附录 · 2026-08-21 17:40 CST（连续两轮无自动项）

P84–P85 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N8 规格。

### 对标增量（相对 P83 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Vue Flow MIT `Controls` / `fitView` | 一键适配包围盒 | 换画布引擎 |
| Figma / Excalidraw 适配视口快捷键 | `Shift+1` 适配全部 | 抄闭源快捷键表 |
| 本仓已有 `fitCanvas()`（按节点坐标 `fitBounds`） | 默认 `fitView` 会漏 only-render-visible 离屏节点 | 新坐标表 |

MiniMap / 撤销重做 / 底栏「适配」按钮已在。身份特征在素材中心，dashboard 摘要无该字段，不进 N9。

### N9 落地规格（N6–N8 之后，不在本轮实现）

底栏 `@click="fitCanvas"` 已正确；Controls 的 fit-view 仍走 Vue Flow 默认 `fitView`；`onCanvasKeydown` 无适配快捷键。

- **用户可见**：`Shift+1`（及 Controls 扫描图标）调用现有 `fitCanvas()`，适配全部钉选节点；输入框/拖拽中忽略。
- **复用**：`fitCanvas()` 的 `studioFlow.fitBounds` + 左 110px 安全区；`onCanvasKeydown` 现有 editable/`isDragging` 早退。
- **合同**：源码测试钉 `event.key === "1" && event.shiftKey` 在 `fitCanvas()` 之前；Controls `@fit-view` 调 `fitCanvas` 而非只 `onControlViewportChanged`；无窗口。
- **不做**：改 T23；抢 N6 ⌘G / N7 试听 / N8 别名；新 minimap。

24h-perf 不抢 View/N6。

## P87 附录 · 2026-08-21 17:45 CST（连续两轮无自动项）

P86–P87 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N9 规格。

### 对标增量（相对 P85 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Vue Flow MIT `zoomTo` | 保持中心缩放到 100% | 换画布引擎、新视口表 |
| Figma `Shift+0` 缩放到 100% | 快捷键语义 | 抄闭源实现 |
| 本仓 `onControlViewportChanged` | Controls 缩放后读 store 落盘 | 绕过 layout CAS |

工程切换复位 `zoom=0.72` 是默认布局，不是 100%。入库 description 硬编码属 N8 邻接，不进 N10。

### N10 落地规格（N6–N9 之后，不在本轮实现）

`onCanvasKeydown` 无缩放到 100%；Controls 只有步进缩放。

- **用户可见**：`Shift+0` 在当前中心 `zoomTo(1)`；输入框/拖拽中忽略。
- **复用**：`studioFlow.zoomTo(1, { duration: 180 })` + 现有 `onControlViewportChanged` 持久化；`onCanvasKeydown` editable/`isDragging` 早退。
- **合同**：源码测试钉 `event.key === "0" && event.shiftKey` 在 `zoomTo(1` 之前；不调用 `fitCanvas`；无窗口。
- **不做**：改 T23；抢 N6 ⌘G / N7 试听 / N8 别名 / N9 `fitCanvas`；把默认 0.72 当成 100%。

24h-perf 不抢 View/N6。

## P89 附录 · 2026-08-21 17:50 CST（连续两轮无自动项）

P88–P89 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N10 规格。

### 对标增量（相对 P87 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma `Shift+2` 缩放到所选 | 选区包围盒适配 | 抄闭源实现 |
| 本仓 `fitCanvas()` + `syncSelectionSnapshot` | 对 `selected` 子集算 `fitBounds` | 新选区表、默认 `fitView` |
| Vue Flow MIT `node.selected` | 已有 `selectionCount` | 换画布引擎 |

无选区时不退化为全画布适配（那是 N9）。洋葱皮/身份特征仍不进本刀。

### N11 落地规格（N6–N10 之后，不在本轮实现）

底栏「适配」和 `fitCanvas()` 只看全部 `nodes`；`onCanvasKeydown` 无选区适配。

- **用户可见**：有选区时 `Shift+2` 适配所选节点；无选区空操作；输入框/拖拽中忽略。
- **复用**：`fitCanvas()` 的 188×200 包围盒 + 左 110px 安全区；`source.filter(n => n.selected)` 与 `syncSelectionSnapshot` 同源。
- **合同**：源码测试钉 `event.key === "2" && event.shiftKey` 且 `selected.length` 为 0 时不调 `fitBounds`；无窗口。
- **不做**：改 T23；抢 N6 ⌘G / N7 试听 / N8 别名 / N9 全画布 / N10 `zoomTo(1)`。

24h-perf 不抢 View/N6。

## P91 附录 · 2026-08-21 17:54 CST（连续两轮无自动项）

P90–P91 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N11 规格。

### 对标增量（相对 P89 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| SillyTavern / CCv3 角色卡说明（行为） | 入库可选说明 | AGPL 源码、personality/lorebook |
| 本仓 `create_studio_asset.description` | ingest 改传用户文本 | 改 Core 资产模型 |
| 检查器已展示 `selection.asset.description` | 只补写入，不新 UI 块 | 检查器改成可编辑（另刀） |

视口快捷键已锁 N9–N11，不再铺。身份特征 dashboard 摘要无字段，仍不进。

### N12 落地规格（N6–N11 之后，不在本轮实现）

`ingestCharacterCanvasPack` 把 `description` 写死为「画布{角色/场景/道具}库入库」。表单只有名称。检查器已能显示说明。

- **用户可见**：入库可选说明；空则保留现有模板句；检查器立刻能读到。
- **复用**：`studioCreateAssetPayloadSchema.description`；`IngestCharacterCanvasPackInput` 增可选 `description`；trim 后非空才覆盖模板。
- **合同**：源码测试钉 envelope `description` 来自输入而非写死模板；busy 时入库仍 fail-closed；无窗口。
- **不做**：CHARX；lorebook；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口。

24h-perf 不抢 View/Inspector/N6。

## P93 附录 · 2026-08-21 17:59 CST（连续两轮无自动项）

P92–P93 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N12 规格。

### 对标增量（相对 P91 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Vue Flow MIT `deleteKeyCode` | 键盘卸钉必须走 CAS | 默认从 `nodes` 数组删、不写 layout |
| Excalidraw / Figma Delete 所选 | Delete/Backspace 卸钉或删线 | 抄闭源快捷键表、系统剪切板 |
| 本仓 `togglePinnedNode` + `deleteSelectedDraftEdge` | 已有写入口 + `pinActionBusy` | 新删除 IPC / 新坐标表 |

入库说明/别名已锁 N8/N12。视口快捷键已锁 N9–N11。身份特征 dashboard 摘要无字段，仍不进。网格吸附（Background `gap=24`）另刀，不进 N13。

### N13 落地规格（N6–N12 之后，不在本轮实现）

VueFlow `:delete-key-code="() => false"` 挡住默认删节点。底栏有「删除所选连线」，库行有「移出画布」，`onCanvasKeydown` 无 Delete/Backspace。

- **用户可见**：选中节点后 Delete/Backspace 走现有 `togglePinnedNode` 卸钉；有 `selectedDraftEdgeId` 时优先 `deleteSelectedDraftEdge`。输入框/拖拽中忽略。
- **复用**：`togglePinnedNode` + `pinActionBusy` 早退；`deleteSelectedDraftEdge`；`onCanvasKeydown` 现有 editable/`isDragging` 早退。保持 `:delete-key-code="() => false"`，禁止 Vue Flow 本地删节点。
- **合同**：源码测试钉 `event.key === "Delete" || event.key === "Backspace"` 在卸钉/删线之前；busy 时 fail-closed 不 mutate；无窗口。
- **不做**：改 T23；启用 Vue Flow 默认 delete；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明。

24h-perf 不抢 View/Node/N6。

## P95 附录 · 2026-08-21 18:04 CST（连续两轮无自动项）

P94–P95 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N13 规格。

### 对标增量（相对 P93 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma / Excalidraw 网格吸附（行为） | 可选 24px 网格圆整 | 抄闭源、替换对象吸附 |
| Vue Flow MIT `snapToGrid` | 仅作对照 | 打开 `:snap-to-grid`（会与 `applySnap` 抢 position） |
| 本仓 `computeCanvasSnap` + Background `gap=24` | 对象吸附之后再 round | 新坐标表、改 layout CAS 语义 |

Delete 卸钉已锁 N13。视口快捷键已锁 N9–N11。身份特征仍不进。

### N14 落地规格（N6–N13 之后，不在本轮实现）

Background 点阵已是 24px。拖拽已有节点对齐吸附（`computeCanvasSnap`，成组拖禁用）。无网格圆整。

- **用户可见**：底栏「网格吸附」默认关；开启后单节点拖拽在对象吸附之后把坐标 round 到 24。成组拖仍跳过吸附。
- **复用**：`applySnap` 末尾可选 `Math.round(x/24)*24`；`onNodeDragStop` 仍 flush 末帧；落盘仍走现有 `scheduleLayoutPersist`。不启用 Vue Flow `:snap-to-grid`。
- **合同**：源码测试钉开关默认关、开启后单节点坐标为 24 倍数、成组拖不 round；busy/`isDragging` 收尾仍清参考线；无窗口。
- **不做**：改 T23；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13 Delete。

24h-perf 不抢 View/Node/N6。

## P97 附录 · 2026-08-21 18:07 CST（连续两轮无自动项）

P96–P97 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N14 规格。

### 对标增量（相对 P95 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma / Excalidraw 方向键微移（行为） | Arrow 1px / Shift+Arrow 24px | 抄闭源快捷键表 |
| 本仓 `applyPositionMap` + undo 栈 | 键盘位移走现有落盘 | 新坐标表 |
| 本仓底栏 `applyAlign` | 仍用按钮；键盘不抢对齐 | 把对齐绑到方向键 |

网格吸附已锁 N14。Delete 卸钉已锁 N13。视口快捷键已锁 N9–N11。

### N15 落地规格（N6–N14 之后，不在本轮实现）

`onCanvasKeydown` 仅处理 ⌘Z/Ctrl+Y/导演白名单/Escape。底栏对齐/均分已有按钮。无方向键微移。

- **用户可见**：有选区时 Arrow 平移 1px；Shift+Arrow 平移 24px（对齐 Background `gap`）。输入框/拖拽/无选区忽略。
- **复用**：`selectedGeometries` + `applyPositionMap` + undo 栈 + `scheduleLayoutPersist`；`onCanvasKeydown` 现有 editable/`isDragging` 早退。
- **合同**：源码测试钉 `ArrowUp/Down/Left/Right` 在 `applyPositionMap` 之前；Shift 步长 24；无选区不 mutate；无窗口。
- **不做**：改 T23；方向键改走对齐；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13 Delete / N14 网格。

24h-perf 不抢 View/Node/N6。

## P99 附录 · 2026-08-21 18:09 CST（连续两轮无自动项）

P98–P99 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N15 规格。

### 对标增量（相对 P97 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma / Excalidraw ⌘A 全选（行为） | 全选当前画布节点 | 抄闭源、系统「全选网页」 |
| Vue Flow `nodes.selected` | 写 v-model 选中旗标 | 只选视口内可见节点 |
| 本仓 `:selection-key-code="true"` | 框选已开；补键盘全选 | 改框选手势 |

方向键微移已锁 N15。网格/Delete/视口已锁。Space+拖平移另刀，不进 N16。

### N16 落地规格（N6–N15 之后，不在本轮实现）

框选始终可用。`onCanvasKeydown` 无 ⌘A。空画布应空操作。

- **用户可见**：⌘A / Ctrl+A 选中当前 `nodes` 全部（含离屏、因 only-render-visible 暂未挂 dimensions 的节点）。输入框/拖拽中忽略。
- **复用**：`nodes.value` 上写 `selected=true`；`onCanvasKeydown` 现有 editable/`isDragging` 早退。不改 layout CAS。
- **合同**：源码测试钉 `(metaKey||ctrlKey) && key==="a"` 在全选之前；空图不 mutate；无窗口。
- **不做**：改 T23；只选视口；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13 Delete / N14 网格 / N15 微移。

24h-perf 不抢 View/Node/N6。

## P101 附录 · 2026-08-21 18:11 CST（连续两轮无自动项）

P100–P101 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N16 规格。

### 对标增量（相对 P99 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma / Excalidraw Space+拖平移（行为） | 按住空格左键拖视口 | 抄闭源、改默认左键框选 |
| Vue Flow MIT `pan-on-drag` | 按键期间临时把 `0` 加入按钮集 | 永久 `:pan-on-drag="true"` |
| 本仓 `:pan-on-drag="[1, 2]"` | 中键/右键平移保持 | 新手势库 |

⌘A 已锁 N16。方向键/网格/Delete/视口已锁。

### N17 落地规格（N6–N16 之后，不在本轮实现）

中键/右键已能平移。左键是框选。无 Space 临时平移。

- **用户可见**：按住 Space 时左键拖动画布平移（光标 grab）；松开恢复框选。输入框忽略 Space。中键/右键行为不变。
- **复用**：现有 `:pan-on-drag` 绑 computed；keydown/keyup Space 把 `0` 临时并入 `[1,2]`；`onCanvasKeydown` 现有 editable 早退；`preventDefault` 避免页面滚动。
- **合同**：源码测试钉 Space keydown 后 pan-on-drag 含 0、keyup 回到 `[1,2]`；editable 不切换；无窗口。
- **不做**：改 T23；左键默认改平移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13 Delete / N14 网格 / N15 微移 / N16 ⌘A。

24h-perf 不抢 View/Node/N6。

## P103 附录 · 2026-08-21 18:13 CST（连续两轮无自动项）

P102–P103 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N17 规格。

### 对标增量（相对 P101 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma / Excalidraw Escape 取消选区（行为） | 关掉工具后清节点选中 | 抄闭源、中断正在进行的拖拽 |
| 本仓 `onCanvasKeydown` Escape | 已关连线/帮助/加号/导演面板 | 新快捷键栈 |
| Vue Flow `nodes.selected` | 写 false | 调用 `rebuildGraph` 二次重建 |

Space 平移已锁 N17。⌘A 已锁 N16。⌘D 复制另刀，不进 N18。

### N18 落地规格（N6–N17 之后，不在本轮实现）

Escape 已关闭 `connectMode` / 帮助 / 加号菜单 / 导演面板 / 草稿连线，并 `rebuildGraph()`。不把 `nodes.selected` 置 false。

- **用户可见**：Escape 在现有弹层/连线收口之后，再取消全部节点选中。输入框内忽略。拖拽中不打断 `isDragging`。
- **复用**：现有 Escape 块末尾对 `nodes.value` 写 `selected=false`；不新增 `rebuildGraph`。editable 时不改选区。
- **合同**：源码测试钉 Escape 路径含 `selected=false`；有弹层时仍先关弹层；无选区不 mutate；无窗口。
- **不做**：改 T23；Escape 变成卸钉；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13 Delete / N14 网格 / N15 微移 / N16 ⌘A / N17 Space。

24h-perf 不抢 View/Node/N6。

## P105 附录 · 2026-08-21 18:16 CST（连续两轮无自动项）

P104–P105 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N18 规格。

### 对标增量（相对 P103 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Illustrator / 矢量编辑器反选（行为） | Shift+⌘A 翻转 `selected` | 抄闭源 |
| 本仓 `nodes.selected` + N16 ⌘A | 只写旗标 | 改 layout CAS |
| 本仓钉选 `asset:${id}` | 同一资产只能一钉 | ⌘D 复制（要实例 ID 表，属正式工程） |

Escape 取消选区已锁 N18。⌘A 已锁 N16。layout 仅 `{x,y}`，z-order 不进（要改 Core 表）。

### N19 落地规格（N6–N18 之后，不在本轮实现）

无反选。空图或全选后再反选应变成全不选。

- **用户可见**：Shift+⌘A / Shift+Ctrl+A 翻转当前 `nodes` 的 `selected`。输入框/拖拽中忽略。
- **复用**：`nodes.value` 上 `selected = !selected`；`onCanvasKeydown` 现有 editable/`isDragging` 早退。不写 layout CAS。
- **合同**：源码测试钉 `shiftKey && (metaKey||ctrlKey) && key==="a"` 在翻转之前；空图不 mutate；无窗口。
- **不做**：改 T23；⌘D 复制钉选；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13 Delete / N14 网格 / N15 微移 / N16 ⌘A / N17 Space / N18 Escape。

24h-perf 不抢 View/Node/N6。

## P107 附录 · 2026-08-21 18:19 CST（连续两轮无自动项）

P106–P107 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N19 规格。

### 对标增量（相对 P105 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma Alt+对齐（行为） | Alt+方向键对齐选区 | 抄闭源、把无修饰 Arrow 改成对齐 |
| 本仓 `applyAlign` + 底栏按钮 | 键盘只调已有函数 | 新对齐算法 |
| N15 Arrow 微移 | 必须 `altKey` 才对齐 | 覆盖 N15 |

反选已锁 N19。均分/居中仍用按钮。

### N20 落地规格（N6–N19 之后，不在本轮实现）

底栏六点对齐按钮已接 `applyAlign`。`onCanvasKeydown` 无 Alt+Arrow。无修饰的 Arrow 留给 N15。

- **用户可见**：选中 ≥2 节点时 Alt+Left/Right/Up/Down 分别 `left/right/top/bottom`。不足 2 或拖拽/输入框忽略。居中与均分仍走按钮。
- **复用**：`applyAlign`（已含 undo + `applyPositionMap`）；`onCanvasKeydown` editable/`isDragging` 早退；`preventDefault` 以免 Electron 当历史后退。
- **合同**：源码测试钉 `altKey && Arrow*` 在 `applyAlign` 之前且无 `altKey` 不走对齐；选区 <2 不 mutate；无窗口。
- **不做**：改 T23；无修饰 Arrow 改对齐；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13 Delete / N14 网格 / N15 微移 / N16–N19。

24h-perf 不抢 View/Node/N6。

## P109 附录 · 2026-08-21 18:21 CST（连续两轮无自动项）

P108–P109 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N20 规格。

### 对标增量（相对 P107 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma 居中/均分快捷键（行为） | Alt+H/V 居中；Alt+Shift+H/V 均分 | 抄闭源 |
| 本仓 `applyAlign('centerX'|'centerY')` | 键盘只调已有函数 | 新对齐算法 |
| 本仓 `applyDistribute('x'|'y')` | ≥3 才均分 | 把均分绑到 Arrow |

N20 已锁四边对齐。无修饰 Arrow 属 N15。

### N21 落地规格（N6–N20 之后，不在本轮实现）

底栏居中与均分只有按钮。N20 规格明确「居中与均分仍走按钮」。

- **用户可见**：选中 ≥2 时 Alt+H → `centerX`，Alt+V → `centerY`。选中 ≥3 时 Alt+Shift+H → 水平均分，Alt+Shift+V → 垂直均分。不足阈值、拖拽、输入框忽略。
- **复用**：`applyAlign` / `applyDistribute`（已含 undo + `applyPositionMap`）；`onCanvasKeydown` editable/`isDragging` 早退。
- **合同**：源码测试钉 `altKey && key h/v` 在居中之前、`altKey && shiftKey && key h/v` 在均分之前；选区不足不 mutate；无窗口。
- **不做**：改 T23；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13 Delete / N14 网格 / N15 微移 / N16–N20 Alt+Arrow。

24h-perf 不抢 View/Node/N6。

## P111 附录 · 2026-08-21 18:23 CST（连续两轮无自动项）

P110–P111 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N21 规格。

### 对标增量（相对 P109 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Excalidraw 图层/连线显隐（行为） | 快捷键切边 | 抄闭源、新边模型 |
| 本仓 `toggleEdges` + 底栏按钮 | 键盘只调已有函数 | 改 edge 数据结构 |
| 导演 `mod+e` | 必须带 Shift 才切边 | 抢 N5/导演定位 |

居中/均分已锁 N21。小地图切换另刀，不进 N22。

### N22 落地规格（N6–N21 之后，不在本轮实现）

底栏「隐藏/显示连线」已接 `toggleEdges`。`onCanvasKeydown` 无 E。导演白名单 `mod+e` 是定位 earliest。

- **用户可见**：Shift+E 切换连线显隐。输入框/拖拽忽略。不带 Shift 的 E 不触发（避免与打字/导演混淆）。
- **复用**：现有 `toggleEdges()` 与 `data-testid="managed-canvas-toggle-edges"`；`onCanvasKeydown` editable/`isDragging` 早退。
- **合同**：源码测试钉 `shiftKey && key==="e" && !metaKey && !ctrlKey && !altKey` 在 `toggleEdges` 之前；无窗口。
- **不做**：改 T23；抢 `mod+e`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N21。

24h-perf 不抢 View/Node/N6。

## P113 附录 · 2026-08-21 18:28 CST（连续两轮无自动项）

P112–P113 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N22 规格。

### 对标增量（相对 P111 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Vue Flow MIT `MiniMap` | 快捷键只切已有组件显隐 | 换画布引擎、新小地图实现 |
| Excalidraw / Penpot 视图层开关（行为） | Shift+修饰切辅助层 | 抄闭源、Zen 模式整页隐藏 |
| 本仓 `showMiniMap` + 视图菜单按钮 | 抽出 `toggleMiniMap` 对标 `toggleEdges` | 改 MiniMap 节点投影 |

N22 已锁切连线，并写明「小地图切换另刀」。工作流过滤另刀，不进 N23。

### N23 落地规格（N6–N22 之后，不在本轮实现）

视图菜单「隐藏/显示小地图」内联改 `showMiniMap`，无独立函数、无 testid。`onCanvasKeydown` 无 M。导演白名单无 M 和弦。`Background` 已常驻 gap=24，不属本刀。

- **用户可见**：Shift+M 切换小地图。输入框/拖拽忽略。不带 Shift 的 M 不触发。
- **复用**：现有 `showMiniMap` 与 `<MiniMap v-if="showMiniMap">`；抽出 `toggleMiniMap()` 对称 `toggleEdges`；菜单按钮加 `data-testid="managed-canvas-toggle-minimap"`；`onCanvasKeydown` editable/`isDragging` 早退。
- **合同**：源码测试钉 `shiftKey && event.key.toLowerCase()==="m" && !metaKey && !ctrlKey && !altKey` 在 `toggleMiniMap` 之前；无窗口。
- **不做**：改 T23；关 `Background`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N22 Shift+E。

24h-perf 不抢 View/Node/N6。

## P115 附录 · 2026-08-21 18:30 CST（连续两轮无自动项）

P114–P115 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N23 规格。

### 对标增量（相对 P113 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma / Penpot 图层过滤（行为） | 快捷键切「只看工作流 / 全部」 | 抄闭源、新过滤引擎 |
| 本仓 `toggleWorkspaceMode` | 键盘只调已有函数 | 改 projection/workflow 语义 |
| 导演 `mod+shift+w` | 必须无 meta/ctrl 才切过滤 | 抢 15s 分镜建议 |

N23 已锁小地图，并写明「工作流过滤另刀」。时间线重排另刀，不进 N24。

### N24 落地规格（N6–N23 之后，不在本轮实现）

视图菜单「只看工作流 / 查看全部」已接 `toggleWorkspaceMode`，按钮无 testid。`onCanvasKeydown` 无 W。导演白名单 `mod+shift+w` 是分镜建议。空工作区切 projection 已 fail-closed（errorMessage）。

- **用户可见**：Shift+W 在 workflow ↔ projection 间切换。输入框/拖拽忽略。带 ⌘/Ctrl 的 Shift+W 不触发。无钉选时切 projection 仍报错不 mutate。
- **复用**：现有 `toggleWorkspaceMode()`（已含空钉选早退、切 projection 关 connectMode、rebuildGraph + persist）；菜单按钮加 `data-testid="managed-canvas-toggle-workspace-mode"`；`onCanvasKeydown` editable/`isDragging` 早退。
- **合同**：源码测试钉 `shiftKey && event.key.toLowerCase()==="w" && !metaKey && !ctrlKey && !altKey` 在 `toggleWorkspaceMode` 之前；`mod+shift+w` 仍走导演；无窗口。
- **不做**：改 T23；抢导演 `mod+shift+w`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N23 Shift+M。

24h-perf 不抢 View/Node/N6。

## P117 附录 · 2026-08-21 18:32 CST（连续两轮无自动项）

P116–P117 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N24 规格。

### 对标增量（相对 P115 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `applyTimelineLayout` + 底栏两钮 | 键盘只调已有函数 | 新排布算法、改 pinned 语义 |
| Storyboarder / Kitsu 按场次排（行为） | Shift 普通 / Shift+Alt 强制 | 抄闭源、回写剧本 |
| 导演和弦 | 无 T；勿绑 `mod+shift+t`（打开 trace） | 抢导演 |

N24 已锁工作流过滤，并写明「时间线重排另刀」。视图刷新另刀，不进 N25。

### N25 落地规格（N6–N24 之后，不在本轮实现）

底栏「按时间线排布」=`applyTimelineLayout(false)`，「强制时间线」=`applyTimelineLayout(true)`。`onCanvasKeydown` 无 T。导演 `mod+shift+t` 是打开 trace。函数已：拖拽中 return；无 15s 节点写 errorMessage。

- **用户可见**：Shift+T 尊重钉选重排；Shift+Alt+T 强制全部。输入框/拖拽/loading/空节点忽略。无时间线节点仍报错不 mutate。
- **复用**：现有 `applyTimelineLayout`（undo + `applyPositionMap` + persist）；`data-testid="managed-canvas-timeline-layout"` / `-force`；`onCanvasKeydown` editable/`isDragging` 早退。
- **合同**：源码测试钉 `shiftKey && !altKey && key.toLowerCase()==="t" && !metaKey && !ctrlKey` 在 `applyTimelineLayout(false)` 之前；`shiftKey && altKey && key.toLowerCase()==="t"` 在 force 之前；`mod+shift+t` 仍走导演；无窗口。
- **不做**：改 T23；抢导演 `mod+shift+t`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N24 Shift+W。

24h-perf 不抢 View/Node/N6。

## P119 附录 · 2026-08-21 18:34 CST（连续两轮无自动项）

P118–P119 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N25 规格。

### 对标增量（相对 P117 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `refreshAll` + 视图菜单「刷新」 | 键盘只调已有函数 | 新刷新管线、绕过写闸 |
| Electron F5 默认窗口重载 | `preventDefault` 后走画布刷新 | 真的 `location.reload` |
| 导演 `mod+shift+.` | 投影刷新 ≠ 画布 `refreshAll` | 抢导演和弦 |

N25 已锁时间线重排，并写明「视图刷新另刀」。连线模式另刀，不进 N26。

### N26 落地规格（N6–N25 之后，不在本轮实现）

视图菜单「刷新」已接 `refreshAll`，按钮无 testid、`:disabled="loading"`。`onCanvasKeydown` 无 F5。导演 `mod+shift+.` 是刷新投影。函数已：`canvasDisposed` 早退、`refreshSequence` 失效。

- **用户可见**：F5 刷新画布数据/布局。输入框/拖拽/loading 忽略。必须 `preventDefault`，禁止 Electron 整窗重载。不绑 ⌘R。
- **复用**：现有 `refreshAll()`；菜单按钮加 `data-testid="managed-canvas-refresh"`；`onCanvasKeydown` editable/`isDragging`/`loading` 早退。
- **合同**：源码测试钉 `key==="F5" && !metaKey && !ctrlKey && !altKey` 在 `refreshAll` 之前且 `preventDefault`；`mod+shift+.` 仍走导演；无窗口。
- **不做**：改 T23；`location.reload`；抢导演 `mod+shift+.` / `mod+shift+r`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N25 Shift+T。

24h-perf 不抢 View/Node/N6。

## P121 附录 · 2026-08-21 18:36 CST（连续两轮无自动项）

P120–P121 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N26 规格。

### 对标增量（相对 P119 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma / Excalidraw 工具键（行为） | 无修饰字母切工具 | 抄闭源、换画布引擎 |
| 本仓 `toggleConnectMode` + 顶栏「连线」 | 键盘只调已有函数 | 改两次点击连线状态机 |
| N22 Shift+E | 切边显隐 ≠ 连线工具 | 把 C 绑成 toggleEdges |

N26 已锁 F5 刷新，并写明「连线模式另刀」。帮助层另刀，不进 N27。

### N27 落地规格（N6–N26 之后，不在本轮实现）

顶栏「连线」已接 `toggleConnectMode`，`data-testid="managed-canvas-connect-mode"`。函数已清 `pendingConnectionSourceId` 并 `stripPendingOutline`。Escape 已关 connectMode（N18）。`onCanvasKeydown` 无 C。导演白名单无 C。

- **用户可见**：无修饰 C 切换连线模式。输入框/拖拽忽略。Shift/⌘/Alt+C 不触发。再按 C 或 Escape 退出。
- **复用**：现有 `toggleConnectMode()`；`onCanvasKeydown` editable/`isDragging` 早退。
- **合同**：源码测试钉 `key.toLowerCase()==="c" && !shiftKey && !metaKey && !ctrlKey && !altKey` 在 `toggleConnectMode` 之前；Shift+E 仍属 N22；无窗口。
- **不做**：改 T23；抢 N22 Shift+E；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N26 F5。

24h-perf 不抢 View/Node/N6。

## P123 附录 · 2026-08-21 18:38 CST（连续两轮无自动项）

P122–P123 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N27 规格。

### 对标增量（相对 P121 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 桌面 F1 帮助（行为） | 开合已有帮助卡 | 新帮助系统、浏览器帮助页 |
| 本仓 `helpOpen` + 顶栏「帮助」 | 抽出 `toggleHelp` | 改 help-card 文案/DOM |
| 导演 `mod+/` | `/` 与 `?`(Shift+/) 同键位族 | 抢导演面板 |

N27 已锁连线模式，并写明「帮助层另刀」。添加菜单另刀，不进 N28。

### N28 落地规格（N6–N27 之后，不在本轮实现）

顶栏「帮助」内联改 `helpOpen`，无独立函数、无 testid。帮助卡 `id="managed-canvas-help-card"`。Escape 已关帮助并焦点归还（N18）。`onCanvasKeydown` 无 F1。导演 `mod+/` 是导演面板。

- **用户可见**：F1 开合帮助卡。输入框/拖拽忽略。再按 F1 或 Escape 关闭。不绑 `?` / `Shift+/` / `mod+/`。
- **复用**：现有 `helpOpen` / `helpTriggerEl` / help-card；抽出 `toggleHelp()`；按钮加 `data-testid="managed-canvas-help"`；`onCanvasKeydown` editable/`isDragging` 早退。
- **合同**：源码测试钉 `key==="F1" && !metaKey && !ctrlKey && !altKey` 在 `toggleHelp` 之前且 `preventDefault`；`mod+/` 仍走导演；无窗口。
- **不做**：改 T23；抢导演 `mod+/`；打开系统/浏览器帮助；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N27 C。

24h-perf 不抢 View/Node/N6。

## P125 附录 · 2026-08-21 18:40 CST（连续两轮无自动项）

P124–P125 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N28 规格。

### 对标增量（相对 P123 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Figma / Excalidraw 工具面板开合（行为） | 无修饰字母开合已有菜单 | 抄闭源、新节点类型 |
| 本仓 `addMenuOpen` + 「添加」 | 抽出 `toggleAddMenu` | 改 libraryTabs / chooseAddKind |
| N16 ⌘A / N19 Shift+⌘A | 必须无 meta/shift 才开菜单 | 抢全选/反选 |

N28 已锁帮助层，并写明「添加菜单另刀」。素材库开合另刀，不进 N29。

### N29 落地规格（N6–N28 之后，不在本轮实现）

顶栏「添加」内联改 `addMenuOpen`，已有 `data-testid="managed-canvas-add-node"`。Escape 已关菜单并焦点归还（N18）。`onCanvasKeydown` 无 A。导演白名单无 A。

- **用户可见**：无修饰 A 开合添加菜单。输入框/拖拽忽略。⌘A / Shift+⌘A / Shift+A 不触发。再按 A 或 Escape 关闭。
- **复用**：现有 `addMenuOpen` / `addTriggerEl` / `chooseAddKind`；抽出 `toggleAddMenu()`；`onCanvasKeydown` editable/`isDragging` 早退。
- **合同**：源码测试钉 `key.toLowerCase()==="a" && !shiftKey && !metaKey && !ctrlKey && !altKey` 在 `toggleAddMenu` 之前；⌘A 仍属 N16；无窗口。
- **不做**：改 T23；抢 N16/N19；改添加种类；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N28 F1。

24h-perf 不抢 View/Node/N6。

## P127 附录 · 2026-08-21 18:42 CST（连续两轮无自动项）

P126–P127 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N29 规格。

### 对标增量（相对 P125 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Blender / Penpot 侧栏开合（行为） | 无修饰字母切已有库面板 | 抄闭源、新库表 |
| 本仓 `toggleLibrary` + 「素材库」 | 键盘只调已有函数 | 改分页/虚拟窗口 |
| 本仓 `toggleGlobalResourceLibrary` | 剧本资源另刀 | 把 L 绑成全局资源 |

N29 已锁添加菜单，并写明「素材库开合另刀」。剧本资源开合另刀，不进 N30。

### N30 落地规格（N6–N29 之后，不在本轮实现）

顶栏「素材库」已接 `toggleLibrary`，`data-testid="managed-canvas-open-library"`。函数已：当前库开着则 `closeLibrary`，否则 `openLibraryFor(libraryTab)`。`onCanvasKeydown` 无 L。导演白名单无 L。

- **用户可见**：无修饰 L 开合当前工程素材库。输入框/拖拽忽略。Shift/⌘/Alt+L 不触发。若正开剧本资源，L 切到当前库（沿用现有语义）。
- **复用**：现有 `toggleLibrary()`；`onCanvasKeydown` editable/`isDragging` 早退，`void toggleLibrary()`。
- **合同**：源码测试钉 `key.toLowerCase()==="l" && !shiftKey && !metaKey && !ctrlKey && !altKey` 在 `toggleLibrary` 之前；不调用 `toggleGlobalResourceLibrary`；无窗口。
- **不做**：改 T23；抢全局资源钮；改库分页；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N29 A。

24h-perf 不抢 View/Node/N6。

## P129 附录 · 2026-08-21 18:45 CST（连续两轮无自动项）

P128–P129 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N30 规格。

### 对标增量（相对 P127 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `toggleGlobalResourceLibrary` + 「剧本资源」 | 键盘只调已有函数 | 改全局资源分页/跨剧本写入 |
| N30 无修饰 L | Shift+L 才开全局库 | 把 L 绑成剧本资源 |
| 导演 `mod+shift+r` | 打开剧本阅读 ≠ 本侧栏 | 抢导演 |

N30 已锁当前素材库，并写明「剧本资源开合另刀」。核对来源另刀，不进 N31。

### N31 落地规格（N6–N30 之后，不在本轮实现）

顶栏「剧本资源」已接 `toggleGlobalResourceLibrary`，`data-testid="managed-canvas-open-global-resources"`。函数已：global 开着则 `closeLibrary`，否则切 `libraryMode=global` 并 `resetGlobalResources`。`onCanvasKeydown` 无 Shift+L。

- **用户可见**：Shift+L 开合跨剧本资源侧栏。输入框/拖拽忽略。无修饰 L 仍属 N30。⌘/Alt+L 不触发。
- **复用**：现有 `toggleGlobalResourceLibrary()`；`onCanvasKeydown` editable/`isDragging` 早退，`void toggleGlobalResourceLibrary()`。
- **合同**：源码测试钉 `shiftKey && key.toLowerCase()==="l" && !metaKey && !ctrlKey && !altKey` 在 `toggleGlobalResourceLibrary` 之前；无修饰 L 不进该函数；无窗口。
- **不做**：改 T23；改跨剧本写入语义；抢 N30 L；抢导演 `mod+shift+r`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N29 A。

24h-perf 不抢 View/Node/N6。

## P131 附录 · 2026-08-21 18:48 CST（连续两轮无自动项）

P130–P131 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N31 规格。

### 对标增量（相对 P129 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `verifyLocalProductionSource` + 「核对外部来源」 | 键盘只调已有函数 | 新核对管线、绕过写闸 |
| N26 F5 `refreshAll` | F6 才核对来源 | 把 F5 绑成核对 |
| 导演 `mod+shift+.` | 投影刷新 ≠ 来源核对 | 抢导演 |

N31 已锁剧本资源，并写明「核对来源另刀」。画布主题另刀，不进 N32。

### N32 落地规格（N6–N31 之后，不在本轮实现）

视图菜单「核对外部来源」已接 `verifyLocalProductionSource`，`data-testid="managed-canvas-verify-source"`，`:disabled="loading || localProductionPreviewLoading"`。函数已：API 缺失早退、`localSourceVerificationSequence` 失效。`onCanvasKeydown` 无 F6。

- **用户可见**：F6 显式核对外部来源。输入框/拖拽/loading/正在核对忽略。必须 `preventDefault`。不绑 F5。
- **复用**：现有 `verifyLocalProductionSource()`；`onCanvasKeydown` editable/`isDragging`/`loading`/`localProductionPreviewLoading` 早退，`void verifyLocalProductionSource()`。
- **合同**：源码测试钉 `key==="F6" && !metaKey && !ctrlKey && !altKey && !shiftKey` 在 `verifyLocalProductionSource` 之前且 `preventDefault`；F5 仍属 N26；无窗口。
- **不做**：改 T23；`refreshAll` 代替核对；抢 N26 F5 / N28 F1；抢导演 `mod+shift+.`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N31 Shift+L。

24h-perf 不抢 View/Node/N6。

## P133 附录 · 2026-08-21 18:53 CST（连续两轮无自动项）

P132–P133 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N32 规格。

### 对标增量（相对 P131 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `setCanvasTheme` + 视图菜单主题 radio | 键盘只调已有函数与 `MANAGED_CANVAS_THEMES` 顺序 | 新存储键、第四套皮肤、改 CSS 变量名 |
| N27 C 连线 / N32 F6 核对 | Shift+D 才循环主题 | 把 D/F6/C 绑成主题 |
| Figma/Penpot 主题在设置里 | 只给已有三套皮肤加快捷键 | 抄闭源设置页、系统 appearance |

N32 已锁核对来源，并写明「画布主题另刀」。节点搜索另刀，不进 N33。

### N33 落地规格（N6–N32 之后，不在本轮实现）

视图菜单「画布主题」已接 `setCanvasTheme(theme.id)`，三套 `light/dark/paper`。`onCanvasKeydown` 无 D / Shift+D。无独立 cycle 函数。

- **用户可见**：Shift+D 按 `MANAGED_CANVAS_THEMES` 顺序循环浅色→深色→米色。输入框/拖拽忽略。无修饰 D 不触发。⌘/Ctrl/Alt+D 不触发。必须 `preventDefault`。
- **复用**：现有 `setCanvasTheme`；下一 id = 当前 index+1 取模；`onCanvasKeydown` editable/`isDragging` 早退，`void setCanvasTheme(nextId)`。不新增 localStorage 键。
- **合同**：源码测试钉 `shiftKey && key.toLowerCase()==="d" && !metaKey && !ctrlKey && !altKey` 在 `setCanvasTheme` 之前且 `preventDefault`；无修饰 D 不进该函数；无窗口。
- **不做**：改 T23；新主题 id；抢 N27 C / N32 F6；抢导演 `mod+/`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N32 F6。

24h-perf 不抢 View/Node/N6。

## P135 附录 · 2026-08-21 18:59 CST（连续两轮无自动项）

P134–P135 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N33 规格。

### 对标增量（相对 P133 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓进度搜索 `timelineProgressQuery` + `focusTimelineSearchResult` | 键盘只聚焦已有 input | 新搜索索引、改 T12 投影查询 |
| Figma ⌘F 开图层搜索（行为） | 挡住浏览器页内查找 | 抄闭源搜索 UI |
| N28 F1 / N26 F5 / N32 F6 | 必须带 ⌘/Ctrl | 无修饰 F 绑搜索 |

N33 已锁主题循环，并写明「节点搜索另刀」。回车定位唯一命中另刀，不进 N34。

### N34 落地规格（N6–N33 之后，不在本轮实现）

顶栏已有 `data-testid="managed-canvas-timeline-progress-query"`，`@input` 走 `scheduleFocusTimelineSearchResult`。`onCanvasKeydown` 无 ⌘F。input 无 ref。

- **用户可见**：⌘F / Ctrl+F 聚焦进度搜索框并选中现有查询。必须 `preventDefault`（挡住浏览器查找）。其它 input/textarea 内忽略。拖拽忽略。无修饰 F 不触发。
- **复用**：现有 query input；补 `ref` 后 `focus()`+`select()`。不改过滤/投影语义。
- **合同**：源码测试钉 `(metaKey||ctrlKey) && key.toLowerCase()==="f" && !altKey && !shiftKey` 在 query `focus` 之前且 `preventDefault`；F1/F5/F6 仍属 N28/N26/N32；无窗口。
- **不做**：改 T23；新搜索后端；抢导演 `mod+e`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N33 Shift+D。

24h-perf 不抢 View/Node/N6。

## P137 附录 · 2026-08-21 19:02 CST（连续两轮无自动项）

P136–P137 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N34 规格。

### 对标增量（相对 P135 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `focusTimelineSearchResult`（唯一命中才 `setCenter`） | 搜索框 Enter 只调已有函数 | 改 T12 投影、多命中自动挑第一条 |
| N34 ⌘F 聚焦查询框 | Enter 才定位 | 把 ⌘F 绑成定位 |
| 浏览器 type=search Enter | 必须继续 preventDefault | 触发表单提交 |

N34 已锁聚焦搜索，并写明「回车定位唯一命中另刀」。多命中下一条/F3 另刀，不进 N35。

### N35 落地规格（N6–N34 之后，不在本轮实现）

进度搜索 input 已 `@keydown.enter.prevent`，未调用 `focusTimelineSearchResult`。函数已：命中 ≠1 早退；单命中 `studioFlow.setCenter`。

- **用户可见**：查询框内按 Enter 定位唯一命中单元。0/多命中不移动视口。必须继续 `preventDefault`。Shift/⌘/Alt+Enter 不触发。其它输入框忽略。
- **复用**：把 `.prevent` 改成 `@keydown.enter.prevent="focusTimelineSearchResult"`（或等价 `void focusTimelineSearchResult()`）。不改 150ms 输入防抖、不改投影匹配。
- **合同**：源码测试钉 query input 的 Enter 在 `focusTimelineSearchResult` 之前且 preventDefault；⌘F 仍属 N34；无窗口。
- **不做**：改 T23；多命中循环；抢 N34 ⌘F；抢 N18 Escape；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N34 ⌘F。

24h-perf 不抢 View/Node/N6。

## P139 附录 · 2026-08-21 19:04 CST（连续两轮无自动项）

P138–P139 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N35 规格。

### 对标增量（相对 P137 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓命中列表 `searchUnitsFromProjection` / `matchedUnitIds` | 只循环已有数组并复用 `setCenter` 锚点 | 改 T12 投影、新搜索后端 |
| 浏览器 F3 查找下一个（行为） | 必须 preventDefault | 打开系统查找条 |
| N35 Enter 唯一命中 | F3 才走多命中 | 把 Enter 改成循环 |

N35 已锁 Enter 唯一命中，并写明「多命中下一条/F3 另刀」。Escape 清空查询另刀，不进 N36。

### N36 落地规格（N6–N35 之后，不在本轮实现）

`focusTimelineSearchResult` 在 `unitIds.length !== 1` 时直接 return。无游标。`onCanvasKeydown` 无 F3。

- **用户可见**：查询非空时 F3 定位下一条命中、Shift+F3 上一条，到头环绕。0 命中不移动。必须 `preventDefault`。⌘/Ctrl/Alt+F3 不触发。其它 input/textarea（查询框除外）忽略。
- **复用**：同一套 unitIds 与 raw/generation-state/unit 锚点 + `studioFlow.setCenter`。查询变化时游标归零。不改 N35 Enter 唯一命中语义。
- **合同**：源码测试钉 `key==="F3" && !metaKey && !ctrlKey && !altKey` 在循环定位之前且 `preventDefault`；`shiftKey` 反向；F1/F5/F6 仍属 N28/N26/N32；无窗口。
- **不做**：改 T23；改 Enter 成循环；抢 N35 Enter / N34 ⌘F / N28 F1 / N26 F5 / N32 F6；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N35 Enter。

24h-perf 不抢 View/Node/N6。

## P141 附录 · 2026-08-21 19:06 CST（连续两轮无自动项）

P140–P141 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N36 规格。

### 对标增量（相对 P139 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `timelineProgressQuery` / `timelineProgressReview` | Escape 只清已有查询状态 | 改 T12 投影、清画布选区 |
| 浏览器查找 Escape 关查找条（行为） | 有内容才拦截 | 第一次 Escape 就走 N18 |
| N18 Escape 关弹层 | 查询有内容时先清查询 | 把 N18 改成只清搜索 |

N36 已锁 F3 循环，并写明「Escape 清空查询另刀」。查询已空时 Escape 先失焦另刀，不进 N37。

### N37 落地规格（N6–N36 之后，不在本轮实现）

`onCanvasKeydown` 对 Escape 不区分查询框，直接关帮助/添加/导演/视图并 `rebuildGraph`。查询 input `data-testid="managed-canvas-timeline-progress-query"`。

- **用户可见**：焦点在进度搜索框且查询或审片筛选非空时，Escape 清空两者并 `preventDefault`，不关弹层、不清连线。查询已空则不拦截，交给 N18。
- **复用**：写空 `timelineProgressQuery` / `timelineProgressReview`。不改 N18 后续关弹层顺序。
- **合同**：源码测试钉 query 焦点 + 非空时 `key==="Escape"` 在清空查询之后 `return`，且不调用 `closeViewMenu`/`rebuildGraph`；空查询仍进入 N18 路径；无窗口。
- **不做**：改 T23；抢 N18 关弹层；抢 N36 F3 / N35 Enter / N34 ⌘F；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N36 F3。

24h-perf 不抢 View/Node/N6。

## P143 附录 · 2026-08-21 19:09 CST（连续两轮无自动项）

P142–P143 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N37 规格。

### 对标增量（相对 P141 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓进度搜索 input | 空查询 Escape 只 `blur()` | 同键关弹层/清选区 |
| 浏览器查找第二条 Escape 关查找框（行为） | 有内容仍属 N37 | 把 N18 绑到查询框 |
| N37 非空才拦截 | 已空才失焦 | 空查询也清 N18 状态 |

N37 已锁非空 Escape 清查询，并写明「查询已空时 Escape 先失焦另刀」。审片筛选下拉键盘另刀，不进 N38。

### N38 落地规格（N6–N37 之后，不在本轮实现）

查询 input 无 ref。N37 规定空查询不拦截、落入 N18，会导致焦点仍在框内时第一次空 Escape 就关帮助/添加。

- **用户可见**：焦点在进度搜索框且查询与审片筛选都空时，Escape `blur()` 查询框并 `preventDefault`，不关弹层。再按一次 Escape 才走 N18。
- **复用**：N34 将补的 query `ref`；`HTMLInputElement.blur()`。不改 N37 非空清空、不改 N18 关弹层顺序。
- **合同**：源码测试钉 query 焦点 + 双空时 `key==="Escape"` 在 `blur` 之后 `return`，且不调用 `closeViewMenu`/`rebuildGraph`；非空仍属 N37；无窗口。
- **不做**：改 T23；抢 N37 清查询；抢 N18 关弹层；抢 N36 F3 / N35 Enter / N34 ⌘F；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N37 Escape。

24h-perf 不抢 View/Node/N6。

## P145 附录 · 2026-08-21 19:11 CST（连续两轮无自动项）

P144–P145 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N38 规格。

### 对标增量（相对 P143 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `timelineProgressReview` + `@change="focusTimelineSearchResult"` | 只循环已有 option 值 | 新筛选后端、改投影 |
| N20 Alt+Arrow 对齐 | 仅查询框 editable 内才循环筛选 | 画布上 Alt+Arrow 改筛选 |
| 原生 select 键盘 | 查询框内不必先 Tab 到 select | 抄闭源筛选 UI |

N38 已锁空查询 Escape 失焦，并写明「审片筛选下拉键盘另刀」。筛选框 Escape 回查询框另刀，不进 N39。

### N39 落地规格（N6–N38 之后，不在本轮实现）

`timelineProgressReview` option 顺序：`"" | any-pending | pass | rework | reject | none`。查询框内 Alt+Arrow 现被 `editable` 早退，N15/N20 不处理。

- **用户可见**：焦点在进度搜索框时 Alt+ArrowDown 下一项、Alt+ArrowUp 上一项，到头环绕；改完调用 `focusTimelineSearchResult`。必须 `preventDefault`。⌘/Ctrl+Alt+Arrow 不触发。画布非输入区仍属 N20。
- **复用**：写 `timelineProgressReview` 后 `void focusTimelineSearchResult()`。不改 option 集合、不改 N35 Enter。
- **合同**：源码测试钉 query 焦点 + `altKey && (key==="ArrowDown"\|\|key==="ArrowUp") && !metaKey && !ctrlKey` 在写入 review 之前且 `preventDefault`；无修饰 Arrow 仍属 N15；无窗口。
- **不做**：改 T23；抢 N20 对齐 / N15 微移；抢 N37/N38 Escape；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N38 Escape。

24h-perf 不抢 View/Node/N6。

## P147 附录 · 2026-08-21 19:13 CST（连续两轮无自动项）

P146–P147 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N39 规格。

### 对标增量（相对 P145 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 review select + N34 query ref | Escape 只还焦点 | 清筛选值、关弹层 |
| N37/N38 查询框 Escape | 仅 select 焦点走本刀 | 查询框 Escape 改回焦 |
| 原生 select Escape 关列表 | 列表已关后再还焦 | 改 option 集合 |

N39 已锁查询框循环筛选，并写明「筛选框 Escape 回查询框另刀」。宫格时间线条键盘另刀，不进 N40。

### N40 落地规格（N6–N39 之后，不在本轮实现）

`data-testid="managed-canvas-timeline-progress-review"` 无 Escape 处理；select 属 editable，当前 Escape 仍落入 N18 关弹层。

- **用户可见**：焦点在审片筛选下拉时 Escape 聚焦进度搜索框并 `preventDefault`，不清筛选值、不关弹层。查询框内 Escape 仍属 N37/N38。
- **复用**：N34 query `ref.focus()`。不改 `timelineProgressReview`、不改 N18 关弹层顺序。
- **合同**：源码测试钉 review select 焦点 + `key==="Escape"` 在 query `focus` 之后 `return`，且不调用 `closeViewMenu`/`rebuildGraph`；查询框路径仍属 N37/N38；无窗口。
- **不做**：改 T23；抢 N37 清查询 / N38 失焦 / N18 关弹层；抢 N39 Alt+Arrow；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N39 Alt+Arrow。

24h-perf 不抢 View/Node/N6。

## P149 附录 · 2026-08-21 19:15 CST（连续两轮无自动项）

P148–P149 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N40 规格。

### 对标增量（相对 P147 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `panelTimelineStrip` + `focusPanelOnCanvas` | 键盘只调已有函数 | 新时间线投影、改 N25 重排 |
| Premiere/Figma 相邻对象 `[`/`]`（行为） | 空条忽略 | 抄闭源时间线 |
| N15 Arrow / N20 Alt+Arrow | 无修饰方括号才循环格 | 把方向键绑成宫格 |

N40 已锁筛选 Escape 回焦，并写明「宫格时间线条键盘另刀」。Home/End 首末格另刀，不进 N41。

### N41 落地规格（N6–N40 之后，不在本轮实现）

`data-testid="managed-canvas-panel-timeline"` 芯片按钮已 `@click="focusPanelOnCanvas"`。`onCanvasKeydown` 无 `[`/`]`。

- **用户可见**：无修饰 `]` 下一条、`[` 上一条宫格芯片，到头环绕，调用 `focusPanelOnCanvas`。空条忽略。输入框/拖拽忽略。⌘/Ctrl/Alt/Shift+方括号不触发。
- **复用**：现有 `panelTimelineStrip` 顺序；当前选中 panel 作游标，否则 `]` 从 0、`[` 从末项。必须 `preventDefault`。
- **合同**：源码测试钉 `!editable && (key==="["\|\|key==="]") && !metaKey && !ctrlKey && !altKey && !shiftKey` 在 `focusPanelOnCanvas` 之前且 `preventDefault`；N25 Shift+T 仍属时间线重排；无窗口。
- **不做**：改 T23；抢 N25 Shift+T / N15 微移 / N20 对齐；抢 N39 查询框 Alt+Arrow；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N40 Escape。

24h-perf 不抢 View/Node/N6。

## P151 附录 · 2026-08-21 19:18 CST（连续两轮无自动项）

P150–P151 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N41 规格。

### 对标增量（相对 P149 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `panelTimelineStrip[0]` / 末项 + `focusPanelOnCanvas` | 只定位已有芯片 | 改条顺序、新投影 |
| 列表 Home/End 首末项（行为） | 输入框内保持原生 | 查询框 Home 跳宫格 |
| N41 `[`/`]` 相邻循环 | Home/End 才跳端点 | 把 `[` 改成 Home |

N41 已锁相邻循环，并写明「Home/End 首末格另刀」。时间线条芯片 roving tabindex 另刀，不进 N42。

### N42 落地规格（N6–N41 之后，不在本轮实现）

`onCanvasKeydown` 无 Home/End。空 `panelTimelineStrip` 忽略。

- **用户可见**：非输入区无修饰 Home 定位第一条芯片、End 定位末条，调用 `focusPanelOnCanvas`。空条忽略。输入框/select/拖拽忽略。⌘/Ctrl/Alt/Shift+Home/End 不触发。
- **复用**：`panelTimelineStrip[0]` 与末项 `panelId`；必须 `preventDefault`（挡住页面滚动）。
- **合同**：源码测试钉 `!editable && (key==="Home"\|\|key==="End") && !metaKey && !ctrlKey && !altKey && !shiftKey` 在 `focusPanelOnCanvas` 之前且 `preventDefault`；`[`/`]` 仍属 N41；无窗口。
- **不做**：改 T23；抢 N41 相邻循环 / N15 微移 / N10 `Shift+0`；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N41 `[`/`]`。

24h-perf 不抢 View/Node/N6。

## P153 附录 · 2026-08-21 19:23 CST（连续两轮无自动项）

P152–P153 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N42 规格。

### 对标增量（相对 P151 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Toolbar roving tabindex（行为） | 条内只留一个 `tabindex=0` | 改成 listbox、虚拟滚动 |
| 本仓 `.panel-timeline-strip button` 已 click 定位 | 只改焦点/tabindex | 方向键顺便 `focusPanelOnCanvas` |
| N41/N42 画布级循环/首末 | 芯片已聚焦时 Arrow/Home/End 停在条内 | 把 `[`/`]` 改成 Arrow |

N42 已锁画布级 Home/End，并写明「芯片 roving tabindex 另刀」。条内 PageUp/PageDown 跳 10 格另刀，不进 N43。

### N43 落地规格（N6–N42 之后，不在本轮实现）

芯片现全部默认可 Tab，无 `tabindex`/`role="toolbar"`。

- **用户可见**：`.panel-timeline-strip` 标 `role="toolbar"`（`aria-label` 已有）。仅当前芯片 `tabindex="0"`，其余 `-1`。焦点已在某颗芯片时：无修饰 ArrowLeft/ArrowRight 移焦相邻芯片（循环）；Home/End 移焦首/末芯片。空条忽略。Enter/Space 仍走原生 button → `focusPanelOnCanvas`。输入框/select/拖拽忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有 chip `<button>` 与 `panelTimelineStrip`；必须 `preventDefault`（挡住 N15 微移与 N42 画布 Home/End）。方向键**不**调用 `focusPanelOnCanvas`。
- **合同**：源码测试钉芯片 `tabindex` 0/-1；`event.target` 为条内 button 且 `ArrowLeft|ArrowRight|Home|End` 且无修饰时 `preventDefault` 且不进 N15/N42；`[`/`]` 仍属 N41；无窗口。
- **不做**：改 T23；抢 N41 `[`/`]` / N42 画布 Home/End / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N42。

24h-perf 不抢 View/Node/N6。

## P155 附录 · 2026-08-21 19:26 CST（连续两轮无自动项）

P154–P155 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N43 规格。

### 对标增量（相对 P153 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Toolbar PageUp/PageDown（行为） | 条内跳 ±10，夹到端点 | 循环、改条顺序 |
| 本仓 `panelTimelineStrip` 长度 | 只移焦，步进 `min(10, length-1)` | 方向键顺便 `focusPanelOnCanvas` |
| N43 Arrow 相邻 / N42 Home/End 端点 | Page 才跳页 | 把 Arrow 改成 Page |

N43 已锁 roving Arrow/Home/End，并写明「条内 PageUp/PageDown 跳 10 格另刀」。无芯片焦点时 PageUp 画布级跳格另刀，不进 N44。

### N44 落地规格（N6–N43 之后，不在本轮实现）

`onCanvasKeydown` 无 PageUp/PageDown。

- **用户可见**：焦点已在条内芯片时，无修饰 PageUp 前跳 10 格、PageDown 后跳 10 格，不足则夹到首/末。空条忽略。不循环。Enter/Space 仍走原生 button。输入框/select/拖拽/无芯片焦点忽略（页面滚动保留）。⌘/Ctrl/Alt/Shift+Page 不触发。
- **复用**：N43 的 toolbar roving 索引；必须 `preventDefault`。Page**不**调用 `focusPanelOnCanvas`。
- **合同**：源码测试钉 `event.target` 为条内 button 且 `(key==="PageUp"\|\|key==="PageDown") && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且步进 10 夹端点；Arrow 仍属 N43；`[`/`]` 仍属 N41；无窗口。
- **不做**：改 T23；抢 N43 Arrow/Home/End / N42 画布 Home/End / N41 `[`/`]` / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N43。

24h-perf 不抢 View/Node/N6。

## P157 附录 · 2026-08-21 19:29 CST（连续两轮无自动项）

P156–P157 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N44 规格。

### 对标增量（相对 P155 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `panelTimelineStrip` + `focusPanelOnCanvas` | 画布级跳页才定位 | 芯片焦点下抢 N44 |
| N41 `[`/`]` 相邻循环 | Page 跳 10、夹端点不环绕 | 把 `[` 改成 Page |
| N44 芯片内 Page | 无芯片焦点才走本刀 | 输入框 Page 翻页 |

N44 已锁芯片焦点 Page，并写明「无芯片焦点时 PageUp 画布级跳格另刀」。侧栏单元轨 PageUp 另刀，不进 N45。

### N45 落地规格（N6–N44 之后，不在本轮实现）

`onCanvasKeydown` 无画布级 PageUp/PageDown。

- **用户可见**：非输入区、焦点不在条内芯片时，无修饰 PageUp 前跳 10 格、PageDown 后跳 10 格，不足夹到首/末，调用 `focusPanelOnCanvas`。空条忽略。不循环。输入框/select/拖拽/芯片焦点忽略。⌘/Ctrl/Alt/Shift+Page 不触发。
- **复用**：`panelTimelineStrip` 顺序；当前选中 panel 作游标，否则 PageDown 从 0、PageUp 从末项。必须 `preventDefault`。芯片焦点仍属 N44（只移焦、不定位）。
- **合同**：源码测试钉 `!editable && !chipButton && (key==="PageUp"\|\|key==="PageDown") && !metaKey && !ctrlKey && !altKey && !shiftKey` 在 `focusPanelOnCanvas` 之前且 `preventDefault` 且步进 10 夹端点；芯片 Page 仍属 N44；`[`/`]` 仍属 N41；无窗口。
- **不做**：改 T23；抢 N44 芯片 Page / N43 Arrow / N42 画布 Home/End / N41 `[`/`]` / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N44。

24h-perf 不抢 View/Node/N6。

## P159 附录 · 2026-08-21 19:32 CST（连续两轮无自动项）

P158–P159 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N45 规格。

### 对标增量（相对 P157 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `.unit-list .library-item` + `selectUnit` | 页内跳 ±10 夹端点 | 改分页 cursor / 新 SQL |
| N45 画布级 Page 跳格 | 单元轨焦点时优先本刀 | 单元 Page 去翻宫格 |
| `unitsNext`/`unitsPrevious` 分页钮 | 仍用鼠标/另刀键盘 | Page 直接翻页 |

N45 已锁无芯片焦点画布 Page，并写明「侧栏单元轨 PageUp 另刀」。分页钮键盘与单元轨 Arrow roving 另刀，不进 N46。

### N46 落地规格（N6–N45 之后，不在本轮实现）

单元轨 `.library-item` 仅 `@click="selectUnit"`，无 Page 键。`pin-button` 不参与。

- **用户可见**：焦点在 `.unit-list .library-item`（非钉选钮）时，无修饰 PageUp 前跳 10 条、PageDown 后跳 10 条，不足夹到本页首/末，调用 `selectUnit`。空页忽略。不循环。不调用 `unitsNext`/`unitsPrevious`。输入框/芯片/画布/拖拽忽略。⌘/Ctrl/Alt/Shift+Page 不触发。
- **复用**：当前 `unitsPage.page.items`；必须 `preventDefault`。落地时 N45 的 `!chipButton` 还须排除本选择器，避免单元轨 Page 被画布跳格抢走。
- **合同**：源码测试钉 `event.target.closest(".unit-list .library-item") && (key==="PageUp"\|\|key==="PageDown") && !metaKey && !ctrlKey && !altKey && !shiftKey` 在 `selectUnit` 之前且 `preventDefault` 且步进 10 夹端点；不出现 `unitsNext`；芯片/画布 Page 仍属 N44/N45；无窗口。
- **不做**：改 T23；抢 N45 画布 Page / N44 芯片 Page / N41 `[`/`]` / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N45。

24h-perf 不抢 View/Node/N6。

## P161 附录 · 2026-08-21 19:35 CST（连续两轮无自动项）

P160–P161 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N46 规格。

### 对标增量（相对 P159 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `unitsPrevious`/`unitsNext` 分页钮 | 只调现有 guarded 翻页 | 改 cursor SQL / 新投影 |
| N46 页内 Page 跳 10 条 | Alt+Page 才翻页 | 无修饰 Page 去翻页 |
| N20 Alt+Arrow 画布对齐 / N39 查询 Alt+Arrow | 键是 Page 不是 Arrow | 单元轨 Alt+Arrow 抢对齐 |

N46 已锁页内 Page，并写明「分页钮键盘另刀」。单元轨 Arrow roving 另刀，不进 N47。

### N47 落地规格（N6–N46 之后，不在本轮实现）

分页钮已 `@click` + `:disabled`，无快捷键。

- **用户可见**：焦点在 `.unit-list .library-item` 或 `managed-canvas-units-prev/next` 时，Alt+PageUp 上一页、Alt+PageDown 下一页，调用现有 `unitsPrevious`/`unitsNext`。无下一页/上一页或 `loading` 时忽略。无修饰 Page 仍属 N46。芯片/画布/查询框/拖拽忽略。⌘/Ctrl/Shift+Page 不触发。
- **复用**：现有 pager 与 `guarded("units")`；必须 `preventDefault`。翻页后焦点落到新页第一条 `.library-item`（没有则停在分页钮）。
- **合同**：源码测试钉 `unitListOrPager && altKey && (key==="PageUp"\|\|key==="PageDown") && !metaKey && !ctrlKey && !shiftKey` 在 `unitsPrevious`/`unitsNext` 之前且 `preventDefault`；无 `altKey` 的 Page 仍属 N46；N45 画布 Page 仍无 Alt；无窗口。
- **不做**：改 T23；抢 N46 页内 Page / N45 画布 Page / N44 芯片 Page / N20 Alt+Arrow / N39 查询 Alt+Arrow；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N46。

24h-perf 不抢 View/Node/N6。

## P163 附录 · 2026-08-21 19:38 CST（连续两轮无自动项）

P162–P163 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N47 规格。

### 对标增量（相对 P161 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Listbox roving tabindex（行为） | 页内只留一个 `tabindex=0` | 改成虚拟列表、新 SQL |
| 本仓 `.unit-list .library-item` 已 click `selectUnit` | 只改焦点/tabindex | 方向键顺便 `selectUnit` |
| N43 芯片 Arrow / N15 画布微移 | 单元轨焦点时停在列表 | 把 Arrow 绑成宫格/微移 |

N47 已锁 Alt+Page 翻页，并写明「单元轨 Arrow roving 另刀」。素材库 `.library-item` roving 另刀，不进 N48。

### N48 落地规格（N6–N47 之后，不在本轮实现）

单元轨 `.library-item` 全部默认可 Tab，无 `tabindex`。`pin-button` 不参与。

- **用户可见**：`.unit-list` 内仅当前 `.library-item` `tabindex="0"`，其余 `-1`。焦点已在某条时：无修饰 ArrowUp/ArrowDown 移焦相邻条（循环）；Home/End 移焦本页首/末。空页忽略。Enter/Space 仍走原生 button → `selectUnit`。钉选钮/芯片/画布/输入框/拖拽忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有 `.library-item` 与 `unitsPage.page.items`；必须 `preventDefault`（挡住 N15 微移）。方向键**不**调用 `selectUnit`。
- **合同**：源码测试钉单元轨 `.library-item` `tabindex` 0/-1；`event.target.closest(".unit-list .library-item") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N43；芯片 Arrow 仍属 N43；无窗口。
- **不做**：改 T23；抢 N43 芯片 Arrow / N15 微移 / N42 画布 Home/End / N47 Alt+Page / N46 页内 Page；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N47。

24h-perf 不抢 View/Node/N6。

## P165 附录 · 2026-08-21 19:40 CST（连续两轮无自动项）

P164–P165 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N48 规格。

### 对标增量（相对 P163 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Listbox roving（行为） | 可见行只留一个 `tabindex=0` | 拆虚拟窗口 / 改 T23 |
| 本仓 `visibleLibraryAssets` + `selectAsset` | 只改焦点 | Arrow 顺便 `selectAsset` |
| N48 单元轨 Arrow | 素材视口才走本刀 | 单元轨/芯片 Arrow 混用 |

N48 已锁单元轨 roving，并写明「素材库行 roving 另刀」。剧本/提示词 `.text-list` roving 另刀，不进 N49。

### N49 落地规格（N6–N48 之后，不在本轮实现）

素材 `.library-item` 全部默认可 Tab，无 `tabindex`。列表已虚拟化。

- **用户可见**：`[data-testid="managed-canvas-assets-virtual-viewport"]` 内仅当前 `.library-item` `tabindex="0"`，其余 `-1`。焦点已在某条时：无修饰 ArrowUp/ArrowDown 在 **当前可见窗口** 移焦相邻条（循环）；Home/End 移焦可见首/末。空窗忽略。Enter/Space 仍走原生 button → `selectAsset`。钉选钮/拖拽/单元轨/芯片/输入框忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`visibleLibraryAssets`；必须 `preventDefault`（挡住 N15）。方向键**不**调用 `selectAsset`，**不**滚动去加载窗口外项。
- **合同**：源码测试钉视口内 `.library-item` `tabindex` 0/-1；`event.target.closest("[data-testid=\\"managed-canvas-assets-virtual-viewport\\"] .library-item") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N48/N43；无窗口。
- **不做**：改 T23；抢 N48 单元轨 Arrow / N43 芯片 Arrow / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N48。

24h-perf 不抢 View/Node/N6。

## P167 附录 · 2026-08-21 19:43 CST（连续两轮无自动项）

P166–P167 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N49 规格。

### 对标增量（相对 P165 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Listbox roving（行为） | 列表只留一个 `tabindex=0` | 改成虚拟窗口 |
| 本仓 `.text-list .library-item` 已 click 设 selection | 只改焦点 | Arrow 顺便写 `selection` |
| N48/N49 单元/素材 Arrow | 剧本/提示词 tab 才走本刀 | 混用素材视口选择器 |

N49 已锁素材可见行 roving，并写明「剧本/提示词 `.text-list` roving 另刀」。媒体库 `.media-library-item` roving 另刀，不进 N50。

### N50 落地规格（N6–N49 之后，不在本轮实现）

`.text-list .library-item` 全部默认可 Tab，无 `tabindex`。`pin-button` 不参与。

- **用户可见**：`.text-list` 内仅当前 `.library-item` `tabindex="0"`，其余 `-1`。焦点已在某条时：无修饰 ArrowUp/ArrowDown 移焦相邻条（循环）；Home/End 移焦列表首/末。空列表忽略。Enter/Space 仍走原生 button 写 `selection`。钉选钮/素材视口/单元轨/芯片/输入框/拖拽忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`filteredTextDocuments`；必须 `preventDefault`（挡住 N15）。方向键**不**写 `selection`。
- **合同**：源码测试钉 `.text-list .library-item` `tabindex` 0/-1；`event.target.closest(".text-list .library-item") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N48/N49/N43；无窗口。
- **不做**：改 T23；抢 N49 素材 Arrow / N48 单元轨 Arrow / N43 芯片 Arrow / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N49。

24h-perf 不抢 View/Node/N6。

## P169 附录 · 2026-08-21 19:46 CST（连续两轮无自动项）

P168–P169 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N50 规格。

### 对标增量（相对 P167 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Listbox roving（行为） | 本页只留一个 `tabindex=0` | 把 div 改成 button 破坏拖拽 |
| 本仓 `.media-library-item` 仅 drag + `pin-button` | 只补焦点 | Arrow/Enter 顺便 `togglePinnedNode` |
| N50 剧本行 Arrow | 媒体 tab 才走本刀 | 混用 `.text-list` |

N50 已锁剧本/提示词 roving，并写明「媒体库 `.media-library-item` roving 另刀」。媒体分页 Alt+Page 另刀，不进 N51。

### N51 落地规格（N6–N50 之后，不在本轮实现）

`.media-library-item` 是不可聚焦的 `div`（无 `@click`）。钉选只走旁边 `pin-button`。

- **用户可见**：`.media-library-list` 内仅当前 `.media-library-item` `tabindex="0"`，其余 `-1`。焦点已在某条时：无修饰 ArrowUp/ArrowDown 移焦相邻条（循环）；Home/End 移焦本页首/末。空页忽略。Enter/Space **不**钉选。钉选钮/剧本行/素材视口/单元轨/芯片/输入框/拖拽忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`mediaPage.items`；必须 `preventDefault`（挡住 N15）。方向键**不**调用 `togglePinnedNode` / `onLibraryDragStart`。
- **合同**：源码测试钉 `.media-library-item` `tabindex` 0/-1；`event.target.closest(".media-library-item") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N48/N49/N50/N43；无窗口。
- **不做**：改 T23；抢 N50 剧本 Arrow / N49 素材 Arrow / N48 单元轨 Arrow / N43 芯片 Arrow / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N50。

24h-perf 不抢 View/Node/N6。

## P171 附录 · 2026-08-21 19:49 CST（连续两轮无自动项）

P170–P171 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N51 规格。

### 对标增量（相对 P169 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `mediaPrevious`/`mediaNext` 分页钮 | 只调现有 guarded 翻页 | 改 cursor SQL |
| N47 单元轨 Alt+Page | 媒体 tab 才走本刀 | 媒体 Alt+Page 去翻单元 |
| N51 媒体 Arrow | 无修饰 Arrow 仍只移焦 | 无修饰 Page 翻媒体页 |

N51 已锁媒体行 roving，并写明「媒体分页 Alt+Page 另刀」。素材库 `assetsPrevious`/`assetsNext` Alt+Page 另刀，不进 N52。

### N52 落地规格（N6–N51 之后，不在本轮实现）

媒体分页钮已 `@click` + `:disabled`，无快捷键。

- **用户可见**：焦点在 `.media-library-item` 或 `managed-canvas-media-prev/next` 时，Alt+PageUp 上一页、Alt+PageDown 下一页，调用现有 `mediaPrevious`/`mediaNext`。无下一页/上一页或 `loading` 时忽略。无修饰 Arrow 仍属 N51。单元轨/素材视口/芯片/查询框/拖拽忽略。⌘/Ctrl/Shift+Page 不触发。
- **复用**：现有 pager 与 `guarded("media")`；必须 `preventDefault`。翻页后焦点落到新页第一条 `.media-library-item`（没有则停在分页钮）。
- **合同**：源码测试钉 `mediaListOrPager && altKey && (key==="PageUp"\|\|key==="PageDown") && !metaKey && !ctrlKey && !shiftKey` 在 `mediaPrevious`/`mediaNext` 之前且 `preventDefault`；N47 仍只认单元轨；N45 画布 Page 仍无 Alt；无窗口。
- **不做**：改 T23；抢 N47 单元 Alt+Page / N51 媒体 Arrow / N45 画布 Page / N20 Alt+Arrow；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N51。

24h-perf 不抢 View/Node/N6。

## P173 附录 · 2026-08-21 19:54 CST（连续两轮无自动项）

P172–P173 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N52 规格。

### 对标增量（相对 P171 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `assetsPrevious`/`assetsNext` 分页钮 | 只调现有 guarded 翻页 | 改 cursor SQL |
| N47 单元轨 Alt+Page | 素材视口才走本刀 | 素材 Alt+Page 去翻单元 |
| N52 媒体 Alt+Page | 素材 tab 才走本刀 | 素材 Alt+Page 去翻媒体 |
| N49 素材 Arrow | 无修饰 Arrow 仍只移焦 | 无修饰 Page 翻素材页 |

N52 已锁媒体分页 Alt+Page，并写明「素材库 `assetsPrevious`/`assetsNext` Alt+Page 另刀」。全局资源 `globalResourcesPrevious`/`globalResourcesNext` 与检查器 appearances 分页 Alt+Page 另刀，不进 N53。

### N53 落地规格（N6–N52 之后，不在本轮实现）

素材分页钮已 `@click` + `:disabled`，无快捷键。

- **用户可见**：焦点在 `managed-canvas-assets-virtual-viewport .library-item` 或 `managed-canvas-assets-prev/next` 时，Alt+PageUp 上一页、Alt+PageDown 下一页，调用现有 `assetsPrevious`/`assetsNext`。无下一页/上一页或 `loading` 时忽略。无修饰 Arrow 仍属 N49。单元轨/媒体 tab/芯片/查询框/拖拽忽略。⌘/Ctrl/Shift+Page 不触发。
- **复用**：现有 pager 与 `guarded("assets")`；必须 `preventDefault`。翻页后焦点落到新页第一条可见 `.library-item`（没有则停在分页钮）。
- **合同**：源码测试钉 `assetListOrPager && altKey && (key==="PageUp"\|\|key==="PageDown") && !metaKey && !ctrlKey && !shiftKey` 在 `assetsPrevious`/`assetsNext` 之前且 `preventDefault`；N47 仍只认单元轨；N52 仍只认媒体库；N45 画布 Page 仍无 Alt；无窗口。
- **不做**：改 T23；抢 N47 单元 Alt+Page / N52 媒体 Alt+Page / N49 素材 Arrow / N45 画布 Page / N20 Alt+Arrow；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N52。

24h-perf 不抢 View/Node/N6。

## P175 附录 · 2026-08-21 19:57 CST（连续两轮无自动项）

P174–P175 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N53 规格。

### 对标增量（相对 P173 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `globalResourcesPrevious`/`globalResourcesNext` 分页钮 | 只调现有翻页 | 改 cursor SQL / 写入当前工程 |
| N47/N52/N53 侧栏 Alt+Page | 全局资源侧栏才走本刀 | 去翻单元/媒体/素材 |
| N31 Shift+L | 仍只开合侧栏 | Alt+Page 开合侧栏 |

N53 已锁素材分页 Alt+Page，并写明「全局资源 pager 与 appearances 分页 Alt+Page 另刀」。检查器 appearances `appearancesPrevious`/`appearancesNext` Alt+Page 另刀，不进 N54。全局资源卡 Arrow roving 另刀，不进 N54。

### N54 落地规格（N6–N53 之后，不在本轮实现）

全局资源分页钮已 `@click` + `:disabled`，无快捷键。列表只读，不钉选。

- **用户可见**：焦点在 `.global-resource-card` 或 `managed-canvas-global-resources-prev/next` 时，Alt+PageUp 上一页、Alt+PageDown 下一页，调用现有 `globalResourcesPrevious`/`globalResourcesNext`。无下一页/上一页或 `globalResourceLoading` 时忽略。单元轨/素材视口/媒体 tab/芯片/查询框/拖拽忽略。⌘/Ctrl/Shift+Page 不触发。
- **复用**：现有 pager 与 `loadGlobalResources`；必须 `preventDefault`。翻页后焦点落到新页第一条 `.global-resource-card`（没有则停在分页钮）。
- **合同**：源码测试钉 `globalResourceListOrPager && altKey && (key==="PageUp"\|\|key==="PageDown") && !metaKey && !ctrlKey && !shiftKey` 在 `globalResourcesPrevious`/`globalResourcesNext` 之前且 `preventDefault`；N47/N52/N53 仍只认各自侧栏；N45 画布 Page 仍无 Alt；无窗口。
- **不做**：改 T23；抢 N47 单元 Alt+Page / N52 媒体 Alt+Page / N53 素材 Alt+Page / N45 画布 Page / N20 Alt+Arrow / N31 Shift+L；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N53。

24h-perf 不抢 View/Node/N6。

## P177 附录 · 2026-08-21 20:00 CST（连续两轮无自动项）

P176–P177 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N54 规格。

### 对标增量（相对 P175 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| 本仓 `appearancesPrevious`/`appearancesNext` 分页钮 | 只调现有 guarded 翻页 | 改 cursor SQL |
| N47/N52/N53/N54 侧栏 Alt+Page | 检查器出场 pager 才走本刀 | 去翻单元/媒体/素材/全局资源 |
| `focusAppearance` 行 click | 仍只点行定位宫格 | Alt+Page 顺便 focusAppearance |

N54 已锁全局资源 Alt+Page，并写明「appearances 分页 Alt+Page 另刀」。出场行 Arrow roving 与全局资源卡 Arrow 另刀，不进 N55。

### N55 落地规格（N6–N54 之后，不在本轮实现）

检查器出场分页钮已 `@click` + `:disabled`，无快捷键。仅 `selection.kind === "asset"` 时 pager 存在。

- **用户可见**：焦点在 `.appearance-list button` 或 `managed-canvas-appearances-prev/next` 时，Alt+PageUp 上一页、Alt+PageDown 下一页，调用现有 `appearancesPrevious`/`appearancesNext`。无下一页/上一页、`loading`、或非资产选中时忽略。侧栏/芯片/查询框/拖拽/画布忽略。⌘/Ctrl/Shift+Page 不触发。
- **复用**：现有 pager 与 `guarded("appearances:"+assetId)`；必须 `preventDefault`。翻页后焦点落到新页第一条 `.appearance-list button`（没有则停在分页钮）。不调用 `focusAppearance`。
- **合同**：源码测试钉 `appearanceListOrPager && altKey && (key==="PageUp"\|\|key==="PageDown") && !metaKey && !ctrlKey && !shiftKey` 在 `appearancesPrevious`/`appearancesNext` 之前且 `preventDefault`；N47/N52/N53/N54 仍只认各自侧栏；N45 画布 Page 仍无 Alt；无窗口。
- **不做**：改 T23；抢 N47 单元 Alt+Page / N52 媒体 Alt+Page / N53 素材 Alt+Page / N54 全局资源 Alt+Page / N45 画布 Page / N20 Alt+Arrow；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N54。

24h-perf 不抢 View/Node/Inspector/N6。

## P179 附录 · 2026-08-21 20:03 CST（连续两轮无自动项）

P178–P179 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N55 规格。

### 对标增量（相对 P177 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Listbox roving tabindex（行为） | 页内只留一个 `tabindex=0` | 改成虚拟列表、新 SQL |
| 本仓 `.appearance-list button` 已 click `focusAppearance` | 只改焦点/tabindex | 方向键顺便 `focusAppearance` |
| N48–N51 侧栏 Arrow | 检查器出场行才走本刀 | 混用 `.library-item` |
| N55 出场 Alt+Page | 无修饰 Arrow 仍只移焦 | 无修饰 Page 翻出场页 |

N55 已锁出场分页 Alt+Page，并写明「出场行 Arrow roving 另刀」。全局资源卡 Arrow 另刀，不进 N56。

### N56 落地规格（N6–N55 之后，不在本轮实现）

`.appearance-list button` 目前全部默认可 Tab，无 roving。仅资产检查器有此列表。

- **用户可见**：`.appearance-list` 内仅当前行按钮 `tabindex="0"`，其余 `-1`。焦点已在某条时：无修饰 ArrowUp/ArrowDown 移焦相邻条（循环）；Home/End 移焦本页首/末。空列表忽略。Enter/Space 仍走现有 `focusAppearance`。分页钮/侧栏/芯片/输入框/拖拽/画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`appearancesPage.page.items`；必须 `preventDefault`（挡住 N15）。方向键**不**调用 `focusAppearance` / `appearancesPrevious` / `appearancesNext`。
- **合同**：源码测试钉 `.appearance-list button` `tabindex` 0/-1；`event.target.closest(".appearance-list button") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N48/N49/N50/N51/N43；N55 仍只认 Alt+Page；无窗口。
- **不做**：改 T23；抢 N55 出场 Alt+Page / N51 媒体 Arrow / N50 剧本 Arrow / N49 素材 Arrow / N48 单元轨 Arrow / N43 芯片 Arrow / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N55。

24h-perf 不抢 View/Node/Inspector/N6。

## P181 附录 · 2026-08-21 20:06 CST（连续两轮无自动项）

P180–P181 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N56 规格。

### 对标增量（相对 P179 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Listbox roving tabindex（行为） | 页内只留一个 `tabindex=0` | 改成虚拟列表、新 SQL |
| 本仓 `.global-resource-card` 只读 `li` | 只补焦点 | Enter 写入当前工程 / 钉选 |
| N48–N51 / N56 Arrow | 全局资源视口才走本刀 | 混用 `.library-item` / `.appearance-list` |
| N54 全局资源 Alt+Page | 无修饰 Arrow 仍只移焦 | 无修饰 Page 翻全局资源页 |

N56 已锁出场行 Arrow，并写明「全局资源卡 Arrow 另刀」。节点操作钮 `.node-action-buttons button` Arrow 另刀，不进 N57。

### N57 落地规格（N6–N56 之后，不在本轮实现）

`.global-resource-card` 是不可聚焦的 `li`（无 `@click`）。列表只读，不钉选。

- **用户可见**：`.global-resource-list` 内仅当前 `.global-resource-card` `tabindex="0"`，其余 `-1`。焦点已在某条时：无修饰 ArrowUp/ArrowDown 移焦相邻卡（循环）；Home/End 移焦本页首/末。空页忽略。Enter/Space **不**写入、不钉选。分页钮/出场行/素材视口/单元轨/芯片/输入框/拖拽忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`globalResourcePage.items`；必须 `preventDefault`（挡住 N15）。方向键**不**调用 `openResourceCenter` / `globalResourcesPrevious` / `globalResourcesNext`。
- **合同**：源码测试钉 `.global-resource-card` `tabindex` 0/-1；`event.target.closest(".global-resource-card") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N48/N49/N50/N51/N56/N43；N54 仍只认 Alt+Page；无窗口。
- **不做**：改 T23；抢 N54 全局资源 Alt+Page / N56 出场 Arrow / N51 媒体 Arrow / N50 剧本 Arrow / N49 素材 Arrow / N48 单元轨 Arrow / N43 芯片 Arrow / N15 微移 / N31 Shift+L；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N56。

24h-perf 不抢 View/Node/Inspector/N6。

## P183 附录 · 2026-08-21 20:09 CST（连续两轮无自动项）

P182–P183 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N57 规格。

### 对标增量（相对 P181 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Toolbar/Listbox roving（行为） | 组内只留一个 `tabindex=0` | 改 action 语义 / 新 SQL |
| 本仓 `.node-action-buttons` 纵向 grid + click `runNodeAction` | 只改焦点/tabindex | 方向键顺便 `runNodeAction` |
| N56/N57 列表 Arrow | 检查器操作钮才走本刀 | 混用出场行/全局资源卡 |
| `selectedNodeBusy` / `:disabled` | 跳过不可用钮 | busy 时 Arrow 仍发 IPC |

N57 已锁全局资源卡 Arrow，并写明「节点操作钮 Arrow 另刀」。素材库 `.library-tabs button` Arrow 另刀，不进 N58。

### N58 落地规格（N6–N57 之后，不在本轮实现）

`.node-action-buttons button` 目前全部默认可 Tab，无 roving。纵向 `display:grid`。`!action.enabled` 时 `:disabled`。

- **用户可见**：`.node-action-buttons` 内仅当前可用钮 `tabindex="0"`，其余 `-1`。焦点已在某钮时：无修饰 ArrowUp/ArrowDown 移焦相邻**可用**钮（循环，跳过 disabled）；Home/End 移焦首/末可用钮。无可用钮或面板未开忽略。Enter/Space 仍走现有 `runNodeAction`（disabled 不发）。出场行/全局资源/侧栏/芯片/输入框/拖拽/画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`nodeActionPanel.actions`；必须 `preventDefault`（挡住 N15）。方向键**不**调用 `runNodeAction`。
- **合同**：源码测试钉 `.node-action-buttons button` `tabindex` 0/-1；`event.target.closest(".node-action-buttons button") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N56/N57/N48–N51/N43；disabled 钮不获 `tabindex=0`；无窗口。
- **不做**：改 T23；抢 N57 全局资源 Arrow / N56 出场 Arrow / N51–N48 / N43 / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N57。

24h-perf 不抢 View/Node/Inspector/N6。

## P185 附录 · 2026-08-21 20:12 CST（连续两轮无自动项）

P184–P185 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N58 规格。

### 对标增量（相对 P183 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Tabs 手动激活 + roving | 组内只留一个 `tabindex=0` | Arrow 立刻 `openLibraryFor` 打请求 |
| 本仓 `#managed-canvas-library .library-tabs` 3 列 grid | 只改焦点/tabindex | 改 tab 集合 / SQL |
| N58 节点操作 Arrow | 当前素材库 tabs 才走本刀 | 混用检查器操作钮 |
| N30 L / N31 Shift+L | 仍只开合侧栏 | Arrow 开合侧栏 |

N58 已锁节点操作钮 Arrow，并写明「素材库 `.library-tabs button` Arrow 另刀」。全局资源 `.global-resource-tabs button` Arrow 另刀，不进 N59。

### N59 落地规格（N6–N58 之后，不在本轮实现）

`#managed-canvas-library .library-tabs button` 目前全部默认可 Tab，无 roving。3 列 grid。`@click="openLibraryFor"`。

- **用户可见**：当前工程素材库 tabs 内仅当前钮 `tabindex="0"`，其余 `-1`。焦点已在某 tab 时：无修饰 ArrowLeft/ArrowRight 移焦相邻 tab（循环，按 DOM 序）；Home/End 移焦首/末。侧栏未开忽略。Enter/Space 仍走现有 `openLibraryFor`。方向键**不**切换 `libraryTab`。全局资源 tabs/检查器操作钮/出场行/芯片/查询框/拖拽/画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`libraryTabs`；必须 `preventDefault`（挡住 N15）。方向键**不**调用 `openLibraryFor` / `openGlobalResourcesFor`。
- **合同**：源码测试钉 `#managed-canvas-library .library-tabs button` `tabindex` 0/-1；`event.target.closest("#managed-canvas-library .library-tabs button") && (ArrowLeft|ArrowRight|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N58/N57/N56/N48–N51/N43；`.global-resource-tabs` 不认本刀；无窗口。
- **不做**：改 T23；抢 N58 节点操作 Arrow / N57 全局资源卡 / N31 Shift+L / N30 L / N29 A / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N58。

24h-perf 不抢 View/Node/Inspector/N6。

## P187 附录 · 2026-08-21 20:15 CST（连续两轮无自动项）

P186–P187 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N59 规格。

### 对标增量（相对 P185 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Tabs 手动激活 + roving | 组内只留一个 `tabindex=0` | Arrow 立刻 `openGlobalResourcesFor` 打请求 |
| 本仓 `.global-resource-tabs` 复用 3 列 grid | 只改焦点/tabindex | 改分类集合 / SQL |
| N59 当前素材库 tabs | 全局资源侧栏才走本刀 | 混用 `#managed-canvas-library .library-tabs` |
| N31 Shift+L | 仍只开合侧栏 | Arrow 开合侧栏 |
| `globalResourceLoading` `:disabled` | 跳过不可用 tab | loading 时 Arrow 仍切类打请求 |

N59 已锁当前素材库 tabs，并写明「全局资源 `.global-resource-tabs` Arrow 另刀」。添加菜单 `.add-menu button` Arrow 另刀，不进 N60。

### N60 落地规格（N6–N59 之后，不在本轮实现）

`.global-resource-tabs button` 目前全部默认可 Tab，无 roving。`globalResourceLoading` 时 `:disabled`。`@click="openGlobalResourcesFor"`。

- **用户可见**：全局资源 tabs 内仅当前**可用**钮 `tabindex="0"`，其余 `-1`。焦点已在某 tab 时：无修饰 ArrowLeft/ArrowRight 移焦相邻**可用** tab（循环，按 DOM 序）；Home/End 移焦首/末可用。侧栏未开或 loading 全 disabled 时忽略。Enter/Space 仍走现有 `openGlobalResourcesFor`（disabled 不发）。方向键**不**切换 `globalResourceCategory`。当前素材库 tabs/全局资源卡/检查器/芯片/查询框/拖拽/画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`globalResourceCategories`；必须 `preventDefault`（挡住 N15）。方向键**不**调用 `openGlobalResourcesFor` / `openLibraryFor`。
- **合同**：源码测试钉 `.global-resource-tabs button` `tabindex` 0/-1；`event.target.closest(".global-resource-tabs button") && (ArrowLeft|ArrowRight|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N59/N58/N57/N54/N48–N51/N43；disabled 钮不获 `tabindex=0`；`#managed-canvas-library .library-tabs` 仍属 N59；无窗口。
- **不做**：改 T23；抢 N59 素材库 tabs / N57 全局资源卡 / N54 全局资源 Alt+Page / N31 Shift+L / N30 L / N29 A / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N59。

24h-perf 不抢 View/Node/Inspector/N6。

## P189 附录 · 2026-08-21 20:18 CST（连续两轮无自动项）

P188–P189 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N60 规格。

### 对标增量（相对 P187 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Menu + roving | 打开后组内只留一个 `tabindex=0` | 改成弹窗 / 新节点类型 |
| 本仓 `#managed-canvas-add-menu` 2 列 grid + `chooseAddKind` | 只改焦点/tabindex | Arrow 顺便 `chooseAddKind` |
| N29 A | 仍只开合菜单 | Arrow 开合菜单 |
| N18 Escape | 仍关弹层 | Arrow 关菜单 |
| N59/N60 tabs | 添加菜单才走本刀 | 混用侧栏 tabs |

N60 已锁全局资源 tabs，并写明「添加菜单 `.add-menu button` Arrow 另刀」。浮动工具栏 `.floating-tools > button` Arrow 另刀，不进 N61。

### N61 落地规格（N6–N60 之后，不在本轮实现）

`#managed-canvas-add-menu button` 目前全部默认可 Tab，无 roving。2 列 grid。仅 `addMenuOpen` 时存在。`@click="chooseAddKind"`。

- **用户可见**：添加菜单打开时，菜单内仅当前项 `tabindex="0"`，其余 `-1`。焦点已在某项时：无修饰 ArrowUp/Down/Left/Right 按 DOM 序移焦相邻项（循环）；Home/End 移焦首/末。菜单未开忽略。Enter/Space 仍走现有 `chooseAddKind`。方向键**不**开合菜单、不调用 `chooseAddKind`。触发钮 `managed-canvas-add-node`、侧栏 tabs、检查器、芯片、画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`libraryTabs` + 现有 `chooseAddKind`；必须 `preventDefault`（挡住 N15）。打开后焦点落到第一项。
- **合同**：源码测试钉 `#managed-canvas-add-menu button` `tabindex` 0/-1；`event.target.closest("#managed-canvas-add-menu button") && (ArrowUp|ArrowDown|ArrowLeft|ArrowRight|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N29/N59/N60/N58/N43；N29 仍只认 A；N18 仍关弹层；无窗口。
- **不做**：改 T23；抢 N29 A / N18 Escape / N59 素材库 tabs / N60 全局资源 tabs / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N60。

24h-perf 不抢 View/Node/Inspector/N6。

## P191 附录 · 2026-08-21 20:22 CST（连续两轮无自动项）

P190–P191 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N61 规格。

### 对标增量（相对 P189 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Toolbar 纵向 roving | 组内只留一个 `tabindex=0` | 改工具语义 / 新快捷键 |
| 本仓 `.floating-tools` 纵向 grid | 只改焦点/tabindex | Arrow 顺便 toggleLibrary / toggleConnectMode |
| N61 添加菜单 | 顶层触发钮才走本刀 | 混用 `#managed-canvas-add-menu button` |
| N29 A / N30 L / N31 Shift+L / N27 C / N28 F1 | 仍只各自快捷键 | Arrow 当快捷键 |

N61 已锁添加菜单 Arrow，并写明「浮动工具栏 `.floating-tools > button` Arrow 另刀」。底部 `.bottom-tools` / `.align-tools` Arrow 另刀，不进 N62。

### N62 落地规格（N6–N61 之后，不在本轮实现）

顶层工具钮：`.floating-tools > .add-menu-wrap > button`（添加）与 `.floating-tools > button`（素材库/剧本资源/连线/帮助）。目前全部默认可 Tab，无 roving。纵向 `display:grid`。

- **用户可见**：工具栏内仅当前顶层钮 `tabindex="0"`，其余 `-1`。焦点已在某钮时：无修饰 ArrowUp/ArrowDown 移焦相邻顶层钮（循环，DOM 序：添加→素材库→剧本资源→连线→帮助）；Home/End 移焦首/末。Enter/Space 仍走现有 click（开合添加/素材库/剧本资源/连线/帮助）。方向键**不**调用这些 handler。`#managed-canvas-add-menu` 内项、底部视图工具、检查器、芯片、画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有五个 click handler；必须 `preventDefault`（挡住 N15）。
- **合同**：源码测试钉 `floatingToolbarButton && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N61/N29/N30/N31/N27/N28/N43；`#managed-canvas-add-menu button` 仍属 N61；无窗口。
- **不做**：改 T23；抢 N61 添加菜单 / N29 A / N30 L / N31 Shift+L / N27 C / N28 F1 / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N61。

24h-perf 不抢 View/Node/Inspector/N6。

## P193 附录 · 2026-08-21 20:28 CST（连续两轮无自动项）

P192–P193 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N62 规格。

### 对标增量（相对 P191 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Toolbar 横向 roving | 组内只留一个 `tabindex=0` | 改底栏语义 / 新快捷键 |
| 本仓 `.bottom-tools` 横向 flex-wrap | 只改焦点/tabindex | Arrow 顺便 fitCanvas / applyAlign / toggleEdges |
| N62 浮动工具栏 | 底栏顶层钮才走本刀 | 混用 `.floating-tools` |
| N20 Alt+Arrow / N21 Alt+H/V / N9 / N22 Shift+E / N25 Shift+T | 仍只各自快捷键 | Arrow 当对齐/适配/连线/时间线 |

N62 已锁浮动工具栏 Arrow，并写明「底部 `.bottom-tools` / `.align-tools` Arrow 另刀」。`.view-menu` 弹出钮 / Vue Flow `Controls` Arrow 另刀，不进 N63。

### N63 落地规格（N6–N62 之后，不在本轮实现）

底栏可见钮：`.bottom-tools > button`（适配/撤销/重做/显隐连线/时间线/强制时间线，以及条件出现的删线/清空）与 `.bottom-tools .align-tools button`（`selectionCount>=2` 才挂）。目前全部默认可 Tab，无 roving。横向 `display:flex; flex-wrap:wrap`。撤销/重做/时间线等可 `:disabled`。

- **用户可见**：底栏内仅当前**可用**钮 `tabindex="0"`，其余 `-1`。焦点已在某钮时：无修饰 ArrowLeft/ArrowRight 移焦相邻**可用**钮（循环，DOM 序）；Home/End 移焦首/末可用。Enter/Space 仍走现有 click。方向键**不**调用 `fitCanvas` / `undoLayout` / `redoLayout` / `applyAlign` / `applyDistribute` / `toggleEdges` / `applyTimelineLayout` / `deleteSelectedDraftEdge` / `clearWorkflowCanvas`。disabled 钮不获 `tabindex=0`、不进循环。`.view-menu` summary/弹出、Vue Flow Controls、浮动工具栏、检查器、芯片、画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有 click handler；必须 `preventDefault`（挡住 N15）。
- **合同**：源码测试钉 `bottomToolbarButton && (ArrowLeft|ArrowRight|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N62/N20/N21/N9/N22/N25/N43；`.floating-tools` 仍属 N62；`.view-menu-pop button` 不认本刀；无窗口。
- **不做**：改 T23；抢 N62 浮动工具栏 / N20 Alt 对齐 / N21 居中均分 / N9 fitCanvas / N22 Shift+E / N25 Shift+T / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N62。

24h-perf 不抢 View/Node/Inspector/N6。

## P195 附录 · 2026-08-21 20:31 CST（连续两轮无自动项）

P194–P195 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N63 规格。

### 对标增量（相对 P193 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Menu 纵向 roving | 组内只留一个 `tabindex=0` | 改菜单语义 / 新快捷键 |
| 本仓 `.view-menu-pop` 纵向 grid + `role="menu"` | 只改焦点/tabindex | Arrow 顺便 toggleMiniMap / refreshAll |
| N63 底栏 | 菜单打开后的项才走本刀 | 混用 `.bottom-tools > button` |
| N23 Shift+M / N24 Shift+W / N26 F5 / N32 F6 | 仍只各自快捷键 | Arrow 当快捷键 |

N63 已锁底栏 Arrow，并写明「`.view-menu` 弹出钮 / Vue Flow Controls Arrow 另刀」。主题 radio / Vue Flow `Controls` Arrow 另刀，不进 N64。

### N64 落地规格（N6–N63 之后，不在本轮实现）

`.view-menu-pop > button`：小地图、工作流过滤、刷新、核对外部来源。目前全部默认可 Tab，无 roving。纵向 `display:grid`。仅 `details.view-menu` 打开时存在。刷新/核对可 `:disabled`。

- **用户可见**：菜单打开时，菜单内仅当前**可用**项 `tabindex="0"`，其余 `-1`。焦点已在某项时：无修饰 ArrowUp/ArrowDown 移焦相邻**可用**项（循环，DOM 序：小地图→工作流→刷新→核对外部来源）；Home/End 移焦首/末可用。菜单未开忽略。Enter/Space 仍走现有 click。方向键**不**调用 `showMiniMap` 赋值 / `toggleWorkspaceMode` / `refreshAll` / `verifyLocalProductionSource`。disabled 项不获 `tabindex=0`、不进循环。`.view-menu-theme` radio、`summary`、底栏、浮动工具栏、Vue Flow Controls、检查器、芯片、画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有 click handler；必须 `preventDefault`（挡住 N15）。打开后焦点落到第一可用项。
- **合同**：源码测试钉 `event.target.closest(".view-menu-pop > button") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N63/N23/N24/N26/N32/N43；`.bottom-tools > button` 仍属 N63；`.view-menu-theme button` 不认本刀；无窗口。
- **不做**：改 T23；抢 N63 底栏 / N23 Shift+M / N24 Shift+W / N26 F5 / N32 F6 / N33 Shift+D / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N63。

24h-perf 不抢 View/Node/Inspector/N6。

## P197 附录 · 2026-08-21 20:34 CST（连续两轮无自动项）

P196–P197 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N64 规格。

### 对标增量（相对 P195 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Radio Group 手动激活 + roving | 组内只留一个 `tabindex=0` | Arrow 立刻 `setCanvasTheme` |
| 本仓 `.view-menu-theme` 横向 flex + `role="radiogroup"` | 只改焦点/tabindex | 改主题集合 / 存储路径 |
| N64 视图菜单项 | 主题 radio 才走本刀 | 混用 `.view-menu-pop > button` |
| N33 Shift+D | 仍只循环主题 | Arrow 当快捷键 |

N64 已锁菜单项 Arrow，并写明「主题 radio / Vue Flow Controls Arrow 另刀」。Vue Flow `Controls` Arrow 另刀，不进 N65。

### N65 落地规格（N6–N64 之后，不在本轮实现）

`.view-menu-theme > button[role="radio"]`：浅色 / 深色 / 米色（`MANAGED_CANVAS_THEMES` DOM 序 light→dark→paper）。目前全部默认可 Tab，无 roving。横向 `display:flex`。仅 `details.view-menu` 打开时存在。`@click="setCanvasTheme(theme.id)"`。

- **用户可见**：菜单打开时，主题组内仅当前 radio `tabindex="0"`，其余 `-1`。焦点已在某 radio 时：无修饰 ArrowLeft/ArrowRight 移焦相邻项（循环）；Home/End 移焦首/末。菜单未开忽略。Enter/Space 仍走现有 `setCanvasTheme`。方向键**不**调用 `setCanvasTheme`、不改 `canvasTheme`。`.view-menu-pop > button`、底栏、浮动工具栏、Vue Flow Controls、检查器、芯片、画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：`MANAGED_CANVAS_THEMES`；必须 `preventDefault`（挡住 N15）。
- **合同**：源码测试钉 `event.target.closest(".view-menu-theme > button") && (ArrowLeft|ArrowRight|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N64/N33/N63/N43；`.view-menu-pop > button` 仍属 N64；无窗口。
- **不做**：改 T23；抢 N64 菜单项 / N33 Shift+D / N63 底栏 / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N64。

24h-perf 不抢 View/Node/Inspector/N6。

## P199 附录 · 2026-08-21 20:37 CST（连续两轮无自动项）

P198–P199 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N65 规格。

### 对标增量（相对 P197 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Toolbar 纵向 roving | 组内只留一个 `tabindex=0` | 改 Vue Flow Controls 语义 / fork 库源 |
| 本仓 `#managed-studio-flow .vue-flow__controls` 底左纵向栈 | 只改焦点/tabindex | Arrow 顺便 zoom / fitView / 切交互锁 |
| N65 主题 radio | Controls 才走本刀 | 混用 `.view-menu-theme` |
| N9 / N10 / N11 / N63 适配 | 仍只各自快捷键/底栏 | Arrow 当视口命令 |

N65 已锁主题 radio，并写明「Vue Flow Controls Arrow 另刀」。StoryWorkbench / `App.vue` 遗留 Controls Arrow 另刀，不进 N66。

### N66 落地规格（N6–N65 之后，不在本轮实现）

`#managed-studio-flow .vue-flow__controls-button`：放大、缩小、适配全部、锁定/解锁交互（DOM 类 `controls-zoomin` / `controls-zoomout` / `controls-fitview` / 交互锁钮）。目前全部默认可 Tab，无 roving。Vue Flow 默认纵向栈。`@zoom-in/@zoom-out/@fit-view` 仅回调 `onControlViewportChanged`。

- **用户可见**：Controls 内仅当前钮 `tabindex="0"`，其余 `-1`。焦点已在某钮时：无修饰 ArrowUp/ArrowDown 移焦相邻钮（循环，DOM 序）；Home/End 移焦首/末。Enter/Space 仍走现有 click。方向键**不**缩放、不适配、不切交互锁、不调用 `onControlViewportChanged`。底栏适配、视图菜单、主题 radio、浮动工具栏、检查器、芯片、画布、故事图 Controls、遗留画布 Controls 忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有 Vue Flow Controls click；必须 `preventDefault`（挡住 N15）。不 fork `@vue-flow/controls`。
- **合同**：源码测试钉 `event.target.closest("#managed-studio-flow .vue-flow__controls-button") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N15/N9/N10/N11/N63/N65/N43；`.view-menu-theme button` 仍属 N65；`.bottom-tools`「适配」仍属 N63；无窗口。
- **不做**：改 T23；抢 N9–N11 视口 / N63 底栏适配 / N65 主题 radio / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N12 说明 / N13–N65；改 StoryWorkbench / App 遗留 Controls。

24h-perf 不抢 View/Node/Inspector/N6。

## P201 附录 · 2026-08-21 20:40 CST（连续两轮无自动项）

P200–P201 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N66 规格。

### 对标增量（相对 P199 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Toolbar 纵向 roving | 组内只留一个 `tabindex=0` | 改 Vue Flow Controls 语义 / fork 库源 |
| 本仓 `[data-testid="story-graph-connect"] .vue-flow__controls` 底左纵向栈 | 只改焦点/tabindex | Arrow 顺便 zoom / fitView / 切交互锁 |
| N66 受管画布 Controls | 故事图才走本刀 | 混用 `#managed-studio-flow` |
| App 遗留 Controls | 另刀 | 本刀改 `App.vue` |

N66 已锁受管画布 Controls，并写明「StoryWorkbench / App 遗留 Controls Arrow 另刀」。`App.vue` `LegacyVueFlowControls` Arrow 另刀，不进 N67。

### N67 落地规格（N6–N66 之后，不在本轮实现）

`[data-testid="story-graph-connect"] .vue-flow__controls-button`：放大事件图、缩小、适配全部事件、锁定/解锁交互。目前全部默认可 Tab，无 roving。Vue Flow 默认纵向栈。仅事件图挂载时存在（`events.length` 且非阅读器主栏）。

- **用户可见**：故事图 Controls 内仅当前钮 `tabindex="0"`，其余 `-1`。焦点已在某钮时：无修饰 ArrowUp/ArrowDown 移焦相邻钮（循环，DOM 序）；Home/End 移焦首/末。图未挂载忽略。Enter/Space 仍走现有 click。方向键**不**缩放、不适配、不切交互锁。受管画布 Controls、遗留画布 Controls、底栏、检查器、芯片、画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有 Vue Flow Controls click；必须 `preventDefault`。不 fork `@vue-flow/controls`。不改 `connecting` 连线语义。
- **合同**：源码测试钉 `event.target.closest("[data-testid='story-graph-connect'] .vue-flow__controls-button") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N66/N15/N9/N43；`#managed-studio-flow .vue-flow__controls-button` 仍属 N66；无窗口。
- **不做**：改 T23；抢 N66 受管 Controls / N9–N11 视口 / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N12 说明 / N13–N66；改 `App.vue` 遗留 Controls / 事件写入 handler。

24h-perf 不抢 View/Node/Inspector/N6。

## P203 附录 · 2026-08-21 20:43 CST（连续两轮无自动项）

P202–P203 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N67 规格。

### 对标增量（相对 P201 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Toolbar 纵向 roving | 组内只留一个 `tabindex=0` | 改 Vue Flow Controls 语义 / fork 库源 |
| 本仓 `#production-flow` + `LegacyVueFlowControls` 底左纵向栈 | 只改焦点/tabindex | Arrow 顺便 zoom / fitView / 切交互锁 |
| N66 / N67 | 遗留生产画布才走本刀 | 混用 `#managed-studio-flow` / `story-graph-connect` |
| App 遗留 `group` 节点 | 不动 | 当 N6 实现 |

N67 已锁故事图 Controls，并写明「`App.vue` `LegacyVueFlowControls` Arrow 另刀」。受管 MiniMap 键盘另刀，不进 N68。

### N68 落地规格（N6–N67 之后，不在本轮实现）

`#production-flow .vue-flow__controls-button`：放大、缩小、适配、锁定/解锁交互。目前全部默认可 Tab，无 roving。Vue Flow 默认纵向栈。仅遗留生产画布挂载时存在（`LegacyVueFlow` `v-else`，非 loading 空屏）。

- **用户可见**：遗留 Controls 内仅当前钮 `tabindex="0"`，其余 `-1`。焦点已在某钮时：无修饰 ArrowUp/ArrowDown 移焦相邻钮（循环，DOM 序）；Home/End 移焦首/末。画布未挂载忽略。Enter/Space 仍走现有 click。方向键**不**缩放、不适配、不切交互锁。受管 Controls、故事图 Controls、检查器、芯片、画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有 `LegacyVueFlowControls` click；必须 `preventDefault`。不 fork `@vue-flow/controls`。不改 `createCanvasEntity('group')` / N6。
- **合同**：源码测试钉 `event.target.closest("#production-flow .vue-flow__controls-button") && (ArrowUp|ArrowDown|Home|End) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且不进 N66/N67/N15/N9/N43；`#managed-studio-flow .vue-flow__controls-button` 仍属 N66；`[data-testid='story-graph-connect'] .vue-flow__controls-button` 仍属 N67；无窗口。
- **不做**：改 T23；抢 N66 受管 Controls / N67 故事图 Controls / N9–N11 视口 / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N12 说明 / N13–N67；改遗留分组/连线写入。

24h-perf 不抢 View/Node/Inspector/N6。

## P205 附录 · 2026-08-21 20:46 CST（连续两轮无自动项）

P204–P205 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N68 规格。

### 对标增量（相对 P203 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| Vue Flow MiniMap `pannable` + 键盘等价 | 焦点在小地图时 Arrow 平移视口 | fork `@vue-flow/minimap` / 改 SVG 节点 id |
| 本仓 `data-testid="managed-canvas-minimap"` + `studioFlow.getViewport` | 只加 tabindex/keydown | Arrow 顺便 zoom / 关小地图 |
| N23 Shift+M | 仍只切显隐 | Arrow 当 N23 |
| N15 / N17 | 画布微移/Space 拖平移 | MiniMap 焦点时漏进 N15 |

N68 已锁遗留 Controls，并写明「受管 MiniMap 键盘另刀」。帮助卡 / MiniMap 节点键盘选中另刀，不进 N69。

### N69 落地规格（N6–N68 之后，不在本轮实现）

`[data-testid="managed-canvas-minimap"]`（`v-if="showMiniMap"`，`:pannable="true"` `:zoomable="true"`）。目前 SVG 默认不进 Tab。仅受管画布。

- **用户可见**：小地图可见时，容器 `tabindex="0"`。焦点已在小地图内时：无修饰 ArrowUp/Down/Left/Right 平移主画布视口（复用 `studioFlow.getViewport` + 现有 setViewport/setCenter 路径，步进固定小增量）。小地图隐藏忽略。方向键**不**缩放、不切换 `showMiniMap`、不选节点。Controls、底栏、菜单、检查器、芯片、画布忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有 `pannable` 鼠标平移语义；必须 `preventDefault`（挡住 N15）。不 fork MiniMap。
- **合同**：源码测试钉 `event.target.closest("[data-testid='managed-canvas-minimap']") && (ArrowUp|ArrowDown|ArrowLeft|ArrowRight) && !metaKey && !ctrlKey && !altKey && !shiftKey` 时 `preventDefault` 且调用视口平移、不进 N15/N23/N17/N66/N68/N43；N23 仍只认 Shift+M；无窗口。
- **不做**：改 T23；抢 N23 Shift+M / N15 微移 / N17 Space 平移 / N66 Controls / N9–N11 视口；抢 N6 ⌘G / N7 试听 / N8 别名 / N12 说明 / N13–N68。

24h-perf 不抢 View/Node/Inspector/N6。

## P207 附录 · 2026-08-21 20:50 CST（连续两轮无自动项）

P206–P207 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N69 规格。

### 对标增量（相对 P205 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Dialog 初焦 + Tab 环 | 打开后焦点进 dialog | 改成系统/浏览器弹窗 |
| 本仓 `#managed-canvas-help-card` + 关闭钮 + `helpTriggerEl` | 只改焦点 | 改帮助文案 / 新快捷键 |
| N28 F1 | 仍只开合 | Tab/初焦当 F1 |
| N18 Escape | 仍关卡并归还 | 本刀重写 Escape |

N69 已锁 MiniMap Arrow，并写明「帮助卡键盘另刀」。MiniMap 节点键盘选中另刀，不进 N70。

### N70 落地规格（N6–N69 之后，不在本轮实现）

`#managed-canvas-help-card`：`role="dialog"`，`v-if="helpOpen"`，唯一可聚焦控件是关闭钮（`aria-label="关闭帮助"`）。目前打开后焦点仍留在触发钮/画布。

- **用户可见**：帮助卡打开后，焦点落到关闭钮。Tab/Shift+Tab 在 dialog 内循环（当前仅关闭钮）。关闭后焦点归还 `helpTriggerEl`（沿用 N18）。F1 仍走 N28 开合。Escape 仍走 N18，不在本刀重绑。输入框/拖拽忽略。⌘/Ctrl/Alt 修饰不改。
- **复用**：现有 `helpOpen` / `helpTriggerEl` / 关闭 `@click`；必须在打开后 `focus()` 关闭钮。
- **合同**：源码测试钉 `helpOpen` 为真时 `#managed-canvas-help-card button[aria-label="关闭帮助"]` 获焦；dialog 内 Tab `preventDefault` 且焦点不逃出；N28 仍只认 F1；N18 仍关帮助；无窗口。
- **不做**：改 T23；抢 N28 F1 / N18 Escape / N69 MiniMap / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N69；改帮助文案。

24h-perf 不抢 View/Node/Inspector/N6。

## P209 附录 · 2026-08-21 20:53 CST（连续两轮无自动项）

P208–P209 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N70 规格。

### 对标增量（相对 P207 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG 列表 roving + 自定义 MiniMap 槽 | `data-node-id` + tabindex | 把业务 id 写回 SVG `id`（重复 DOM id） |
| 本仓 `#node-managedStudio` rect + `studioFlow.setCenter` | 只改焦点/选中 | Enter 走 `onNodeClick` 连线副作用 |
| N69 容器 Arrow 平移 | 焦点在容器才平移 | 节点焦点 Arrow 当 N69 |
| N16 ⌘A | 仍全选 | MiniMap Enter 当全选 |

N70 已锁帮助卡 Tab，并写明「MiniMap 节点键盘选中另刀」。连线横幅键盘另刀，不进 N71。

### N71 落地规格（N6–N70 之后，不在本轮实现）

`[data-testid="managed-canvas-minimap"] .vue-flow__minimap-node`：自定义 rect，当前无 `id`、无 tabindex。槽可取 Vue Flow `id` 但不写入 HTML `id`。

- **用户可见**：小地图可见时，节点 rect 用 `data-node-id`（禁止 HTML `id`）+ roving `tabindex`（仅当前 0）。焦点已在某 rect 时：无修饰 Arrow 按 DOM 序移焦相邻节点（循环）；Home/End 首/末。Enter/Space 选中对应画布节点并 `setCenter`，**不**走 `onNodeClick` 连线分支。焦点在 MiniMap 容器（非 rect）时 Arrow 仍属 N69 平移。隐藏忽略。⌘/Ctrl/Alt/Shift+Arrow 不触发。
- **复用**：现有自定义 MiniMap 槽 + `studioFlow` 选中/`setCenter`；必须 `preventDefault`（挡住 N15/N69）。
- **合同**：源码测试钉 rect 无 `id=`、有 `data-node-id`；`event.target.closest(".vue-flow__minimap-node") && (ArrowUp|ArrowDown|ArrowLeft|ArrowRight|Home|End)` 时 `preventDefault` 且不进 N69/N15/N16/N43；Enter 不调用 `onConnect`；无窗口。
- **不做**：改 T23；抢 N69 容器平移 / N16 ⌘A / N15 微移 / N70 帮助卡；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N70；恢复 MiniMap SVG HTML id。

24h-perf 不抢 View/Node/Inspector/N6。

## P211 附录 · 2026-08-21 20:57 CST（连续两轮无自动项）

P210–P211 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N71 规格。

### 对标增量（相对 P209 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA status + 关闭后归还焦点 | 退出钮可 Tab，退出后回连线钮 | 把 `role="status"` 改成 dialog / 打开时抢焦 |
| 本仓 `.connect-banner` + `toggleConnectMode` | 只加 testid/焦点 | 重绑 Escape / C |
| N27 C | 仍只开合连线 | 横幅 Enter 当 C |
| N18 Escape | 仍关连线 | 本刀重写 Escape |

N71 已锁 MiniMap 节点键盘，并写明「连线横幅键盘另刀」。检查器 dialog 另刀，不进 N72。

### N72 落地规格（N6–N71 之后，不在本轮实现）

`.connect-banner`：`v-if="connectMode"`，`role="status"`，退出钮 `@click="toggleConnectMode"` 文案「退出（Esc）」，目前无 testid。打开连线不抢画布焦点。

- **用户可见**：连线模式开启时退出钮可 Tab。Enter/Space 仍走现有 click。退出后（按钮或 N27/N18）焦点归还 `data-testid="managed-canvas-connect-mode"`。打开时**不**自动聚焦横幅（保持 status）。Escape 仍走 N18，C 仍走 N27。
- **复用**：现有 `toggleConnectMode`；按钮加 `data-testid="managed-canvas-connect-exit"`。
- **合同**：源码测试钉 `.connect-banner button` 含 `managed-canvas-connect-exit`；退出路径 focus 回 `managed-canvas-connect-mode`；`role="status"` 仍在；N18/N27 合同不改；无窗口。
- **不做**：改 T23；抢 N27 C / N18 Escape / N62 浮动栏 / N71 MiniMap；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N71；把横幅改成 modal。

24h-perf 不抢 View/Node/Inspector/N6。

## P213 附录 · 2026-08-21 21:03 CST（连续两轮无自动项）

P212–P213 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N72 规格。

### 对标增量（相对 P211 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA complementary + 关闭后归还焦点 | 关闭钮可 Tab，关闭后回画布 | 把 `aside` 改成 dialog / aria-modal / 打开时抢焦 |
| 本仓 `.inspector-close` + `@close="selection = null"` | 只加 testid/焦点 | 重绑 Escape / 清选区 |
| N18 Escape | 仍关弹层后清选区 | 本刀重写 Escape |
| N70 帮助卡 dialog | 仍只帮助卡 | 检查器当 modal |

N72 已锁连线横幅退出，并写明「检查器 dialog 另刀」。导演动作面板 dialog 另刀，不进 N73。素材库关闭钮另刀，不进 N73。

### N73 落地规格（N6–N72 之后，不在本轮实现）

`CanvasInspectorPanel` 根是 `<aside class="canvas-inspector" aria-label="画布节点详情">`，关闭钮 `.inspector-close` `aria-label="关闭详情"` `@click="emit('close')"`，目前无 testid。`v-if="selection"` 打开，打开不抢画布焦点。

- **用户可见**：检查器打开时关闭钮可 Tab。Enter/Space 仍走现有 click。关闭后（关闭钮）焦点归还 `#managed-studio-flow`（节点仍在则落到该节点）。打开时**不**自动聚焦检查器（保持 complementary）。Escape 仍走 N18。不设 `role="dialog"` / `aria-modal`。
- **复用**：现有 `emit('close')` / 父级 `selection = null`；按钮加 `data-testid="managed-canvas-inspector-close"`。
- **合同**：源码测试钉 `.inspector-close` 含 `managed-canvas-inspector-close`；关闭路径 focus 回 `#managed-studio-flow`；根节点仍是 aside 无 `role="dialog"`；N18/N70/N72 合同不改；N55/N56/N58 仍属检查器内列表/操作；无窗口。
- **不做**：改 T23；抢 N18 Escape / N70 帮助卡 / N72 连线横幅 / N55–N56 出场 / N58 节点操作 / N7 试听；抢 N6 ⌘G / N8 别名 / N9–N11 视口 / N12 说明 / N13–N72；把检查器改成 modal。

24h-perf 不抢 View/Node/Inspector/N6。

## P215 附录 · 2026-08-21 21:07 CST（连续两轮无自动项）

P214–P215 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N73 规格。

### 对标增量（相对 P213 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA APG Dialog 初焦 + Tab 环 | 打开后焦点进 dialog | 改成系统/浏览器弹窗 / 写命令 |
| 本仓 `#director-action-panel` 已 `role="dialog"` + 过滤框 + 关闭钮 | 只改焦点 | 新快捷键 / 把动作改成 execute_command |
| N70 帮助卡 | 仍只帮助卡 | 导演面板当帮助 |
| N18 Escape | 仍关卡 | 本刀重写 Escape |
| N73 检查器 | 仍 complementary | 导演面板当检查器 |

N73 已锁检查器关闭钮，并写明「导演动作面板 dialog 另刀」。素材库关闭钮另刀，不进 N74。

### N74 落地规格（N6–N73 之后，不在本轮实现）

`[data-testid="director-action-panel"]`：`v-if="open"`，已 `role="dialog"`，可聚焦控件=关闭钮 `director-panel-close`、过滤框 `director-panel-filter`、未 disabled 的 `.director-action`。目前打开后焦点仍留在导演钮/画布。N18 会关面板，但只把焦点还给帮助/添加钮。

- **用户可见**：面板打开后，焦点落到过滤框。Tab/Shift+Tab 在 dialog 内循环（关闭钮 → 过滤框 → 可用动作钮；disabled 跳过）。关闭后（关闭钮或 N18，且当时帮助/添加未开）焦点归还 `data-testid="managed-canvas-director-toggle"`。Escape 仍走 N18 关面板。⌘/ 仍只开合，不改。输入框内字符键不触发画布快捷键。
- **复用**：现有 `directorPanelOpen` / `@close` / `director-panel-filter`；必须在打开后 `focus()` 过滤框；Tab `preventDefault`。
- **合同**：源码测试钉 `directorPanelOpen` 为真时 `[data-testid="director-panel-filter"]` 获焦；dialog 内 Tab 焦点不逃出；关闭路径 focus 回 `managed-canvas-director-toggle`（不覆盖 N18 已有帮助/添加归还）；N70 仍只认帮助卡；N18 仍关导演面板；无窗口。
- **不做**：改 T23；抢 N70 帮助卡 / N18 Escape / N73 检查器 / N15 微移；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N73；给动作列表加 content-visibility；把只读动作改成写命令。

24h-perf 不抢 View/Node/Inspector/N6。

## P217 附录 · 2026-08-21 21:10 CST（连续两轮无自动项）

P216–P217 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N74 规格。

### 对标增量（相对 P215 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA complementary + 关闭后归还焦点 | 关闭钮可 Tab，关闭后回对应开库钮 | 把 `aside` 改成 dialog / 打开时抢焦 |
| 本仓 `aria-label="关闭素材库"` / `关闭剧本资源` | 只加 testid/焦点 | 重绑 L / Shift+L / Escape |
| N30 L / N31 Shift+L | 仍只开合 | 关闭钮当快捷键 |
| N73 检查器关闭 | 仍只检查器 | 侧栏当检查器 |

N74 已锁导演面板 dialog，并写明「素材库关闭钮另刀」。错误横幅关闭钮另刀，不进 N75。

### N75 落地规格（N6–N74 之后，不在本轮实现）

当前素材库 `#managed-canvas-library` 与剧本资源 `#managed-canvas-global-resource-library` 都是 `aside`，关闭钮分别 `aria-label="关闭素材库"`（`libraryOpen=false`）与 `aria-label="关闭剧本资源"`（`closeLibrary()`），目前无 testid。打开侧栏不抢画布焦点。

- **用户可见**：侧栏打开时对应关闭钮可 Tab。Enter/Space 仍走现有 click。关闭当前库后焦点归还 `data-testid="managed-canvas-open-library"`；关闭剧本资源后归还 `data-testid="managed-canvas-open-global-resources"`。打开时**不**自动聚焦侧栏（保持 complementary）。L/Shift+L 仍走 N30/N31。Escape **不**新绑关库（N18 现状不关侧栏，本刀不扩）。
- **复用**：现有 `closeLibrary` / `libraryOpen=false`；关闭钮分别加 `data-testid="managed-canvas-library-close"` 与 `managed-canvas-global-library-close`。
- **合同**：源码测试钉两关闭钮含上述 testid；关闭路径 focus 回对应开库钮；根节点仍是 aside 无 `role="dialog"`；N30/N31/N18/N49/N59/N60 合同不改；无窗口。
- **不做**：改 T23；抢 N30 L / N31 Shift+L / N18 Escape / N73 检查器 / N74 导演面板 / N49 素材行 / N59–N60 tabs；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N74；把侧栏改成 modal。

24h-perf 不抢 View/Node/Inspector/N6。

## P219 附录 · 2026-08-21 21:13 CST（连续两轮无自动项）

P218–P219 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N75 规格。

### 对标增量（相对 P217 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA alert + 关闭后归还焦点 | 关闭钮可 Tab，关闭后回画布 | 把 `role="alert"` 改成 dialog / 出现时抢焦 |
| 本仓 `.canvas-error` + `errorMessage = ''` | 只加 testid/焦点 | 重绑 Escape / 清选区 |
| N72 连线横幅 status | 仍只连线横幅 | 错误条当连线横幅 |
| N18 Escape | 仍关弹层 | 本刀用 Escape 清错误 |

N75 已锁素材库关闭钮，并写明「错误横幅关闭钮另刀」。帮助卡关闭选择器仍属 N70，不进 N76。

### N76 落地规格（N6–N75 之后，不在本轮实现）

`.canvas-error`：`v-if="errorMessage"`，`role="alert"`，关闭钮 `aria-label="关闭错误"` `@click="errorMessage = ''"`，目前无 testid。错误出现不抢画布焦点。

- **用户可见**：横幅可见时关闭钮可 Tab。Enter/Space 仍走现有 click。关闭后焦点归还 `#managed-studio-flow`。出现时**不**自动聚焦横幅（保持 alert）。Escape 仍走 N18，**不清** `errorMessage`。
- **复用**：现有 `errorMessage`；按钮加 `data-testid="managed-canvas-error-close"`。
- **合同**：源码测试钉 `.canvas-error button` 含 `managed-canvas-error-close`；关闭路径 focus 回 `#managed-studio-flow`；`role="alert"` 仍在；N18/N72/N70 合同不改；无窗口。
- **不做**：改 T23；抢 N18 Escape / N72 连线横幅 / N70 帮助卡 / N75 侧栏关闭；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N75；把 alert 改成 modal。

24h-perf 不抢 View/Node/Inspector/N6。

## P221 附录 · 2026-08-21 21:17 CST（连续两轮无自动项）

P220–P221 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N76 规格。

### 对标增量（相对 P219 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA 二次确认 + 关闭后归还焦点 | 二次确认后钮卸载，焦点回画布 | `window.confirm` / 系统弹窗 |
| 本仓 `managed-canvas-clear-view` + `clearConfirmationArmed` | 只补焦点/宣告 | 改清空语义 / 去掉二次确认 |
| N18 Escape | 仍解除武装 | 本刀把 Escape 改成执行清空 |
| N63 底栏 Arrow | 仍只底栏 | 清空钮 Arrow 当确认 |

N76 已锁错误横幅关闭。帮助卡关闭 testid 另刀，不进 N77。

### N77 落地规格（N6–N76 之后，不在本轮实现）

`[data-testid="managed-canvas-clear-view"]`：仅 `workspaceMode==='workflow'` 时挂载，`:aria-pressed="clearConfirmationArmed"`，首次 click 武装 4s，二次 click 执行 `clearWorkflowCanvas` 并把 `workspaceMode` 设为 `projection`（钮随即卸载）。目前二次确认后焦点丢失。

- **用户可见**：首次 click 后焦点仍在该钮，文案「再点一次确认清空」需可被辅助技术读到（`aria-pressed` 已有；补 `aria-live="polite"` 或等价）。二次确认执行后（钮卸载）焦点归还 `#managed-studio-flow`。Escape 仍走 N18 `resetClearConfirmation`，**不**执行清空。不出现系统/浏览器 confirm。
- **复用**：现有 `clearWorkflowCanvas` / `clearConfirmationArmed`；不得改 4s 超时与投影模式切换。
- **合同**：源码测试钉二次确认路径（`clearConfirmationArmed` 为真时再调 `clearWorkflowCanvas`）之后 focus `#managed-studio-flow`；武装态无 `window.confirm`；N18 仍只解除武装；N63 仍不把 Arrow 当清空；无窗口。
- **不做**：改 T23；抢 N18 Escape / N63 底栏 Arrow / N24 Shift+W / N76 错误横幅；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N76；改成 `window.confirm`。

24h-perf 不抢 View/Node/Inspector/N6。

## P223 附录 · 2026-08-21 21:19 CST（连续两轮无自动项）

P222–P223 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N77 规格。

### 对标增量（相对 P221 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA dialog 关闭后归还触发钮 | 关闭钮 click 也归还 `helpTriggerEl` | 改成系统弹窗 / 重写 Tab 环 |
| 本仓 `#managed-canvas-help-card` 关闭钮无 testid | 只加 testid/click 焦点 | 改帮助文案 / 新快捷键 |
| N70 初焦 + Tab 环 | 仍只打开路径 | 本刀重做初焦 |
| N18 Escape | 仍关并归还 | 本刀重绑 Escape |
| N28 F1 | 仍只开合 | 关闭钮当 F1 |

N77 已锁清空二次确认，并写明「帮助卡关闭 testid 另刀」。视图菜单 summary 初焦另刀，不进 N78。

### N78 落地规格（N6–N77 之后，不在本轮实现）

`#managed-canvas-help-card button[aria-label="关闭帮助"]`：目前无 testid，`@click="helpOpen = false"` 不归还焦点。N18 Escape 才会 `helpTriggerEl.focus()`。N70 已锁打开后初焦该钮与 Tab 环。

- **用户可见**：关闭钮加 `data-testid="managed-canvas-help-close"`。Enter/Space/click 关闭后焦点归还帮助触发钮（与 N18 同一 `helpTriggerEl`）。打开路径仍属 N70（初焦关闭钮）。Escape 仍走 N18。F1 仍走 N28。
- **复用**：现有 `helpOpen` / `helpTriggerEl`；关闭 handler 在置 `helpOpen=false` 后 `focus()` 触发钮。
- **合同**：源码测试钉关闭钮含 `managed-canvas-help-close`；关闭 click 路径 focus `helpTriggerEl`；N70 初焦/Tab 合同不改；N18/N28 合同不改；无窗口。
- **不做**：改 T23；抢 N70 初焦 Tab / N18 Escape / N28 F1 / N77 清空确认；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N77；改帮助文案。

24h-perf 不抢 View/Node/Inspector/N6。

## P225 附录 · 2026-08-21 21:24 CST（连续两轮无自动项）

P224–P225 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N78 规格。

### 对标增量（相对 P223 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| WAI-ARIA Menu 关闭后归还触发器 | Escape/点外关闭后回 summary | 改成弹窗 / 重写 Arrow 环 |
| 本仓 `details.view-menu` + `closeViewMenu()` | 只补焦点/testid | 改菜单项 / 主题集合 |
| N64 打开后第一可用项 + Arrow | 仍只打开路径 | 本刀重做 roving |
| N18 Escape | 仍关菜单 | 本刀重绑 Escape |
| N65 主题 radio | 仍只 radio | 关闭时焦点进主题 |

N78 已锁帮助关闭钮，并写明「视图菜单 summary 初焦另刀」。打开初焦仍属 N64；本刀只补关闭归还。诊断 details 另刀，不进 N79。

### N79 落地规格（N6–N78 之后，不在本轮实现）

`details.view-menu`：`summary` 目前无 testid，`aria-label="视图选项"`。N18 / 点菜单外会 `closeViewMenu()`（去 `open`），但焦点不回 summary（N18 只归还帮助/添加）。N64 已锁打开后焦点落到第一可用 `.view-menu-pop > button`。

- **用户可见**：`summary` 加 `data-testid="managed-canvas-view-menu"`。菜单曾打开时：Escape（N18）或点外关闭后，若当时帮助/添加未开，焦点归还该 summary。打开路径仍属 N64（第一可用项）。Arrow 仍走 N64/N65。Enter/Space 在 summary 上仍走原生 details 开合。
- **复用**：现有 `viewMenuEl` / `closeViewMenu`；关闭路径在去掉 `open` 后 `summary.focus()`。
- **合同**：源码测试钉 `.view-menu > summary` 含 `managed-canvas-view-menu`；`closeViewMenu` 路径在菜单曾开且帮助/添加未开时 focus summary；N18 帮助/添加归还优先于本刀；N64/N65 合同不改；无窗口。
- **不做**：改 T23；抢 N64 菜单 Arrow / N65 主题 radio / N18 Escape / N78 帮助关闭；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N78；把 details 改成自定义弹层。

24h-perf 不抢 View/Node/Inspector/N6。

## P227 附录 · 2026-08-21 21:27 CST（连续两轮无自动项）

P226–P227 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N79 规格。

### 对标增量（相对 P225 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `.flow-caption.technical-diagnostics` + `managed-canvas-dom-counts` | 只加 summary testid | 改诊断字段 / 默认展开 |
| N79 视图菜单 | 仍只视图菜单 | 诊断当菜单、Escape 关诊断 |
| N18 Escape | 仍关弹层 | 本刀用 Escape 关诊断 |

N79 已锁视图菜单关闭归还，并写明「诊断 details 另刀」。检查器 `.inspector-diagnostics` 另刀，不进 N80。

### N80 落地规格（N6–N79 之后，不在本轮实现）

画布 `details.flow-caption.technical-diagnostics`：`summary` 文案「诊断详情」，目前无 testid。内层已有 `managed-canvas-dom-counts`。不是弹层；N18 现状不关它。

- **用户可见**：`summary` 加 `data-testid="managed-canvas-diagnostics"`。Enter/Space 仍走原生 details 开合。打开不抢画布焦点（保持 disclosure）。Escape **不**关闭本 details（不扩 N18）。不设 `role="dialog"` / `role="menu"`。
- **复用**：现有 details/summary；不得默认展开、不得改 DOM 计数文案。
- **合同**：源码测试钉 `.flow-caption.technical-diagnostics > summary` 含 `managed-canvas-diagnostics`；无 `role="dialog"`；N18 仍不关该 details；`managed-canvas-dom-counts` 仍在内部；无窗口。
- **不做**：改 T23；抢 N79 视图菜单 / N18 Escape / N73 检查器；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N79；把诊断改成 modal；改检查器/素材库其它 `.technical-diagnostics`。

24h-perf 不抢 View/Node/Inspector/N6。

## P229 附录 · 2026-08-21 21:30 CST（连续两轮无自动项）

P228–P229 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N80 规格。

### 对标增量（相对 P227 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `.inspector-diagnostics`（资产/文稿两处互斥） | 只加 summary testid | 改诊断字段 / 默认展开 |
| N80 画布诊断 | 仍只画布 `.flow-caption` | 检查器当画布诊断 |
| N73 检查器关闭 | 仍只关闭钮 | 诊断开合当关闭检查器 |

N80 已锁画布诊断，并写明「检查器 `.inspector-diagnostics` 另刀」。素材库其它 `.technical-diagnostics` 另刀，不进 N81。

### N81 落地规格（N6–N80 之后，不在本轮实现）

`CanvasInspectorPanel` 两处互斥 `details.inspector-diagnostics`（资产 / 剧本·提示词）：`summary` 文案「诊断详情」，目前无 testid。不是弹层；N18 现状不关它。

- **用户可见**：两处 `summary` 均加 `data-testid="managed-canvas-inspector-diagnostics"`（同一时刻至多一处挂载）。Enter/Space 仍走原生 details 开合。打开不抢检查器内其它控件焦点。Escape **不**关闭本 details（不扩 N18，也不关检查器）。不设 `role="dialog"` / `role="menu"`。
- **复用**：现有 details/summary；不得默认展开、不得改 ID/修订字段。
- **合同**：源码测试钉 `.inspector-diagnostics > summary` 含 `managed-canvas-inspector-diagnostics`；无 `role="dialog"`；N18/N73/N80 合同不改；无窗口。
- **不做**：改 T23；抢 N73 检查器关闭 / N80 画布诊断 / N55–N56 出场 / N58 节点操作；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N80；把诊断改成 modal；改素材库其它 `.technical-diagnostics`。

24h-perf 不抢 View/Node/Inspector/N6。

## P231 附录 · 2026-08-21 21:34 CST（连续两轮无自动项）

P230–P231 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N81 规格。

### 对标增量（相对 P229 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `MaterialStudioView` 详情栏 `.technical-diagnostics` | 只加 summary testid | 改诊断字段 / 默认展开 / 铺到每一列表行 |
| N80 画布诊断 / N81 检查器诊断 | 仍只各自面板 | 素材库当画布/检查器 |
| `studio-text-revision-history` | 仍只修订列表 | 本刀改修订历史 |

N81 已锁检查器诊断，并写明「素材库其它 `.technical-diagnostics` 另刀」。列表行诊断与审片诊断另刀，不进 N82。

### N82 落地规格（N6–N81 之后，不在本轮实现）

`MaterialStudioView` 详情栏 `section.detail-section > details.technical-diagnostics`：`summary` 文案「诊断详情」，目前无 testid。可同时出现多段（图片/正文等）。不是弹层。

- **用户可见**：详情栏这些 `summary` 加 `data-testid="material-studio-diagnostics"`。Enter/Space 仍走原生 details 开合。打开不抢详情栏其它控件。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。版本行/关联行里的 `.technical-diagnostics` **不**加本 testid。
- **复用**：现有 details/summary；不得默认展开、不得改 SHA/修订字段、不得动 `studio-text-revision-history`。
- **合同**：源码测试钉 `MaterialStudioView` 中 `section.detail-section > details.technical-diagnostics > summary` 含 `material-studio-diagnostics`；无 `role="dialog"`；N80/N81 选择器不改；无窗口。
- **不做**：改 T23；抢 N80 画布诊断 / N81 检查器诊断 / N49 素材行 Arrow；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N81；把诊断改成 modal；改审片/列表行诊断。

24h-perf 不抢 View/Node/Inspector/N6。

## P233 附录 · 2026-08-21 21:40 CST（连续两轮无自动项）

P232–P233 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N82 规格。

### 对标增量（相对 P231 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `StudioContinuityReviewView` 头栏 `.next-action-diagnostics` | 只加 summary testid | 改下一动作文案 / 默认展开 / 铺到资产·冲突·批次行 |
| N80/N81/N82 诊断 | 仍只各自面板 | 审片当画布/检查器/素材库 |
| `studio-review-identity-*` | 仍只历史提交身份 | 本刀改 Review 历史 |

N82 已锁素材库详情栏诊断，并写明「列表行诊断与审片诊断另刀」。资产/冲突/批次/Head revision 行内诊断与生成控制诊断另刀，不进 N83。

### N83 落地规格（N6–N82 之后，不在本轮实现）

`StudioContinuityReviewView` 头栏「唯一下一动作」`details.technical-diagnostics.next-action-diagnostics`：`summary` 文案「诊断详情」，目前无 testid。不是弹层。N18 现状不关它。

- **用户可见**：该 `summary` 加 `data-testid="studio-continuity-next-action-diagnostics"`。Enter/Space 仍走原生 details 开合。打开不抢头栏其它控件。Escape **不**关闭本 details（不扩 N18）。不设 `role="dialog"` / `role="menu"`。资产卡/冲突卡/批次卡/`.review-head` 里的 `.technical-diagnostics` **不**加本 testid。
- **复用**：现有 details/summary；不得默认展开、不得改 `nextAction.command`/`reason` 文案、不得动 `studio-review-identity-*`。
- **合同**：源码测试钉 `.next-action-diagnostics > summary` 含 `studio-continuity-next-action-diagnostics`；无 `role="dialog"`；N80/N81/N82 选择器不改；无窗口。
- **不做**：改 T23；抢 N80 画布诊断 / N81 检查器诊断 / N82 素材库详情诊断；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N82；把诊断改成 modal；改资产/冲突/批次/生成控制诊断。

24h-perf 不抢 View/Node/Inspector/N6。

## P235 附录 · 2026-08-21 21:43 CST（连续两轮无自动项）

P234–P235 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N83 规格。

### 对标增量（相对 P233 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `StudioGenerationControlView` 宫格技术消息 `.technical-diagnostics` | 只加 summary testid | 改 `isTechnicalGenerationMessage` / 默认展开 / 改主文案 |
| N80–N83 诊断 | 仍只各自面板 | 生成控制当画布/检查器/素材库/审片头栏 |
| `studio-pack-identity` / `studio-result-identity-*` | 仍只冻结包与结果行 | 本刀改身份 details |

N83 已锁审片下一动作诊断，并写明「生成控制诊断另刀」。资产/冲突/批次/Head revision 行内诊断另刀，不进 N84。

### N84 落地规格（N6–N83 之后，不在本轮实现）

`StudioGenerationControlView` 冻结包区：当 `historyTargetKind === 'panel'` 且 `isTechnicalGenerationMessage(detail.selectedPanel.generation.message)` 时挂载的 `details.technical-diagnostics`（无 `pack-identity` / `result-identity` class）：`summary` 文案「诊断详情」，目前无 testid。不是弹层。

- **用户可见**：该 `summary` 加 `data-testid="studio-generation-message-diagnostics"`。Enter/Space 仍走原生 details 开合。打开不抢详情栏其它控件。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。`studio-pack-identity` 与结果行 `studio-result-identity-*` **不**改。
- **复用**：现有 details/summary 与 `isTechnicalGenerationMessage`；不得默认展开、不得改人话主文案 `generationMessageText`、不得改判定正则。
- **合同**：源码测试钉该 `isTechnicalGenerationMessage` 处 `details.technical-diagnostics > summary` 含 `studio-generation-message-diagnostics`；无 `role="dialog"`；`studio-pack-identity` / `studio-result-identity-` 仍在；N80–N83 选择器不改；无窗口。
- **不做**：改 T23；抢 N80 画布诊断 / N81 检查器诊断 / N82 素材库详情诊断 / N83 审片下一动作诊断；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N83；把诊断改成 modal；改审片资产/冲突/批次行诊断。

24h-perf 不抢 View/Node/Inspector/N6。

## P237 附录 · 2026-08-21 21:46 CST（连续两轮无自动项）

P236–P237 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N84 规格。

### 对标增量（相对 P235 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `StudioContinuityReviewView` `.review-head` 诊断 | 只加 summary testid | 改 Head revision 文案 / 默认展开 / 铺到历史行 |
| N80–N84 诊断 | 仍只各自面板 | 审片头栏当画布/检查器/素材库/生成控制 |
| `studio-review-identity-*` | 仍只历史提交身份 | 本刀改 Review 历史 |

N84 已锁生成控制技术消息诊断，并写明「资产/冲突/批次/Head revision 行内诊断另刀」。Head revision 实际在 `.review-head`（单块，非整表行）。资产/冲突/批次行另刀，不进 N85。

### N85 落地规格（N6–N84 之后，不在本轮实现）

`StudioContinuityReviewView` 画面审片 `.review-head > details.technical-diagnostics`：`summary` 文案「诊断详情」，内层 `Head revision …`，目前无 testid。不是弹层。N18 现状不关它。

- **用户可见**：该 `summary` 加 `data-testid="studio-continuity-review-head-diagnostics"`。Enter/Space 仍走原生 details 开合。打开不抢审片区其它控件。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。资产卡/冲突卡/批次卡/历史 `studio-review-identity-*` **不**加本 testid。N83 `.next-action-diagnostics` 不改。
- **复用**：现有 details/summary；不得默认展开、不得改 `headRevision` 展示、不得动 `review-rework-guidance`。
- **合同**：源码测试钉 `.review-head > details.technical-diagnostics > summary` 含 `studio-continuity-review-head-diagnostics`；无 `role="dialog"`；N80–N84 选择器不改；无窗口。
- **不做**：改 T23；抢 N80–N84 / N83 下一动作诊断；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N84；把诊断改成 modal；改资产/冲突/批次行诊断。

24h-perf 不抢 View/Node/Inspector/N6。

## P239 附录 · 2026-08-21 21:49 CST（连续两轮无自动项）

P238–P239 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N85 规格。

### 对标增量（相对 P237 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `StudioContinuityReviewView` `.asset-control` 诊断 | 只加 summary 共享 testid | 改资产状态文案 / 默认展开 / 铺到冲突·批次 |
| N80–N85 诊断 | 仍只各自面板 | 资产卡当画布/检查器/Review 头栏 |
| `studio-review-identity-*` | 仍只历史提交身份 | 本刀改 Review 历史 |

N85 已锁 Review 头栏诊断，并写明「资产/冲突/批次行另刀」。冲突/批次行另刀，不进 N86。

### N86 落地规格（N6–N85 之后，不在本轮实现）

`StudioContinuityReviewView` 一致性检查 `article.asset-control > details.technical-diagnostics`：`summary` 文案「诊断详情」，内层资产 ID，目前无 testid。可同时挂载多张资产卡。不是弹层。

- **用户可见**：这些 `summary` 加 `data-testid="studio-continuity-asset-diagnostics"`（共享 id，多卡可同时存在）。Enter/Space 仍走原生 details 开合。打开不抢资产卡其它控件。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。冲突卡/批次卡/`.review-head`/`.next-action-diagnostics` **不**加本 testid。
- **复用**：现有 details/summary；不得默认展开、不得改九字段网格、不得动 `continuity-assets` 分页。
- **合同**：源码测试钉 `.asset-control > details.technical-diagnostics > summary` 含 `studio-continuity-asset-diagnostics`；无 `role="dialog"`；N80–N85 选择器不改；无窗口。
- **不做**：改 T23；抢 N80–N85 / N83 下一动作 / N85 Review 头栏；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N85；把诊断改成 modal；改冲突/批次行诊断。

24h-perf 不抢 View/Node/Inspector/N6。

## P241 附录 · 2026-08-21 21:52 CST（连续两轮无自动项）

P240–P241 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N86 规格。

### 对标增量（相对 P239 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `StudioContinuityReviewView` `.conflict-section article` 诊断 | 只加 summary 共享 testid | 改冲突文案 / 默认展开 / 铺到批次 |
| N80–N86 诊断 | 仍只各自面板 | 冲突卡当资产卡/Review 头栏 |
| `studio-review-identity-*` | 仍只历史提交身份 | 本刀改 Review 历史 |

N86 已锁资产卡诊断，并写明「冲突/批次行另刀」。批次行另刀，不进 N87。

### N87 落地规格（N6–N86 之后，不在本轮实现）

`StudioContinuityReviewView` 冲突检查 `.conflict-section article > details.technical-diagnostics`：`summary` 文案「诊断详情」，内层 `subjectId · conflictId · r…`，目前无 testid。可同时挂载多张冲突卡。不是弹层。

- **用户可见**：这些 `summary` 加 `data-testid="studio-continuity-conflict-diagnostics"`（共享 id，多卡可同时存在）。Enter/Space 仍走原生 details 开合。打开不抢冲突卡其它控件。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。资产卡/批次卡/`.review-head`/`.next-action-diagnostics` **不**加本 testid。
- **复用**：现有 details/summary；不得默认展开、不得改冲突分页、不得动 `continuity-conflicts`。
- **合同**：源码测试钉 `.conflict-section article > details.technical-diagnostics > summary` 含 `studio-continuity-conflict-diagnostics`；无 `role="dialog"`；N80–N86 选择器不改；无窗口。
- **不做**：改 T23；抢 N80–N86 / N86 资产卡；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N86；把诊断改成 modal；改批次行诊断。

24h-perf 不抢 View/Node/Inspector/N6。

## P243 附录 · 2026-08-21 21:54 CST（连续两轮无自动项）

P242–P243 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N87 规格。

### 对标增量（相对 P241 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `StudioContinuityReviewView` `.batch-grid article` 诊断 | 只加 summary 共享 testid | 改批次文案 / 默认展开 / 给 blocking-batch 新加 details |
| N80–N87 诊断 | 仍只各自面板 | 批次卡当资产/冲突/Review 头栏 |
| `studio-review-identity-*` | 仍只历史提交身份 | 本刀改 Review 历史 |

N87 已锁冲突卡诊断，并写明「批次行另刀」。`.blocking-batch` 无 details，不进 N88。

### N88 落地规格（N6–N87 之后，不在本轮实现）

`StudioContinuityReviewView` 六图停检 `.batch-grid article > details.technical-diagnostics`：`summary` 文案「诊断详情」，内层 `checkpoint r… · attestation r…`，目前无 testid。可同时挂载多张批次卡。不是弹层。

- **用户可见**：这些 `summary` 加 `data-testid="studio-continuity-batch-diagnostics"`（共享 id，多卡可同时存在）。Enter/Space 仍走原生 details 开合。打开不抢批次卡其它控件。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。资产卡/冲突卡/`.review-head`/`.next-action-diagnostics`/`.blocking-batch` **不**加本 testid。
- **复用**：现有 details/summary；不得默认展开、不得改停检分页、不得给 blocking-batch 新加 details。
- **合同**：源码测试钉 `.batch-grid article > details.technical-diagnostics > summary` 含 `studio-continuity-batch-diagnostics`；无 `role="dialog"`；N80–N87 选择器不改；无窗口。
- **不做**：改 T23；抢 N80–N87 / N86 资产卡 / N87 冲突卡；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N87；把诊断改成 modal；给 blocking-batch 新增 disclosure。

24h-perf 不抢 View/Node/Inspector/N6。

## P245 附录 · 2026-08-21 21:57 CST（连续两轮无自动项）

P244–P245 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N88 规格。

### 对标增量（相对 P243 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `MaterialStudioView` `.relation-list article` 诊断 | 只加 summary 共享 testid | 改关系文案 / 默认展开 / 铺到版本行 / 改 `.asset-relation-editor` |
| N80–N88 / N82 详情栏 | 仍只各自面板 | 关联行当详情栏/审片卡 |
| `studio-text-revision-history` | 仍只修订列表 | 本刀改修订历史 |

N82 已锁详情栏诊断，并写明「版本行/关联行不加本 testid」。N88 收口审片批次。版本行另刀，不进 N89。

### N89 落地规格（N6–N88 之后，不在本轮实现）

`MaterialStudioView` 派生与组合 `.relation-list article > details.technical-diagnostics`：`summary` 文案「诊断详情」，内层关系 ID，目前无 testid。可同时挂载多行。不是弹层。

- **用户可见**：这些 `summary` 加 `data-testid="material-studio-relation-diagnostics"`（共享 id，多行可同时存在）。Enter/Space 仍走原生 details 开合。打开不抢关系卡其它控件。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。N82 `section.detail-section > details.technical-diagnostics`、`.version-meta` 版本诊断、`.asset-relation-editor` **不**加本 testid。
- **复用**：现有 details/summary；不得默认展开、不得改 rebase 按钮、不得动 `relation-asset-select`。
- **合同**：源码测试钉 `.relation-list article > details.technical-diagnostics > summary` 含 `material-studio-relation-diagnostics`；无 `role="dialog"`；N80–N88 / N82 选择器不改；无窗口。
- **不做**：改 T23；抢 N80–N88 / N82 详情栏；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N88；把诊断改成 modal；改版本行诊断。

24h-perf 不抢 View/Node/Inspector/N6。

## P247 附录 · 2026-08-21 22:00 CST（连续两轮无自动项）

P246–P247 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N89 规格。

### 对标增量（相对 P245 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `MaterialStudioView` `.version-meta` 诊断 | 只加 summary 共享 testid | 改版本文案 / 默认展开 / 改 `.version-visual` |
| N80–N89 / N82 详情栏 / N89 关联行 | 仍只各自面板 | 版本行当详情栏/关联行 |
| `studio-text-revision-history` | 仍只修订列表 | 本刀改修订历史 |

N89 已锁关联行诊断，并写明「版本行另刀」。

### N90 落地规格（N6–N89 之后，不在本轮实现）

`MaterialStudioView` 版本历史 `.versions-section .version-meta > details.technical-diagnostics`：`summary` 文案「诊断详情」，内层版本 ID · SHA，目前无 testid。可同时挂载多行。不是弹层。

- **用户可见**：这些 `summary` 加 `data-testid="material-studio-version-diagnostics"`（共享 id，多行可同时存在）。Enter/Space 仍走原生 details 开合。打开不抢版本卡其它控件。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。N82 详情栏、N89 关联行、`.version-visual` **不**改。
- **复用**：现有 details/summary；不得默认展开、不得改审核按钮、不得动 `versions-section` 含输入控件的布局。
- **合同**：源码测试钉 `.version-meta > details.technical-diagnostics > summary` 含 `material-studio-version-diagnostics`；无 `role="dialog"`；N80–N89 / N82 / N89 选择器不改；无窗口。
- **不做**：改 T23；抢 N80–N89 / N82 详情栏 / N89 关联行；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N89；把诊断改成 modal。

24h-perf 不抢 View/Node/Inspector/N6。

## P249 附录 · 2026-08-21 22:03 CST（连续两轮无自动项）

P248–P249 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N90 规格。

### 对标增量（相对 P247 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `StudioContinuityReviewView` 空态 `.diagnostic-details` | 只加 summary testid | 改 `continuity-query-form` / 默认展开 |
| N80–N90 诊断 | 仍只各自面板 | 空态当下一动作/资产卡 |
| 生成控制 `.plan-id-diagnostics` | 另刀 | 本刀改计划组 |

N90 收口素材库版本行。生成控制计划 ID 诊断另刀，不进 N91。

### N91 落地规格（N6–N90 之后，不在本轮实现）

`StudioContinuityReviewView` 空态 `.review-entry-empty > details.diagnostic-details`：`summary` 文案「诊断详情」，内层 `continuity-query-form`，目前无 testid。不是弹层。

- **用户可见**：该 `summary` 加 `data-testid="studio-continuity-empty-diagnostics"`。Enter/Space 仍走原生 details 开合。打开不抢空态「去画布」按钮。Escape **不**关闭本 details，也不提交表单。不设 `role="dialog"` / `role="menu"`。不得改 `continuity-query-form` 字段。N83 `.next-action-diagnostics` 不改。
- **复用**：现有 details/summary；不得默认展开、不得改 `loadControl`。
- **合同**：源码测试钉 `.review-entry-empty > details.diagnostic-details > summary` 含 `studio-continuity-empty-diagnostics`；无 `role="dialog"`；`continuity-query-form` 仍在内部；N80–N90 选择器不改；无窗口。
- **不做**：改 T23；抢 N80–N90 / N83 下一动作；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N90；把诊断改成 modal；改生成控制 `.plan-id-diagnostics`。

24h-perf 不抢 View/Node/Inspector/N6。

## P251 附录 · 2026-08-21 22:07 CST（连续两轮无自动项）

P250–P251 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N91 规格。

### 对标增量（相对 P249 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `StudioGenerationControlView` `.plan-id-diagnostics` | 只加 summary 共享 testid | 改计划 ID 文案 / 默认展开 / 改重拍按钮 |
| N80–N91 / N84 技术消息 | 仍只各自面板 | 计划 ID 当冻结包身份 |
| `studio-pack-identity` / `studio-result-identity-*` | 仍只冻结包与结果行 | 本刀改身份 details |

N91 已锁审片空态，并写明「生成控制计划 ID 诊断另刀」。绑定工作台 `.binding-diagnostics` 另刀，不进 N92。

### N92 落地规格（N6–N91 之后，不在本轮实现）

`StudioGenerationControlView` 计划组 `.plan-group > details.plan-id-diagnostics`：`summary` 文案「诊断」，内层计划 ID，目前无 testid。可同时挂载多组。不是弹层。

- **用户可见**：这些 `summary` 加 `data-testid="studio-generation-plan-id-diagnostics"`（共享 id，多组可同时存在）。Enter/Space 仍走原生 details 开合。打开不抢「重拍失败/取消节点」。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。N84 `studio-generation-message-diagnostics`、`studio-pack-identity`、`studio-result-identity-*` **不**改。
- **复用**：现有 details/summary；不得默认展开、不得改 `retryPlan`、不得动 `plan-node` 状态。
- **合同**：源码测试钉 `.plan-group > details.plan-id-diagnostics > summary` 含 `studio-generation-plan-id-diagnostics`；无 `role="dialog"`；N80–N91 选择器不改；无窗口。
- **不做**：改 T23；抢 N80–N91 / N84 技术消息；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N91；把诊断改成 modal；改绑定工作台诊断。

24h-perf 不抢 View/Node/Inspector/N6。

## P253 附录 · 2026-08-21 22:12 CST（连续两轮无自动项）

P252–P253 无新 CV/busy 红测。digest 仍 `25c1df9c…` / 1089 files / 21,671,835 bytes。N6 仍未开工（`nodeTypes` 仅 `managedStudio`）。禁止空扫 busy。不改 Vue/ts。earliest **仍是 N6**。不复述 N6–N92 规格。

### 对标增量（相对 P251 表）

| 来源 | 可借 | 禁止 |
|---|---|---|
| HTML disclosure 原生键盘 | summary Enter/Space 开合 | 改成 dialog / 菜单 |
| 本仓 `StudioBindingWorkbench` `.binding-diagnostics` | 只加 summary 共享 testid | 改空镜说明 / 默认展开 / 改冻结钮 |
| N80–N92 / N91 空态 / N92 计划 ID | 仍只各自面板 | 绑定诊断当生成计划 ID |
| 驾驶舱 `.diagnostic-details` | 另刀 | 本刀改 Dashboard |

N92 已锁生成控制计划 ID，并写明「绑定工作台诊断另刀」。驾驶舱头栏/单元/资产诊断另刀，不进 N93。

### N93 落地规格（N6–N92 之后，不在本轮实现）

`StudioBindingWorkbench` 检查器两处 `.binding-diagnostics`：空镜确认 `.empty-review-section > details.binding-diagnostics`（`summary` 文案「诊断详情」，内层 `emptyConfirmation.id`）与生成绑定 `.freeze-section > details.binding-diagnostics`（`summary` 文案「诊断详情」，内层 bindingSet id + fingerprint）。目前无 testid。同一宫格可同时挂载。不是弹层。

- **用户可见**：这些 `summary` 加 `data-testid="studio-binding-diagnostics"`（共享 id，两处可同时存在）。Enter/Space 仍走原生 details 开合。打开不抢 `binding-empty-note` / `binding-confirm-empty` / `binding-freeze`。Escape **不**关闭本 details。不设 `role="dialog"` / `role="menu"`。`binding-empty-confirmation-status` / `binding-set-status` / 提案按钮 **不**加本 testid。
- **复用**：现有 details/summary；不得默认展开、不得改 `confirmSelectedPanelEmpty` / `freezeSelectedPanel`、不得动 `busyAction`。
- **合同**：源码测试钉 `.empty-review-section > details.binding-diagnostics > summary` 与 `.freeze-section > details.binding-diagnostics > summary` 含 `studio-binding-diagnostics`；无 `role="dialog"`；N80–N92 选择器不改；无窗口。
- **不做**：改 T23；抢 N80–N92 / N91 空态 / N92 计划 ID；抢 N6 ⌘G / N7 试听 / N8 别名 / N9–N11 视口 / N12 说明 / N13–N92；把诊断改成 modal；改驾驶舱 `.diagnostic-details`。

24h-perf 不抢 View/Node/Inspector/N6。






























