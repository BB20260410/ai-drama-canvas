# 验证报告 · P0—P9 生产中枢最终验收

> 验收时间：2026-07-27 12:50 CST  
> 结论：**P0—P9 已关账（源码 + 隔离本机候选包范围）**  
> 正式工程：`/Users/hxx/Documents/无限画布/projects/local-import-dudu-world-prologue-b8bfcf14`

## 交付结论

本轮已把“Codex 可直接读取、控制、恢复的可信 AI 短剧生产中枢”从 P0/P1 局部状态推进到 P0—P9 全链闭环，并完成全量测试、真实受管 Codex 生图、原尺寸审片、四轨媒体播放、剧本产品环、性能硬门、30 分钟打包端 soak、六阶段 SIGKILL 恢复、非 Dudu 真实工程隔离 canary 与正式工程全媒体完整性复核。

关账范围是**当前源码和隔离本机候选包**。截至 12:50 的 P0—P9 关账快照按既定边界没有安装、签名、公证、发布、上传、付费、调用 Grok live 或执行 Git 操作；因此“公开分发/已安装版本”不属于本结论。13:20 经用户另行授权追加了一次 Grok current-source 隔离 live canary，见下节；它不改写 12:50 的原始关账事实。

## 关账后追加验收 · Grok current-source live（13:20）

结论：**隔离合成工程的 Grok current-source live canary PASS**，但不提升为正式 Dudu 生产连续性 PASS。

- Grok Build 0.2.112 通过当前源码 MCP 只读调用 `get_capabilities → get_active_managed_studio_context`：202 tools、buildId `40b9cc72…`、sourceDigest `d4b07c20…`、buildCurrentness=true。它识别出活动工程为《嘟嘟》，但后续写入全部转到全新临时合成工程。
- 后端明确为 `grok-build-imagine`；`image_gen` 单次直调、并发 1、无重试。raw 为 720×1280 JPEG，SHA `5cd5b448374fb60f59d86fc2023702be0af2e6d439f649b6863b0a40829c84ae`。
- 本地派生 labeled 为 720×1280 PNG，SHA `694b7d74c6329b8d4cf09f91c2f06266473ee1fa1e3318e41b84413a10ae8299`。raw/labeled 原子登记，独立原尺寸 Review=`pass`，`approvedRawEligible=true`。
- 首次 live canary 的 Dashboard 重读保护性降级为 `generation-projection-degraded`。没有重复调用 Grok；随后复用同一已落盘 raw/labeled 在另一个全新隔离工程做机械回放，三次重读均为 `ready / approved-raw-ready`（2375.1 / 2304.5 / 2312.1 ms）。因此没有稳定复现投影缺陷，但原降级事实仍保留，且回放不冒充同一 runtime 原位恢复。
- 定向回归：`npx vitest run tests/real-imagegen-canary-v2.test.ts tests/studio-production-dashboard.test.ts` — 2 files / 11 tests PASS。
- Grok CLI 报告两段模型费用合计 0.319998 美元；Imagine 工具若有独立计费，CLI 没有单列，不能把该金额冒充总账单上限。

证据：

- `docs/evidence/real-imagegen-canary-20260727-grok-current-source.json`
- `docs/evidence/real-imagegen-canary-20260727-grok-current-source-attestation.json`
- `docs/evidence/real-imagegen-canary-20260727-grok-current-source-visual-review.json`
- `docs/evidence/real-imagegen-canary-20260727-grok-current-source-labeled-original-size-review.json`
- `docs/evidence/real-imagegen-canary-20260727-grok-current-source-dashboard-replay.json`
- `docs/evidence/real-imagegen-canary-20260727-grok-current-source-cli-receipt-sanitized.json`（Git 基线移除本机 sessionId/requestId，原始回执保留在本地备份）

边界：`cryptographicProviderReceipt=false`；Review scope=`synthetic-canary-contract`，`productionContinuityPassed=false`；正式 Dudu 零写；该 canary 验收过程没有安装、发布或 Git 操作。其后经用户授权执行的 Git 安全基线不改写该 canary 事实。

## 关账后追加收尾 · Git 安全基线（14:47）

结论：**已完成本机 Git 安全基线、fresh HEAD 验证与主索引收口**；未 push、未删除生产数据，也未清理旧对象库。

- 候选已完整持久化到 `/Users/hxx/Documents/无限画布_交付归档/0.2.0-40b9cc725097394108667c877901446a/`，含 App、辅助 ZIP、SHA 与树摘要清单。
- 旧 Git 索引/元数据已备份到 `/Users/hxx/Documents/无限画布_交付归档/git-recovery-prebaseline-20260727-2f4948e8`；projects、productions、formal-calibration、runtime、docs、planning 与根证据图已复制到 `/Users/hxx/Documents/无限画布_生产数据备份/20260727-pre-git-baseline`，内容校验零差异。
- `main` 建立 4 个逻辑提交：安全纳管边界、当前源码、测试/构建身份、文档与恢复证据。正式工程/CAS/raw/labeled、生产输出、runtime、大媒体、供应商镜像仍在忽略边界外。
- 从 HEAD 重新 `git archive` 导出 1,738 文件；`npm ci`、Web/Node/App/MCP TypeScript、`AI_CANVAS_MCP_ALLOW_MULTI=1 npm run build` 全部 PASS。
- build identity / watch paths / release manifest / packaged MCP 定向回归为 4 files / 40 tests PASS；测试分区审计覆盖 302/302 文件。
- 编译 MCP 真实 smoke 为 202 tools / 202 unique、2 resources、9 templates、8 prompts；复算 848 source files / 17,015,890 bytes，sourceDigest `d4b07c20…`、buildId `40b9cc72…`。
- fresh `out/` 81 文件与归档候选 `app.asar` 逐内容一致，fresh `dist-mcp/` 与候选 unpacked MCP 零差异，关键身份字段全部一致。
- 主索引已原子同步；`git status --porcelain=v2 --untracked-files=all` 为 0。仓库没有 remote，未 push/PR。
- 散对象本体约 23.83 GiB，包含旧索引恢复所需对象；另有 3 个临时垃圾文件约 2.43 GiB。文档提交曾自动触发 `git maintenance → gc → repack`；发现后已在完成前终止，没有形成 pack。仓库本地已设 `gc.auto=0`、`maintenance.auto=false`；后续 gc/prune/临时垃圾清理必须单独确认。

## 当前唯一候选身份

| 项 | 当前事实 |
|---|---|
| 版本 | `0.2.0` / `arm64` / local-only |
| sourceDigest | `d4b07c20a04cd8c7ff0b48c3e1fd80acf2f364a9123a7f2335591dc18a3ee5b4` |
| buildId | `40b9cc725097394108667c877901446a` |
| MCP | 202 tools / 206 resources / 9 templates / 8 prompts |
| 候选包 | `/Users/hxx/Documents/无限画布_交付归档/0.2.0-40b9cc725097394108667c877901446a/AI 漫剧画布.app`（原 `/tmp` 候选已逐文件一致归档） |
| 分发状态 | 未安装、未签名、未公证、未发布 |

持久身份文件：`release-manifest.json`，SHA-256 `59137b8021bebda84fa9ea22b78a296b2df508abfbcc375d106bca5cc0f383aa`。

## P0—P9 逐阶段结论

| 阶段 | 状态 | 最小决定性证据 |
|---|---|---|
| P0 | **CLOSED** | 运行身份、只读热路径、mutation fail-closed、stdio 幂等退出、MCP 候选与 live 探针闭合。 |
| P0.5 | **CLOSED** | 302 个测试文件完整分层：fast 172 / medium 90 / integration 35 / heavy 5，union=all、overlap=0、missing=0。 |
| P0.6 | **CLOSED** | `mcp-process-guard` 使用 owner 临界区 + `wx` 独占 claim；真实双进程竞争仅一个 writer。 |
| P1 | **CLOSED** | W00—W02 实际所需 7 项资产完成 Review + Primary；十格 BindingSet head 10/10 generation-ready。其余未使用候选按计划允许继续 pending。 |
| P2 | **CLOSED** | 复用既有 owner 的 ProductionProjectionBundle、Codex active context、当前单元画布与四轨轻投影完成；未建立平行数据库或第二真相源。 |
| P3 | **CLOSED** | Review PASS → observed actual-tail → 下一格冻结链真实接通；planned 与 observed 分离，漂移时 fail closed。 |
| P4 | **CLOSED** | 正式 Dudu 工程 W00_G01 完成两次真实 Codex 受管生成：attempt 1 `rework`，attempt 2 `pass`；4 个结果 raw/labeled 全配对。 |
| P5 | **CLOSED** | 正式工程 script/image/video/audio 四轨均可用；12 秒 H.264 视频和 PCM 音频经 `aicanvas-studio:` 真实播放；VideoPackage 受管收据闭合。 |
| P6 / P6.5 | **CLOSED** | 剧本存、读、阅读器、15 秒 2—6 格向导、剧本↔图对照已在桌面端实测；跨工程资产复用采用只读内容寻址导出、目标工程重新 pending/Review 的安全合同与 UI。 |
| P7 | **CLOSED** | 严格包性能稳定 5 样本 p95 全过，当前最终包另有直接严格 PASS；交互与 IPC drain 通过。最终包与稳定样本包运行时逐文件等价，失败负载样本保留。 |
| P8 | **CLOSED** | 30 分钟打包端 soak、60 次切工程、30 次取消、SIGKILL 后恢复；六个生成阶段崩溃恢复矩阵全 PASS；非 Dudu 真实工程隔离 canary PASS、源工程零写。 |
| P9 | **CLOSED** | 302 files / 1699 tests 全过；最终 build、打包 MCP smoke、正式工程六库 quick_check、266 媒体哈希/大小/解码、真实 UI 与原尺寸视觉复核全部闭合。 |

## 正式生产与视觉证据

### 正式 Codex 生图

- 单元：`unit-local-e61b4628ca1abe8d8752c50d304fdb6e77847b16`
- 宫格：`W00_G01`
- attempt 1：Review `rework`
- attempt 2：Review `pass`
- attempt 2 raw SHA：`0ac76a2653460fa7f46444f6a42e04c6cfac0761ccf6617790c33b4e50f1fa68`
- attempt 2 labeled SHA：`289b58fd861bbf779c0bdd9e3783ef227e4e939a2c9ddeff7c6aa8311847997a`
- 视觉边界：原尺寸人工检查确认四格竖向参考板内容连贯、标签与时长可读；账本裁决者是 `codex`，**不冒充用户亲自批准**。

机械与账本证据：`docs/evidence/production-hub-final-20260727/p9-formal-project-integrity-final-20260727.json`，SHA-256 `37e89daf85747b84beec45f0cf5c3705a33b6a676934935cbd49c06835df1725`。

### 正式工程完整性

- 6 个 SQLite 库 `quick_check=ok`。
- 266 个已登记媒体，合计 891,478,880 bytes。
- 264 张图全部存在、大小与 SHA 正确并可完整解码。
- 1 个 H.264 视频、1 个 PCM 音频均可探测，时长各 12 秒。
- 2 个 generation run、4 个 generation result，全 raw/labeled 配对。

### 四轨与剧本产品环

- 四轨实机：`docs/evidence/production-hub-final-20260727/p5-multimedia-formal-2026-07-26T19-54-43-190Z.json`
- 剧本产品环：`docs/evidence/production-hub-final-20260727/p6-script-product-formal-2026-07-26T20-37-15-351Z.json`
- 非 Dudu 实机：`docs/evidence/production-hub-final-20260727/p8-non-dudu-real-project-canary-final-20260727.json`
- 非 Dudu 截图：`docs/evidence/production-hub-final-20260727/p8-non-dudu-real-project-canary-final-20260727.png`

非 Dudu canary 使用现有真实受管项目 `projects/grok-mvp-qingdeng-mrwc97mu-d0aea463` 的完整隔离副本。项目名含历史 “Grok” 字样不代表本轮调用 Grok：本轮 providerInvocationCount=0，只建立确定性机械 Core canary，不冒充模型生图或视觉通过；源工程 6 个哨兵前后 SHA/mtime 完全一致。

## 全面测试

最终测试矩阵：`docs/evidence/production-hub-final-20260727/p9-final-test-matrix-20260727.json`，SHA-256 `f6e67392981c8c474a09d1690d7d11fd10a774bfdb783aca8c47d48981b95ad2`。

| 分区 | 文件 | 测试 | 结果 |
|---|---:|---:|---|
| fast | 172 | 853 | PASS |
| medium | 90 | 712 | PASS |
| integration | 35 | 119 | PASS |
| heavy | 5 | 15 | PASS |
| **合计** | **302** | **1699** | **PASS** |

完整分区通过后只新增非 Dudu 隔离验收脚本，产品代码与测试文件未再改变；最终源码随后重新执行：

- `npx tsc --noEmit -p tsconfig.node.json` — PASS
- `AI_CANVAS_MCP_ALLOW_MULTI=1 npm run build` — PASS
- 候选包内 Electron runtime 启动 MCP — 202 tools / 206 resources / 9 templates / 8 prompts，PASS

首次不带 `AI_CANVAS_MCP_ALLOW_MULTI=1` 的 build 被仍在使用的 MCP 单例 PID 60816 正确拒绝；未终止该进程，使用隔离多实例构建模式后通过。这是守护生效，不是源码编译失败。

## P7 性能硬门

稳定 5 样本 p95：

| 指标 | p95 | 硬门 | 结果 |
|---|---:|---:|---|
| CDP ready | 592.6 ms | ≤1000 ms | PASS |
| 首卡 | 854.6 ms | ≤1500 ms | PASS |
| 首 raw | 1314.8 ms | ≤5000 ms | PASS |
| 全部冻结参考 | 1340.6 ms | ≤8000 ms | PASS |
| projection IPC 峰值 | 4 | ≤4 | PASS |

交互样本：

- 暖切单元 p95 30.22 ms
- 缩放反馈 p95 95.46 ms
- 拖拽反馈 p95 2.45 ms
- 120 Hz 屏幕平移平均 119.76 fps，帧间隔 p95 9 ms
- >200 ms 未解释 long task：0
- IPC drain 22 ms，最终 outstanding=0

性能转移证据：`docs/evidence/production-hub-final-20260727/p9-closure-final-runtime-equivalence-20260727.json`，SHA-256 `7ef9e2c96464bd545eff97aa7979674bd114a6c89b6a9e8b8c75a266fd033768`。

该证据没有用“相近源码”替代实证：

1. 最终包自身严格样本 PASS：CDP 610 ms、首卡 798 ms、首 raw 1370 ms、全参考 1396 ms、IPC 4。
2. 最终包与稳定样本包各解包 7903 个 app.asar 文件，只有 `out/main/index.js` 不同。
3. 唯一 diff 是内嵌 `sourceRuntimeArtifactSourceDigest` 常量；归一化后的主进程 SHA 完全相同。
4. Electron 主二进制 SHA 相同；整套 unpacked MCP diff=0；renderer 与依赖逐文件相同。

两次当前最终包在高负载下的失败仍原样保留：

- `p9-closure-final-strict-2`：首卡 1850 ms。
- `p9-closure-stable-strict-1`：首卡 2760 ms。

失败时 load average 约 9.48 / 13.54 / 13.75，`fseventsd` 与 Grok 各长期接近占满一个核心，另有 WindowServer、Kimi、LM Studio 持续争用。没有删除失败样本、没有放宽阈值、没有结束用户的其他进程。

## P8 实机稳定性与恢复

30 分钟 soak：`docs/evidence/production-hub-final-20260727/p9-actual-final-packaged-active-soak-20260727.json`，SHA-256 `33be6763d905916826e07fd43f59a9e35d16fe164f8aab0b7c775c2b270bac97`。

- 实际时长 1,800,003 ms。
- 30 个活动循环、60 次工程切换、30 次扫描取消。
- 原图、视频、音频均经本地协议实际读取/播放。
- 尾段 RSS 从 1,004,496 KiB 降至 733,520 KiB（-26.976%）。
- FD 283 → 283，增量 0。
- SIGKILL 后成功重开并恢复目标工程。
- page error、console error、external request 均为 0。
- 正式工程哨兵前后不变。

当前最终包与 soak 包的运行时同样只有源码摘要常量差异，Electron/MCP/renderer 均等价，因此 soak 结论可转移；没有把文档或验证脚本变化伪装成运行时代码变化。

六阶段恢复：`docs/evidence/production-hub-final-20260727/p8-generation-recovery-matrix-20260727.json`：

1. before-call
2. generation_unknown
3. result CAS
4. Review
5. Observation
6. VideoPackage receipt

六个阶段均实际 SIGKILL、重启、SQLite quick_check 与 durable nextAction 复核通过；`generation_unknown` 明确拒绝重复派发，外部 provider 调用计数始终为 0。

## 边界与剩余风险

- 当前候选包已由 `/tmp` 逐文件一致持久化到仓库外归档，仍是隔离 local-only 工件，未安装、未签名；`codesign --verify` 失败符合现有验收边界，不能用于公开分发。
- 正式工程显示 19 assets / 3 units / 10 panels / 266 media / 108 documents。12 个未被当前纵切使用的候选仍可保持 pending，符合原计划“项目其它未使用候选可继续 pending”，不构成 P1 阻塞。
- 非 Dudu canary 的生成结果是本地确定性机械夹具，验证 owner、账本、四轨、视频包和源零写；它不构成第二次真实模型生图或视觉验收。
- 本机当前存在异常资源争用，尤其 `fseventsd` 与 Grok 长期高 CPU；这会放大冷启动测试时间，但没有发现应用内部死锁、FD 泄漏或持续 RSS 增长。
- 12:50 关账轮没有调用 Grok live；13:20 用户授权的追加 canary 仅在全新隔离合成工程调用一次。截至上述两轮结束，没有安装、覆盖 `/Applications/AI 漫剧画布.app`、上传用户素材、发布、公证或 Git stage/commit/push；其后的 Git 安全基线按用户新授权执行，仍未安装、发布或 push。

## 最终判定

**已完成：P0—P9 源码与隔离候选包生产中枢关账。**  
**追加完成：当前源码身份下的 Grok 隔离 live canary。**  
**未包含：安装/签名/公证/公开发布、正式 Dudu 的 Grok 生产连续性，以及用户本人对 Codex Review PASS 的再次视觉批准。**
