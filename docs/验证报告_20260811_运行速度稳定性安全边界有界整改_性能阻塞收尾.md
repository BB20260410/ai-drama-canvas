# 运行速度、稳定性与安全边界有界整改 · 性能阻塞收尾

## 结论

本轮为**部分完成 / 性能门阻塞**，不能写成全部完成。

- 两个新增 P1 已关闭：小说分析任务路径/绑定越界，以及 Higgsfield 不可信自证明授权。
- 剪辑台媒体服务端分页、缓存身份和迟到回填门禁已完成。
- 投影 bundle 的重复深查询已减少；同一确定性夹具单次前后对照中，Core 总耗时
  `2945.26 → 2401.39ms`（-18.5%），panel fanout `1404.70 → 1049.18ms`（-25.3%）。
- 最后一次隐藏 T23 的功能、图片解码、交互、IPC 排空和全部参考图预算均通过；
  但首卡 `3339ms > 1500ms`、首张原图 `5647ms > 5000ms`，因此严格运行门失败。
- 已达到两轮纠正上限；没有第三次修改或重跑，也没有进入 package smoke、打包和安装。

## 已完成

1. 小说分析任务路径只从受限任务 ID 派生，任务目录使用 confined/no-replace 写入；
   intent 前在锁内重新核对任务绑定与 Story/章节证据。软链越界和预检后漂移均失败关闭、零 POST。
2. Higgsfield 队列不再接受普通 MCP 调用方自构造的 Unlimited/零扣费证明；当前只保留诊断投影，
   真实程序化 Unlimited 仍必须由供应方提供可验证能力。
3. 剪辑台媒体列表改为服务端分页、身份绑定游标和有界快照查询缓存；Renderer 使用
   root/scan/sequence 门禁，搜索和分类切换不会沿用旧页。
4. 生产投影复用已读 freeze/generation 信息，减少同一 bundle 内的重复深查询；blocked
   stamp 与原直接查询保持一致。
5. 增加默认关闭、仅测试环境启用的阶段时间线与 T23 进程收口；并发 attempt 只允许最新结果写探针。

## 最终 T23 证据

决定性文件：

- 初次最终失败：`docs/evidence/runtime-stability-t23-phases-final-20260811T021900Z-f5fee3b7.json`
- 最后纠正复验：`docs/evidence/runtime-stability-t23-phases-correction-final-20260811T022806Z-f1b48f4a.json`
- 本收尾索引：`docs/evidence/runtime-stability-refactor-bounded-closeout-20260811-f1b48f4a.json`

| 指标 | 初次 | 最后一次 | 严格预算 | 结果 |
|---|---:|---:|---:|---|
| 首卡 | 4564ms | 3339ms | ≤1500ms | FAIL |
| 首张原图 | 7238ms | 5647ms | ≤5000ms | FAIL |
| 全部参考图 | 10649ms | 7473ms | ≤8000ms | PASS |
| 投影 IPC 峰值 | 3 | 4 | ≤4 | PASS |

最后一次交互全部通过：暖切 p95 98.53ms、缩放反馈 p95 59.96ms、拖拽 p95 2.85ms、
平移 119.19fps/帧 p95 9.5ms、long task 0。36 个单元、4 个 raw 与 4 个 reference
均唯一且解码成功，page/console errors 均为 0，IPC outstanding 最终为 0。

阶段时间线仍显示深投影 Core 总计 5673.20ms，其中 current dashboard/binding 1546.24ms、
panel fanout 1339.52ms、observation/review 1500.28ms、adjacent pack/video 1057.93ms。
这说明请求排序已带来改善，但还不足以满足冷路径严格预算；本轮不再扩大到第三次架构改动。

## 实际验证

- 最终有界矩阵：17 files / 178 tests PASS。
- 独立相邻复验：6 files / 31 tests PASS。
- T23 探针与清理：2 files / 21 tests PASS。
- 受管画布加载顺序：1 file / 37 tests PASS。
- `npm run typecheck`：PASS。
- `npm run typecheck:app`：PASS。
- `npm run build`：PASS；当前源码身份
  `f1b48f4a982831765d03be5b43e21945d5c6585903a7b502ed560ce885da3ca1` /
  `6ffb87211e92d54df036cfaecd2a725e` / 220 tools。
- `npm audit --omit=dev --registry=https://registry.npmjs.org`：0 vulnerabilities。
- `git diff --check`：PASS。
- 最后隐藏 T23 strict/interactions：FAIL，仅上述两个冷路径预算失败。

## 未执行及原因

- `npm run package:isolated-smoke`：未运行。T23 源码运行门失败，按合同不得进入唯一 package smoke。
- Developer ID App 打包、签名和替换：未执行。package smoke 未通过前不得更新安装版。
- 全量 fast/medium/integration/heavy：未重跑，避免与有界复验重复并触发测试循环。
- 公证、DMG、发布、上传、付费、真实生图/视频：均未执行。
- Git stage/commit/push：均未执行。

## 当前 App 与数据边界

- 当前源码构建：`f1b48f4a… / 6ffb8721… / 220 tools`，**未安装**。
- 当前 `/Applications/AI 漫剧画布.app`：仍为已验收的
  `c9bb2c87… / 02a1bf9d… / 220 tools`，本轮未修改，App 已关闭。
- 正式工程、素材、CAS、Review 与生成账本写入均为 0；只使用隔离临时夹具。
- 最终检查没有 T23 残留进程或临时目录。

## 剩余项与停止规则

- Blocker：`PERF-FIRST-COLD-CANVAS-01`。若未来继续，必须由用户明确建立新的独立性能 Goal，
  从首卡前的更细粒度阶段采样开始；不得恢复本 Goal 或重复当前两份 T23。
- Deferred P2：GlobalResource hidden harness 默认证据文件名的并发 no-clobber。
- 本轮到此停止，不自动开启下一轮全面审查、性能改造、package smoke 或安装。
