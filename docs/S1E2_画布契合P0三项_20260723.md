# S1E2 × 画布契合 · P0 三项落地（2026-07-23）

**边界声明：** 有序链 formal PASS ≠ 与无限画布产品完美契合。本切片只收敛写入口、commit 稳与 earliest 同一真相。

## 1. 统一 ordered runner + 强制写租约

**正式路径（MCP-only）：**

```bash
npx tsx scripts/s1e2-mcp-only-runner.ts prepare --unit S1E2-U15
npx tsx scripts/s1e2-mcp-only-runner.ts commit --state productions/.../05_canvas/_scratch/s1e2-u15-ordered-state.json
npx tsx scripts/s1e2-mcp-only-runner.ts earliest
```

- require 模式：`AI_CANVAS_WRITE_LEASE_MODE=require`（无租约不准写）
- freeze/dispatch/prepare/commit 经 `executeIdempotentCommand` + 租约
- 半旁路兼容：`s1e2-ordered-runner.ts`；create/bind：`s1e2-uNN-run.ts`
- 单 MCP 主写：`npm run mcp:daemon`；见 `docs/MCP_写租约与唯一写入口_20260723.md`

## 2. Commit 稳 + 六图一键 attest

| 改进 | 位置 |
|------|------|
| 活动上下文 overview **fail-soft**（token 不依赖 dashboard） | `active-managed-studio-context.ts` |
| 六图一键 | `s1e2-ordered-runner.ts attest-batch --batch N`（attestation head CAS 正确） |

```bash
npx tsx scripts/s1e2-ordered-runner.ts attest-batch --batch 2
```

## 3. 审片台 earliest 对齐

| API | 说明 |
|-----|------|
| Core `getStudioEpisodeEarliest` | 集内 sequence + formal 证据 + 六图闸 + 写租约 |
| MCP `get_studio_episode_earliest` | 同上只读 |
| CLI `ordered-runner earliest` | 打印 JSON；`statusLine` 可镜像进 STATUS |

`earliestUnitId` 为人机共同下一步；**不要** STATUS 与 UI 各推一套。

## 仍非完美契合（刻意不关账）

- 产线仍可半旁路 per-unit 脚本  
- 桌面审片 UI 未做全量 Vue 工作台（仅 Core/MCP 投影）  
- 拖入拖出、Commit 向导 UI 未做  

## 测试

- `tests/studio-project-write-lease.test.ts`
- `tests/active-managed-studio-context.test.ts`（fail-soft 后仍应 PASS）
