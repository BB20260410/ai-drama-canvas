# 全项目重构本机安装闭环报告

## 结论

最新版已安装到 `/Applications/AI 漫剧画布.app`，并与工作区
`dist/mac-arm64/AI 漫剧画布.app` 保持同一身份：

- sourceDigest：`c9bb2c8788bc600e24d1952f696fb558d1fe6469fdf6afe0cf9b362547332942`
- buildId：`02a1bf9d8764bff2fb960be0cad17cd4`
- MCP：220 tools
- 版本/架构：0.2.0 / arm64
- 签名：Developer ID deep/strict PASS
- 分发：local-only；未公证、未生成 DMG、未发布

## 实际安装过程

1. 复用已经通过 Phase H 的冻结源码、正式 build、current candidate 和隔离 App 证据，
   未重新运行 fast/medium/integration/heavy、candidate 或 package smoke。
2. 使用 electron-builder `--mac --dir` 制作本机 Developer ID 签名 App，并显式关闭公证。
3. 首次参数把证书完整前缀传给 builder，被其在签名前拒绝；未产生安装副作用。
   唯一纠正为使用 `YIHANG LI (3JS43BTTJ3)`，第二次签名成功。
4. 旧安装版先移入仓库外归档；新版复制、deep/strict 和内嵌 release identity 通过后完成切换。
5. 工作区 `dist/mac-arm64` 同步为同一新版；旧 dist 同样保存在归档中。

## 安装后真实验收

专用安装验收使用隔离 HOME、registry、media runtime 和 userData，全程 accessory 后台运行：

- Developer ID、TeamIdentifier、bundle identifier、arm64：PASS；
- App 自带 Electron runtime 启动 packaged MCP：220 tools；
- sourceDigest/buildId 与冻结源码一致；
- show/focus=0，Dock 不可见，窗口始终 hidden/unfocused；
- 41ms 自然退出，exitCode=0，无 TERM/KILL；
- 验收后 App 未运行，无残留进程。

安装版与工作区 dist 的 release manifest、主可执行、app.asar 和 MCP server 哈希一致。

## 回滚与清理

旧版 `0d17eb62… / 6067ff07…` 已保存在：

`/Users/hxx/Documents/无限画布_交付归档/local-install-20260811T063507-c9bb2c87`

其中包含旧安装版和旧工作区 dist，二者签名均复验通过。两次临时打包目录已移至：

`/Users/hxx/.Trash/AI画布安装临时_20260811T063507-c9bb2c87`

废纸篓未清空，仍可恢复。

## 边界

- 未公证、未发布、未上传、未付费。
- 未写正式项目、CAS、Review 或生成账本。
- 未执行 Git stage、commit 或 push。
- 未覆盖任何既有 final-validation 或历史 PASS 证据。

## 证据

- `docs/evidence/whole-project-refactor-local-install-20260811-c9bb2c87.json`
- `docs/evidence/installed-local-verify-20260811T063507-c9bb2c87.json`
- `docs/evidence/whole-project-refactor-final-20260811-c9bb2c87.json`
