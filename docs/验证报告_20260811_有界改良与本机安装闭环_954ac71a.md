# 无限画布有界改良与本机安装闭环验证报告

## 交付结论

本轮选定范围已完成并通过独立终审：connector 与正式生成终态原子互斥、generation ledger watcher 单 drain、VideoEditor nested/hover 预览有界化均已闭合；冻结源码 `954ac71a… / c7cb5cee… / 220 tools` 已构建 immutable MCP candidate、通过后台隔离打包，并以 Developer ID 本机签名安装到 `/Applications/AI 漫剧画布.app`。

本结论只覆盖本轮有界 Goal，不表示软件从此不存在任何未来可优化项。

## 核心改良

- Higgsfield connector reservation 与 formal call intent/result/bundle/not-invoked/abandon/fail/cancel/retry 在同一 generation SQLite 写事务内双向互斥。
- remote terminal 释放项目级 connector 并发槽，但继续绑定同一 formal run，禁止第二执行 owner。
- direct/command bundle 均在 raw 检查、labeled 渲染、CAS 和 receipt 前失败关闭，拒绝时零孤儿写入。
- generation ledger watcher 改为 single-drain latest-only：事件风暴最多当前轮加一轮最新补算；失败只补一轮；关闭等待在途 drain，关闭后不发送。
- VideoEditor nested/hover 共用物理并发 2；1000 clips 只调度 selected/current/visible/交互相关需求；hover latest-demand；foreground 媒体任务先暂停并排空预览。

## 代码与测试证据

- connector queue：14/14 PASS。
- agent bundle 与 target state：16/16 PASS。
- 最终独立关键矩阵：4 files / 37 tests PASS，P0/P1/P2=0。
- watcher：6/6 PASS。
- VideoEditor preview focused：14/14 PASS。
- `npm run typecheck`：PASS。
- `npm run typecheck:app`：PASS。
- `npm run build`：PASS；live source 与 release manifest 同为 `954ac71a…`。
- official npm registry production audit：190 dependencies，0 vulnerabilities。
- `git diff --check`：PASS。

没有重跑 fast/medium/full 分区，也没有重跑 T23；本轮未修改受管画布启动热路径，定向和相邻测试足以覆盖所选 owner。

## MCP candidate 与隔离 package

- candidate：`mcp-candidate-954ac71a461be527-3962c60699c5bbcd-d3674875`。
- `mcp:current:check`：17 candidates，invalid=0。
- current launcher 真实 stdio：220 tools / 89 resources / 9 resource templates / 8 prompts。
- 唯一隐藏隔离 package smoke：PASS。
- 两阶段终态：terminal SHA `0b5d2d0…` 与 completion marker 绑定同一 runId，canonical lock 已释放。
- 空工程恢复、Effect/Transition、ReviewStudio 均通过；所有 App 隐藏运行，show/focus=0，均自然退出，无残留进程或正式状态漂移。

## 本机签名、安装与回滚

- 目录包：arm64，bundle id `com.hxx.aidramacanvas`，版本 0.2.0。
- 签名：`Developer ID Application: YIHANG LI (3JS43BTTJ3)`；Team `3JS43BTTJ3`；deep/strict PASS。
- 未公证，未生成 DMG，未公开发布。
- 安装态验收：App 自带 Electron runtime 启动 MCP 220 tools，activation policy 为 accessory，Dock hidden，show/focus=0。
- 关闭：547ms 自然退出，exit 0，无 TERM/KILL；验收后 App 已关闭。
- 新版安装：`/Applications/AI 漫剧画布.app` = `954ac71a… / c7cb5cee… / 220`。
- 回滚归档：`/Users/hxx/Documents/无限画布_交付归档/local-install-20260811T135947Z-954ac71a`，旧 d5 安装版与旧 workspace dist 均保留并验签通过。

## 边界

- 未修改正式工程、CAS、Review 或生成资产。
- 外部生成调用、上传、付费均为 0。
- 未 Git stage、commit 或 push。
- Higgsfield 可信 Unlimited adapter 仍不可用；本轮修复的是未来恢复 adapter 前必须成立的账本互斥，不宣称真实 Unlimited 已打通。
- local-only，不公证、不发布、不配置自动更新。

## 决定性证据

- `docs/evidence/bounded-improvement-local-install-final-20260811-954ac71a.json`
- `docs/evidence/isolated-package-smoke-20260811T135145Z-954ac71a-completion.json`
- `docs/evidence/installed-local-verify-20260811T135947Z-954ac71a.json`

