# P1 执行清单（切换后经新 MCP execute_command 执行）

前置：P0 关闭（冻结轮 fast 全绿 + 切换 + live 探针过）。全部命令走 execute_command（写租约 require 模式下先取租约）。

## 完整 ID
- W00 错误的星：`unit-local-e61b4628ca1abe8d8752c50d304fdb6e77847b16`（4 格 W00_G01…G04）
- W01 神：法则照常运行：`unit-local-86a821eb5ff4d804635f4207073c85b31abc87cf`（3 格 W01_G01…G03）
- W02 魔：裂隙开始进食：`unit-local-2fb32d82ba46e9093ff50c888317a5ee9fee6bd5`（3 格 W02_G01…G03）
- panelId 即 W00_G01 等可读名。

## 步骤 A：补建烛龙资产（freeze 硬前置）
1. `create_studio_asset`（category=character，命名对齐 allowed-ref:char-zhulong，expectedRevision=0）
2. 为其登记版本引用媒体 SHA `12191570b77e5b4a2c6c74d3ec322601de41e28c98c49a8a79e97a371fdb82b3`（命令名待查：资产版本登记命令——P1 分析未列，实施时从 command-bus 找 add/register version 命令）
3. `review_studio_asset_version`（decision=approved，依据 scratchpad/p1-visual-precheck-20260726.md 第 7 行结论）
4. `set_studio_primary_authority`

## 步骤 B：6 项已 pending 资产 Review + Primary（全部 expectedRevision=2，依据视觉预检记录）
按序：scene-star、scene-yazihe、scene-yucun、scene-cave-mouth、style-night、style-cine
每项：`review_studio_asset_version`(approved) → `set_studio_primary_authority`
其余 12 项 pending 不动。

## 步骤 C：十格 BindingSet（每格四步链，逐格串行）
每格：
1. `analyze_studio_script_entities`（unitId, panelId, expectedRevisionToken）
2. 对每个 proposal：`resolve_studio_entity_proposal`（按视觉预检矩阵选 selectedAssetId/presence/role）；W00_G01 预期无 proposal → `confirm_studio_panel_empty`
3. `freeze_studio_asset_binding_set`（unitId, panelId, expectedRevisionToken）
格序：W00_G01→G04 → W01_G01→G03 → W02_G01→G03
注意：W01_G01 的 declaredReferences 核对要求烛龙媒体 SHA 已被选中（步骤 A 先行）。

## 验收（P1 门）
- 7 项引用资产全部 approved + Primary，authority events > 0
- 10 格全部有 BindingSet（freeze pack SHA 与 Authority 一致）
- 错 SHA/过期版本进冻结包硬失败的负向验证 1 例
- 证据 JSON 新文件名落 docs/evidence/，更新 STATUS/TASKS/交接

---
# P2 bundle 草稿实施前必修（独立核对代理结论，2026-07-26）
1. 草稿 174 行 `StudioDashboardPanelDetail` → 真实类型名 `StudioDashboardPanelSummary`（studio-production-dashboard.ts:263-278）
2. 草稿 325 行 `StudioContinuityReviewControl["head"]` 为捏造字段，删除（真实接口 studio-continuity-review-control.ts:221-237 无 head）
3. 草稿 893-894 行 episodeStart/EndSeconds 改从 `timelineProjection.unit.*` 取（studio-multimedia-timeline.ts:124-125），unitDetail.unit 无此字段
4. D1 声称复用的 4 个函数（getStudioBindingControl 等）骨架内未真正调用，实施时补真实调用
5. 开放问题裁决（主代理）：a=允许新增对称 predecessor 只读查询；c=用明确 adjacentLocator 字段不伪装 cursor；d=允许 export 私有映射函数；e=不纳入首切片（P7 处理）
