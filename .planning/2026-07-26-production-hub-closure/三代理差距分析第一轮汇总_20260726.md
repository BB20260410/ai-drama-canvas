# 三代理第一轮独立分析要点汇总 · 2026-07-26

## 产品/导演流程代理
- 三卡点：双 UI 心智模型并存（studio 向导 vs 17-tab 旧 canvas）；「开始」不支持跨单元批量（ManagedStudioCanvasView.vue:3999）；时间线无 video/audio 播放绑定 UI（死路）。
- 四诉求覆盖判定：批量吞吐=无；返工=有（GenerationQueueView→新 attempt）；剧本变更传播=弱（仅 input-drift 被动拦截）；跨集复用=空白（资产/Review/Primary 项目内私有，无导出/克隆，每季重走裁决）。
- 修订：+P6.5 跨集资产复用（早于 P7）；时间线 UI 提前到 P2 前；P2 bundle 与 P6 对照合并设计；T13 只做 W00-W02 子集。
- 打分（现→P1-4 后）：内容 2→5；闭环 1→4；Codex 3→6；UI 3→5；性能 2→3。

## 数据权威/Codex 接入代理
- Codex"可读不可控"：active-managed-studio-context nextAction 恒为硬编码 degradedNextAction（L318-330）；真实 per-unit nextAction 在 dashboard 未被入口返回；GenerationSessionSnapshot 全仓 0 命中。
- 一致性锁两端断裂：canonical_identity/composition_hint 0 命中；NextShotContinuitySnapshot（P3 全字段）零调用；实际落库为扁平 13 字段 StudioSeedanceObservedState。
- unknown/扣费扎实（maxCalls=1 多层 fail-closed：freeze-pack-execution-contract:86、studio-generation:2271 等）；真空=ledger 无 agentId/callerId。
- 佐证：现装 manifest 200 工具（f2aeb28a），候选 201（fd2d108b）。
- 修订：P4 前插"Codex 入口整合"（degradedNextAction→真读 dashboard nextAction）；P3 改"schema 收编"（复用 NextShotContinuitySnapshot 不另起）；P4 追加 callerId。
- 打分：内容 4→7；闭环 2→6；Codex 3→7；UI 5→5；性能 5→6。

## 性能/可靠性代理
- P7 可达性：暖切/交互/排空可达；冷启≤1s/首屏≤1.5s/首raw≤5s 结构性风险——天花板=全仓 38 处 DatabaseSync 同步阻塞 + 零 worker offload；managed-project.ts:1333-1398 每次 3 读+2 SHA+7 stat 零缓存。
- 测试体系失真：fast 51 分钟基准 254 文件零计时（reporter 只记失败）；studio-generation-ledger.test.ts 34 例 340 秒峰值 53s 错分 fast。
- 恢复缺口：①mcp-process-guard 锁获取"读→查→写"非原子（SIGKILL 双进程可都写 claim）；②切工程 in-flight lease 未到期无测试；③candidate 误删恢复无测试。
- 修订：json reporter 耗时基线→新增 medium 层；P7 顺序=shell 缓存→Bundle 聚合→worker 最后；P5 加 dirfd batch；P8 补三缺口；P9 加耗时回归门。
- 打分：内容 4→7；闭环 2→6；Codex 3→6；UI 3→5；性能 2→4。

## 三方共识雏形（待互评轮确认）
- 平均分（当前）：内容 3.3、闭环 1.7、Codex 3、UI 3.7、性能 3 —— 总体约 3/10；P1-4 后预测约 5.5/10。
- 到"驾驶舱"标准的最大缺口排序：真实生产闭环（0 数据）> Codex 可控性（入口占位）> 一致性锁落地（schema 断裂）> 批量/跨集产品能力 > 性能硬指标。
